# concept — 인과관계의 거대한 그물망

> 우리가 그리는 것은 점과 선을 잇기만 하는 **단순한 그래프가 아니라 온톨로지
> 기반의 그래프**다. 노드는 뜻이 정해진 타입(인물·사건·장소·시대·조직·개념·
> 작품)을 갖고, 엣지는 출발·도착 타입이 정해진 관계(참여·발생지·소재로 다룸·
> 배경)를 갖는다(§1). 그 위에서 궁극적으로 구축하려는 것은
> **'인과관계의 거대한 그물망'**이다.

이 문장이 이 문서의 전부다. 나머지는 이 문장을 **지금 저장소의 구조 위에서**
어떻게 이루는지를 적는다 — 새 프로젝트를 그리지 않고, 이미 있는 파이프라인
(`ingest → resolve → enrich → extract → promote → serve`)에 무엇을 더하고
무엇을 고칠지만 적는다. 실측은 전부 `data/histgraph.sqlite`, 2026-09-04 다.

---

## 1. 왜 단순한 그래프가 아닌가

단순한 그래프는 점과 선이다. 세종과 훈민정음이 이어져 있다는 것은 알지만
**어떻게** 이어져 있는지는 모른다 — 만들었는지, 반포했는지, 그 이름의 책이
있는지. 그런 그래프는 어떤 질문에도 답하지 못한다. 답하려면 점마다 **무엇**이고
선마다 **무슨 관계**인지가 정해져 있어야 한다. 그것이 온톨로지다.

`src/histgraph/ontology.py` 가 그 자리다. 노드 타입 열, 엣지 타입 스물, 그리고
엣지마다 **출발할 수 있는 타입과 도착할 수 있는 타입**이 적혀 있다.

| 노드 타입 | | 엣지 타입 | 출발 → 도착 |
|---|---|---|---|
| 인물 `person` · 사건 `event` · 장소 `place` | | 참여 `participated_in` | 인물·단체 → 사건 |
| 시대 `period` · 직위 `role` | | 발생 장소 `occurred_at` | 사건 → 장소 |
| 단체·국가·왕조 `org` | | 직위 `held_position` | 인물 → 직위·단체 |
| 개념·주제 `concept` | | **원인 `caused`** | **사건·단체·인물·개념 → 사건·단체·개념** |
| 유물 `heritage` · 예술작품 `artwork` | | 소재로 다룸 `depicts` | 작품 → 인물·사건·장소·단체 |
| 영화·드라마 등 `media` (+ `form`) | | 주제 `about` | 작품 → 개념 |
| | | 배경 `set_in` | 작품 → 시대·장소·왕조 |

타입이 정해져 있다는 것이 사 주는 것은 셋이다.

1. **질의가 된다.** "이 자리를 거쳐 간 인물"(직위), "이 사건을 다룬 작품"(소재),
   "이 일이 부른 일"(원인)은 엣지 타입이 있어야 물을 수 있는 질문이다.
2. **틀린 것이 구조적으로 걸린다.** `validate_edge_endpoints` 가 출발·도착
   타입을 검사하므로, 개념이 사건 자리에 앉으면 `depicts` 가 깨지고 배우가
   인물 자리에 앉으면 `created` 가 깨진다. 실측 — 개념 자리(`concept`)가
   없던 때에 '자살'·'조직범죄'가 사건 노드로 앉아 화면의 `depicts` 157건 중
   진짜 사건은 6건이었다(부록 A). 자리를 만들자 오염이 멎었다(`reclassify`).
3. **소스가 달라도 틀이 같다.** 국가유산청·Wikidata·위키백과·국편·민백이
   전부 `Node`·`Edge` 로 정규화되어 들어온다. 관문(한국어 설명, 매체 구분)도
   `Node.__post_init__` 한 곳이다.

반대편 증거도 있다. 뜻 없는 엣지 `related_to` 가 2,285건이다. 이 선들은
그물망이 아니라 실뭉치다 — 무엇을 물어도 "관련 있다"밖에 답하지 못한다.
Wikidata 의 원인(P828)·결과(P1542)조차 여기 '원인'이라는 **라벨**로 숨어
있었다. 이제 `caused` 타입으로 꺼냈다.

---

## 2. 그물망은 네 층이다

| 층 | 노드 | 엣지 | 답하는 질문 | 실측 |
|---|---|---|---|---:|
| **시간** | 시대 `period` | 시점 `dated_to` · 시대 `from_period` · 발생 시기 | 언제 | 34,861 · 4,365 |
| **사실** | 인물·사건·장소·단체·직위·유물 | 참여 · 발생 장소 · 직위 · 계보(자녀·배우자) · 출생지 · 소속 · 소재지 · 제작 | 누가 어디서 무엇을 | 참여 4,361 · 직위 6,001 · 계보 6,841 |
| **인과** | 사건·단체·개념 (원인 쪽에 인물도) | **원인 `caused`** (원인·배경·계기·영향) | **왜, 그래서** | **22** |
| **콘텐츠** | 작품 `media`·`artwork` | 소재 `depicts` · 주제 `about` · 배경 `set_in` · 원작 `adapted_from` | 무엇으로 되살아났나 | 284 · 161 · 345 · 12 |

전체 노드 43,468 · 엣지 76,285. **사실층은 서 있다.** 시간층은 연표가 이미
그 위에 서 있다. 콘텐츠층은 올해 명단을 채웠다(부록 A).

**인과층이 22건이다.** 그 22건은 전부 Wikidata 에서 왔고, 화면 DB
(`data/korea.sqlite`)에는 **0건**이다. 게다가 22건 중에는 '제2차 세계 대전 →
소련·미국·일본·유엔'처럼 P1542(결과)를 느슨하게 쓴 것과 '상실 → 상실감'처럼
한국사가 아닌 것이 섞여 있다. 병인박해 → 병인양요, 을사조약 → 이토 히로부미
저격, 부마 항쟁 → 10·26 정도가 쓸 만하다.

이것이 지금 이 그래프가 **아직 그물망이 아닌** 이유다. 참여·장소·시대 엣지는
전부 **한 사건 안에 갇힌 별 모양**이다 — 사건 하나를 가운데 두고 인물·장소·
연도가 방사형으로 붙는다. 별과 별을 잇는 선이 없다. 임진왜란(1592)과
병자호란(1636)은 연표에서 44년 떨어져 나란히 설 뿐, 앞의 것이 명의 쇠퇴와
후금의 성장을 거쳐 뒤의 것을 불렀다는 사실은 그래프 어디에도 없다.
**별을 그물로 바꾸는 것이 인과 엣지다.**

---

## 3. 인과층 — 그물망의 씨줄

### 3.1 엣지의 모양

- 방향은 언제나 **원인 → 결과**. 출발은 사건·단체·인물·개념, 도착은 사건·단체·
  개념. 한국사 서술의 인과는 사건끼리만 오가지 않는다 — '후금의 성장'(단체)이
  정묘호란의 배경이고, '동학'(개념)이 동학농민운동의 뿌리며, '임진왜란 → 명의
  쇠퇴'는 단체가 결과다.
- 라벨은 인과의 **종류** (`causes.KINDS`):

  | 라벨 | 뜻 |
  |---|---|
  | 원인 | 이것이 없었으면 그 일이 일어나지 않았다 |
  | 배경 | 그 일이 일어날 조건을 만들었다 |
  | 계기 | 그 일을 촉발한 발단·구실이 되었다 |
  | 영향 | 그 일의 방향·내용에 영향을 주었다 |

- **'어떻게'**는 `props.how` 한 구절(60자 이내 — 더 길면 구절이 아니라 근거의
  되풀이다), 근거는 `props.evidence` 의 원문 인용, 신뢰도는 확실 0.9 · 개연 0.7 ·
  가능 0.5. 구절이 빈 인과 엣지는 두지 않는다 — 화면이 선 위에 그 구절을 쓴다.

### 3.2 어디서 얻나 — 사실층과 같은 답이다

구조화 소스는 주지 않는다. Wikidata 의 인과 속성은 전체 그래프에서 16건이다.
사실층에서 얻은 결론("공공 API 는 개체를 주지만 관계를 주지 않는다")이
인과에서는 더 극단으로 참이다. **개체는 수집하고, 관계는 산문에서 추출한다.**

산문은 있다. 말뭉치(`data/corpus.sqlite`)의 사건 문서:

| 소스 | 사건 문서 |
|---|---:|
| 민족문화대백과 `aks` | 211 |
| 한국사연대기 `nikh` | 180 |
| 한국어 위키백과 `kowiki` | 224 |
| **문서를 가진 사건** | **478** |
| 그중 '배경·원인·결과·영향·의의' 절이 있는 사건 | 201 |

`histgraph causes --dry-run` — 문서 478건, 한 건에 7,000자(배경·결과 절을
먼저 담고 남는 자리에 나머지). 임진왜란·병자호란·정묘호란·인조반정·위화도
회군이 그 안에 있다.

### 3.3 안전장치 — 추출의 일곱에 넷을 더한다

추출 안전장치(근거검증·방향교정·별칭해소·족보제거·소유격오독·소실문형·참여연대)
는 그대로 켠 채로:

1. **근거 구절이 준 문단에 실제로 있어야 한다.** 문장 단위로 되살린다 — 역접
   어미에서 끊긴 인용은 뜻이 뒤집힌다.
2. **양끝이 이미 있는 노드로 풀려야 적는다.** 여기서 노드를 만들기 시작하면
   '명나라의 쇠퇴'·'민심의 이반' 같은 서술구가 노드가 된다. 못 푼 이름은
   세어서 보고한다 — 그 목록이 다음에 만들 노드의 후보다.
3. **연대가 순방향이어야 한다.** 원인의 해가 결과의 해보다 늦으면 버린다.
   모델은 "A 의 배경에 B 가 있다"를 "A 가 B 의 배경"으로 뒤집기도 하는데,
   스키마는 방향을 강제하지 못하고 연대는 한다.
4. **구조화 소스가 반대 방향을 알고 있으면 추출본을 버린다.**

### 3.4 읽기

`chain`(한 노드의 원인·결과 나무)과 `paths`(두 노드 사이의 최단 인과 경로).
`/api/chain`·`/api/path`·`histgraph chain 임진왜란 --to 병자호란`.

### 3.5 판정

| 재는 것 | 기준 |
|---|---:|
| 화면 사건(488) 중 원인 또는 결과를 하나 이상 가진 비율 | ≥ 60% |
| 교과서 사슬 10개 (임진왜란→병자호란, 갑신정변→갑오개혁, 을미사변→아관파천, 3·1운동→임시정부 …) 중 경로가 있는 것 | ≥ 7 |
| 표본 30건 수동 검사 정밀도 | ≥ 0.9 |
| `props.how` 가 빈 엣지 | 0 |

**총계로 재지 않는다.** `caused` 가 2,000건이라도 그 절반이 '제2차 세계 대전 →
소련'이면 그물망이 아니다(§4-2).

### 3.6 지금 상태

`src/histgraph/causes.py`·`caused` 타입·`/api/chain` 은 2026-09-04 에 만들어져
있고 **아직 커밋·테스트 전이다** (다른 세션이 작업 중). 추출은 돌리지 않았다.
화면(`web/`)에는 인과를 그리는 코드가 없다.

---

## 4. 그물망을 짤 때 지키는 것

이 저장소가 지금까지 틀리고 고치며 얻은 원칙이다. 층이 바뀌어도 같다.

1. **개체는 수집으로, 관계는 추출로.** 소스가 관계를 줄 것이라 기대하고 기다리지
   않는다. 사실층(참여 P1344 35건)·콘텐츠층(주제 P921 2%)·인과층(16건) 셋 다
   같았다.
2. **총계가 아니라 대상의 분포로 잰다.** 참여 5,791건의 90.5%가 올림픽이었고,
   `depicts` 157건 중 사건은 6건이었다. 엣지 수가 갑자기 늘면 먼저 의심한다.
3. **모르면 낮은 자리로 떨어뜨린다.** 타입을 모르는 대상은 `event` 가 아니라
   `concept` 다. 개념은 사건보다 회수하기 쉽다. 기본값이 `event` 인 코드는 없다.
4. **방향은 스키마가 못 잡고 연대가 잡는다.** 참여·인과 모두 연도로 검사한다.
   연대가 틀린 것으로 보이는 목록은 지우지 않고 **틀린 날짜를 찾는 창**으로 읽는다.
5. **추출은 노드를 만들지 않는다.** 서술구는 노드가 아니다. 새 노드는 `promote`
   의 관문을 지나서만 생긴다.
6. **엣지마다 근거와 신뢰도.** 구조화 소스 1.0, 산문 추론은 그 미만. 근거 인용이
   원문에 없으면 버린다.
7. **합치고 가르는 것은 점수가 아니라 규칙과 표.** `dedupe` 는 후보만 찾고
   `data/duplicates.tsv` 가 판정한다(후보 55쌍 중 23쌍이 다른 사건). `homonyms`
   는 출처 문서의 주인공만 고친다. 라벨 유사도로는 제1차·제2차 왕자의 난이
   하나가 된다.
8. **정본이 있다.** 말뭉치 우선순위는 민족문화대백과 → 한국사연대기 → 위키백과.
   충돌하면 국편이 맞다.
9. **화면은 한국어만, 출처는 적지 않는다.** `scope`·pre-push·CI 세 관문이 잰다.

---

## 5. 콘텐츠층 — 그물망에 작품이 걸리는 자리

작품은 그물망의 **한 층**이다. 세종을 누르면 『뿌리깊은 나무』가 나오고, 『한산』을
누르면 한산도 대첩과 이순신이 나오는 것은 인과 사슬 위에 작품이 걸려 있을 때
의미가 있다 — 『한산』에서 한산도 대첩으로, 거기서 임진왜란으로, 거기서
병자호란으로 갈 수 있어야 한다.

- 작품은 노드 타입 하나(`media`)에 `props.form`(영화·드라마·다큐·책·만화·게임·
  음악·무대)을 **반드시** 갖는다. 매체별로 타입을 쪼개면 엣지 정의가 여섯 배가
  되고 화면 색이 열다섯이 된다. 매체를 모르는 작품은 노드를 만들지 않는다.
- 작품에서 그물망으로 나가는 엣지는 넷 — 소재 `depicts`(실체에만), 주제
  `about`(개념에만), 배경 `set_in`, 원작 `adapted_from`. 배역·배우는 노드가
  아니라 `depicts` 의 라벨(`이순신 역`)이다.
- **발표연도와 배경연도는 다른 칸이다.** 『한산』은 2022년에 나왔고 1592년을
  다룬다. 연표는 기본적으로 배경(`set_in`)에 세운다.

실측 (2026-09-04): 작품 660편 · `depicts` 284 · `set_in` 345 · `about` 161.
작품 중 사실층으로 나가는 엣지를 가진 것 63%(목표 70%). 명단과 도입부
위키링크에서 얻을 수 있는 것은 다 얻었고, 남은 것은 산문 추출이다.
어떻게 여기까지 왔고 무엇이 함정이었는지는 부록 A.

---

## 6. 단계 — 각 단계는 명령 하나와 판정 하나를 갖는다

| 단계 | 무엇 | 명령 | 판정 | 상태 |
|---|---|---|---|---|
| 0 | 온톨로지 정리 — 개념 자리, 사건 기본값 제거 | `reclassify` | `depicts` 대상 중 사건·인물 아닌 것 0 | **끝 (09-04)** |
| 1 | 사실층 정본 — 국편·민백 말뭉치, 역할, 중복, 동명 | `nikh`·`corpus`·`roles`·`dedupe`·`homonyms` | 관문 셋 종료 코드 0 | 진행 중 |
| 2 | **인과층** — 산문에서 원인·결과 | `causes` → `chain` | §3.5 | **시작** |
| 3 | 콘텐츠층 — 명단, 도입부, 산문 | `works`·`works --harvest`·`extract --types media` | 사실층 엣지 보유 ≥ 70%, `depicts` ≥ 600 | 명단 끝 · 산문 남음 |
| 4 | 화면 — 사슬 보기 | `serve` | 아래 | 안 함 |
| 5 | 매체 확장 — 도서·게임·다큐·음악 | 국내 API 활용신청 | `form` 세 종 이상 두 자리 | 대기 |

**4단계 화면**이 이 문서의 그림을 사용자에게 보이는 자리다.

- 노드 패널에 **원인 / 결과** 줄. 선 위에 `props.how` 구절. 누르면 그쪽으로 뿌리가 옮겨 간다.
- 두 노드를 고르면 **사이의 인과 경로**를 그린다(`/api/path`).
- 사건·인물 패널에 '이 사건을 다룬 작품' 줄. 콘텐츠 토글(기본 켜짐).
- 연표의 두 축 — 기본은 배경, 토글로 발표연도.
- 판정: 화면 사건 10개를 눌러 원인·결과가 붙어 나오는 비율 ≥ 6/10, 작품이
  붙어 나오는 비율 ≥ 6/10.

순서는 **1 → 2 → 3** 이다. 인과 추출은 사실층의 노드에 양끝을 걸어야 하므로
(§3.3-2), 중복·동명이 정리되지 않은 노드에 걸면 엣지가 엉뚱한 노드에 붙는다.
콘텐츠층은 인과층과 독립이라 병행할 수 있다.

---

## 7. 하지 않을 것

- **`related_to` 로 잇지 않는다.** 뜻이 없는 엣지는 그물망이 아니라 실뭉치다.
  새 관계가 필요하면 타입을 만든다. 지금 2,285건은 갈라 낼 목록이다.
- **서술구를 노드로 만들지 않는다.** '민심의 이반'·'국력의 쇠퇴'는 개체가
  아니다. 어느 나라·어느 사건의 일인지 원문이 말하면 그 이름을 쓰고, 안 하면
  적지 않는다.
- **배우·감독·제작진 인물망.** 역사 그래프가 연예인 그래프가 된다 — 스포츠
  오염과 같은 실패다. 감독은 `created` 로 작품에만 붙인다.
- **점수로 합치지 않는다.** 판정은 표(`duplicates.tsv`·`ko_labels.tsv`)와 규칙이다.
- **인과를 배경지식으로 적지 않는다.** 원문에 서술된 것만. 모델이 아는
  인과라도 근거 인용이 없으면 버린다.
- 포스터·이미지(저작권), 평점·리뷰·흥행(사실이 아니다), OTT 링크(변한다),
  노드 타입 6종 분리(§5).

---

## 8. 정해야 할 것

1. **Wikidata 의 인과 22건을 둘 것인가.** '제2차 세계 대전 → 소련'은 P1542
   (결과)를 넓게 쓴 것이지 인과가 아니다. 제안: 양끝이 둘 다 사건인 짝만
   `caused` 로 남기고 나머지는 `related_to` 로 되돌린다. 산문 추출이 같은 짝을
   근거와 함께 다시 찾으면 그때 올라온다.
2. **결과 쪽에 인물을 허용할 것인가.** '갑신정변 → 김옥균의 망명'은 결과가
   인물이 아니라 인물의 사건이다. 제안: 허용하지 않는다. 필요하면 그 사건을
   노드로 세운다(`promote`).
3. **인과의 종류 넷이 충분한가.** '전제'·'반작용'이 나올 수 있다. 제안: 넷으로
   시작하고, 모델이 넷 중 고르지 못한 것이 쌓이면 그 목록을 보고 늘린다.
4. **외국 작품 범위** — 중국·일본 드라마가 한국사를 다루는 경우. 제안:
   `depicts` 대상이 우리 노드면 국적을 묻지 않는다.
5. **웹툰·뮤지컬·판소리** — `form` 에 넣을 것인가. 판소리는 `heritage` 와 겹친다.
6. **배경연도를 모르는 작품** — 제안: 연표에서 숨긴다. 화면이 단정하지 않는
   쪽이 이 저장소의 규약이다.

---

## 부록 A · 콘텐츠층은 어떻게 여기까지 왔나 (실측)

### A.1 개념이 사건 행세를 하고 있었다 (0단계, 끝)

`depicts` 157건이 가리키는 대상 라벨 분포 — 조직범죄 20, 보복 11, 연쇄살인범 7,
**6.25 전쟁 6**, 자살 5, 시간 여행 4 … 대상 타입은 `event` 151 · `person` 6 인데
그 151개 중 역사 사건은 6.25 전쟁뿐이었다. 원인은 한 줄 — `fetch_media` 가
P921(주제)의 값을 타입 모른 채 `event` 로 만들었다. `concept` 타입과 `about`
엣지를 만들고 `reclassify` 로 77개를 옮겼다(개념 71 · 인물 4 · 단체 1 · 시대 1).
결과 `depicts` 157 → 21 (전부 실체), `about` 0 → 135. 덤으로 이봉창·윤봉길·
이춘재·김헌창이 사건 노드였고 '진주성 전투'는 동음이의 문서였다.

### A.2 Wikidata 는 작품 개체는 주고 관계는 주지 않는다

한국 작품(P495=Q884) 중 주제(P921)를 가진 비율 — 영화 10,074편 중 2.2%,
드라마 4,374편 중 1.8%, 문학 138편 중 15%. 원작 P144 354건, 등장 P1441 73건.
음악·다큐는 0. **엣지는 수집이 아니라 추출로 얻는다** — 사실층과 같은 결론.
새 클래스 QID 는 반드시 `doctor` 로 라벨을 확인한다(Q184188 은 '프랑스의
캉통'이었다).

### A.3 명단은 위키백과 분류에 있었다 (1단계, 끝)

'분류:조선 역사 드라마' 143 · 고구려 8 · 신라 8 · 대한민국 8 · 백제 3 —
분류 115개에서 문서 599건. 분류 이름이 관계를 그대로 말한다: '이순신을 소재로
한'(`depicts`), '조선을 배경으로 한'(`set_in`), '조선 역사 드라마'(`form`).
분류명은 추측하지 않고 검색으로 확인한다('분류:한국의 사극'은 0건이었다).
결과 작품 96 → 638, `depicts` 21 → 183, `set_in` 0 → 327. `set_in` 47%
(목표 80%)는 배경을 말하는 분류가 나무의 절반에만 있어서다.

### A.4 인포박스는 소재를 적지 않는다 (2단계 앞부분, 끝)

도입부 위키링크에서 관문 셋(단서 어구 · 만든 사람 걸러내기 · 몰년)을 걸어 캤다.
발표일 109 → 382, 원작 12, `depicts` 184 → 284, `set_in` 327 → 345. 표본 30건
정밀도 30/30. 본문 전체로 규칙을 넓혀도 한 건도 늘지 않았다 — 단서 어구는
도입부에만 있고 줄거리는 관계를 단서 없이 적는다. 남은 것은 산문 추출이다.

### A.5 콘텐츠 고유의 안전장치 다섯

8. **배역·배우 차단** — '등장 인물' 절은 `enrich` 에서 잘라 낸다. 족보 목록과 같은
   함정이다. 승격 때 현대인(몰년·생년)은 버린다.
9. **개념은 사건이 아니다** — 확인 안 되면 `concept`.
10. **픽션 인물은 승격하지 않는다** — 작품 문서에만 나오는 이름은 노드가 아니다.
11. **동명 작품은 연도로 가른다** — 『춘향전』은 영화만 여럿. 라벨+`form`+발표연도.
12. **작품은 사실이 아니다** — `depicts` 신뢰도를 구조화 1.0 / 도입부 0.8 /
    줄거리 0.6 으로 갈라 적고, 고증 논란 서술은 `props` 에 남긴다.

### A.6 국내 소스

영화진흥위원회 KMDb·국립중앙도서관·문화포털은 데이터셋별 활용신청이 필요하다.
키가 유효해도 신청 전엔 인증 실패가 HTTP 200 으로 온다. 막힌 소스를 기다리며
멈추지 않는다.

---

## 부록 B · 손댈 파일

| 파일 | 무엇을 |
|---|---|
| `src/histgraph/ontology.py` | `caused` 엣지 (있음) · §8-2 결정에 따라 도착 타입 |
| `src/histgraph/causes.py` | 산문에서 인과 추출, `chain`·`paths` (있음, 커밋·테스트 전) |
| `src/histgraph/sources/wikidata.py` | P828/P1542 → `caused` (있음) · §8-1 결정에 따라 사건 짝만 |
| `src/histgraph/server.py` | `/api/chain`·`/api/path` (있음) |
| `web/relations.js`, `web/graph.js` | 원인·결과 줄, 선 위의 `how` 구절, 두 노드 사이 경로 |
| `web/timeline.js` | 배경·발표 두 축 |
| `src/histgraph/extract.py` · `promote.py` | 작품 문서용 프롬프트, 안전장치 8~12 |
| `tests/test_pipeline.py` | `causes` 안전장치 넷 (근거·기존 노드·연대·구조화 반대) |
| `src/histgraph/scope.py` | 파생본에 `caused` 를 실어 나른다 (지금 화면 DB 에 0건) |

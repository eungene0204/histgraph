# concept — 역사와 콘텐츠를 한 그래프에

역사적 사실(인물·사건·장소·시대·정체·조직)과 그것을 다룬 현대 콘텐츠
(영화·드라마·책·음악·다큐멘터리·게임)를 **같은 그래프 안에서** 오갈 수 있게
만든다. 세종을 누르면 『뿌리깊은 나무』가 나오고, 『한산』을 누르면 한산도
대첩과 이순신이 나오는 상태가 목표다.

이 문서는 그 목표를 **지금 저장소의 구조 위에서** 어떻게 이룰지를 정한다.
새 프로젝트를 그리지 않는다 — 이미 있는 파이프라인
(`ingest → resolve → enrich → extract → promote → serve`)에 무엇을 더하고
무엇을 고칠지만 적는다.

---

## 1. 지금 어디까지 와 있나

실측 (`data/histgraph.sqlite`, 2026-09-04):

| 노드 타입 | 개수 | | 엣지 타입 | 개수 |
|---|---:|---|---|---:|
| person 인물 | 29,651 | | dated_to 시점 | 31,675 |
| heritage 유물 | 3,204 | | born_in 출생지 | 6,955 |
| period 시대 | 2,525 | | held_position 직위 | 5,896 |
| place 장소 | 1,291 | | child_of 자녀 | 3,713 |
| event 사건 | 733 | | participated_in 참여 | 1,660 |
| role 직위 | 546 | | occurred_at 발생지 | 277 |
| org 단체·국가 | 400 | | **depicts 소재로 다룸** | **157** |
| **media 영화·드라마** | **96** | | created 제작 | 62 |
| artwork 예술작품 | 75 | | | |

사실층은 서 있다. **콘텐츠층은 사실상 비어 있다** — 작품 96편, 작품에서
역사로 나가는 엣지 157건. 게다가 그 157건이 대부분 역사가 아니다(§3).

---

## 2. 답해야 하는 질문은 셋뿐이다

1. **콘텐츠를 어떤 타입으로 앉히나** — 영화·드라마·책·음악·다큐·게임을
   노드 타입 6개로 쪼갤 것인가, 하나로 둘 것인가. (§4)
2. **콘텐츠와 사실을 잇는 엣지를 어디서 얻나** — 이 프로젝트의 원래 결론이
   "공공 API 는 개체를 주지만 관계를 주지 않는다"였다. 콘텐츠도 똑같은지
   측정한다. (§5, §6)
3. **틀린 연결을 어떻게 막나** — 배우를 역사 인물로, 주제어를 사건으로
   만들지 않는 규칙. (§7)

나머지(화면·순서)는 이 셋이 정해지면 따라온다.

---

## 3. 먼저 알아야 할 것 — 이미 들어온 96편은 역사를 다루고 있지 않았다

`depicts` 157건이 가리키는 대상의 라벨 분포(실측):

| 대상 | 엣지 수 | 이것은 |
|---|---:|---|
| 조직범죄 | 20 | 주제 |
| 보복 | 11 | 주제 |
| 연쇄살인범 | 7 | 주제 |
| **6.25 전쟁** | **6** | **사건** |
| 자살 | 5 | 주제 |
| 시간 여행 | 4 | 주제 |
| 초자연적 현상 · 성매매 · 사형 | 각 3 | 주제 |
| 테러리즘 · 처녀성 · 자주권 · 야구 · 하인 … | 각 2 | 주제 |

대상 타입은 `event` 151, `person` 6. **그런데 그 event 151개 중 실제 역사
사건은 6.25 전쟁 6건뿐이다.** 나머지는 전부 '자살'·'간통죄'·'조직범죄'
같은 **주제어**이고, 이것들이 지금 우리 그래프에 **사건 노드로 앉아 있다.**

원인은 한 줄이다 — `sources/wikidata.py:fetch_media` 가 P921(주제)의 값을
받아 타입을 모른 채 `event` 로 만들어 넣는다.

```python
# 소재의 실제 타입은 미확정 — 사건으로 가정하고, 다른 소스가 같은
# QID 를 더 정확한 타입으로 덮어쓰면 갱신된다.
nodes.setdefault(sid, Node(..., type="event", ...))
```

덮어쓸 다른 소스는 오지 않았다. 이것은 README 의 **스포츠 오염과 같은
모양**이다 — 엣지 총계(157)만 보면 콘텐츠가 역사에 붙어 있는 것 같지만,
**대상 노드의 라벨 분포를 보면 6건이다.**

> 콘텐츠층에서 되풀이하지 말 것: 작품 수와 depicts 수로 진척을 재지 않는다.
> **depicts 가 가리키는 대상 중 사건·인물의 비율**로 잰다.

---

## 4. 온톨로지 — 요청된 개체를 지금 스키마에 앉히기

### 4.1 대응표

| 요청된 개체 | 지금 타입 | 결정 |
|---|---|---|
| 인물 Person | `person` | 그대로 |
| 사건 Event | `event` | 그대로 |
| 장소 Location | `place` | 그대로 |
| 시간 Time | `period` + `dated_to` | 그대로 (`time:1592` 노드가 시간축) |
| 국가 State | `org` | 그대로 — 왕조=시대 이중성은 `from_period` 가 이미 처리 |
| 조직 Organization | `org` | 그대로 |
| 문화 Culture | **없음** | **`concept` 타입 신설** (§4.2) |
| 영화·드라마·책·음악·다큐·게임 | `media` | **`media` 유지 + `props.form` 서브타입** (§4.3) |

### 4.2 결정 1 — `concept`(개념·주제) 타입을 만든다

§3 이 보여주듯 지금 그래프에는 '개념'이 앉을 자리가 없어서 개념이 사건
행세를 하고 있다. 요청서의 '문화(Culture)'도 같은 자리를 필요로 한다 —
과거제·성리학·판소리는 사건도 조직도 유물도 아니다.

```python
"concept": "개념·주제",   # 사상·제도·풍습·장르·소재
```

- 새 엣지 `about`(주제): `media`·`artwork` → `concept`. P921 의 기본 착지점.
- `depicts`(소재로 다룸)는 **실체를 가리킬 때만** 쓴다 — 도착 타입은
  `person`·`event`·`place`·`org`.
- 이 하나로 §3 의 오염이 구조적으로 막힌다. **모르면 event 가 아니라
  concept 로 떨어뜨린다** — 개념은 사건보다 회수하기 쉽다.

### 4.3 결정 2 — 콘텐츠는 노드 타입 6개가 아니라 `media` + 서브타입

`props.form` ∈ `{film, series, documentary, book, music, game, comic, stage}`.
타입을 쪼개지 않는 이유:

- `EDGE_TYPES` 의 출발·도착 타입 목록이 여섯 배로 늘어난다. 지금 `depicts`
  한 줄로 끝나는 것이 여섯 줄이 되고, 새 엣지를 더할 때마다 여섯 번 적어야
  한다. 빠뜨린 하나가 곧 조용한 스키마 오류가 된다.
- 화면의 색이 9색에서 15색이 된다. README 는 이미 "색이 타입을 말한다"로
  정리했고 모양은 걷어냈다. 15색은 구분이 아니라 소음이다.
- 사용자가 실제로 하는 질의는 "이 사건을 다룬 **작품**"이지 "이 사건을 다룬
  **게임**"이 아니다. 매체별 필터는 화면의 토글로 충분하다.

대신 **`form` 은 비워둘 수 없게 한다.** `Node.__post_init__` 에서
`type == "media"` 인데 `props["form"]` 이 없으면 `OntologyError` 를 던진다.
설명(description)과 달리 이건 나중에 채울 수 있는 값이 아니다 —
수집하는 쪽이 반드시 알고 있는 값이고, 비면 화면에서 영영 구분이 안 된다.

### 4.4 결정 3 — 새 엣지 네 개

| 엣지 | 방향 | 뜻 | 근거 |
|---|---|---|---|
| `about` | `media·artwork → concept` | 주제·소재 | P921 의 대부분이 여기다 |
| `set_in` | `media·artwork → period·place` | **작품의 배경** 시대·장소 | 연표의 두 축(§8) |
| `adapted_from` | `media → media·artwork` | 원작 | Wikidata P144 실측 354건 |
| `part_of` | 기존 | 시리즈·시즌 | 이미 있음, 그대로 쓴다 |

`depicts` 는 기존 정의를 유지하되 도착 타입에서 개념을 몰아낸다.
**배역-배우는 넣지 않는다** — 배역명은 `depicts` 엣지의 `label` 에 적는다
(`label="이순신 역"`). 배우를 노드로 들이면 그래프가 역사망이 아니라
연예인망이 된다(§10).

### 4.5 발표연도와 배경연도는 다른 칸에 넣는다

`media` 노드의 `start_date` 는 **개봉·방영일**이다. 화면의 `_year_of` 는
`start_date` 를 먼저 보므로, 지금 구조에서 『한산』은 연표의 **2022년**에
선다 — 임진왜란에서 430년 떨어진 자리다.

- 발표연도: `start_date` + `dated_to`(label=`공개`) — 기존 규약 그대로
  (`dated_to` 는 이미 label 로 출생/사망/시작/종료를 가른다).
- 배경연도: `set_in` → `time:1592`. 연표는 **기본적으로 이쪽**을 쓴다.

---

## 5. 콘텐츠도 개체는 주고 관계는 주지 않는다 (실측)

Wikidata 에서 한국 작품(P495=Q884)을 세어 봤다:

| 매체 | 클래스 | 작품 수 | 그중 P921(주제) 보유 | 비율 |
|---|---|---:|---:|---:|
| 영화 | Q11424 | 10,074 | 226 | 2.2% |
| 텔레비전 시리즈 | Q5398426 | 4,374 | 78 | 1.8% |
| 문학작품 | Q7725634 | 138 | 21 | 15% |
| 비디오 게임 | Q7889 | 256 | 6 | 2.3% |
| 책 | Q571 | 7 | 2 | — |
| 음악작품 | Q2188189 | 0 | 0 | — |
| 다큐멘터리 영화 | Q93204 | 0 | 0 | — |

관계 속성은 더 얇다:

| 속성 | 한국 영화+TV 엣지 수 |
|---|---:|
| P144 원작 | 354 (영화 183, TV 171) |
| P1441 작품에 등장 | 73 |
| P674 등장인물 | 27 |
| P921 주제가 실존인물(Q5) | 23편 |

그리고 그 P921 440건이 가리키는 서로 다른 주제 252개 중 **우리 그래프에
이미 있는 것이 117개인데, 타입 분포가 event 95 / person 20 / org 1 /
place 1** — §3 에서 본 그 오염 그대로다.

> **결론: 사실층에서 얻은 결론이 콘텐츠층에서도 같다.** 작품 개체는
> 14,000건 넘게 있지만 역사와 잇는 엣지는 세 자리 수도 안 되고, 그나마도
> 대부분 주제어다. 음악·다큐는 Wikidata 에 아예 없다.
> **엣지는 수집이 아니라 추출로 얻는다.**

QID 는 위 표에 적힌 것만 검증됐다(라벨 대조 완료). 새 클래스를 더할 때는
**반드시 `doctor` 로 라벨을 확인한다** — 이 저장소에는 Q34049 를 고려로 알고
쓴 전력이 있다. (이번에도 확인 삼아 던진 Q184188 은 '프랑스의 캉통'이었다.)

---

## 6. 그러면 어디서 얻나 — 위키백과 분류가 명단이고, 산문이 엣지다

### 6.1 명단: 한국어 위키백과 분류 (실측)

| 분류 | 문서 수 |
|---|---:|
| 분류:조선 역사 드라마 | 143 |
| 분류:고구려 역사 드라마 | 8 |
| 분류:신라 역사 드라마 | 8 |
| 분류:대한민국의 역사 드라마 | 8 |
| 분류:백제 역사 드라마 | 3 |
| **합집합(중복 제거)** | **173** |

Wikidata 가 주제를 붙여 준 한국 드라마가 78편인데, **위키백과 분류는
드라마만으로 173편을 시대별로 갈라 준다.** 게다가 분류 자체가
`조선/고구려/신라/백제` 로 나뉘어 있어 **`set_in` 의 초기값을 분류에서
곧바로 얻는다.** 영화·게임·도서도 같은 방식으로 분류를 찾는다 —
단, **분류명은 추측하지 말고 검색으로 확인한다.** 이번에 '분류:한국의 사극',
'분류:대한민국의 텔레비전 사극' 은 넷 다 0건이었다. 실제로 존재하는 이름은
'분류:대한민국의 역사 드라마'였다.

### 6.2 엣지: 문서 인트로가 관계를 문장으로 말해 준다

```
《한산: 용의 출현》 … 김한민 감독의 《명량》의 차기작으로 명량대첩의 5년 전인
한산도 대첩을 모티브로 하여 …
《대조영》 … 고구려 말기부터 발해 건국까지 대조영의 생애를 다루었다.
《선덕여왕》 … 신라의 선덕여왕을 주인공으로 한 픽션이다.
```

세 문장에 `depicts`(한산도 대첩, 대조영, 선덕여왕), `set_in`(고구려 말기~발해),
`adapted_from`/`part_of`(명량의 차기작), 그리고 **'픽션'이라는 고증 신호**가
전부 들어 있다. `enrich` 가 이미 이 인트로를 가져오는 코드를 갖고 있다.

- **금맥 섹션**: '역사적 사실'(『대조영』에 있다), '줄거리', 인트로.
- **지뢰 섹션**: '등장 인물' — 배우 이름과 배역 이름이 섞인 목록이다.
  README 의 **"족보 목록은 모델을 망가뜨린다 — 지우고 넣는다"** 와 정확히
  같은 함정이다. 목록을 그대로 넣으면 배우가 역사 인물로 승격된다.
  **`enrich` 단계에서 잘라낸다.**

### 6.3 국내 소스는 나중에, 그리고 활용신청부터

영화진흥위원회 KMDb(영화 상세·줄거리), 국립중앙도서관(도서), 문화포털은
모두 **데이터셋별 활용신청**이 필요하다 — 키가 유효해도 신청 전에는
인증 실패가 HTTP 200 으로 돌아온다. 신청 상태를 `doctor` 에 항목으로 넣고,
승인 전까지는 위키백과 경로만으로 진행한다. **막힌 소스를 기다리며
멈추지 않는다.**

---

## 7. 콘텐츠 고유의 안전장치 — 기존 일곱에 다섯을 더한다

추출 안전장치(근거검증·방향교정·별칭해소·족보제거·소유격오독·소실문형·
참여연대)는 그대로 켠 채로:

8. **배역·배우 차단** — '등장 인물' 섹션은 추출 입력에서 제거.
   승격(`promote`) 단계에서 배우 이름 후보는 `wd:` 인물과 몰년/생년으로
   대조해 현대인이면 버린다.
9. **개념은 사건이 아니다** — 추출된 대상이 인물·사건으로 확인되지 않으면
   `concept` 로 떨어뜨린다. `event` 는 기본값이 아니다.
10. **픽션 인물은 승격하지 않는다** — 작품 문서에만 등장하고 사실층 어디에도
    없는 이름은 노드로 만들지 않는다. `promote` 의 네 관문에 '출처가 작품
    문서 하나뿐' 조건을 더한다.
11. **리메이크·동명 작품은 연도로 가른다** — 『춘향전』은 영화만 여럿이다.
    **라벨 유사도로 합치지 않는다**(제1차/제2차 요동정벌을 한 노드로 만든
    그 실패와 같은 자리다). 가르는 것은 점수가 아니라 규칙: 라벨+form+발표연도.
12. **작품은 사실이 아니다** — `depicts` 의 confidence 를 갈라 적는다.
    구조화 소스 1.0 / 인트로 문장 0.8 / 줄거리 추론 0.6. 문서에 '역사적 사실'
    또는 고증 논란 서술이 있으면 `props` 에 남겨 화면이 말할 수 있게 한다.

---

## 8. 화면 — 사실과 콘텐츠가 한 화면에 있으려면

- **사건 패널**: '이 사건을 다룬 작품' 줄을 붙인다(`depicts` 역방향).
  인물 패널도 같다.
- **콘텐츠 토글**: 기본은 켜짐. 끄면 `media` 노드와 그 엣지를 숨긴다.
  사실만 보고 싶은 사용자가 콘텐츠에 밀려 사실을 못 보면 안 된다.
- **연표의 두 축**: 기본은 배경(`set_in`), 토글로 발표연도.
  『한산』이 1592년 옆에 서는 것이 이 프로젝트의 그림이고, 2022년에 서는 것은
  다른 그림이다. 둘 다 맞지만 **기본값은 배경**이다.
- **색은 늘리지 않는다** — `media` 는 지금 색 그대로. `form` 은 라벨 옆
  작은 글자로만 구분한다.
- **화면에 영어와 출처를 쓰지 않는다** — 매체 이름도 `film` 이 아니라 '영화'.

---

## 9. 단계 — 각 단계는 명령 하나와 판정 하나를 갖는다

### 0단계 · 오염 청소 (선행, 다른 것보다 먼저) — **끝났다 (2026-09-04)**
`concept` 타입과 `about` 엣지 도입, `fetch_media` 의 `type="event"` 기본값 제거,
기존 event 노드 중 개념 재분류 (`reclassify`).
- **판정**: `depicts` 대상 중 `event`·`person` 비율 100%, 그중 실제 역사 사건이
  아닌 것 0건.
- **결과**: 노드 77개 재분류(개념 71 · 인물 4 · 단체 1 · 시대 1), 보류 11.
  `depicts` 157 → 21건으로 전부 실체(사건 13 · 인물 7 · 단체 1),
  `about` 0 → 135건, 어긋난 엣지 0건.
  판정 사다리와 만들며 틀린 것들은 README "개념이 사건 행세를 하고 있었다".
- **덤으로 드러난 것**: 이봉창·윤봉길·이춘재·김헌창이 **사건 노드**로 앉아
  있었고, '진주성 전투'는 실은 위키미디어 **동음이의어 문서**였다.
  후자는 지우는 판단이라 보류로 넘겼다.

### 1단계 · 작품 명단 — **끝났다 (2026-09-04). 판정 하나는 못 채웠다**
`histgraph works` — 위키백과 분류(§6.1)에서 작품 문서를 긁어 `media` 노드
생성, 분류 이름에서 `form`·`depicts`·`set_in` 을 얻는다.
- **판정**: `media` ≥ 300 → **638 ✓** · `form` 100% → **100% ✓** ·
  `set_in` ≥ 80% → **47% ✗**
- **결과**: 분류 115개에서 문서 599건(QID 98%), 작품 96 → 638개.
  `depicts` 21 → 183건, `set_in` 0 → 327건. **추출 없이** 얻은 엣지다.
- **명단만이 아니었다.** 분류 이름이 관계를 그대로 말한다 —
  '이순신을 소재로 한'(`depicts`), '조선을 배경으로 한'(`set_in`),
  '조선 역사 드라마'(`form`). 2단계에서 할 일로 적어 둔 것의 절반을
  분류가 이미 해 주었다.
- **`set_in` 47%의 이유**: 80%는 재기 전에 적은 숫자였다. 배경을 말하는
  분류가 나무의 절반쯤에만 있고, Wikidata 는 이 작품들에 아무것도 주지
  않는다(같은 592편에 P840 0건 · P144 0건 · P921 3건). 소재의 시대에서
  배경을 유도해도 22편밖에 늘지 않는다. **나머지는 2단계 산문에서 온다.**

### 2단계 · 산문에서 엣지
`enrich --types media`(등장인물 섹션 제거 포함) → `extract --types media` →
`promote`. 1단계를 마친 지금 출발점은 **작품 638편 중 380편(59%)이 이미
사실층으로 나가는 엣지를 갖고 있고, 260편은 하나도 없다.**
- **판정**: `media` 중 사실층 노드로 나가는 엣지를 가진 비율 ≥ 70%,
  표본 30건 수동 검사 정밀도 ≥ 0.9, `depicts` 총계 ≥ 600.
- **앞부분은 LLM 없이 끝냈다 (2026-09-04)** — 인포박스에 소재 칸이 없다는
  것을 확인하고, 도입부 위키링크에서 관문 셋(단서 어구·만든 사람 걸러내기·
  몰년)을 걸어 캤다. 발표일 109 → 382편, 원작 12건, `depicts` 184 → 284건,
  `set_in` 327 → 345건. **표본 30건 정밀도 30/30 ✓**
- **남은 기준은 산문 추출이 있어야 한다**: 사실층 엣지 보유 63%(목표 70%),
  `depicts` 284(목표 600). 본문 전체까지 규칙을 넓혀도 한 건도 늘지 않았다 —
  단서 어구는 도입부에만 있고 줄거리는 관계를 단서 없이 적는다.

### 3단계 · 화면
사건·인물 패널의 작품 줄, 콘텐츠 토글, 연표 두 축.
- **판정**: 조선 그래프에서 임의의 주요 사건 10개를 눌러 작품이 붙어 나오는
  비율 ≥ 6/10.

### 4단계 · 매체 확장
도서·게임·다큐·음악. 여기서 국내 API(§6.3) 승인 여부가 갈린다.
- **판정**: `form` 별 노드 수가 영화·드라마 외에 세 종 이상 두 자리.

각 단계는 앞 단계의 판정을 통과한 뒤에만 시작한다. 특히 **0단계를 건너뛰면
2단계의 정밀도 측정이 의미를 잃는다** — 이미 틀린 엣지가 섞인 채로 재기 때문.

---

## 10. 하지 않을 것

- **배우·감독·제작진 인물망** — 역사 그래프가 연예인 그래프가 된다.
  스포츠 오염(참여 엣지의 90.5%가 올림픽 출전)과 정확히 같은 실패다.
  감독은 `created` 로 작품에만 붙이고 인물망을 확장하지 않는다.
- **포스터·스틸 이미지** — 저작권. 화면은 텍스트와 그래프로 간다.
- **평점·리뷰·흥행 순위** — 사실이 아니고, 이 그래프가 답할 질문도 아니다.
- **OTT 시청 링크** — 자주 변하고 지역마다 다르다.
- **노드 타입 6종 분리** (§4.3).

---

## 11. 정해야 할 것

1. **외국 작품 범위** — 중국·일본 드라마가 한국사를 다루거나, 한국 작품이
   외국사를 다루는 경우. P495 로 한국을 거는 지금 방식은 전자를 놓친다.
   (제안: `depicts` 대상이 우리 그래프 노드면 국적을 묻지 않는다.)
2. **웹툰·뮤지컬·판소리** — `form` 에 넣을 것인가. 판소리는 `heritage` 와
   겹친다(『춘향가』는 무형유산이자 작품이다).
3. **배경연도를 모르는 작품** — 연표에서 숨길 것인가, 발표연도로 세울 것인가.
   (제안: 숨긴다. 화면이 단정하지 않는 쪽이 이 저장소의 규약이다.)

---

## 부록 · 손댈 파일

| 파일 | 무엇을 |
|---|---|
| `src/histgraph/ontology.py` | `concept` 타입, `about`·`set_in`·`adapted_from` 엣지, `form` 필수 검사 |
| `src/histgraph/sources/wikidata.py` | `fetch_media` 의 `type="event"` 기본값 제거, P144/P577/클래스 확장 |
| `src/histgraph/sources/wikipedia.py` | 분류 순회로 작품 명단, '등장 인물' 섹션 절단 |
| `src/histgraph/extract.py` | 작품 문서용 프롬프트와 관계 목록, 안전장치 8~12 |
| `src/histgraph/promote.py` | 배우·픽션 인물 차단 관문, 동명 작품 분리 규칙 |
| `src/histgraph/server.py` | 작품 줄, 콘텐츠 토글, `_year_of` 의 배경연도 우선 |
| `web/graph.js`, `web/timeline.js` | 표시와 두 축 |
| `src/histgraph/cli.py` | `ingest works`, 타입 한정 `enrich`/`extract` |

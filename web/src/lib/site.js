// 화면 바닥의 저작권 한 줄.
//
// **화면에 영어를 두지 않는 규칙(CLAUDE.md §1)의 예외다** — 저작권 표시는
// 나라를 가리지 않고 이 문구로 굳었으므로 옮기지 않는다 (2026-09-09 사용자
// 결정: "원문 그대로"). 같은 날 **어느 화면에나 같은 문장으로 선다**로 넓혔다
// ("© 2026 histgraph 왜 이거만 보이지? 뒷 문장 어디감?") — 전에는 정적 장
// (`/n/`)에만 전체 문장이 서고 그래프·내 역사 화면에는 앞머리만 있었다.
//
// 봐주는 글자는 **이 상수 하나에서만 온다.** 관문(web/tests/render.test.mjs)이
// 이 문장만 걷어내고 나머지 영어를 잡으므로, 문구를 늘리면 다시 걸린다.
// 정적 장의 같은 줄은 `src/histgraph/pages.py` 의 COPYRIGHT 다 — 두 상수는
// 글자 하나까지 같아야 하고 tests/test_pipeline.py 가 그것을 잰다.
export const COPYRIGHT = '© 2026 histgraph. All rights reserved.';

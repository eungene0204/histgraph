# 관계망 화면은 자바스크립트가 그려서 로봇이 읽을 것이 없다. 대신 글로 읽는
# 장(/인물/세종·/사건/임진왜란…)과 방침·약관은 서버가 글로 내주므로 그대로
# 읽힌다. 어귀는 /n/ 이고 거기서 갈래마다 목록으로 이어진다.
User-agent: *
Allow: /

# /api 는 화면이 쓰는 자리다. 사람이 읽을 글이 없고, 같은 자료가 글로 읽는
# 장에 이미 있다 — 로봇이 두 번 받아 갈 까닭이 없다.
Disallow: /api/

Sitemap: https://www.histgraph.space/sitemap.xml

# 인계 문서

새 세션이 맥락을 다시 파악하지 않고 바로 이어서 작업하기 위한 문서.
설계 배경과 실측 근거는 [README.md](README.md)에 있다. 여기는 **지금 상태와
다음에 할 일**만 적는다.

## 지금 상태 (2026-09-11)

```
화면 그래프   노드 14,198 · 엣지 36,565 · 별칭 6,635 (data/korea.sqlite ← 화면이 읽는 것)
시대 묶음     고조선~대한민국 (기원전 2333~) — `scope.BUNDLES.korea`, 시대 열둘
재위 띠       왕 212 (신라 56 · 고려 34 · 백제 32 · 조선 29 · 고구려 28 · 발해 15 ·
              가야 14 · 후백제 2 · 태봉 1 · 대한제국 2) · 대통령 14
말뭉치        문서 12,302 · 문단 56,234
화면          uv run histgraph serve  →  http://127.0.0.1:8100 (이 프로젝트 전용 포트)
테스트        1,707/1,707 통과   uv run tests/test_pipeline.py · 화면 (cd web && npm test)
환경          uv (Python 3.11) — `uv sync --extra mlx` 로 잡는다
```

환경은 uv 가 관리한다. `PYTHONPATH=src` 는 더 필요 없다 — 프로젝트가
편집 가능 설치로 들어가 있어서 `uv run histgraph …` 가 `src/` 를 바로 읽는다.
추출을 돌리려면 `--extra mlx` 가 있어야 한다 (없으면 `mlx_lm` 임포트에서
죽는다). 원격 추출은 `--extra anthropic`.

엣지 수가 줄어든 것은 **자기순환 1,258건을 지웠기 때문**이다. `timeline`
이 연도 노드를 자기 자신에 잇고 있었다 (`time:1443 → time:1443`).
양쪽 원인(timeline·extract)을 다 막았으므로 다시 생기지 않는다.

수집 소스별 산출:

| 소스 | 인증 | 산출 |
|---|---|---|
| 국가유산청 | 불필요 | 유물 4,647 · 산문 2,903건 |
| Wikidata SPARQL | 불필요 | 인물 28,897 · 관계 골격 · 별칭 4,962 |
| 한국어 위키백과 | 불필요(UA 필수) | 서사 1,327건 · 사건 103건 · 인포박스 관계 676건 |
| LLM 추출 (로컬 MLX) | 키 불필요 | 관계 965건 (participated_in 454) |
| 승격 보강 (`promote`) | 불필요 | 신규 노드 69 · 관계 189건 |
| 공공데이터포털 / 문화광장 | **활용신청 대기** | 0 |

## 2026-09-13 — 빈 사람의 '내 역사' 단추에 말풍선을 세운다

지시: "내 역사가 비어있는 사람들에게 [내 역사] 버튼에 버블 상자를 만들어서
'내 역사를 입력해 보세요' 라고 … 입력을 유도 해줘."

2026-09-09 에 세운 빛(`.era.beckon`)은 '여기를 보라'까지고 **무엇을 하라는
말이 없었다** — 그 말은 `title` 에만 있어 마우스를 올려야 뜬다 (손가락으로
보는 사람에게는 없는 말이다). 단추 아래 한 줄을 세웠다.

- 관문은 빛과 같은 `lifeEmpty` 하나다 — `null`(아직 안 읽었다)에는 아무 말도
  안 세운다. 로그인 전에도 선다 (09-09 결정: "로그인 안 했을때도 똑같은 효과").
- 링크 **안에** 두어 말풍선을 눌러도 같은 데로 간다. 읽어 주는 기계에는
  `title` 이 같은 말을 하므로 `aria-hidden` 으로 감춘다.
- 자리는 절대 자리라 머리 줄이 안 밀린다(빛과 같은 규칙). 나올 때 한 번
  밝아지고 멈춘다 — 머리 줄에서 계속 움직이는 것이 둘이면 글자를 방해한다.
- 바탕은 화면을 뒤집은 무채색이다 (어두운 화면은 흰 종이, 밝은 화면은 검은
  종이). 강조색 보라의 뜻('누를 수 있는 것')을 빌리지 않는다.

`web/src/App.jsx` · `web/style.css` (`.era .life-nudge`) · 검사 다섯
(`web/tests/render.test.mjs`). 5173 과 8100 둘 다 CDP 로 봤다.

## 2026-09-12 — 기원전 18 ~ 서기 42 (세 번째 빈 구간)

지적: "이 기간에 주요 사건이 없나?" — 백제 건국(기원전 18)과 가야 건국(42)
사이다. 셋을 세웠다: 국내성 천도(3) · 고구려의 부여 정벌(22) · 낙랑 정복(32).
셋 다 민백에만 항목이 있다 (「국내성천도」·「대소」·「괴유」·「호동왕자」).

**이름에 판정을 담지 않았다.** 32년의 '낙랑'을 한나라 군현으로 볼지 따로 선
나라로 볼지는 학설이 갈린다 — 민백이 둘 다 적으므로 이름은 '낙랑 정복'으로
두고 설명에 두 견해를 다 적었다. 낙랑군 노드(313년에 축출된 그 군현)에는
잇지 않았다.

곁가지로 **호동의 날짜**를 고쳤다. Wikidata 가 생년을 50년으로 주는데 그가
죽은 해(32)보다 뒤다. 위키백과 도입부와 민백이 '? ~ 32년'이라 적으므로
생년은 비우고 몰년을 32년으로 적었다 (편집 계층).

## 2026-09-12 — 1~2세기 (42~172년 사이가 비어 있었다)

지적: "이 시절 사건을 추가해줘" — 가야 건국(42)과 좌원 전투(172) 사이에
사건이 하나도 없었다. 다섯을 `data/events.tsv` 에 세웠다: 동옥저 정벌(56) ·
황산진구 전투(77) · 황산하 전투(115) · 한나라의 고구려 침공 (121년) ·
진대법 시행(194).

**이 시대는 민백에서 왔다.** 국편 연대기는 「태조대왕」 하나뿐이고, 신라와
가야가 낙동강 하류에서 싸운 두 전투는 민백에만 항목이 있다. 민백 항목은
`aks` 가 **이미 선 노드에 글을 붙이는** 자리라 노드로 세우는 길이 없어,
문서를 받아 읽고 표에 적었다 (`aks.fetch_article` — 한 쪽 2초).

이름에 해를 적은 것은 하나다 — 고구려와 한나라는 105·118·121·146·168·172년
에 거듭 싸워서 '한나라의 고구려 침공'만으로는 어느 것인지 모른다 (`황산 전투
(936년)` 과 같은 꼴).

**을파소**(고구려 국상, 진대법의 주역)는 그래프에 있는데 화면에 없었다 —
앞 절에 적은 고대 항목의 시대 문제다. 편집 계층에 `props.polity=고구려` 를
적어 살렸다. 같은 자리에 아직 78개가 남아 있다.

## 2026-09-12 — 나라의 끝을 연표에 세운다 (멸망 4건 → 12건)

지적: "각 나라의 멸망 년도가 빠졌어." 연표에 서 있던 끝은 넷뿐이었다 —
백제(660) · 금관가야(532) · 발해(926) · 한일병합(1910). 나머지는 **나라
노드의 `end_date` 로만** 있고 사건으로는 없었다. 노드의 끝 날짜는 그 노드를
열어야 보이고 연표에는 눈금이 서지 않는다.

아홉을 `data/events.tsv` 에 세웠다: 고조선(기원전 108) · 부여(494) ·
대가야(562) · 고구려(668) · 태봉(918) · 신라(935) · 후백제(936) ·
고려(1392) · 대한제국 선포(1897). 마지막 것만 멸망이 아니라 이름이 바뀐
것이라 그렇게 적었다 — 조선의 끝이 거기다. 근거는 모두 국편 우리역사넷의
연대기(위만·보장왕·진흥왕·경순왕·궁예·견훤·태조 이성계·고종)이고 부여만
민백 정의다. 위키백과에는 '고구려의 멸망'이 고구려-당 전쟁으로 넘어가고
나머지는 문서 자체가 없다 (전부 조회로 확인).

표가 두 가지를 못 하고 있었다.

- **기원전을 못 적었다.** 날짜 칸이 `^\d{3,4}` 라 고조선의 끝을 적을 수
  없었다. `기원전 108` 로 적으면 `_iso` 가 XSD 셈법(`-0107`)으로 옮긴다 —
  사람이 적는 표에 `-0107` 을 쓰게 하지 않는다.
- **해가 네 자리가 아니었다.** `494`·`918` 을 그대로 넣고 있어서, 같은 칸을
  문자열로 견주는 자리마다 494년이 660년보다 뒤였다 (기원전이 뒤집히는 것과
  같은 함정). 이제 `0494` 로 채워 넣는다. 옛 줄도 `overrides --reapply` 로
  함께 고쳤다.

`시대` 칸에 **대한제국은 없다** — `scope.ERAS` 에 그 이름의 시대가 없고
조선의 `successor_events` 로 담긴다. 테스트가 이것을 잡았다.

결과: 화면 DB 의 멸망·끝 사건 12건. 연표에서 고조선 멸망(기원전 108)부터
대한제국 선포(1897)까지 나라마다 끝이 선다.

## 2026-09-12 — 3~4세기가 비어 있었다 (사건 3건 → 15건)

지적: 연표의 214~371년 구간에 왕의 재위 띠만 서고 사건이 하나도 없었다 —
"여기 사건이 없는데 데이터를 더 찾아봐."

세어 보니 맞았다. 화면 DB 의 200년대 사건이 **0건**, 300년대가 2건이었다.
채운 길이 셋이다.

**하나, 위키백과에 문서가 있는데 시드 표에 없던 것** (`EVENT_SEEDS`). 이
시대는 사건 문서가 드물어 **분류를 훑어 찾았다** — 분류:고구려가 참전한
전쟁·분류:242년 한국 따위. 다섯을 더했다 (고구려-위 전쟁 242 · 공손연의 난
238 · 고구려-백제 전쟁 369 · 고구려-왜 전쟁 391 · 광개토대왕의 전쟁 392)
그리고 백제의 멸망(660). '발기의 난'은 인물 문서로 넘어가 뺐다 (이자겸의
난과 같은 함정). 시드는 날짜를 안 들고 오므로 `infobox --types event` 로
인포박스의 기간을 채웠다.

**둘, 어느 문서도 항목으로 갖고 있지 않은 것** (`data/events.tsv`). 정본이
임금의 연대기 안에 문장으로만 적어 둔 일 넷이다 — 낙랑군 축출(313) ·
전연의 고구려 침입(342) · 치양 전투(369) · 태학 설립(372). 근거는
국편 우리역사넷 「미천왕」·「고국원왕」·「소수림왕」이고, 그 글이 `data/raw/
nikh/yeondaegi.xlsx` 에 이미 있어 네트워크 없이 읽었다.

**셋, 이미 그래프에 있는데 화면에 못 들던 것.** 포상팔국의 난(209)은
원본에 설명 4,377자와 날짜까지 갖고 있었는데 파생본에 없었다. 국편 연대기의
**고대 항목은 시대 자릿수로 나라를 못 가려**(`nikh.ERA_DIGIT` 의 0·1)
`props.seed_era` 가 비고, 그래서 `scope` 의 씨앗이 되지 못한다. 본문이
말하는 나라를 편집 계층에 적어 두 건을 살렸다 (포상팔국의 난 → 가야,
정사암 회의 → 백제). **같은 까닭으로 아직 못 드는 고대 항목이 79개 더
있다** (유적 67 · 인물 7 · 단체 4 · 개념 1). 인물 쪽은 대개 wd 노드와
겹치므로 시대를 주기 전에 `dedupe` 로 봐야 한다.

고친 날짜 하나: 고구려-백제 전쟁은 인포박스가 244년이라 적는데 **문서 본문도
정본도 369년 치양에서 시작한다.** 편집 계층에 369 로 적었다.

결과 (화면 DB, 150~500년): 사건 3건 → 15건. 214~371 구간에 일곱이 섰다.
`tests/test_pipeline.py` 1,709 통과 · `check_korean` 통과 · `dedupe --type
event` 후보 27쌍 전부 판정 있음.

## 2026-09-12 — 연표의 기원전은 '전'으로 줄이지 않는다

지적: "고조선 건국을 전2333 이라고 했는데 기원전을 말한거지? 그냥 '기원전'
이라고 써줘." 연표의 연도 칸만 접두어를 줄여 쓰고 있었다 (`shortYear`) —
그래프·인과 도면(`graph-view.yearOf`)과 상세·정적 장(`relations.fmtDate`·
`pages`)은 이미 '기원전 2333' 으로 적고 있었으므로 한 자리만 딴말을 한 셈이다.

줄여 쓴 까닭은 자리였다: 연도 칸은 38px 이고 그 오른쪽 8px 뒤에 세로축이
선다 (`AXIS_X`). '기원전 2333' 은 한 줄로 59px 이라 안 든다. 칸을 넓히면
축과 이름이 다 밀리므로 **접두어만 숫자 위에 올렸다** (`yearHtml` →
`.tl-bce`, 8.5px). 숫자는 제자리에 남아 오른쪽 정렬도 이름과의 기준선도
그대로다. 한 줄이 먹는 높이는 30px(`GAP`)이라 위 줄과 닿지 않는다.

재위 띠의 두 해는 **앞에서 한 번만** 적는다 (`yearRange`) — '기원전
220~194'. 두 번 적으면 같은 말을 되풀이하고 70px 칸을 넘긴다. 준왕의 띠만
두 줄로 접히고, 그 위아래 라벨과는 266px 떨어져 있다.

개인 역사 연표도 같은 함수를 쓴다 (`life.js` 의 세 자리). 테스트 둘
(`layout.test.mjs` — 칸의 글자, 접두어를 따로 올리는 것). CDP 로 5173·8100
둘 다에서 봤다: 고조선건국 위에 '기원전', 아래에 '2333'.

## 2026-09-11 — 고조선부터 후삼국까지 (`scope korea` = 고조선~대한민국)

사용자 요청: "고조선 부터 후삼국시대 까지 역사를 추가하자, 필요한 자료를 찾아서
역사를 추가해줘". 묶음의 바닥이 918년에서 **기원전 2333년**으로 내려갔다.
설계 배경과 실측은 README "고대를 넣으며 배운 것" 절에 적었다. 여기는 무엇을
어디에 넣었는지만 적는다.

### 시대 (`scope.ERAS`·`BUNDLES`)

여섯을 새로 두고 셋(고구려·백제·신라)은 있던 것을 손봤다.

| 키 | 이름 | 정체 | 나라인가 |
|---|---|---|---|
| `gojoseon` | 고조선 | Q28405 | 예 (기원전 2333~108) |
| `wonsamguk` | 원삼국 시대 | Q716528 | 아니오 — 부여·옥저·동예·삼한이 사는 자리 |
| `gaya` | 가야 | Q28084 | 예 (42~562, 금관가야·대가야를 함께) |
| `balhae` | 발해 | Q28322 | 예 (698~926) |
| `husamguk` | 후삼국 시대 | Q698268 | 아니오 — 후백제·태봉이 사는 자리 |

`silla` 에 `person_polities=["신라","통일신라"]` 를 주었다 — 통일신라는 시대가
아니라 신라의 뒷부분이라, 안 담으면 668년 뒤가 통째로 빈다 (조선과 대한제국의
관계와 같다).

### 시드 (`sources/wikipedia.py`)

문서명은 **전부 조회로 확인했다** (넘겨주기 포함). 사건 24 · 단체 31 · 개념 20 ·
임금 148. 인물 문서로 넘어가는 이름은 뺐다 — '김헌창의 난'·'장보고의 난'이
인물로 가고, 있던 표에 섞여 있던 '장보고'(인물)도 사건 표에서 걷어냈다.
문서가 없는 것도 뺐다: 평양 천도·웅진 천도·포상팔국의 난·적고적의 난.

임금 라벨은 **대개 문서명 그대로다**. 삼국의 시호는 묘호와 달리 거의 안 겹쳐서,
고려·조선처럼 왕조를 앞에 붙이면 읽기만 길어진다. 겹치는 것만 가른다 —
백제·발해의 성왕·무왕, 발해의 문왕·강왕·정왕·희왕·간왕·선왕·고왕, 고구려
태조대왕(고려·조선 태조와 헷갈린다), 신라 선덕왕(선덕여왕과 다른 사람이다).

### 고친 곳 (고대를 넣어야 드러난 것들)

- `timeline.bce_text` — **기원전은 XSD 셈법이라 한 해 어긋난다**. `-0036` 이
  기원전 37년이다. 적는 자리 다섯을 옮겼고(파이썬 둘·화면 넷) 셈하는 쪽은
  그대로 뒀다. 시험도 그만큼 고쳤다 (`전57`→`전58`).
- `timeline._year_of` — 기원전 상한을 5000년으로. `2100` 에 걸려 고조선의
  `-2332` 를 버리고 있었다.
- `wikidata.spans_from_rows` — 시작·끝을 해로 견준다. 문자열로 견주어
  고조선·위만조선의 존속 기간을 버리고 있었다.
- `infobox.infobox_date`·`_reign_point` — '기원전 109년'을 서기 109년으로 읽던
  것, 한두 자리 해(온조왕 18년·수로왕 42년)를 안 받던 것.
- `overrides` 에 엣지의 `moved_to` 를 두었다 (노드의 `merged_into` 와 같은 말).
  `positions` 가 옮긴 자리를 `deleted` 로 적어, 다시 수집한 재위 날짜가 그
  자리에 못 닿고 있었다 (임금 15명의 띠가 비어 있었다).
- `positions` — 일반 자리가 하나 더 있었다(`wd:Q12097` '왕'). 시대 엣지가 둘
  이상이면 국적(`props.polity`)으로 가르고, 통일신라·금관가야·대가야는 같은
  왕위로 본다 (`SEAT_MERGE`).
- `reclassify` — 연호(Q698350)는 사건이 아니라 `period` 다.
- `cmd_scope` — 주소를 원본에서 짓는 자리가 빠져 있어 `scope` 가 끝에서 죽었다
  (`minted` 미정의). 뽑기 **전에** 원본에서 한 번 짓는다.
- `infobox.ingest` — SQL 자리표 변수가 인포박스 표식 사전을 덮어써 죽고 있었다.

### 표에 적은 판정

- `data/duplicates.tsv` — 고대 인물 여섯 쌍(진흥왕·수로왕·헌덕왕·위만·원측·
  광개토대왕)과 사건 넷. 국편 노드와 Wikidata 노드가 같은 사람을 둘로 세우고
  있었다.
- `data/roles.tsv` — 새로 들어온 참여 16건 (삼별초의 난 다섯 · 민무구의 옥 둘 ·
  기축옥사 · 인조반정 둘 · 이괄의 난 · 나주괘서사건 · YS-11기 납북 · 민청학련 ·
  훈련병 사망 · 요동 정벌의 광개토대왕).
- `data/chronology.tsv` — 고조선-한 전쟁의 날짜(기원전 109~108)와 거짓 인과 셋.
- `data/terms.tsv` — 문무왕 (인포박스가 재위의 시작을 `?` 로 적어 두었다).
- `data/ko_labels.tsv` — 가야 멸망 · 가야와 고대 일본의 관계 · 정개 둘(후백제·태봉).

### 남은 것

- **고대 인물 260명이 화면에 없다.** Wikidata 가 관계를 하나도 안 준 사람들이라
  `scope` 가 고립으로 뺀다 (아도·왕산악·이문진·의연 — 설명은 있다). 관계는
  산문에 있으므로 `corpus`(민백 고대 항목 6,700건) → `extract` 로 캐야 한다.
- 삼국사기·삼국유사 원문이 `data/raw/` 에 없다. 있으면 `founding` 처럼 정본
  기사로 날짜를 세울 수 있다 (지금은 고려사·실록뿐).
- 고대 사건은 39건이다. 그중 열하나는 `data/events.tsv` 로 세운 것이다 —
  평양 천도·한성 함락·사비 천도·고구려의 불교 수용·신라의 불교 공인·우산국
  복속·금관가야 멸망·발해 건국·발해 멸망·후백제 건국·후고구려 건국. 교과서가
  시대의 마디로 적는 일인데 **어느 자료도 항목으로 갖고 있지 않아서**(위키백과
  문서가 없고 민백·국편에도 항목이 없다) 정본의 문장을 근거로 세웠다. 더 세울
  것이 있으면 같은 자리다 — 포상팔국의 난·대가야 멸망(562)이 남아 있다.
- **`가야 멸망`(wd:Q5528735, 562년) 노드에 설명이 없어 화면에서 빠진다.** 이름만
  한국어로 적어 두었다. 한국어 위키백과에 문서가 없고 민백에도 그 이름의 항목이
  없다 — 사람이 적을 자리(`events.tsv`)는 **새 노드만** 세우므로 이 노드에는
  못 쓴다. 같은 일을 다른 이름으로 또 세우면 §1-4 를 어긴다.

## 2026-09-11 — 검색에 잡히는 주소 (`/인물/조선-세종`)

사용자 요청: 모든 개체가 검색 가능한 SEO 페이지를 갖게 할 것. 프롬프트는
Next.js(App Router·`generateMetadata`·`app/sitemap.ts`)를 전제했지만 이
프로젝트는 파이썬 표준 라이브러리 HTTP + Vite/React 다. **스택을 갈지 않고
같은 것을 지금 자리에 만들었다** (사용자 결정) — 서버가 이미 장을 글로
찍어내고 있어 SSR 이익은 같고, 배포·API·로그인·개인 역사를 건드리지 않는다.

### 주소 (`src/histgraph/slugs.py` · 새 파일)

`/n/wd:Q12345` → `/인물/조선-세종`. **한글 슬러그** (사용자 결정) — 주소창도
사람이 읽는 자리라 §1 이 걸리고, 로마자 표기에는 정본이 없다(이정 李霆/李貞).

- `slugs` 표에 쌓기만 한다. 이름·타입이 바뀌면 새 줄을 얹고 옛 줄은
  `current=0` 으로 남아 301 로 새 주소를 가리킨다. **옛 `/n/<id>` 도 301** —
  색인에 올라 있는 주소를 끊지 않는다. 주소를 아직 못 받은 노드는 옛 주소로
  그대로 열린다 (301 → 404 고리를 막는다).
- 겹치면 연대로 가른다(`강순-1898년`). 연대가 없으면 id 로 짧은 표(61건).
  실측 12,399개 중 덧붙인 것은 185개뿐.
- 매체는 갈래가 주소다 — `/영화/`·`/드라마/`·`/책/`·`/게임/` (타입은 그대로
  `media` 하나다. 온톨로지는 안 쪼갰다).
- **원본에서 짓고 파생본이 물려받는다.** `scope` 가 스스로 한다. 파생본만
  맞출 때는 `slugs --inherit data/histgraph.sqlite`.

### 장 (`src/histgraph/pages.py`)

이름·갈래·시기 → 이 사이트의 말·요약 → **주요 사실**(시대·소재지·출생지·
직위·소속처럼 속성으로 읽히는 관계를 앞의 표로 올렸다) → **연표**(연도를
아는 이웃) → **이어진 것**(상대의 갈래로 먼저 가르고 관계 이름으로 묶는다).
`/n/` 은 어귀로 남고 갈래 목록 `/인물/`(한 쪽 100장, `?p=2`)이 새로 섰다.

로봇에게만 하는 말은 `<script>`·`<meta>` 안에만 있다 — Schema.org
(`@graph` = WebPage + Person·Event·Place·Organization·CreativeWork·Movie·
TVSeries·Book·VideoGame·DefinedTerm), `BreadcrumbList`, `canonical`,
`og:*`·`twitter:*`, 여는 그림(`web/public/og.png`, `tools/make_og.py` 가
표준 라이브러리로 그린다). 유산은 국가유산청 사진을 https 로 고쳐 쓴다.

`/sitemap.xml` 은 갈래마다 파일 하나를 드는 목록이다. **색인에 올리는 문턱은
그대로** (요약 120자 + 관계 3건) — 12,399장 중 3,123장이 넘는다.

### 남은 것

- **배포에서 rewrite 를 확인할 것.** `vercel.json` 에 `/:segment/:slug`
  꼴의 넓은 규칙 셋을 더했다. 정적 파일이 먼저 걸리는 순서(filesystem →
  rewrites)에 기대고 있다 — 첫 배포 뒤 `/privacy.html`·`/assets/*` 가
  그대로 열리는지, `/인물/조선-세종` 이 200 인지 한 번 본다.
- 서치 콘솔에 새 사이트맵을 다시 낸다. `HISTGRAPH_SITE_VERIFICATION` 을
  넣으면 정적 장과 리액트 화면 양쪽에 주인 확인표가 붙는다 (안 넣어도 이미
  GA4 로 확인된다).
- 유산 사진을 **장 안에** 세울지는 아직 안 정했다 (지금은 링크 펼침용
  `og:image` 에만 쓴다). 남의 서버 그림을 광고가 붙은 장에 거는 일이라
  2026-09-05 애드센스 건과 같은 자리에서 판단해야 한다.

## 2026-09-11 — 자료에 항목이 없는 일 (`events --table` · 프로야구 개막)

사용자: "현대사 역사에 프로야구 개막이 없네? 추가 해줘." **없는 것이 맞았고,
빠뜨린 것이 아니라 담을 자리가 없었다.**

- 사건 시드(`wikipedia.EVENT_SEEDS`)는 **문서 이름을 적는 표**다. 위키백과에는
  `KBO 리그`(단체)와 `1982년 한국프로야구`(시즌)뿐이고, 시즌 문서는 스포츠
  클래스라 `prune` 이 지운다. 시드로는 1982년 3월 27일에 선 일을 못 담는다.
- 민족문화대백과에는 글이 있다 (`E0060462`). 그런데 항목이 '프로야구'(개념)라
  **개막이 항목으로 서지 않는다.** 개막일·개막경기(MBC 청룡·삼성 라이온즈)·
  전두환 시구·창립총회(1981-12-11)·여섯 팀·초대 총재 서종철이 전부 그 글의
  문장으로만 있다. 정본이 문장으로는 적었는데 항목으로는 없는 경우다.

그래서 마지막 자리를 하나 만들었다 — `data/events.tsv` + `handmade.py`,
`uv run histgraph events --table` (네트워크 불필요, 편집 계층에 적는다).
표 한 줄이 노드 하나와 관계 몇을 세운다:

    이름<TAB>날짜<TAB>시대<TAB>관계:대상 id=근거;…<TAB>설명<TAB>근거

관문은 넷이다. (1) 이름·설명에 한글이 있어야 한다 (§1) — 설명이 비면 `scope`
가 뺀다 (§1-3). (2) 근거 칸의 **주소**가 곧 화면의 출처 한 줄이다
(`provenance` 가 민백·국편·위키백과를 주소로 가른다) — 그래서 설명 안에는
자료 이름을 적지 않는다. (3) 관계마다 제 근거를 한국어로 적는다 — 화면이 그
관계의 근거로 그대로 띄운다 (§1-6). (4) 같은 이름이 이미 있으면 세우지 않고
종료 코드 1 로 묻는다 (합치는 것은 `dedupe`), 관계의 대상이 없으면 그 엣지만
건너뛰고 센다.

선 것: `kr:event:프로야구 개막` (1982-03-27) · 전두환 참여(시구) · 서울특별시
발생 장소 · 1982년 시점. 원본과 파생본에 한 번씩 돌리고 `timeline`·`central`
을 다시 걸었다. 연표의 1982년에 선다 (`/api/timeline` 확인).

**이 표는 짧을수록 좋다.** 문서가 있는 일은 시드로 가는 것이 맞다 — 거기
적으면 설명도 주소도 문서가 들고 온다.

## 2026-09-11 — 관리실을 탭 넷으로 (`/console`)

배포에 `HISTGRAPH_ADMIN_EMAILS` 가 없어서 **관리자가 관리실에 못 들어가고
있었다** ("내가 관리자인데 왜 못들어가지?"). 로컬 `.env` 에만 있었다. 넣고
다시 배포했다 — 환경변수는 **새 배포부터** 읽힌다.

들어가 보니 볼 것이 가입자 목록 한 장뿐이라 탭을 넷으로 갈랐다.

| 탭 | 무엇을 |
|---|---|
| 가입자 | 누가 언제 와서 언제 다녀갔나 · 이레 사이의 새 가입·방문 |
| 쓰임새 | 내 역사를 만든 사람 수·마지막 시각 · 갈피 · 지금 들어와 있는 사람 |
| 그래프 | 화면이 읽는 DB 의 타입별 노드·선·인과·설명 비율·마지막 수집 |
| 살림 | 로그인·가입자 표·봉투 열쇠·밖의 모델·관리자 수·그래프 파일 |

- **탭은 서버가 가른다** (`?tab=`). 자바스크립트로 감추면 네 탭 자료를 늘 다
  세야 한다. 링크 하나에 한 탭이면 그 탭의 셈만 돈다.
- **개인 자료는 세기만 한다.** 쓰임새 탭은 `life_docs` 에서 주인 번호와 고친
  시각만 뽑는다 — 문서 칸(`doc`)은 **질의에 담지 않고**, 테스트가 그 질의를
  들여다보며 잰다 (CLAUDE.md §1-10·§1-11).
- 며칠 치 셈은 파이썬에서 낸다 (`console._recent`). 시각 칸이 한쪽은 글자
  (SQLite)고 한쪽은 시각형(Postgres)이라 SQL 로 견주면 저장소마다 달리 읽힌다.
- 그래프 탭이 세는 파일은 `console.ACTIVE_DB` 다 — `server.serve` 와 배포
  진입점이 자기가 연 파일을 적어 둔다. 안 적히면 같은 규칙으로 다시 찾는다.
- 화면 글자는 여전히 한국어뿐이다. 파일 이름·환경변수 이름도 안 적는다 —
  탭마다 영어 검사가 따로 걸려 있다.

## 2026-09-11 — 연표 항목의 해가 노드와 따로 놀았다

지적: "서울대병원 감금 년도는 1977년인데 왜 1975년으로 표시 된거지?" 노드는
1976(구간 1976~1978)인데 **연표 항목만 1975** 였다 — 모델이 항목의 해를 나이나
앞뒤에서 어림해 적고, 아무도 둘을 견주지 않았다. 화면이 그리는 것은 항목이다.

`life.mark_follows_node` 로 **노드가 이기게** 했다 (`validate`·`refine` 두 곳,
그리고 `web/src/lib/life.js` `normalize` 가 같은 규칙을 따라 적는다). 고치는
자리가 노드이기 때문이다 — 표도 사람의 '편집'도 노드를 고치는데, 항목이 제 해를
들고 버티면 고친 것이 화면에 안 나온다. 실측: 그 문서의 어긋난 항목이 1건에서
0건이 됐고, 선거 표가 고친 1971 도 연표까지 따라간다.

물러나는 자리 둘 — 항목이 스스로 날짜를 말했으면(`date_text`) 그것이 남고,
노드의 날짜가 어림이면(`'20대 초반'`) 항목의 나이가 이긴다 (2026-09-08 결정을
지킨다).

**남는 것은 모델의 어림이다.** 그 문장에 해가 없으면 참값은 아무도 모른다
(실제 서울대병원 감금은 1977년 12월이고 이야기는 그 해를 안 적었다). 사람이
'편집'으로 적으면 그 값이 선다.

## 2026-09-11 — 대수가 붙은 선거의 해를 표가 정한다 (`gate_elections`)

사용자가 김대중의 생애를 내 역사에 넣고 물었다: "1967년은 6대 대통령 선거인데
7대 대통령 선거로 기록 됐네, 내가 입력을 잘못 했나?" 이야기를 열어 보니 **입력은
맞았다.**

- 일곱째 글: "1967년 신민당 창당에 참여하였고, **제7대 국회의원 선거**에서 목포시
  … 당선"
- 여덟째 글: "**제7대 대통령 선거**에서 겨우 2~3개 시군 차이까지 추격" ·
  "신민당은 김대중을 **제8대 국회의원 선거**에서 전국구로 공천"

해를 말한 문장은 1967년 총선 하나뿐이고 대선·8대 총선 문장에는 해가 없다.
그래서 추출이 이름과 해를 엇갈려 붙였다 — `제7대 대통령 선거 출마(1967)` ·
`제8대 국회의원 선거 출마 및 당선(1967)`. 둘 다 실제로는 1971년이다.

**그래프는 못 고쳐 준다** — 수집이 준 선거 노드는 제헌 총선과 제21대 대선
둘뿐이다. 그래서 정본을 표로 들였다: `data/elections.tsv` (대통령 21 · 국회의원
22 줄, 종류·대수·날짜·근거). 관문 `life.gate_elections` 이 이름에서 '제N대 ○○
선거'를 읽어 표의 해로 맞추고, 연표 항목의 해도 옮기고 나이는 비운다(다시 센다).
**`validate` 와 `refine` 두 곳에 걸었다** — 새 답도 옛 그래프도 지난다. 사용자의
문서는 화면을 새로 열면 두 줄이 1971 로 선다.

이어진 물음: "년도를 정확하게 말했는데 왜 그래프에서 빠진거지?" 문서를 열어
보니 **아무도 버리지 않았다** — `notes` 에 사유가 한 줄도 없고(버렸으면 남는다)
모델이 그 문장을 애초에 안 냈다. 이야기가 적은 해와 그래프에 선 해를 견주면
1952·1963·1964·1970·1980이 비어 있었다.

그래서 **세어서 보여 준다** (`web/src/lib/life.js` `missingYears` → '내가 적은
이야기' 상자 머리). 해를 적은 문장을 찾아 그 해에 아무 사건도 없으면 그 문장을
도로 세우고, 누르면 입력창으로 간다 — 다시 넣으면 모델이 그때 읽는다. 지어내지
않는다. 재는 것은 사건이 선 해다 (사람 노드는 생몰년이라 삶 전체를 덮으므로
빼고, 사건의 구간은 스무 해까지만 덮는 것으로 본다). 검사 9건.

## 2026-09-11 — 계정에서 지운 내 역사가 사본에서 되살아났다

사용자 요청으로 로컬·배포의 내 역사를 지웠는데 "아직 내 역사가 보이는데?" —
화면이 브라우저 사본을 집어 그리고, 계정이 비어 있으니 **그것을 계정에 다시
올렸다** (실측: 지운 지 13분 만에 로컬 줄이 되살아났다).

부팅이 갈라야 할 두 가지의 겉모습이 같았다 — 둘 다 '계정은 비었고 브라우저에는
있다' 이다. **주인 표가 가른다** (`lifestore.localAfterAccount`):

- 표가 없으면 **로그인 전에 적은 글**이다 → 이어받아 계정에 올린다.
- 내 표가 찍혀 있는데 계정에 없으면 **계정에서 지운 것**이다 → 사본도 지운다.
- 계정을 **못 읽었으면**(네트워크) 아무 판단도 하지 않는다 — 못 읽은 것을
  지운 것으로 읽으면 멀쩡한 자료가 사라진다.

**한 번 더 새어 나갔다** ("아직도 잘 보여") — 사용자의 사본에는 표가 **아예
없었고**(표가 생기기 전에 남은 것) 그것을 '로그인 전에 적은 글'로 읽어 또
올렸다. 없는 표와 빈 표는 다르다: 로그인 전에 적은 글에는 `writeLife(doc, '')`
가 **빈 표**를 찍는다. 표가 아예 없다는 것은 로그인한 채로 적어 계정에
올렸던 옛 사본이라는 뜻이고, 그 계정이 비었다면 지운 것이다.

검사 6건이 늘었다 (`web/tests/lifestore.test.mjs`).

## 2026-09-11 — 개인정보처리방침 제5판 (잠금 조치)

봉투 암호화를 넣었으니 제9조의 안전성 조치 목록에 한 줄을 더했다 — 계정에
담긴 '내 역사' 문서는 문서마다 다른 열쇠로 잠그고 여는 열쇠를 데이터베이스
안에 두지 않는다는 것, 그리고 **이 잠금이 미치지 않는 곳**(본인 브라우저의
사본 · 제4조의 인공지능 처리)까지 같이 적었다.

- 공고일·시행일 모두 9월 11일이다. 제12조에 단서를 하나 더 두었다 —
  **이용자에게 불리하지 않은 변경**(안전성 조치를 더하는 것, 이미 하는 일을 더
  자세히 적는 것, 잘못 적힌 곳을 바로잡는 것)은 받는 항목도 쓰는 곳도 늘지
  않으므로 공고와 같은 날 시행할 수 있다. 제4판이 쿠키에 관해 둔 단서와 같은 꼴이다.
- 약관은 안 건드렸다 (방침의 판을 참조하는 자리가 없다).

## 2026-09-11 — 내 역사를 봉투에 넣어 담는다 (`secretbox`)

사용자: "사용자의 역사 입력을 암호화 해서 저장 하나?" → 안 했다. 전송만
암호화(TLS)였고 `life_docs.doc` 은 평문 jsonb 였다. 이어서 "그럼 봉투 암호화를
진행 하자".

- **봉투**: 문서마다 일회용 열쇠(DEK)를 뽑아 문서를 잠그고, 그 열쇠를 큰
  열쇠(KEK, `HISTGRAPH_DATA_KEY`)로 다시 잠가 함께 담는다. 큰 열쇠를 갈 때
  문서를 전부 다시 잠글 필요 없이 작은 열쇠만 다시 싸면 된다.
- **표는 안 고쳤다.** 봉투가 문서 있던 자리(`doc` jsonb)에 그대로 든다 —
  `{"enc":"histgraph-v1","kid":…,"key":…,"doc":…}`.
- **AES 가 아니다.** 본체는 표준 라이브러리만으로 돌고(`dependencies = []`)
  배포 함수에는 `requirements.txt` 조차 없다. 그래서 HMAC-SHA256 계수 모드로
  열쇠줄기를 뽑아 xor 하고 encrypt-then-MAC 으로 봉인한다 (nonce 는 문서마다
  새로, 견주기는 상수 시간). 의존성을 들일 수 있으면 `_lock`·`_unlock` 둘만
  AES-GCM 으로 갈면 된다.
- **화면은 하나도 안 바뀐다.** 잠그고 푸는 자리는 `auth.life_doc` 과
  `accounts.put_life`·`life_of` 넷뿐이다.
- **못 열 때 빈 문서를 주지 않는다** — 화면이 '자료 없음'으로 읽으면 다음
  저장이 빈 것으로 덮어써 삶이 사라진다. 503 으로 말한다.
- `uv run histgraph datakey` — 상태를 세고(`--new` 열쇠 만들기 · `--seal`
  평문·옛 봉투 다시 잠그기 · `--prod` 배포 표). 열쇠를 `.env` 에 자동으로
  쓰지 않는다: 배포 환경변수와 같은 값이어야 하는데 한쪽에만 들어가면 서로
  못 여는 봉투가 생긴다.
- **배포까지 끝냈다 (09-11).** Vercel 환경변수(Production·Preview·Development)에
  같은 열쇠를 넣고 main 에 푸시한 뒤, 로컬 1건(노드 51 · 이야기 17)과
  네온 1건(노드 53 · 이야기 18)을 잠갔다. 배포 로그에서 `/api/my/life` 200 ·
  `/api/life/job` 200 을 확인했고 화면도 그대로 선다.
  잠그기 전 평문은 `data/life/neon-before-seal-20260911-090559.json` 에 받아 두었다.
- **버셀 명령줄 도구를 깔고 프로젝트를 이었다** (`vercel link --project histgraph`).
  `vercel link` 가 `.gitignore` 에 덧붙이는 `.env*` 한 줄은 되돌렸다 — 앞 세 줄이
  같은 일을 하면서 `.env.example` 을 예외로 두는데 그 예외를 덮는다.
  **배포된 자리는 curl·헤드리스 브라우저로 못 두드린다** (Vercel Security
  Checkpoint 가 403 을 준다) — 확인은 사람 브라우저나 `vercel logs` 로 한다.
- 검사 13건이 늘었다 (봉투 왕복 · 원문이 안 남는지 · 한 글자만 고치면 안 열리는지 ·
  남의 열쇠 · 열쇠 갈이 · 열쇠 없을 때 503).

## 2026-09-11 — 내 역사에는 주인이 있다 (한 컴퓨터를 나눠 쓸 때)

사용자: "다른 사용자의 역사는 남이 절대 볼 수 없는거지?" → 점검 결과 **서버는
막혀 있었고 브라우저가 뚫려 있었다.** 이어서 "심각한 문제네. 확실하게 근본적으로
문제를 해결해".

무엇이 뚫려 있었나 — 계정에 담은 문서는 세션 쿠키로만 갈리므로(`auth.life_doc`
은 `user_id` 로만 찾는다) 남의 것을 부를 길이 없다. 그런데 같은 문서가
**브라우저(localStorage `life-json`)에도** 한 벌 남고, 로그아웃이 그것을 지우지
않았다. 그래서 한 컴퓨터를 나눠 쓰면 이렇게 됐다:

1. 갑이 로그인해 내 역사를 만들고 로그아웃한다 (또는 그냥 창을 닫는다).
2. 을이 같은 브라우저로 들어온다. 을의 계정이 비어 있으면 화면이 **브라우저에
   남은 갑의 문서**를 집어 그린다.
3. 그리고 그것을 **을의 계정에 저장한다** (`keepInAccount`) — 갑의 삶이 을의 자료가 된다.

세운 규칙 하나: **남기는 자료에는 주인을 적고, 지금 보는 사람과 다르면 읽지
않고 지운다.** 관문이 셋이다.

| 어디서 | 무엇을 |
|---|---|
| `web/src/lib/lifestore.js` | 브라우저에 남기는 넷(문서·주인표·보낸 이야기·못 보낸 글)을 한 길로 모으고, 표가 다르면 그 자리에서 지운다 |
| `auth.life_viewer` | 개인 역사를 내주는 자리(`/api/life/job`·`/api/life/story`·`/api/life/analyze`)가 세션을 본다 — 두 껍데기(로컬 서버·서버리스)가 같은 규칙을 부른다 |
| `server.LifeAnalysis.status(viewer)` | 돌고 있는 분석에 임자를 적어, 표가 다른 사람에게는 **없는 일**로 답한다 |

- 주인 표는 `auth.owner_tag` — 사용자 번호의 HMAC 이라 되돌릴 수 없고, 기기가
  달라도 같은 사람이면 같다. `/api/me` 가 `owner` 로 준다.
- **로그아웃에 기대지 않는다.** 창을 닫고 갔든 세션이 만료됐든, 다음 사람이 여는
  순간 표가 갈려 지워진다. 로그아웃·탈퇴도 같이 지우지만 그것은 두 번째 겹이다.
- **표가 없는 옛 자료는 지우지 않는다** — 로그인 전에 적은 글이라 주인이 아직
  없다. 그 사람이 로그인하면 그때 표가 붙고, 그때부터 남이 못 읽는다.
- 곁가지 둘도 같이 막았다. `/api/life/job` 은 결과를 통째로 들고 있는데 세션을
  안 봤고(로컬 서버 한 대를 여럿이 본다), `/api/life/story` 는 이 컴퓨터의
  `data/life/*.txt` 를 내주는데 `serve --host 0.0.0.0` 이면 같은 망 아무나
  읽었다 — 그 파일은 계정이 아니라 폴더에 붙어 있어 **이 컴퓨터에서만** 읽는다.
- 배포에서 이 둘은 엣지 캐시에서도 뺐다 (`LIFE_GETS`). `/api/me` 와 같은 이유다.
- 검사: `web/tests/lifestore.test.mjs` 17건 + 파이썬 쪽 주인 표·문 검사.
  브라우저로도 확인했다(CDP) — 갑의 표가 적힌 자료를 두고 을로 들어가면
  노드 0개에 저장소가 비고, 갑으로 들어가면 18개가 그대로 선다.

## 2026-09-11 — 그래프를 3D 로 갈아탔다 (`3d-force-graph`)

사용자: "3d-force-graph를 이용해서 우리 그래프를 3d로 만들어줘". 물어서 받은 결정
둘 — **2D 캔버스는 걷어내고 3D 만 남긴다**, **한국사 그래프와 내 역사 그래프 둘 다**
(두 화면이 같은 `GraphView` 를 쓴다).

- **의존성 넷**: `3d-force-graph` 1.80 · `three` 0.186 · `three-spritetext`(이름표) ·
  `d3-force-3d`(배치 검사를 Node 에서 돌리려고). `d3-force`(2D)는 뺐다. 첫 화면에
  얹히는 것은 +16 kB 이고 엔진 1,561 kB(gzip 421)는 늦게 받는다.
- **`3d-force-graph` 는 Node 에서 import 가 터진다**(`window` 없음). 배치 검사와 서버
  렌더링 검사가 `graph-view.js` 를 Node 에서 읽으므로 엔진은 생성자에서
  `await import()` 로 늦게 싣고, 그 전에 온 `setData`·`select` 는 담아 뒀다 붙은 뒤에
  싣는다.
- **부호는 뜻을 옮기지 않고 표현만 옮겼다** (§4): 색상=타입 · 크기=차수 · 굵기=인과 ·
  조명=밝기. 3D 선은 원기둥이라 `setLineDash` 가 없어 **확실성(파선)은 줄무늬
  알파맵을 되풀이한 관**으로 그린다 — 가는 실로 그렸으면 `caused` 2,604건 중
  2,575건이 conf<1 이라 인과의 굵기가 화면에서 사라졌다. **깊이에는 아무 뜻도 안
  싣는다** (카메라가 사용자 것이라 돌리면 사라진다).
- 자세한 규칙은 `.claude/agents/graph-drawer.md` §3·§4·§5·§11·§12, 수치는 `design.md` §4.

**눌러 보고 잡은 것 셋** (CDP, 5173·8100 둘 다):

1. **내 역사 그래프가 아예 안 그려졌다.** 엔진도 붙고 노드 51개도 서는데
   `renderer.info.render.frame` 이 1 에서 멈춰 있었다. `d3ReheatSimulation()` 이 그
   자리에서 `engineRunning` 을 세우는데 `state.layout` 은 kapsule 의 1ms 디바운스
   digest 가 만든다 — 그 사이 프레임이 없는 배치를 짚어 터지고, 라이브러리는 예외
   뒤에 다음 프레임을 안 건다(고리가 영영 죽는다). 자료가 엔진보다 **먼저** 오는
   길에서만 났다 (내 역사는 `onReady` 가 생성자 자리에서 `setData` 를 부른다).
   막은 것 둘: 엔진이 한 번이라도 돈 뒤에만 되데우기 · 우리 RAF 가 1.5초마다 고리가
   죽었는지 재서 되살리기. **검사가 부르는 차례를 잰다.**
2. **첫 화면에 이름표가 하나도 없었다.** 106개가 자리다툼에서 이기고도 거리 문턱에서
   전부 투명했다(카메라 2308, 문턱 1221). 자를 **꽉 차는 거리**로 바꾸고(그래프마다
   크기가 다르다) 글자는 화면 크기를 지키게 했다(초점 12.5px·나머지 11px). 0 → 83개.
3. **그래프가 캔버스의 3분의 1만 썼다.** 라이브러리 `zoomToFit` 은 자식(이름표)까지
   재고 늘 멀찍이 선다. 노드의 자리와 반지름만 세는 `_fit` 을 따로 짰다 — 거리
   2308 → 1461, 세로 채움 87% → 100%.

**옛 병 하나를 같이 고쳤다** (3D 와 무관, HEAD 를 워크트리에 꺼내 재 보니 거기서도
같다): 내 역사 화면이 통째로 하얘지던 것. `LifeBoard` 가 `.life-body` 를 `innerHTML`
로 다시 쓰면서 React 가 그린 `.life-booting` 을 같이 지웠고, React 가 그것을 거두려다
`removeChild` 로 터졌다. 안내를 `.life-over` 로 판 위에 얹었다 — **`LifeBoard` 가 쓰는
칸(`.life-head`·`.life-body`)에 React 가 자식을 두지 않는다**가 규칙이다.

검증: 노드를 눌러 상세·연표·조명이 따라오는 것까지 확인했다(4군 6진 개척 → 1428년).
라이트 테마도 그린다. `npm test` 146·117·251·130 · `npm run build` · 파이썬 1,578 ·
`check_korean.py` 통과.

## 2026-09-11 — 화면은 조선에서 연다 (`scope.BUNDLE_ROOT`)

"페이지를 처음 열었을때 보이는 타임 라인을 조선으로 해줘." 묶음의 **맨 앞
시대**(고려)가 곧 화면의 중심이었다 — 주소가 비면 `server.root` 가 고려를
내주고 연표가 918년에 섰다.

가른 것은 **두 자리가 하나였던 것**이다. 맨 앞 시대는 연표의 **바닥**이고
(`_polities` → `floor`, 918년 앞의 뼈대·이웃·재위를 덜어낸다), 여는 시대는
**처음 보이는 자리**다. 묶음 차례를 조선으로 바꿔 여는 대신 여는 시대를
따로 적었다 (`scope.BUNDLE_ROOT = {"korea": "joseon"}` · `opening_eras`) —
바닥까지 조선으로 올리면 고려 사건 568건 중 그 시대 몫이 연표에서 통째로
사라진다 (CLAUDE.md 1-3 뒷절, "이 시기 왕들때는 아무런 일이 없었어?").

검증: CDP 로 5173·8100 둘 다 열었다. 주소가 `#wd:Q28179`, 연표 머리가
'1392년 ~ 1897년', 첫 화면이 1380~1399 (위화도 회군 · 조선건국 · 제1차
왕자의 난, 왼쪽 띠에 태조·정종·태종). 축은 그대로 915~2029 라 고려는
위로 올리면 그대로 있다. 테스트 넷을 더했다 (`opening_eras` · 바닥은 고려).

## 2026-09-10 — 작품에 만든 사람을 이었다 (`creators` · `data/creators.tsv`)

"작품과 작품을 만든 사람의 엣지가 없어. 예를 들면 세한도는 김정희가 만들었는데
둘간의 엣지가 없어." 실측이 그대로였다 — 작품 1,777개(예술작품 19 · 유물 1,584 ·
영화·드라마 174)에 `created` 엣지가 **17건**뿐이었다.

| | 원본 | 화면 DB |
|---|---|---|
| `created` 엣지 | 65 → **504** | 17 → **250** |

역할별로는 저술 72 · 편찬 65 · 글씨 48 · 그림 27 · 제작 15 · 발원 13 이다
(화면 DB 기준). 규칙과 함정은 CLAUDE.md §1-9 에 적었다.

**후보는 기계가 찾고 판정은 사람이 한다.** 작품마다 이름·설명·말뭉치 문단을
붙여 놓고 **인물 이름을 먼저 놓고** 훑는다 — 창작 동사로 먼저 훑으면 '그림'·
'건물'·'이것'이 사람이 된다 (실측: 후보 850건 중 태반). 이름 뒤 창작의 틀
다섯(`paren`·`subj`·`poss`·`etc`·`label`)에 걸린 것만 후보다. **주격 조사가
이름에 붙어 있어야 한다** — 띄어쓰기를 넘겨 받으면 '미루어 보아 이 칠불은'의
'보아'가 가수 보아가 된다 (그 하나로 후보가 1,526 → 888 로 줄었다).

**889쌍을 전수로 읽어 `data/creators.tsv` 에 적었다.** 만든 사람이 443, 아닌
것이 443, 이미 서 있던 거짓 엣지를 지운 것이 6 이다. 틀이 맞아도 절반이 아닌
까닭은 CLAUDE.md §1-9 의 목록에 있다 (초상의 주인공 · 명한 임금 · 서문만 쓴
사람 · 묶음 · 같은 이름의 딴 작품 · 동명이인).

**동명이인이 가장 컸다.** 이름이 여럿을 가리키면 후보는 하나로 두고 갈 수 있는
노드를 다 담아, **판정이 노드 id 로 답한다.** 실측으로 갈린 것들: 조각승
수연(守衍)과 무용대사 수연 · 탄은 이정(李霆)과 이순신의 아버지 이정(李貞) ·
화승 설잠과 김시습의 법호 설잠 · 1739년 화승 채원과 고려의 채원 · 시조작가
안민영과 1970년생 영화배우 안민영. 우리 그래프에 그 사람이 없고 현대 동명이인만
있으면 잇지 않았다 (김명국·유숙·정수영·김현성 같은 화가·명필이 그렇게 빠졌다).

**온톨로지를 한 칸 넓혔다.** `created` 의 도착 타입에 `media` 를 더했다 —
소설·시·영화에도 지은 사람이 있는데(『김약국의 딸들』은 박경리가 썼다) 매체를
빼 두면 `works` 가 실어 온 작품이 만든 이를 영영 못 받는다.

**아직 안 한 것.** 원본에만 있는 `ex:artwork:` 48건(황진이의 시조들, 신사임당의
초충도 등)은 추출이 낸 엣지 그대로라 역할 라벨이 없다 — 화면 DB 에는 없다.

## 2026-09-10 — 파(派)를 노드로 세웠다 (`clans` · `data/clans.tsv`)

"파를 노드로 세우자" · "다음으로 큰 파를 추가해". 본관 둘, 파 248개가 섰다.

| 본관 | 파 | 자료 | 파조를 인물 노드에 이은 것 |
|---|---|---|---|
| 전주 이씨 | 182 | 장서각 기록유산DB 왕실족보 | 86 |
| 김해 김씨 | 66 | 한국어 위키백과 '김해 김씨' 분파 절 | 1 |

**새 노드 타입을 만들지 않았다.** `org` 에 `props.kind="clan"` 과
`props.clan_level`(본관·파)로 앉혔다. 까닭은 `clans.py` 머리글과
`ontology.type_label` 에 있다 — 타입을 늘리면 EDGE_TYPES 의 출발·도착 목록과
화면 색이 같이 는다.

**엣지 넷.** 파 `part_of` 상위 파 또는 본관 · 본관 `related_to` 본관 지명
(label `본관`) · 파조 `member_of` 파 (label `파조`).

**본관을 지명에 잇는 것은 참이면서 닻이다.** 김해 김씨는 파조 가운데 우리
그래프에 있는 사람이 하나(삼현파 파조 김관 金管, 판도판서)뿐이라 지명에 걸지
않으면 씨족 나무 전체가 씨앗에서 한 홉 밖이라 `scope` 가 통째로 잘라 낸다.
나무는 `scope.close_clans` 가 위아래로 한 덩어리로 데려온다 (`close_places`
와 같은 자리).

**함정 넷을 밟았다.**

- **파조를 이름으로 이으면 다 틀린다.** 김해 김씨 파조 이름으로 그래프를 훑으면
  스무 명 남짓 걸리는데 **한자가 맞는 사람은 하나도 없었다** — 생원공파 파조
  김구(金銶) 자리에 백범 김구(金九)·청풍 김구(金構)·부령 김구(金坵)가 서고,
  횡성공파 파조 김영서(金永瑞) 자리에 1882년생 독립운동가가 선다. 전주 이씨는
  `함양군`·`담양군`·`영양군` 이 같은 이름의 **장소** 노드와 부딪힌다 (君 vs 郡).
  그래서 노드 id 는 사람이 표에 적고 빈 칸은 빈 채로 둔다.
- **한자가 다른 같은 파.** 장서각 목록이 `칠산군파` 를 漆山君派·㓒山君派 둘로
  센다. 이름으로 합치고 수록 인물수를 더했다. 다만 `순평군파(桂城君派)` 처럼
  **남의 파 한자가 붙은 줄은 별칭으로 두지 않는다** — 그대로 두면 계성군파가
  다음 병합에서 남의 파와 하나가 된다.
- **상위가 표에 없으면 가지가 통째로 사라진다.** 장서각에 '효령대군파'는 우산
  이름으로만 있고 줄이 없어서, 그 아래 63개가 전부 본관 바로 아래로 올라가
  세 층이 두 층이 됐다. 우산 두 줄(효령대군파·양녕대군파)을 표에 적고,
  **상위가 표에 없으면 종료 코드 1** 로 묻게 했다.
- **표가 바뀌면 옛 엣지가 남는다.** 김해 김씨를 더했을 때 노드 이름은 그대로라
  살아남고 '전주 이씨에 속한다'는 옛 엣지만 남아 김해의 파 43개가 전주 이씨
  아래에 섰다. `build` 가 `source='clans'` 엣지를 **먼저 다 지우고** 세운다.

**검색이 띄어쓰기를 넘게 했다** (`server.GraphAPI.search`). 라벨이 '전주 이씨'인데
'전주이씨'를 치면 한 줄도 안 나왔다. 라벨·별칭과 검색어에서 공백을 빼고 한 번 더
견준다 — '3·1운동'으로 '3·1 운동'을 찾는 것도 같은 고침이다.

**아직 안 한 것.** 사람마다 파를 붙이는 일. 왕실족보에서 `이원익` 을 찾으면
241건이 나오고 완풍군파·의안대군파·주계군파에 다 있다. 생몰년·부명으로 가려야
하고, 그것도 표가 판정할 일이다.

## 2026-09-10 — 성씨·본관·파 자료를 받아 왔다 (`data/raw/`)

"성씨들의 파 데이터를 가지고 있나?" — 없었다. 본관은 `한국역대인명정보` 의
`ORIGINNAME` 한 칸(27,035명 중 16,791명, 382종)뿐이고 파는 어디에도 없었다.
훑어 보니 **파를 통째로 담은 공개 자료 자체가 없다.** 나무위키가 제일
촘촘한데 CC BY-NC-SA 라 광고 붙은 이 저장소에는 못 쓴다 (문장을 바꿔 써도
2차적저작물이라 조건이 따라온다). 그래서 쓸 수 있는 두 곳에서 받았다.

| 파일 (`data/raw/`, 저장소 밖) | 줄 | 무엇 |
|---|---|---|
| `통계청_2015인구주택총조사_성씨본관별인구.csv` | 14,567 | 성씨·본관·코드·인구 (2015, 5인 이상) |
| `통계청_2015인구주택총조사_성씨별인구.csv` | 533 | 성씨·코드·인구 |
| `장서각_왕실족보_전주이씨_파.tsv` | 187 | 파·한자·상위 대파·파조·수록 인물수·파조 노드 |

**통계청** 표는 `DT_1IN15SD`. 오픈API 키 없이 화면이 쓰는 길로 받았다 —
받는 길과 함정(EUC-KR·코드 없음)은 기억 `histgraph-kosis-surname-clan-table`
에 적어 두었다. 김해 김씨 4,456,700 으로 공표값과 맞다. 우리 본관과 붙여
보면 16,791명 중 16,243명(96.7%)이 짝이 맞고, 안 맞는 548은 대개 이름 자리에
임금 시호가 들어 있는 줄이다 (`숙 전주`·`경 개성`).

**파**는 장서각 기록유산DB 왕실족보(`jsg.aks.ac.kr/vj/jb`)에서 왔다. 전주 이씨
왕실 계보라 다른 성씨는 없다. 파 187개에 수록 인물이 1,433,484명이고,
파조 이름이 우리 인물 노드와 그대로 맞는 것이 86개다 (완풍군·여양군·시중공 등
98개는 노드가 아직 없다). `함양군`·`담양군`·`영양군` 은 같은 이름의 **장소**
노드가 있어 뺐다 — 君과 郡이다.

**사람마다 파를 붙이는 것은 아직 못 한다.** 이름만으로는 안 된다 — 왕실족보
에서 `이원익` 을 찾으면 241건이 나오고 완풍군파·의안대군파·주계군파에 다
있다. 붙이려면 생몰년·부명으로 가려야 한다 (§1-2 와 같은 함정).

**그래프에 넣을지는 아직 정하지 않았다.** 파를 노드로 세울지, 인물의 칸으로
둘지가 먼저다. 노드로 세우면 그리는 규칙(`.claude/agents/graph-drawer.md`)을
같이 정해야 한다.

## 2026-09-10 — 연인은 친구가 아니고, 안 말한 달은 적지 않는다

사용자: "사용자가 '남자친구'나 '여자친구' '연인' 같은 표현을 하면 관계를 연인으로
설정 해줘. 그리고 사용자가 년도만 언급하면 노드의 기간을 년도만 표시해줘 달과 날짜를
추측해서 표시하지마."

### 연인 (`partner_of` · '연인')

**'여자친구'에는 '친구'가 들어 있다.** 그래서 다듬기의 친구 규칙(`_FRIEND`)이 그 사람을
동창과 같은 이름으로 세우고 있었다 — 그 삶에서 가장 가까운 사람이 '친구'로 서 있었다.
관계 타입을 하나 늘렸다 (사람 → 사람, 대칭이라 화살촉 없음, 무게는 가족·친구와 같은 0.8).

근거를 대는 자리가 둘이고, **연인을 친구보다 먼저 본다**:

| 어디서 | 무엇을 보고 |
|---|---|
| `tidy_edges` (원본·화면 같은 규칙) | 관계의 설명·그 사람의 설명에 든 말 — 모델이 '친구'로 적어 온 것도 다시 본다 |
| `link_people` | **이름을 부르는 그 문장** — 설명에 아무 말이 없어도 이야기가 그렇게 부르면 올린다 |

가르는 것 둘: 남의 연인은 내 연인이 아니고('김일권의 여자친구 이수아'), 근거는 **한 문장
안에** 있어야 한다 ('여자친구와 헤어졌다. 그 뒤 김일권을 만났다' 의 김일권은 아니다).
결혼한 뒤의 호칭(아내·남편)은 그대로 호칭 표(`_KIN_TERMS`)가 맡는다 — 배우자는 연인 칸이
아니다. 올라설 수 있는 것은 묽은 관계(만남·친구·함께 일함·같은 학교)뿐이고 가족은 안 건드린다.

### 해만 말했으면 해까지 (`trim_dates`)

모델은 해만 아는 날을 **1월 1일**로 적어 온다 (위키데이터도 같은 버릇이다). 그것을 그대로
세우면 읽는 사람에게 '2002-01-01' 은 잰 날이 된다. 이제 이야기가 말한 만큼만 남는다:

| 이야기가 | 노드에 |
|---|---|
| '1997년에 입학했다' | `1997` (모델이 적은 `1997-03-02` 를 자른다) |
| '1998년 4월에 이사했다' | `1998-04` |
| '2010년 5월 8일에 결혼했다' | `2010-05-08` |

연표 항목의 **날짜 글**(`date_text`)도 같이 자른다 — 화면의 날짜 줄은 그것을 먼저 보므로,
노드만 자르면 '1997' 옆에 '1997년 3월 2일'이 선다. **자르기만 하고 비우지는 않는다**:
근거 없는 해를 비우는 것은 `gate_dates` 의 일이고, 사건은 해를 잃으면 연표에서 내려간다.

근거로 대는 것은 **말한 것 전부**다 (부르는 쪽이 준 원문 + 그래프에 실린 이야기). 서버의
`/api/life/refine` 은 이 컴퓨터의 옛 원문 파일을 주므로, 한쪽만 보면 사람이 새로 적은 달을
잘라 버린다 — 실측으로 겪었다.

건드린 자리: `src/histgraph/life.py`(`partner_of`·`_PARTNER`·`_partner_says`·`trim_dates`) ·
`web/src/lib/life.js`(같은 규칙) · `web/src/lib/graph-view.js`(`MUTUAL`) ·
`.claude/agents/graph-drawer.md` §7·§11 · 테스트 둘. 지시문(`life_prompt.md`)은 안 고쳤다 —
사용자가 쓴 그대로다.

**서버는 코드를 다시 읽지 않는다** — 8100 을 다시 띄워야 이 규칙이 화면에 온다.

### 그 뒤 — '연애'도 시절이다

사용자가 같은 자리에서 실제로 넣어 보고: "'2009년 안다영이라는 여자친구를 만났어.' 이렇게
입력 했는데 '2009-01-01 ~ 2009-12-31 · 만 27세 · 사회생활' 노드에 이렇게 설명 했어. 2009년만
보여야 하고 사회생활이 아니고 연애를 한거야."

날짜는 위의 `trim_dates` 가 이미 자르는 것이었다 — **띄워 둔 서버가 옛 코드였다.** 시작과 끝을
둘 다 '2009'로 자르면 화면은 물결 없이 '2009' 한 번만 세운다 (`till` 은 끝이 시작과 다를 때만).

시절은 없던 것이었다. `LIFE_STAGES` 에 **'연애'**를 더했다 (사회생활 다음, 창업 앞). 사귄
사람을 만난 사건이 그 시절을 열고(`_romance`·`romance` — `_PARTNER` 와 같은 말 표를 쓴다),
띠는 결혼·이별에서 닫는다(`STAGE_END`). **군복무를 먼저 본다** — 공익 시절에 만난 사람의
항목은 '군복무'라고 2026-09-09 에 이미 정했다. 목록은 모델에게 주는 스키마의 enum 이기도 해서
모델도 이제 '연애'를 고를 수 있다.

### 또 그 뒤 — 모델이 세운 만남 사건은 반쪽으로 온다

같은 노드에서 둘이 더 나왔다 (사용자: "함께에 '안다영'이 있어야 하고, 여전히 사회생활이라고
보이고 있어"). 실제 문서를 열어 보니 `met_person_an_dayeong` 이 **우리 코드가 아니라 모델이
세운 것**이었다 — `meet_events` 가 세우는 것과 이름 규칙까지 같아서(모델이 지시문에 실린 옛
아이디를 보고 흉내 냈다) `ev_id in by_id` 로 걸려 그냥 지나쳤다. 그 노드는 이랬다:

| | 모델이 준 것 | 우리가 세우는 것 |
|---|---|---|
| 설명 | 없음 | 이야기가 그 사람을 부른 문장 |
| 날짜 | `2009-01-01` ~ `2009-12-31` | `2009` |
| 참여 | 사건 → 나 한 줄 (뒤집힘) | 나 → 사건 '만남' · 그 사람 → 사건 '함께' |
| 덤 | 사건 → 사건 자기순환 | 없음 |

설명이 없으니 시절도 못 읽었다. 넷을 고쳤다:

- **이미 선 만남 사건도 손본다** (`_mend_meet`) — 양끝 둘을 세우고(뒤집혀 있으면 바로 세운다),
  빈 설명을 이야기의 그 문장으로 채우고, `participants` 에 둘을 넣는다.
- **뒤집혀 온 참여는 바로 세운다** — `RELAX` 에 `experienced|event|person` 을 더했다
  (양끝을 맞바꾸는 코드는 이미 있었다).
- **자기순환은 버린다** — `_during` 이 만들지 않고, `tidy_edges` 가 남은 것을 지운다
  (한국사 그래프에서 1,258건을 지운 그 규칙이 개인 쪽에는 없었다).
- **시절의 근거를 하나 더 둔다** — 설명이 비어도 **이름을 단 사람**을 이야기가 연인이라
  부르면 연애다 (`_romance` 가 `_partner_says` 를 부른다).

### 또 그 뒤 — 띠와 노드는 다른 것을 말한다

사용자: "이건 공익생활중에 만났으니 군복무라고 써있는데 오해하기 쉬운거야 타임라인은 그냥
군복무라고 써도 되지만 노드엔 연애라고 표시해줘."

'정혜림을 만남'(2002)은 공익 시절의 일이라 띠가 '군복무'였고, 상세의 날짜 줄이 그 띠를 그대로
읽어 **'2002 · 만 20세 · 군복무'** 로 섰다. 한 칸이 둘을 겸하고 있었다.

연표 항목이 이제 둘을 따로 든다:

| 칸 | 무엇을 말하나 | 그 만남에서 |
|---|---|---|
| `life_stage` | 그때가 **어떤 시절**이었나 (띠) | 군복무 |
| `node_stage` | 그 일이 **무엇**이었나 (상세의 날짜 줄) | 연애 |

`_romance`·`romance` 에서 군복무 검사를 뺐다 — 그것은 '이 일이 연애인가'를 재는 자리이고,
군복무가 띠를 먼저 가져가는 것은 연표 고리가 정한다 (2026-09-09 결정 그대로). 사람이 손으로
고른 시절(`stage_said`)에는 이 칸을 달지 않는다.

## 2026-09-10 — 노드에서 '동안'을 내렸다 (`SEQUENCE_ONLY`)

사용자: "노드에서 '동안'이라는 메뉴를 삭제해줘."

`during`('동안')은 사건이 다른 사건·시기 **안에** 있다는 말인데, 두 해가 연표에
나란히 서 있어 그 포함은 눈으로 읽힌다 — 상세에 이름을 한 번 더 적는 줄이었다.
2026-09-08 에 `before`('다음')·`after`('이전')를 내린 그 자리(`SEQUENCE_ONLY`)에
같이 넣었다. 원본과 화면이 **같은 표**를 본다 (`life.py` · `web/src/lib/life.js`).

**뜻이 있는 `during` 은 그대로 남는다** — 버리는 자리가 RELAX **뒤**라서다:

| 모델이 적어 온 것 | 어디로 |
|---|---|
| 사람 ↔ 사건 | 참여(`experienced`) — 역할이 선의 이름 |
| 사건 → 단체·자리 | 곳(`at`) |
| 사건 → 사건 · 사건 → 시기 | **버린다** |

'그 시절에 만났다'를 고치는 규칙(`_during`)도 그대로다. 정혜림이 공익근무를 한
것으로 서지 않는 것이 그 규칙의 값이고, 만남이 그 시절 안에 있었다는 것은 연표의
두 해가 말한다.

계정·브라우저에 남은 옛 그래프도 새로고침 한 번에 사라진다 — 다듬기가 읽을 때마다
도는 자리라서다 (`normalize` · `/api/life/refine`). `overlapped`('겹침')는 그대로 두었다.

건드린 자리: `src/histgraph/life.py` · `web/src/lib/life.js` · 테스트 둘(실측 그래프에
사건 → 사건 `during` 을 넣어 관문이 잡는지 잰다). CDP 로도 확인했다.

## 2026-09-10 — 입력창은 늘 빈 칸에서 연다

사용자: "지금 내 역사 입력창에 입력을 하면 마지막 입력이 남아서 보이고 있어.
사용자가 입력창을 다시 열면 그냥 placeholder만 보여줘."

적다 만 글을 브라우저(`localStorage['life-story']`)에 남기고 있었다. 상자를 닫았다
열어도, 새로고침한 뒤에 열어도 지난번 글이 칸에 서 있었고 — **그 글이 보낸 것인지
아닌지는 화면 어디에도 없다.**

이제 칸에 선 글은 **이번에 쓰는 글 하나**만 뜻한다. 글은 `draftRef` 에만 있고
(상태가 아니라 ref 다 — 글자마다 화면을 다시 그릴 이유가 없다), 상자를 닫으면
걷힌다. 브라우저에는 아무것도 안 남는다.

| 무엇을 하면 | 칸에 무엇이 |
|---|---|
| 처음 열기 · 닫았다 열기 · 새로고침한 뒤 열기 | 보기글뿐 |
| 적는 중 · 기록 모달을 폈다 닫기 | 쓰던 글 그대로 |
| 기록에서 옛 글 고르기 · '적은 글 되돌리기' | 그 글 (쓰던 글이 있으면 아래에 붙는다) |
| '입력' 을 누른 뒤 | 빈 칸 |
| 모델이 읽는 동안 | 보낸 글 (2026-09-09 결정 — 몇 분을 기다리는 사람이 무엇을 보냈는지 봐야 한다) |

**기록 모달은 상자를 닫지 않는다.** 전에는 모달을 열며 상자를 접었는데, 이제는
접는 것이 곧 글을 걷는 것이라 옛 글을 골라 와도 쓰던 것이 사라진다 (2026-09-08
"쓰던 것을 삼키지 않는다"). 모달은 덮기만 한다.

건드린 자리: `web/src/components/LifeView.jsx`(`draftRef`·`putDraft`·`closeWriting`·
`StoryBox`) · `web/tests/render.test.mjs`. CDP 로 5173·8100 둘 다에서 여섯 갈래를
눌러 봤다.

## 2026-09-10 — 더한 역사가 연표 밖으로 밀려나지 않는다 (`LifeBoard.viewYear`)

사용자: "사용자가 내 인생에 추가로 역사를 입력하고 타임라인에 추가 되면 타임라인을
추가된 시점으로 이동 시켜줘."

옮겨 가기는 하고 있었다 (2026-09-09 `addedFocus`) — **머무르지를 못했다.** CDP 로
재 보니 답이 온 순간에는 더한 2013년이 판 한가운데 섰는데(scrollTop 0 → 1531),
입력 상자를 닫자 그 사건이 화면 밖으로 나갔다.

까닭은 **자의 눈금이 판 높이로 잡히기 때문**이다 (`buildScale` 은 `bodyH` 로 잰다).
상자가 닫히며 판이 516px 에서 792px 로 커지자 자가 3,848px 에서 4,860px 로 길어졌고,
`layout()` 은 픽셀 자리(`scrollTop`)를 그대로 되돌려 놓았다 — 같은 픽셀이 딴 해다.

그래서 **다시 그릴 때 지키는 것을 픽셀에서 해로 바꿨다.**

- `yearAt(lay, y)` — 자 위의 자리를 해로 되읽는다 (`lifeLayout.at` 의 거꾸로).
  눈금은 해마다 폭이 다르므로(사건이 몰린 해가 늘어난다) 사이는 그 해의 폭으로
  나눠 소수로 준다.
- `LifeBoard.viewYear()`·`toYear()` — 판 **위 모서리**에 선 해를 적어 두고 다시
  세운다. 한가운데로 재지 않는 것은 이 함수를 부르는 때가 `ResizeObserver` 라
  판의 새 높이가 이미 들어와 있어서다 — 새 높이의 절반을 옛 자에 대면 엉뚱한
  해가 나온다. 위 모서리는 높이와 무관하다.
- 머리('태어나기 전')를 보고 있으면(`scrollTop <= off`) 지킬 해가 없어 그대로 둔다.

**연표에 새로 선 것이 새 노드만은 아니다.** 옛 노드가 이제야 해를 얻어 줄에 서기도
한다 ('그때가 1998년이었다') — 그때는 `added.ids` 가 비어 화면이 안 움직였다.
`life.merge` 가 `timeline_ids` 를 따로 세고, `LifeView.finish` 가 그것을 먼저 보고
자리를 고른다.

건드린 자리: `web/src/lib/life.js`(`yearAt`·`LifeBoard.center`·`viewYear`·`toYear`·
`layout`) · `web/src/components/LifeView.jsx`(`finish`) · `src/histgraph/life.py`
(`merge` 의 `timeline_ids`) · 테스트 둘. CDP 로 5173·8100 둘 다에서 세 갈래를
눌러 봤다 (로컬 폴링 · 배포 한 번에 · 새 노드 없이 연표만 는 것): 셋 다 더한 해가
서고, 상자를 닫아도 그 자리에 남는다.

## 2026-09-10 — 내 역사에서 한국사를 그 자리에서 본다 (`openWorld`)

사용자: "내 역사에서 한국사 사건을 연표에서 클릭하면 한국사 페이지로 이동하는데
그러지 말고 한국사 그래프와 노드 정보를 그 페이지에서 바로 보여줘 이동하지 말고."

가운데 열의 한국사 사건은 `/#{id}` 링크라 누르면 한국사 장으로 옮겨 갔다 —
내 연표를 잃고 돌아오려면 뒤로가기를 눌러야 했다. 이제 **한 캔버스가 둘을
그린다**: 내 관계망이거나, 고른 한국사 사건의 주변 관계이거나.

| 어디를 누르면 | 무엇이 되나 |
|---|---|
| 가운데 열의 사건 · '태어나기 전'의 줄 · 상세의 '그 무렵의 한국사' | 캔버스가 그 사건의 주변 관계(`/api/graph`), 오른쪽이 한국사 상세(`DetailPanel`) |
| 그 상세의 관계 목록 | 그 노드로 이어 간다 (자취가 쌓여 '←' 로 되짚는다) |
| 안내 줄의 '내 역사로 돌아가기' · 상세의 닫기 · 내 사건 고르기 | 내 관계망으로 되돌아온다 |

- 상세는 **한국사 장이 쓰는 그 부품**이다 — 설명·출처 한 줄·인과 사슬·관계
  목록을 여기서 다시 그리면 같은 노드가 두 화면에서 다른 글이 된다.
- 설정 상자의 범례·시작점·펼침 깊이도 지금 캔버스에 선 그래프의 것으로 바뀐다
  (`/api/meta`·`/api/seeds`). 한국사를 보는데 내 그래프의 타입 수가 적혀 있으면
  그 수는 거짓말이다.
- 링크는 남겼다 — cmd·ctrl·가운데 단추는 전처럼 한국사 장을 새 탭에 연다.
  그래프에 없는 사건(`kind: 'extra'`)은 `<span>` 이라 열 자리가 없다.
- 캔버스가 다시 만들어져도(StrictMode·테마) 한국사를 보던 중이면 그 그래프를
  다시 싣는다 (`loadGraph` 가 `worldIdRef` 를 먼저 본다).

건드린 자리: `web/src/components/LifeView.jsx`(`openWorld`·`leaveWorld`) ·
`web/src/lib/life.js`(`LifeBoard.onHistory`, 마크의 `k-self`) · `web/style.css`
(`.stage-back`) · `design.md` §12 · `.claude/agents/graph-drawer.md` §8·§11.
CDP 로 5173·8100 둘 다에서 눌러 봤다 (주소가 그대로고, 연표·상세·범례가 함께 바뀐다).

## 2026-09-10 — 중요한 노드를 무엇으로 재나 (`central`)

사용자: "한국사나, 내 역사에서 중요한 노드를 판단할때 centrality 이론을
사용하고 있나?" — 쓰고 있던 것은 연결 차수 하나뿐이었다. 이어서 "가장 개선된
centrality 이론을 차용해서 우리 그래프에서 적용해".

**여섯을 다 돌려 보고 골랐다.** 판정 기준은 연대를 아는 사건 564건의 시대
분포를 얼마나 왜곡하는가다 (README "무엇이 중심인가" 절에 표가 있다). 기대가
컸던 조화 중심성(Boldi–Vigna 2014, 흩어진 그래프에서도 정의되고 공리적으로
가장 낫다)이 가장 심하게 몰렸고 — 상위 100건의 93%가 조선 — k-코어와 고유벡터
계열은 임진왜란 해전 22건이 만든 클리크에 통째로 빨려 들었다. 위키백과 해전
인포박스가 같은 장수 열둘을 스물두 번 되풀이해 적어 둔 탓이다.

남은 것은 **타입 가중 PageRank** 다 (`src/histgraph/central.py`). 텔레포트가
큰 덩어리의 쏠림을 막아 주고, 무게를 온톨로지로 갈라(행위 1.0 · 좌표 0.25 ·
때·족보 0.15) 문서 길이가 곧 중요도가 되는 것을 막는다. 잎이 **돌려주는** 몫도
줄였다 — 자리 25개를 혼자 지낸 김명준(1870년)이 인물 4위로 문 앞에 서 있었다.

- `centrality` 표에 미리 적고, 표가 없는 DB 는 **조용히 차수로 물러난다.**
  `scope` 가 파생본을 만들며 스스로 돌린다. 원본에는 `uv run histgraph central`.
- 바뀐 것은 **차례뿐**이다: 시작점 12개, 검색 정렬, 예산에 걸렸을 때 남길 이웃,
  같은 이름·같은 해의 연표 항목 중 남길 것. 자르지 않고(연표 568건 그대로,
  고려 81건 그대로) 크기로도 말하지 않는다 — 반지름은 지금도 차수다
  (`graph-drawer.md` §12.20 에 적었다).
- 개인 역사도 같은 자를 쓴다 (`web/src/lib/life.js` `nodeWeight`).
- 화면 딱지가 `차수 상위만 표시` → `중심에 가까운 것만 표시` 로 바뀌었고
  설정 상자의 시작점 안내도 같은 말을 쓴다.

**다음에 할 일이 하나 생겼다.** `role` 노드 285개 중 202개가 한 사람만 앉은
자리다 — '삼천리 필진'·'만몽박람회 상담역'처럼 친일인명사전의 이력 줄이 자리
노드로 서 있다. 지금은 무게가 잎 되돌림으로 눌러 두고 있을 뿐이다.
(→ 아래 절에서 했다. **진단이 반쯤 틀렸다는 것도 그때 알았다.**)

## 2026-09-10 — 대통령 자리가 일곱 조각이었다 (`dedupe` 자리 규칙)

위 절이 남긴 일을 하려고 202개를 열어 보니 **앞의 진단이 반쯤 틀렸다.** 태반은
이력 줄이 아니라 진짜 관직이었다 — 조광조의 사간원 정언, 최만리의 집현전 박사는
우리 그래프에 그 한 명만 있을 뿐 자리는 참이다. 진짜 병은 따로 있었다:
**같은 자리가 여러 노드로 갈라져 있었다.** 대통령 하나가 일곱 조각이었다
(`대통령` 4명 · `대한민국 대통령` 15명 · `1대 대통령` · `2대 대통령` ·
`대한민국 초대 대통령` · `대한민국 임시정부 대통령` · `대통령 대리`). 산문
추출이 만든 `ex:role:` 노드가 Wikidata 의 자리와 따로 서 있던 것이라, 왕 자리가
09-09 에 앓은 그 병이다 (`positions`).

`dedupe` 에 여섯째 규칙을 더했다 — **한쪽 이름이 다른 쪽으로 끝나면** 후보
(`role` 에만 건다). 포함이 아니라 끝맺음인 것이 중요하다: 포함으로 재면
'조선 왕세자'가 '조선 왕'의 후보가 된다. 두 글자 자리 이름은 총칭이라 아예
뺀다(`SEAT_MIN`) — 실측 36쌍 중 35쌍이 다른 자리였다.

- 후보 60쌍을 `data/duplicates.tsv` 에 판정했다. **합친 것은 16쌍뿐이다** —
  가르는 기준은 기관·나라·품계만 앞에 붙었는가 하나다. '동부승지'는 승지 여섯
  중 하나이고 '법무부 장관'은 남과 북이 따로 있다.
- 한 사람짜리 자리 202 → 189. 남은 것은 자료가 얇은 진짜 관직이다.
- 원본·파생본에 다 씌웠고 무게를 다시 쟀다.

**덤으로 §1 위반을 하나 잡았다.** 아홉 문서의 설명이 크메르 문자 두 글자
(`ពᐙ`)와 빈 줄로 시작하고 있었다 — 대한민국 대통령·국무총리·대법원장·
국회의장·헌법재판소장·햇볕정책 등. 뒤에 한글이 있어서 한국어 관문이 놓친다.
`wikipedia.clean_lead` 가 이제 도입부 첫 줄에 한글도 한자도 로마자도 없으면
그 줄을 뗀다. 있던 아홉 건은 편집 계층에 적어 고쳤다.

**아직 남은 것 둘.**
1. 자리가 아닌 것이 자리로 서 있다 — '청백리'(포상) · '선비'(신분) · '정7위'·
   '종6위'(품계) · '백작'·'훈1등 자작'(작위) · '좌리공신 2등'(녹훈) ·
   '삼천리 필진'(잡지 필자) · '신간회 결성 참여자'. 합칠 것이 아니라 타입을
   내리거나 관계를 낮출 일이라 `dedupe` 가 다룰 수 없다.
2. 자리는 맞는데 사람이 틀린 것 — 최만리에게 고려 관직 '문하시중'이,
   장덕수에게 '대통령 권한대행 겸 내각 수반'이 붙어 있다. `homonyms`·`roles`
   쪽 일이다.

## 2026-09-09 — 띠의 이름은 취임한 날 자리에 선다 (`dateRuler`)

사용자: "이재명 대통령은 2025년 6월에 취임했어 그럼 2025년 6월 오른쪽에 이름을
둬야 하는거야."

날짜를 표에 바로잡아 놓고도 화면은 여전히 해로만 앉히고 있었다. 처음엔 해를
소수로 고르게 나눠 앉혔는데(`server._year_at`), 그것으로도 모자랐다 — **몰린
해는 사건이 날짜 순으로 한 칸(GAP 30)씩 선다**(`placeMarks`). 2025년에 사건이
아홉 서 있어 그 해 칸이 270px 인데, 6월 4일을 해의 42% 자리에 앉히면 이름이
4월 사건 옆에 섰다.

그래서 `dateRuler` 가 자를 둘로 나눠 준다.

- 그 해에 **사건이 서 있으면 그 사건들 사이**에 앉힌다. 앞뒤 사건의 가운데다 —
  한 사건의 칸에 딱 맞추면 그 사건이 곧 취임인 것처럼 읽힌다.
- 사건이 **없는 해**는 서버가 준 소수로 나눈다 (`_year_at`).

실측(화면): 이재명이 `제21대 대통령 선거`(06-03)와 `2025년 한반도 집중호우`
(07-16) 사이에, 윤석열이 `윤석열 대통령 퇴진 운동`(2022-05-01)과 `이태원 참사`
(10-29) 사이에, 최규하가 `12·12 군사 반란`(1979-12-12)과 `1980 언론통폐합사건`
사이에 선다.

개인 역사도 같은 자를 쓴다 — 눈금이 되는 것은 **역사 열**이다 (개인 열은
사람마다 달라 자가 될 수 없다).

## 2026-09-09 — 대통령의 띠는 취임한 날에서 시작한다 (`terms` · `data/terms.tsv`)

사용자: "대통령 타임라인을 대통령 취임날을 기준으로 만들어줘."

띠의 날짜는 Wikidata 의 P39 한정어에서 온다. 임금은 그것으로 맞는데
**대통령은 취임한 날이 아닌 것이 넷 섞여 있었다.**

| 사람 | 있던 날 | 그 날의 정체 | 취임일 |
|---|---|---|---|
| 박정희 | 1962-03-24 | 윤보선 사임 뒤 권한대행을 맡은 날 | 1963-12-17 (5대) |
| 최규하 | 1979-10-26 | 박정희 피살로 권한대행을 맡은 날 | 1979-12-21 (10대) |
| 전두환 | 1980-08-27 | 통일주체국민회의가 뽑은 날 | 1980-09-01 (11대) |
| 윤보선 | 끝 1962-03-24 | 박정희가 권한대행이 된 날 | 끝은 사임한 1962-03-22 |

`fetch_reigns` 가 같은 (인물, 자리)의 문장을 **가장 이른 시작**으로 모으므로
권한대행 문장이 하나만 있어도 그것이 띠의 시작이 된다. `drop_nested_terms`
는 못 잡는다 — 그건 남의 임기 **한가운데서** 시작하는 임기를 빼는 규칙이고
(황교안), 권한대행은 앞 사람이 물러난 **뒤에** 시작한다.

판정은 표가 한다 (`data/terms.tsv`, 근거는 대통령기록관 pa.go.kr 의 취임식
기록). `uv run histgraph terms` 가 씌우고, **표에 없는 대통령이 남으면 종료
코드 1** 로 묻는다. `reigns` 도 끝에 이 표를 다시 씌운다 — 안 씌우면 돌릴
때마다 위키 값으로 되돌아간다.

한 사람이 여러 대를 지내도 띠는 하나다(사용자 결정) — 사람이 시간을
'박정희 때'로 읽지 '제7대 때'로 읽지 않는다. 권한대행 구간은 어느 띠에도
안 들어가므로 1962~1963 처럼 **대통령이 없는 빈 데**가 생기고, 그것이 참이다.

## 2026-09-09 — "왕 노드에 왜 고려왕만 연결되어 있지?" (`positions`)

화면의 `왕(王)` 노드에 고려 왕 34명만 서 있었다. 조선 임금 28명은 `조선 임금`
이라는 **다른 노드**에, 그것도 직위가 아니라 **단체(org)** 로 앉아 있었고,
추출이 산문에서 만든 `ex:role:고려 왕`·`ex:role:조선 왕` 이 한 명씩 물고 따로
서 있었다. 한 자리가 넷으로 갈려 있던 셈이다.

원인은 우리 규칙이 아니라 자료였다. **Wikidata 는 왕조마다 자리 항목이
다르다** — 조선에는 전용 항목(Q22304810)이 있지만 고구려·고려 임금은 일반
항목인 '왕(王)'(Q12087706)·'군주'(Q116)를 나눠 쓴다. 고려·고구려 임금 자리는
Wikidata 에 항목 자체가 없다 (`wbsearchentities` 로 한국어·영어 모두 0건).
`fetch_monarch_positions` 의 머리글이 그 사실을 이미 적어 두고 있었다 — 재위
띠는 그래서 양쪽 다 받았지만, **그래프의 노드는 갈린 채였다.**

거기에 버그가 하나 겹쳤다. `_labels` 가 P31 을 **아무거나 하나** 집어 왔다.
'조선 임금'은 P31 이 둘(자리 Q4164871 · 군주 Q116)인데 군주가 먼저 와서
`WD_CLASS_TO_TYPE` 에 없는 값으로 떨어졌고, `_infer_type('held_position')`
의 기본값이 `org` 라 단체로 앉았다. 아는 클래스를 먼저 고르도록 고쳤다
(직위 대상 744개 중 185개가 같은 이유로 단체로 앉아 있다).

**가르는 것은 `uv run histgraph positions`.** 일반 자리에 걸린 참여를 그
사람의 왕조(`from_period`)로 갈라 `ex:role:{왕조} 왕` 으로 옮기고, 왕조 전용
자리는 이름·타입만 바로잡는다. 짐작으로 옮기지 않는다 — 시대 엣지가 없어
왕조를 모르는 사람(고구려 임금 7명)과 재위 근거가 없는 참여(임해군·순화군,
지금 Wikidata 에 그 P39 자체가 없다)는 **세어서 보여만 준다.**

옮긴 뒤:

```
고려 왕   34명  (ex:role:고려 왕)
조선 왕   28명  (wd:Q22304810)
군주       2명  (wd:Q116 — 재위 근거가 없어 남은 왕자 둘)
```

두 자리가 편집 계층에 남는다. 옛 엣지에 `deleted` 를 적어 다음 수집이 일반
자리 엣지를 되살려도 저장소가 다시 지우고, 새 엣지에는 재위 표식과 날짜를
다시 적는다. 새 자리 노드는 `props.wd_position` 에 원래 QID 를 들고 있고
`reigns` 가 그 QID 로 Wikidata 에 물어본다 — 안 남기면 `ex:` 자리에 앉은
고려 왕 34명이 재위 띠를 영영 못 받는다. `cmd_reigns` 의 `dst LIKE 'wd:%'`
조건을 그래서 걷어냈다.

연표의 띠 이름이 `왕(王)`·`조선 임금` 에서 `고려 왕`·`조선 왕` 으로 바뀐다.

## 2026-09-09 — 지금 일어나는 일을 주마다 걷는다 (`recent` · launchd)

사용자: "현대사가 2025년 대통령 선거가 끝인데 그 이후 사건을 주기적으로 수집
할 수 있도록 사건 스케쥴러를 만들자."

사건 시드 표(`wikipedia.EVENT_SEEDS`)는 **사람이 손으로 적는 명단**이라
어제 일어난 일을 담지 못한다. 그래서 해마다 저절로 자라는 자리에서 걷는다 —
한국어 위키백과의 `분류:{해}년 대한민국` (하위 분류 한 단계까지).

그 분류에는 사건과 드라마와 프로야구가 함께 산다 (실측: 후보 92건 중 사건
42 · 드라마 13 · 스포츠 16). 가르는 자리가 셋이다.

| 무엇으로 | 무엇을 가르나 |
|---|---|
| Wikidata 클래스 계층 (뿌리 `Q1190554`) | 드라마·작품은 사건 계층에 안 닿아 저절로 빠진다 |
| `filters.is_sports` · 스포츠 클래스 | 리그·시즌·선수단은 계층에 닿으므로 한 번 더 거른다 |
| 문서 자신의 분류 (`DROP/KEEP_CATEGORY`) | **QID 가 없는 새 문서** — 후보 넷 중 하나가 그렇다 |

QID 없는 문서를 버릴 수 없었던 이유: 어제 생긴 문서일수록 Wikidata 가
없는데, 하필 그쪽이 사고·화재·붕괴·재판처럼 연표에 서야 할 것들이다
(서산영덕고속도로 30중 추돌사고·인천 쿠팡 물류센터 화재·윤석열 비상계엄
재판). 그런 것은 `works` 가 작품에 쓰는 자리와 같은 `kowiki:{문서명}` 으로
세운다. 나중에 Wikidata 가 붙어도 **이름이 같으면 다시 만들지 않는다** —
중복을 만들어 놓고 `dedupe` 로 되찾는 것보다 안 만드는 쪽이 싸다.

날짜는 **문서 첫 문장**이 Wikidata 보다 앞선다. Wikidata 는 정밀도를 값과
따로 적어서 해만 아는 항목도 `2026-01-01` 로 돌아오는데(실측: 인천 송도 총격
사건·대전 자동차 부품 공장 화재가 그렇게 1월 1일에 섰다), 정의 문장은 그런
거짓 정밀도를 만들지 않는다. 괄호 안의 날짜는 읽지 않는다 — 정의 문장이
사람을 부를 때 다는 생몰년이다 ('노동자 김충현(1975년 ~ …)' 때문에 2025년
태안화력발전소 사고가 1975년에 섰다).

**판정이 안 서면 세우지 않고 이름만 찍는다.** 사람이 `data/recent.tsv` 에
한 줄 적으면 다음 주에 들어온다 (`판정<TAB>문서명<TAB>날짜<TAB>근거`).
지금 그 표에 여섯 줄이 있다 — 권한대행 '정부' 셋과 K-휴머노이드 연합은
체제·단체라 뺐고, 누리호 4차 발사와 윤석열 대통령 지지 시위는 기계가 못 갈라
넣었다.

첫 수집으로 사건 42건이 화면 DB 에 섰다 (1994년 봉대산 방화부터 2026-09-16
한-중앙아시아 정상회의까지). 연표가 그 자리에 세우는 것을 확인했다.

**주마다 도는 것은 launchd 다** (매주 월요일 09:00, `tools/scheduler/`).

```
sh tools/scheduler/install.sh              # 건다
sh tools/scheduler/install.sh --run        # 지금 한 번
sh tools/scheduler/install.sh --uninstall  # 걷는다
```

`weekly.sh` 가 하는 일: 화면 DB 에 커밋 안 된 수정이 있으면 **그 주는 걷지
않는다** (다른 세션이 만지는 중일 수 있고 DB 는 바이너리라 통째로 실려
간다) → 원본·파생본에 한 번씩 수집 → `check_korean` · 파이프라인 테스트 →
화면 DB 만 이름을 대고 커밋 → `git push origin HEAD` (pre-push 훅이 한 번 더
잰다). 로그는 `~/.cache/histgraph/recent/{날짜}.log`.

남은 것 둘:

1. 새 사건은 **엣지가 없다** (차수 0). 연표에는 서지만 그래프에서는 외딴
   점이다. 인포박스·추출로 참여자를 잇는 것은 아직 안 걸었다.
2. 설명이 위키백과 도입부 그대로다. `paraphrase`(MLX)를 돌려야 우리 말로
   새로 쓴 글이 되는데, 35GB 를 잡는 모델이라 주간 작업에 넣지 않았다.
   화면은 그동안 도입부와 출처 한 줄을 낸다 (설계된 물러남).

## 2026-09-09 — 내 역사: 상세에서 고치고, 두 계정 사이를 옮긴다 (`lifesync`)

### 상세의 '삭제' 옆에 '편집'

사용자: "'삭제' 옆에 '편집'버튼을 만들어줘 … 그 노드에 표시된 모든 정보를
사용자가 직접 편집 … 완료 버튼 누르면 그래프와 연표에 바로 반영". 상세에 선
차례 그대로 칸이 된다 — 종류·이름·날짜·장소·설명, 연표(날짜 글·시절), 전환점,
함께, 그 무렵의 한국사, 만약 없었다면. 고치는 것은 지우기와 같은 **날것의
문서**(`life.editNode`)라 브라우저와 계정에 같이 남고, 완료를 누르면 `adopt`
가 다시 받아들여 그래프·연표가 그 자리에서 다시 선다.

칸에 **없는** 것이 셋이다. **감정**은 같은 날 내렸다 ("노드의 '감정' 메뉴도
삭제해 필요 없어") — 모델이 사건마다 붙인 한 낱말이라 근거가 없다. **1~10
점수**도 칸으로 두지 않는다 (화면이 안 그리는 값을 고치라고 내밀면 그 눈금이
칸으로 되돌아온다). **관계**는 사용자가 빼라고 했다 ("편집 메뉴에서 관계를
빼라고") — 화면에 선 선 가운데는 문서에 없는 것이 있어(섬 잇기·참여자 풀기·
가족 호칭을 코드가 긋는다) 칸으로 내밀면 사람이 고른 것과 코드가 그은 것이
한 자리에서 섞인다. 실제로 관계를 빼도 `linkOrphans` 가 곧바로 다시 그었다.

### 고친 것이 화면에 안 서던 자리 둘 (2026-09-09 지적)

사용자: "노드 편집에서 편집을 하고 완료 버튼을 눌렀는데 편집내용이 반영이 안
되고 편집전 정보가 유지 되고 있어." 전수로 눌러 보니(브라우저를 CDP 로 몰아
45 노드 × 칸마다) **이름·날짜·장소·설명·종류·전환점·함께는 다 들어갔고**, 두
칸이 안 섰다.

- **시절.** 칸이 고른 값은 문서에 들어가는데 화면이 옛 값을 그렸다 — 연표의
  단계를 셈하는 규칙(한 방향 · 같은 해 앞 항목 · 앞뒤가 같을 때 · 군복무)이
  사람이 고른 칸을 **그 자리에서 도로 덮고** 있었다. 실측: 사용자 문서의 연표
  12건 중 8건이 그렇게 되돌아갔다. 사람이 고른 칸에 표식(`stage_said`)을 남기고
  셈이 그것을 건너뛴다 — 날짜에 이미 있던 규칙과 같다(`dateSaid`). 표식이 붙은
  칸은 **뒤따르는 항목의 바닥(`cur`)도 되지 않는다**: 한 칸을 고쳤는데 안 건드린
  뒷일까지 따라 움직이면 그것도 '내가 안 한 일'이다. `web/src/lib/life.js`
  `normalize` 와 `histgraph.life.refine` **두 곳에 같이 건다** — 화면이 그리는
  자리와 새로고침이 지나는 자리가 다르다.
- **끝.** 칸은 '끝'을 받는데 상세의 날짜 줄은 시작만 그렸다. 끝을 아는 일은 한
  점이 아니라 기간이라 `1990-03-01 ~ 2004-02-15` 로 선다 (시작과 같은 날이면
  기간이 아니므로 그대로 둔다).

`histgraph serve` 는 코드를 다시 읽지 않으므로, 새로고침 뒤에도 시절이 남는
것을 보려면 API 를 한 번 다시 띄운다.

### 로컬과 배포의 내 역사가 갈린다 — `histgraph lifesync`

사용자: "지금 로컬과 프로덕션의 내 역사가 달라". 로컬은
`data/accounts.sqlite`, 배포는 Neon 이고 **둘은 다른 표**다 (2026-09-09 결정:
로컬에서 눌러 본 '지우기'가 배포 가입자에게 닿지 않게 `.env` 의 이름을 갈아
두었다 — `HISTGRAPH_PROD_DATABASE_URL`). 그래서 로컬에서 적은 이야기는 배포에
안 간다. 그날 실측으로 로컬 49 노드·16 덩어리, 배포 36·11 이었다.

옮기는 길을 명령으로 뒀다. **아무 준비도 필요 없다** — `.env` 만 있으면 된다:

```
uv run histgraph lifesync                 # 두 곳을 견줘 본다 (아무것도 안 쓴다)
uv run histgraph lifesync --push          # 로컬 → 배포
uv run histgraph lifesync --pull          # 배포 → 로컬
uv run histgraph lifesync --push --force  # 받는 쪽에만 있는 것을 알면서 덮는다
uv run histgraph lifesync --email a@b.com # 로컬 가입자가 여럿일 때
```

지키는 것 셋:

- **기본은 비교만 한다.** `--push`·`--pull` 이 없으면 아무것도 쓰지 않는다.
- **덮기 전에 잃을 것을 센다.** 받는 쪽에만 있는 노드·이야기가 있으면 멈추고
  수를 찍는다 (`--force` 로 넘긴다). 반대로 한 번 당겨 보라고 적어 준다.
- **쓰기 전에 받는 쪽을 받아 둔다** — `data/life/backup-{neon|local}-{때}.json`
  (저장소 밖이다).

두 표를 잇는 것은 이메일이 아니라 `google_sub` 다 (같은 사람의 id 가 로컬 1 ·
배포 26). `accounts.prod_store()` 가 `HISTGRAPH_PROD_DATABASE_URL` 을 읽는
**유일한 자리**이고, `serve` 도 `open_store()` 도 거기로 오지 않는다 — 로컬은
늘 SQLite 라는 결정이 그대로다. 검사는 배포 자리에도 SQLite 를 세워 네트워크
없이 돈다 (`tests/test_pipeline.py` 의 lifesync 절).

## 2026-09-09 — 관리실 `/console` (가입자 목록)

사용자: "관리자만 접근할수 있는 관리 페이지를 만들자. 주소 끝에 '/console'로
접근하게 해줘. 일단 가입자의 목록만 볼수 있게 해줘."

`src/histgraph/console.py` 한 장이다. 서버가 통째로 그려 준다 — 화면 코드
(`web/`)에는 아무것도 없다. **문은 서버에서 잠근다**: 세션 쿠키로 사람을
확인하고 `HISTGRAPH_ADMIN_EMAILS` 에 그 이메일이 있을 때만 표를 읽는다.
관리자가 아니면 **가입자가 몇 명인지도 말하지 않는다** (403).

| 누가 | 무엇을 본다 |
|---|---|
| 로그인 안 함 | 401 · "관리자 계정으로 로그인" (돌아올 곳을 `next=/console` 로 적어 준다) |
| 로그인했지만 관리자 아님 | 403 · 그 밖에는 아무것도 |
| 관리자 | 이메일 · 이름 · 가입일 · 마지막 로그인 · 상태, 그리고 모두 몇 명인지 |

**주소가 둘이다.** 로컬은 `/console` 로 오지만 배포는 rewrite 가
`/api?__p=console` 로 넘겨 `/api/console` 이 된다 — `auth.ROUTES` 에 두 이름을
함께 적었다 (`pages.route` 가 `/n/` 을 다루는 것과 같은 사정). 손댄 자리는
넷이다: `vercel.json` 의 rewrite · `web/vite.config.js` 의 프록시(개발 서버
5173 이 자기 index.html 을 내주지 않도록) · `api/index.py` 의 임포트 한 줄
(번들은 임포트를 따라가며 담는다 — 늦게 부르기만 하면 배포에서만
ImportError) · `auth.ROUTES`.

두 가지를 고쳐 두었다.

- **시각은 한국 시각으로 세운다.** 두 저장소 다 세계시로 적어서
  (`now()`·`CURRENT_TIMESTAMP`) 그대로 두면 저녁에 가입한 사람이 오전에 온
  것으로 보인다 (`console.when`).
- **어느 표를 보고 있는지 적는다** — '이 컴퓨터의 가입자 표' / '배포 가입자 표'.
  로컬에는 나 혼자 있어서, 섞어 보면 가입자가 한 명인 줄 안다 (이 세션에서
  실제로 그렇게 읽었다). `accounts.where()` 는 파일·제품 이름을 그대로 내므로
  화면에는 안 쓴다 (§1).

터미널로 세는 길은 그대로다 —
`DATABASE_URL="$HISTGRAPH_PROD_DATABASE_URL" uv run histgraph accounts`.

## 2026-09-09 — 방문 통계를 붙였다 (`G-93TJVV76N0`)

구글 애널리틱스(GA4)를 부를 자리를 화면 네 장(그래프·내 역사·방침·약관)과
서버가 그리는 `/n/` 장에 놓았다. **다섯 장이 `web/public/analytics.js` 하나를
부른다** — 광고 번호(`ca-pub-`)가 네 곳에 따로 박혀 있어 하나만 고치면 조용히
어긋나던 것을 되풀이하지 않으려고 그렇게 했다 (`web/tests/render.test.mjs` 가
다른 파일에 `G-` 가 박히면 잡는다).

측정 ID 는 그 파일의 `var ID` 한 줄뿐이다. 비우면 그 순간 꺼지고 요청이 나가지
않는다. 헤드리스 크롬(CDP)으로 네 장을 열어 태그와 집계 요청이 실제로 나가는
것을 확인했다 (`googletagmanager.com/gtag/js?id=G-93TJVV76N0` → `/g/collect`).

**남은 것 둘:**

1. 콘솔에서 보관 기간을 14개월로, 구글 신호 데이터(광고 연동)는 끈 채로 둔다 —
   방침이 "광고와 잇지 않습니다 · 보관 14개월"이라고 적혀 있다. 콘솔 작업은
   사람 몫이다 (가입 시스템과 같다).
2. ~~배포 시점~~ — 끝났다. 방침 **제4판은 2026년 9월 9일 공고·시행**이다
   (사용자 결정: "지금 바로 운영에 올린다"). 제2조가 "방문자 분석 도구를 쓰지
   않습니다"라고 적어 두었던 자리라 30일 공고가 걸릴 뻔했는데, 제12조의 30일은
   **회원의 개인정보**에 관한 불리한 변경에 걸리는 것으로 가르고, **이용자가
   스스로 끌 수 있는 쿠키**에 관한 변경은 무엇을 모으고 어떻게 거부하는지를
   방침에 함께 적어 같은 날 시행할 수 있다는 단서를 제12조에 두었다. 제3판
   (09-16 시행 예정)도 제4판에 담겨 같은 날 시행된다.

방침 제4판이 고친 자리: 머리말(다루는 자리 셋 → 넷) · 제1조 목적 · 제2조 4
(방문 통계 신설, 옛 4 는 5 로) · 제3조 보유 기간 표 · 제4조 국외 이전 표
(구글 행을 로그인/방문 통계로 가름) · 제6조 쿠키 · 제7조 거부 방법 · 제12조 이력.

## 2026-09-09 — 개인 역사를 배포한다 (`life_post` · 요청 하나 안에서 돈다)

사용자: "개인 역사도 이제 배포 해줘". 2026-09-07 에 걸어 둔 `LIFE_PAGE = !VERCEL`
을 푼 세션이다. **스위치만 푸는 것으로는 안 됐다** — 배포된 API 에 물어보니
`POST /api/life/analyze` 가 405 였다. 화면이 이야기를 넣는 유일한 문이 그것이라,
스위치만 풀면 오른쪽 열이 영영 빈 채로 단추만 서 있는 껍데기가 된다.

막힌 것은 셋이었고 셋 다 구조였다:

- **서버리스에는 띄워 두고 물어보는 길이 없다.** 함수가 응답과 함께 죽어서
  스레드도 그것이 적은 상태도 다음 요청이 못 본다 (`LifeAnalysis` + `/api/life/job`).
- **디스크가 읽기 전용이다.** 분석이 끝나면 `data/life/<이름>.json` 을 쓰던 자리.
- **`api/index.py` 의 `do_POST` 가 가입 경로만 받았다** (그 밖은 405).

만든 것:

- `server.run_analysis` — `LifeAnalysis._run` 의 몸통을 모듈 함수로 뽑았다.
  `save=False` 면 파일을 만들지 않고, `step` 을 안 주면 진행을 아무 데도 안 적는다.
- `server.life_post(path, raw, *, blocking, save)` — POST 두 길(analyze·refine)의
  몸통 하나. 로컬은 `blocking=False`(202 + 폴링), 배포는 `blocking=True`(200 + 끝난
  상태). 부르는 자리가 둘이라 여기 있다.
- `api/index.py` — `do_POST` 가 `LIFE_POSTS` 를 받고, `_life_gate` 가 **로그인과
  CSRF 표를 함께** 잰다. 로컬과 반대로 **가입이 안 켜져 있으면 503** 이다: 열린
  인터넷에서 문을 안 잠그면 아무나 남의 이름으로 우리 모델을 부른다.
- `vercel.json` `maxDuration: 300` — 무료 모델 실측 54초.
- 화면(`LifeView`) — 답이 오는 길 둘이 `finish()` 에서 만난다. 가르는 것은 **몸**
  이다(`state` 가 `done`·`error` 면 다 온 것). `/api/life/job` 이 `blocking` 을
  미리 알려 주므로, 한 번에 오는 자리에서는 초를 스스로 세고("서버가 안 세어 준다")
  "창을 닫아도 계속 돕니다" 대신 "이 창을 열어 둔 채로 기다려 주세요" 라고 적는다.
  `postJson` 이 CSRF 표와 쿠키를 함께 싣는다.

**배포에서 세 번 헛짚은 것** — 화면을 올린 뒤 실제로 이야기를 보내 보니 500 이었다:
`FileNotFoundError: /var/task/src/histgraph/life_prompt.md`. 원인이 겹쳐 있었다.

1. `vercel.json` 의 `excludeFiles` 가 문서를 빼려고 적은 `*.md` 가 **패키지가 읽는
   프롬프트까지** 걷어냈다. 뺄 문서는 이름으로 적는다.
2. 파이썬 런타임은 **임포트를 따라간 `.py` 만 담는다.** 그 밖의 파일은
   `includeFiles` 로 이름을 대야 실린다.
3. **진짜 범인은 `.vercelignore` 의 `*.md`** 였다. 그 파일 첫 줄에 "CLI 로 올릴
   때만 쓰인다. Git 연동 배포는 이 파일을 보지 않는다"고 적혀 있어서 두 번을
   헛짚었다 — **본다.** 주석을 사실로 고쳤다.

관문이 두 자리를 다 읽는다: `src/` 의 비-파이썬 파일이 `excludeFiles` 나
`.vercelignore` 의 어느 줄에 걸리면 실패하고, `includeFiles` 가 안 덮어도 실패한다.

**사람이 해야 하는 것 (2026-09-09 에 다 됐다)** — Vercel 환경변수 다섯. 이것이
없으면 배포된 내 역사는 503 이다: `GOOGLE_CLIENT_ID` · `GOOGLE_CLIENT_SECRET` ·
`HISTGRAPH_SESSION_SECRET`(32자 이상) · `DATABASE_URL` · `OPENROUTER_API_KEY`.
`DATABASE_URL` 은 Vercel 대시보드의 Storage → Neon 통합으로 붙였다 (접두사를
`DATABASE` 로 줘야 이름이 맞는다. 리전 us-east-1). 표는 `uv run histgraph accounts
--init` 으로 한 번 세웠다. 구글 콘솔 리디렉션 URI 는 이미 등록돼 있었다.

**끝까지 확인한 것** (임시 계정으로 배포에 실제 요청):

```
/life.html                       200
/api/me                          enabled: true
POST /api/life/analyze  비로그인  401
POST /api/life/analyze  로그인    200 · 41초 · state=done · 파일 None
```

테스트 1,374 · 화면 84.

## 2026-09-09 — 개인정보처리방침·이용약관 제3판 (‘내 역사’ · 외부 모델 국외 이전)

사용자: "구글 로그인/가입 시스템을 구축했고, 내 역사 기능도 넣었으니 개인정보처리방침,
이용약관을 이에 맞게 고쳐야해. 법률과 형식에 맞게." 09-08 판에 없던 것은 **‘내 역사’
이야기가 오픈라우터(미국) → 엔비디아 무료 모델로 나간다는 사실**이었다 — 개인정보
보호법 제28조의8(국외 이전)이 요구하는 항목(이전받는 자·국가·항목·목적·보유 기간·
거부 방법)이 하나도 없었고, 엔비디아의 무료 창구는 "개인정보를 올리지 말라, 보안과
제품 개선을 위해 기록한다"고 밝히고 있다.

- `web/privacy.html` — 법 제30조·시행령 제31조의 항목 순서로 조문(제1~12조)을 다시
  적었다: 목적 · 항목(가입 / 담은 것 / 접속 기록 / 안 모으는 것) · 보유 기간 표 ·
  **위탁·국외 이전 표**(구글·네온·버셀·오픈라우터·엔비디아) · 파기 · 쿠키 · 행태정보
  (애드센스) · 정보주체 권리(법정대리인·자동화 결정) · 안전성 확보 조치 · 보호책임자와
  구제 기관(분쟁조정위 1833-6972 · 118 · 1301 · 182) · 개정 이력. ‘내 역사’ 이야기에
  남의 정보·민감정보가 들어갈 수 있다는 것과 그 책임을 적었다.
- `web/terms.html` — 약관규제법 관례의 조문 형식(제1~18조 + 부칙): 정의 · 약관 변경
  (7일, 불리하면 30일) · 회원가입·계정 관리 · **제7조 ‘내 역사’**(글이 밖으로 나간다,
  결과는 자동 생성이라 틀릴 수 있다, 남을 적을 때의 책임) · **제8조 이용자 콘텐츠**
  (권리는 회원, 운영자는 서비스 범위에서만, 학습·공개 안 함) · 탈퇴·자격 상실(소명
  열흘) · 서비스 종료 30일 통지와 내려받기 · 면책 · 준거법.
- 문서가 "첫 화면에서 알린다"고 적으므로 `LifeView.jsx` `Empty` 의 밖의 모델 문장에
  "글은 밖의 모델 제공자에게 보내집니다 — 개인정보처리방침 제4조" 한 줄을 붙였다.
  `StoryBox` 는 09-08 사용자 결정대로 여전히 아무 말도 안 한다.
- `web/doc.css` 에 표·`ol`·`h3` 스타일. 영어 관문(`render.test.mjs` "사람이 읽는
  글자에 영어가 없다")이 `512KB` 를 잡아 `512킬로바이트`로 고쳤다 — 쿠키 이름·회사의
  로마자 이름은 문서에 못 쓴다.
- 시행일은 공고(09-09) 7일 뒤 **09-16** — 문서 스스로 정한 규칙이다.

**남은 것 (사람 몫)**: (1) Neon 프로젝트를 만들면 리전을 보고 방침 제4조 ‘네온 · 미국’
을 실제 소재지로 맞춘다 (서울 리전이면 "미국 회사 · 서버는 대한민국"). (2) 민감정보·
국외 이전은 법이 **별도 동의**를 요구하는 쪽으로 읽힐 수 있다 — 지금은 ‘입력’ 을 동의로
본다고 적어 두었을 뿐이다. 이야기 상자에 동의 확인 칸을 둘지는 사용자 결정. (3) 모델을
바꾸면(`OPENROUTER_MODEL`) 제4조의 제공자 줄도 다시 본다. 같은 날 사용자 결정: **모델 제공자의
회사명은 문서에 적지 않는다** — '오픈라우터가 중계하는 모델 제공자'로 묶는다.

## 2026-09-08 — 함께한 사람은 이름으로 선다 (`participants` · `nodeLabel`)

사용자: "'1998년 4월 24일 메탈리카 공연을 친구 김일권과 함께 갔어' 라고 입력했어 …
'함께 person_1' 이라고 말하고 있어. 김일권이라고 말해야 했고, 함께한 사람이 없으면
그 '함께' 섹션 자체를 보여주면 안 돼. person_1이라고 변수 이름을 바로 노출 하면 안 돼."

- **아이디는 화면에 내지 않는다** (CLAUDE.md §1). 상세는 `node.participants` 를 그대로
  이어 붙이고 있었고, `nameOf` 는 모르는 아이디를 그대로 되돌려 주고 있었다
  (`byId.get(nid)?.name || nid`). 이제 한 규칙이다 — `life.js` `nodeLabel(byId, id)` 는
  **그래프에 있으면 그 이름, 없으면 한글로 적힌 말일 때만 그대로, 아니면 빈 칸**이다.
  상세의 '관련'·가족·전환점·영향·영향력이 다 이것을 쓴다.
- **'함께' 줄은 나 말고 같이 있던 사람이다.** 아이디를 이름으로 풀고, 주인공과 못 푸는
  식별자를 뺀 뒤 **남는 사람이 없으면 줄 자체를 세우지 않는다.**
- **한 사람은 한 번만 선다.** 함께한 사람은 사건에 이어지므로(아래) 아래 '관련'에도
  선다 — 위아래로 두 번 섰다 (같은 날 사용자: "왜 함께가 두 번 들어가지 한 번만
  보여줘"). '관련'이 맡는다: 역할('함께')이 머리글이고 '당사자 나'와 나란히 선다.
  위의 '함께' 줄에 남는 것은 **그래프에 노드가 없어 선을 못 그은 이름**뿐이다.
- **그래프에 있는 것은 어디서든 눌러서 옮겨간다** (같은 날 사용자: "함께에 있는 사람이
  node에 있으면 링크를 걸어 줘야지"). '관련'의 상대가 이름만 적혀 있었다 — 원인·결과
  (`Rel`)와 같은 단추가 됐고, 분석 탭의 가족·전환점·영향·영향력도 한 `Link` 로 모았다.
  노드가 없는 이름만 글자로 선다.
- **participants 는 한 가지 꼴(노드 id)로 맞춘다** (`life.link_participants` ←
  `refine`, `life.js` `linkParticipants` ← `normalize`). 모델이 id 로도 이름으로도
  적기 때문이다. 푼 사람은 사건에 잇는다 (사람 → 사건 `experienced`, 역할은 `tidy_edges`
  가 '함께'로 단다) — 그래프에도 선이 서고 상세의 '관련'에도 뜬다.
- **모델이 주인공만 적어 놓았다.** 그래서 이야기에서 직접 찾는다
  (`participants_from_story`): **한 문장이 사건과 사람을 함께 부르면** 그 사람도 그
  자리에 있었다. 문장이 사건을 **이름으로 부르거나 달까지 아는 날짜로** 가리킬 때만
  잰다 — "1997년에 입학했고 김일권을 만났어" 처럼 해만 같은 문장은 그 해의 일을 여럿
  담는다. 기억(`Memory`)은 날짜로 잡지 않는다 (공연에 함께 간 사람이 '공연 티켓'에도
  서면 안 된다). 지시문에도 한 줄 더했다 — 함께한 사람을 participants 에 **노드 id 로**
  적고 주인공만 적지 말라고.

실측: 이야기를 사용자가 말한 그대로 두면 `공연 participants = [나, 김일권]` 이 되고
`김일권 -experienced(함께)-> 메탈리카 공연 관람` 이 선다. 티켓은 `[나]` 그대로다.

**곁다리로 잡은 것 — `refine` 안에서 원문이 가려지고 있었다.** 반복문이 `text` 라는
같은 이름을 써(`for text in (…)`, `text = f"{이름} {설명}"`) 마지막 관문
`gate_connections(payload, text)` 가 **원문 대신 남의 인물 노드 설명**을 읽었다.
결과는 조용한 몰살이다 — 이야기가 분명히 부른 역사 연결까지 "이야기가 부르지 않은
사건"으로 버려졌다 (실측: 원문에 '외환 위기'가 있는데도 버림 1건 / 남은 것 0건.
`data/life/나.json` 의 `historical_connections` 가 0 인 것이 이 때문으로 보인다).
이름을 갈랐고(`said_in`·`about`) 테스트로 잡아 둔다. **이미 버려져 저장된 연결은
되살아나지 않는다** — 그 이야기를 다시 읽혀야 다시 선다.

## 2026-09-08 — 연도는 달보다 위에 선다 (`yearCells` · 달까지 아는 날짜가 해를 이긴다)

사용자: "연도가 항상 달보다 위에 올라가야해. 메탈리카 공연은 1998년 이었어."
연표의 개인 열에 `4월 메탈리카 공연 관람` 위에 서고 그 아래 `1998 메탈리카 공연
티켓` 이 섰다 — 시간이 거꾸로 읽힌다.

두 가지가 겹쳤다.

- **해가 틀렸다.** 공연 노드의 날짜는 `1998-04-24` 인데 연표 항목에는 `year: 1997 ·
  age: 15` 로 적혀 왔다 (모델이 나이와 앞뒤 항목에서 어림한 해다). 연표는 항목의
  해로 세우므로 1997 칸에 서고, 연도 칸은 "같은 해의 뒷줄이면 달을 적는다"는 규칙에
  따라 남의 해의 달인 '4월' 을 적었다. → **달까지 아는 날짜(`YYYY-MM` 이상)가 항목의
  해를 이긴다** (`life.month_year` ← `refine`, `life.js` `monthYear` ← `normalize`).
  어림한 나이도 함께 버리고 생년에서 다시 센다 (만 15 → 만 16). 해까지만 아는 날짜
  (`1997`)는 항목의 해를 두고 본다 — 이길 만큼 정확하지 않다.
- **연도 칸이 달 아래에 다시 섰다.** 달을 모르는 뒷줄은 해를 흐리게 되풀이했는데
  (2026-09-06 에 빈 칸이 '연도 미상'으로 읽혀 되돌린 것), 그 되풀이가 달 아래에 서면
  해가 거기서 다시 시작하는 것으로 읽힌다. → **그 해에 달을 이미 적었으면 뒤따르는
  줄은 비운다** (`timeline.yearCells` — 한 해 안에서 달을 적었는지를 들고 간다).
  달이 하나도 없는 해는 예전 그대로 해를 되풀이한다. 시대 연표와 개인 연표가 같은
  함수를 쓴다 (`life.js` 의 `yearText` 는 걷어냈다).
- 덤으로 **달은 그 줄의 해와 같은 날짜일 때만 적는다** (`monthOf(date, year)`) —
  해와 날짜가 어긋난 자료가 또 와도 남의 달을 세우지 않는다.

실측(`data/life/나.json`): 1998 칸이 `1998 메탈리카 공연 관람` · `4월 메탈리카 공연
티켓` 로 섰다. 화면이 읽는 것은 브라우저의 저장본이지만 `normalize` 를 지나므로
새로고침이면 옛 그래프도 제자리로 온다.

## 2026-09-08 — 개인 역사, 학제의 차례는 이야기의 차례를 이긴다 (`life.ladder`)

사용자: "중학교 입학을 먼저 입력하고 초등학교 졸업을 나중에 입력했어. 그래서
중학교 입학이 초등학교 졸업보다 윗쪽에 있어 … 같은 연도에 일어난 일이지만 월을
입력 하지 않아서 사실 우리 서비스도 뭐가 먼저 일어난 일인지 정확하게 몰라. 하지만
논리상 초등학교 졸업이 무조건 먼저 일어나야 하잖아? 이런거 llm 인식해서 정확하게
연표를 만들수 있나?"

**모델에게 물을 것이 아니었다.** 학제는 정해져 있다 — `life.ladder` 가 사건을 층
(초1·중2·고3·대4·대학원5)과 그 층 안의 자리(들어감 0·다님 1·나옴 2)로 한 줄에
세운다: 초등 입학 10 · 초등 졸업 12 · 중학 입학 20 … 대학 졸업 42. 규칙은 둘이다.

- **해가 같으면 이 수가 차례다** (`life.order_by_ladder` ← `refine` 3, 화면은
  `life.js` `orderByLadder` ← `normalize`·`personalMarks`). 달을 모르는 자리를
  규칙이 메운다. **사다리에 없는 사건은 제자리에 둔다** — 자리만 맞바꾼다.
- **해가 이 수를 어기면 해가 틀린 것이다.** 연표는 해의 축 위에 서므로 코드가 해를
  넘어 옮길 수는 없다 — 세어서 `notes` 에 적고, 고치는 것은 이야기가 한다.

그 '이야기가 고치는 길'이 막혀 있었다. 실측: "성내중학교 입학년도를 잘못 말했어
1994년에 입학해서"라고 고쳐 말했는데 화면은 1993년 그대로였다. 모델은 제대로
답했다 (새 id `ms_entry_1994`, 같은 이름 '성내중학교 입학'). 버린 것은 `merge` 다 —
`_fill` 은 **빈 칸만 채우므로** 이미 든 1993 을 이기지 못한다. 그래서 `life.correct`
를 넣었다: 새 답이 **다른 해**를 말하고, 그 해가 **이번 이야기 글에 그대로 있고**,
그 이야기가 이 노드를 부르면(`grounded`) 새 해가 이긴다. 셋이 다 맞아야 고친다 —
모델이 옛 사건을 흐릿하게 되뇐 것으로 정확한 날짜를 덮지 않는다. 고친 것은 노드와
연표 항목(해·나이·`date_text`)에 같이 적고 무엇을 고쳤는지 `notes` 에 남긴다.
`merge(base, add, text)` 의 셋째 자리가 **이번에 더하는 이야기**다.

지시문에도 한 줄 더했다 — 고치는 말이면 그 노드를 **같은 이름으로** 다시 적으라고
(이름을 바꾸면 같은 일이 두 개가 된다. `merge` 는 이름으로 옛 노드를 찾는다).

`data/life/나.json` 은 고쳐서 저장했다 (1993→1994 · 1996→1997). **화면이 읽는 것은
브라우저의 localStorage 이지 이 파일이 아니다** — 이미 서 있는 그래프의 틀린 해는
그 문장을 다시 넣거나 '고쳐서 다시 읽기'를 해야 고쳐진다. 같은 해 안의 차례는 옛
그래프도 열자마자 바로 선다 (규칙이라 모델이 필요 없다).

## 2026-09-08 — 개인 역사: 저장 단추를 없애고, 적은 글을 다시 연다

사용자 둘. "'내 계정에 저장', '계정에서 지우기' 버튼을 지워조. 저장 버튼을
누르지 않아도 사용자가 '입력' 버턴을 누르면 자동으로 저장해줘." 그리고
"'내 역사 입력하기' 오른쪽에 아이콘 하나 만들어서 누르면 사용자가 입력한
사용자의 역사 히스토리를 보여줘. 그래서 잘못된 입력을 고칠 수 있게 해줘."

### 계정에 두는 것은 저절로 일어난다

머리 줄의 단추 둘을 뺐다. 대신 `LifeView.keepInAccount` 한 길을 자료가 바뀌는
자리마다 지난다 — **'입력' 을 누를 때**(분석이 몇 분이라 그 전에 한 번) ·
분석이 끝났을 때 · 노드를 지웠을 때 · **부팅이 브라우저에만 있는 것을 찾았을
때**(단추가 하던 '올리는 길'이다). 로그인이 안 된 자리는 전처럼 브라우저에만.

부팅의 그 자리에는 함정이 하나 있어 갈라 두었다: 계정을 **못 읽은 것**(네트워크)과
계정이 **빈 것**을 가르지 않으면, 한 번 끊긴 자리에서 브라우저의 옛 자료가
계정의 새 것을 덮는다. 읽기가 성공했을 때만 올린다.

### 내가 적은 이야기 (`stories` · `/api/life/story`)

그래프는 사람이 적은 글에서 나온다. 틀린 노드를 하나씩 지우는 것으로는 원본이
안 고쳐진다 — 실제로 원문에 `잠실고딩학교 1학넌때` 가 남아 있고 다시 적은
문단이 그 옆에 있어 같은 친구가 두 번 서 있었다. 그래서 **글을 다시 여는 길**을
둔다.

| 어디 | 무엇을 |
|---|---|
| 문서의 `stories` 열 | `[{at, text}]` — '입력' 한 번이 한 줄. 계정·브라우저를 따라다닌다 (`merge` 가 `base` 를 통째로 복사하므로 수집·더하기가 지우지 않는다) |
| `GET /api/life/story` | 이 열이 없는 **옛 문서**를 위해 이 컴퓨터의 원문(`data/life/나.txt`)을 준다. 화면이 빈 줄로 가른다 (`life.js splitStories` — 서버가 이어 붙일 때 넣는 그 빈 줄이다) |
| 머리 줄 아이콘 | `내 역사 입력하기` 오른쪽. **모달**을 연다 (2026-09-08 사용자: "히스토리 아이콘 누르면 히스토리 내역을 모달창으로 보여주고") |
| 줄을 누르면 | 그 글이 **입력창으로 간다** (같은 사용자: "예전 입력을 클릭하면 우리 인생 입력창에 자동으로 복사해줘"). 적다 만 글은 지우지 않고 아래에 붙인다 (`life.js appendDraft`) |
| `삭제` | 그 줄을 **기록에서만** 뺀다 — 그래프는 그대로. 지운 자리는 브라우저·계정에 바로 남는다 |

**새로 적은 것이 맨 위다** (2026-09-08 사용자). 자리만 뒤집고 **글의 차례는
그대로 둔다** — 뒤에 적은 문단이 앞의 것을 고치는 말('입학년도를 잘못 말했어')
이라 이어 붙일 때 뒤집으면 뜻이 달라진다. 날을 모르는 옛 글에는 아무 말도 적지
않는다 (같은 날 "'적은 날을 모릅니다' 문장을 삭제해").

**함정 하나** (2026-09-08 사용자: "입력을 클릭해도 입력창에 복사가 안 되는 경우가
있어"): 이야기 상자는 옮겨 온 글을 브라우저(`life-story`)에서 읽으므로 옮길 때마다
`key={draftStamp}` 로 **새로 선다.** 그런데 '분석이 끝났으면 칸을 비운다'는 효과가
`[job?.state]` 에 걸려 있어 **세워질 때마다** 돌았다 — 한 번 분석을 끝낸 뒤로는
누를 때마다 방금 옮긴 글이 그 자리에서 지워졌다 (그래서 '경우가 있어'였다).
지금은 **보낼 때** 비우고(`StoryBox send`), 못 보낸 글은 화면이 칸에 되돌린다
(`putDraft` — 서버에 못 닿았을 때·모델이 실패했을 때). 상자를 세우는 효과에
'비우기'를 다시 넣지 말 것. 같은 이유로 `appendDraft` 의 '같은 글이면 넘기기'도
뺐다 — 눌렀는데 아무 일도 없으면 고장으로 보인다.

남은 글로 처음부터 다시 짓던 **'고쳐서 다시 읽기' 는 뺐다** (2026-09-08 사용자).
그래프를 통째로 버리고 몇 분을 기다리는 길이었다. 지금 고치는 길은 둘이다 —
글이 틀렸으면 그 줄을 **입력창으로 옮겨 고쳐 넣고**(더하기), 노드가 틀렸으면
**상세 패널의 '삭제'** 로 그 노드를 뺀다. 모달의 '삭제' 는 기록 한 줄만 지운다.
설명 문단도 두지 않는다 (같은 날: "이 문장을 삭제해줘") — 단추의 `title` 로만 적는다.

## 2026-09-08 — 가입과 로그인 (구글 계정 · `auth` · `neon`)

사용자: "우리는 이제 가입 시스템은 구축 할거야. 구글로그인 시스템은 사용해서
가입자를 받을거야. 보안까지 생각해서 이 시스템은 구축해."

### 아직 남은 일 — **사람이 해야 하는 것 넷** (이걸 하기 전까지 로그인이 안 된다)

설정이 하나라도 없으면 `auth.enabled()` 가 거짓이 된다. **단추는 그대로 서
있고**(2026-09-08 사용자: "그냥 로그인 버튼이 항상 보이게 해줘") 누르면 구글로
보내는 대신 "로그인은 아직 준비 중입니다"라고 답한다. 처음엔 단추째 감췄는데
그러면 설정을 안 넣은 화면에서 이 기능이 없는 것처럼 보였다.

**로컬에서 켜는 데 남은 것은 하나뿐이다** (2026-09-08 에 나머지를 없앴다):

1. **구글 클라우드 콘솔** > 사용자 인증 정보 > OAuth 2.0 클라이언트 ID (웹).
   승인된 리디렉션 URI 에 둘을 그대로 넣고, 받은 값을 `.env` 의
   `GOOGLE_CLIENT_ID`·`GOOGLE_CLIENT_SECRET` 에 적는다 —
   `http://127.0.0.1:8100/api/auth/callback` ·
   `https://www.histgraph.space/api/auth/callback`. OAuth 동의 화면의 범위는
   `openid`·`email`·`profile` 셋뿐이라 심사가 필요 없다. **이건 사람만 할 수
   있다** — 계정 콘솔이라 코드로 못 만든다.

이미 해 둔 것: `HISTGRAPH_SESSION_SECRET` 은 `.env` 에 넣었고,
`HISTGRAPH_ADMIN_EMAILS` 도 적어 두었다. 가입자 표는 **로컬이면 저절로
생긴다** — `DATABASE_URL` 이 없으면 `data/accounts.sqlite` 를 쓴다
(`accounts.py`). `uv run histgraph accounts --init` 로 이미 세워 두었다.

배포에 올릴 때 두 가지가 더 필요하다:

2. **Neon** 프로젝트를 만들고 연결 문자열을 `DATABASE_URL` 에.
   **배포에서는 SQLite 로 물러나지 않는다** — 서버리스 함수의 파일시스템은
   요청마다 사라져서, 물러나면 가입자가 조용히 없어진다. 그래서 `VERCEL` 이
   있고 `DATABASE_URL` 이 없으면 기능이 꺼진다.
3. **Vercel 프로젝트 설정 > Environment Variables** 에 넷을 다 넣는다.
   `.env` 는 배포에 실리지 않는다 — 이걸 빼먹으면 배포에서만 로그인이 안 된다.

세어 보는 것은 `uv run histgraph accounts` (설정이 갖춰졌는지 + 최근 가입자).

### 어떻게 짰나

흐름은 **인가 코드 + PKCE**다. 브라우저에는 토큰을 한 조각도 두지 않는다 —
구글이 주는 것은 전부 서버가 받아 서버에서 쓰고, 브라우저에 남는 것은 뜻
없는 세션 쿠키 하나다. 자세한 이유는 `src/histgraph/auth.py` 머리글에 있다.

| 자리 | 무엇을 |
|---|---|
| `src/histgraph/auth.py` | 왕복·세션·CSRF·엔드포인트 표(`ROUTES`) |
| `src/histgraph/neon.py` | Neon 에 HTTP 로 붙는 최소 클라이언트 + 표(`SCHEMA`) |
| `server.Handler._try_auth` · `api/index.py` | HTTP 껍데기 둘. **표는 하나다** |
| `web/src/lib/auth.js` | 화면 쪽. 토큰은 없고 CSRF 표만 만진다 |
| `web/src/components/AccountMenu.jsx` | 머리 줄 오른쪽 끝 — 로그인 단추 · 계정 메뉴 |

막아 둔 것 (테스트가 전부 관문으로 잡는다 — `[가입·로그인 …]` 절):

- **엣지 캐시.** 배포는 `/api` 를 하루 재운다. 그 규칙이 `/api/me` 에 닿으면
  한 사람의 신원이 다음 사람에게 배달된다 — auth 의 응답은 스스로
  `private, no-store` 를 들고 나가고, `api/index.py` 는 그것을 그대로 쓴다.
  **`api/index.py` 에 캐시 헤더를 다시 일괄로 붙이지 말 것.**
- **열린 리다이렉트.** `next=//evil.example` 은 브라우저가 절대 주소로 읽는다.
- **Host 헤더.** 배포(`VERCEL`)에서는 아예 보지 않는다. 로컬만 되돌이 주소를 받는다.
- **쿠키.** HttpOnly · SameSite=Lax · https 에서는 `__Host-` 접두사, 그리고
  https 요청은 접두사 없는 쿠키를 **보지 않는다** (하위 도메인의 세션 고정).
- **CSRF 두 겹** — `Origin` 대조 + 세션에서 파생한 표. 로그아웃은 POST 만이라
  링크 하나로 남을 로그아웃시킬 수 없다.
- **세션은 해시로만 저장한다.** 표가 새어도 그것으로 남의 자리에 못 앉는다.
- ID 토큰의 `iss`·`aud`·`exp`·`nonce`·`email_verified` 를 전부 확인한다.
  서명을 다시 확인하지 않는 근거는 `decode_id_token` 머리글(OIDC §3.1.3.7).

### 내 역사만 로그인을 요구한다

2026-09-08 사용자: "'내 역사' 버튼을 눌렀을때 로그인 안 돼쓰면 로그인 모달을
보여줘서 로그인을 하게 강제해. 내 역사는 개인별로 다 다르니깐."

그래프·검색·연표·글로 읽는 장은 전부 로그인 없이 열린다. 막는 것은 **사람마다
다른 것 하나**뿐이다. 문은 둘이고 **둘 다 있어야 한다** — 머리 줄의 '내 역사'만
막으면 주소창에 `/life.html` 을 치는 것으로 넘어간다.

| 어디 | 무엇을 |
|---|---|
| `App.jsx` 머리 줄 '내 역사' | 로그인 전이면 옮겨가지 않고 상자를 세운다 (`나중에` 로 닫힌다) |
| `LifeView.jsx` 첫 줄 | 주소로 곧장 와도 같은 상자. 자료를 그리기 전이라 남의 연표가 뒤에 비치지 않는다. 닫는 대신 **돌아갈 자리**를 준다 |

상자는 `LoginModal.jsx` 하나다 — 왜 여기서만 묻는지를 먼저 적고(이유 없이
막는 화면은 그냥 닫힌 문이다), 약관·방침으로 가는 길과 무엇을 받는지를 적는다.

**로그인이 아직 열리지 않았으면(`enabled:false`) 막지 않는다.** 지금이 그
상태다 — 여기서 막으면 설정 넷이 들어오기 전까지 이 컴퓨터에서 내 역사를
아예 못 쓴다. 설정이 들어오는 순간부터 문이 닫힌다.

### 가입하면 생기는 것

즐겨찾기(상세 오른쪽 위 별) · 메모 · 관리자 표시 · **내 역사를 계정에 저장**.
마지막 것은 처음엔 누를 때만 올라갔지만, 2026-09-08 사용자 결정으로 **저절로
올라간다** (위 '저장 단추를 없애고' 절). 개인 역사 장 자체는 여전히 이 컴퓨터
에만 있다 (2026-09-07 결정 — 배포 안 함).

### 문서도 같이 고쳤다

`web/privacy.html` 은 "회원가입도 로그인도 없습니다"라고 적혀 있었다. 가입을
받으면 그 문장이 거짓이 되고 그대로 두는 것은 개인정보보호법 위반이다 —
수집 항목·목적·보유 기간·파기·**국외 이전**(네온·버셀·구글)·열람과 탈퇴의
방법을 새로 적었다. `web/terms.html` 에는 회원 조항(2항)을 넣었다. 수탁자
이름은 한글로 적고 회사 방침으로 링크를 건다 — §1 관문이 로마자를 잡는다.

**남은 확인 하나**: Neon 프로젝트를 어느 리전에 만들지. 방침에는 '미국 등
국외'로 적어 두었다.

## 2026-09-08 — 개인 역사, 관계에 온톨로지를 씌운다 (`life.LIFE_EDGES` · `RELAX` · 역할)

사용자 지적 둘. "지금 그래프의 엣지 설명이 엉망이야. 친구들은 만남이 아니라
'친구'라고 표시해야. 그리고 '뒤', '동안' 이런 설명은 도대체 뭐야?" — 그리고 첫
손질('겪음')에 "너무 추상적이야. 우리가 한국사 그래프 만들때 적용한 방법을
체크해보고 다시 만들어. 그래프 이론, 온톨로지 이론도 다시 공부해서 적용해봐."

실측 그래프(33 관계): 주인공 → 자기 사건 7건이 `after`·`during`(화면 '뒤'·'동안'),
친구 셋이 `met`('만남'), 그 셋이 서로 `worked_with`(확신 0.5 — 이야기에 일한 곳이
없다), 나형철이 양방향 `met`, '잠실고등학교 입학 → 잠실고등학교'가 `studied_at`('수학').
원인은 지시문의 관계 50여 개에 **출발·도착이 없다**는 것 — 모델이 어떤 타입이든
어디에나 걸 수 있었다. 한국사 그래프가 같은 병을 `ontology.EDGE_TYPES`(관계마다
출발·도착 갈래) · `untangle.RELAX`(어긋난 것은 버리지 않고 뜻이 남는 타입으로) ·
`LABEL_HEADS`(역할이 타입 이름을 이긴다)로 고쳤으므로 그 셋을 그대로 옮겼다.

- **`life.LIFE_EDGES`** — 관계마다 (일반 이름, 출발 갈래, 도착 갈래). 갈래는 캔버스의
  여덟 색(`GRAPH_TYPE`). `EDGE_TYPE_KO` 와 관계·이름이 같아야 한다(테스트). 새 관계는
  `experienced`(당사자 — 사람 → 사건, BIO 어휘의 principal) · `at`(곳 — 사건 → 학교·
  장소·전공, 한국사의 `occurred_at`) · `friend_of`(친구) · `schoolmate`(같은 학교).
  RELATIONSHIP 어휘가 `hasMet` 과 `friendOf` 를 가르듯 만남과 친구는 다른 관계다.
- **`RELAX`** — (타입, 출발, 도착) → 옮길 타입. 사람 ↔ 사건의 시간 관계는 참여
  (`experienced`), 사건 → 학교·회사·전공의 `studied_at`·`worked_at`·`during` 은 `at`.
  표에 없는 어긋남은 **그대로 두고 센다** (`tidy_edges` 가 목록을 돌려주고 `refine`
  이 로그에 적는다). 테스트가 RELAX 의 결과가 전부 표에 맞는지, 이미 맞는 짝을
  옮기지 않는지 잰다. `MAX_TARGETS = {born_in: 1}` 도 세어만 준다.
- **역할이 선의 이름이다** (`edge.role`, 한국사의 '주도'·'지휘' 자리). 사람 → 사건은
  사건 이름 꼬리의 술어(`deed_of`: 입학·졸업·이주·창업·입소·소집해제…, '경력 시작'
  처럼 시작·종료는 앞 낱말을 붙인다)고, 술어가 없으면 사건의 종류(전환점·위기·성취·
  실패·결정·기억), 그것도 없으면 일반 이름 '당사자'. 옮기면서 원래 타입이 말하던
  것도 역할로 남는다(`studied_at` → 전공 = '전공'). `edge_label(kind, src, dst, role)`
  은 역할 → 양끝 문맥(`at` 은 도착이 학교면 '학교', 사건 → 장소 `moved_to` 는 '이주지')
  → 일반 이름 순. `before`/`after` 의 일반 이름도 '앞'/'뒤' → '다음'/'이전'.
- 화면 쪽(`life.js`)에 같은 표·같은 다섯 규칙(`tidyEdges`)이 있어 서버 없이 브라우저·
  계정에 남은 옛 그래프도 새로고침으로 고쳐진다. 필터(`graphMeta`)는 일반 이름.
- 지시문(`build_user`)에도 적었다: 주인공의 사건은 `experienced`, 친구는 `friend_of`.

지금 그래프: 나 —입학→ 잠실고등학교 입학 —학교→ 잠실고등학교 · 나 —이주→ 미국으로
이주 —이주지→ 미국 · 나 —친구→ 김일권 —재학→ 잠실고등학교 · 김일권 —같은 학교→ 박정환.

**남은 것**: 나형철은 그래프 어디에도 '친구'라는 말이 없어(설명이 비었다) 아직
'만남'이다 — 이야기 문장을 다시 넣으면 모델이 `friend_of` 로 적는다. 그래프 이론
쪽의 다음 한 수는 **축약**이다: 주인공의 사건 노드는 차수 2(당사자 + 곳)가 대부분이라
`나 —입학 1997→ 잠실고등학교` 한 선으로 접을 수 있다(한국사에서 재위를 노드가
아니라 엣지에 둔 것과 같은 이유). 사건을 그래프에서 못 누르게 되므로 아직 안 했다.

## 2026-09-08 — 개인 역사, 로그인했으면 분석 결과가 계정에 바로 올라간다

"새로고침 해도 친구들이 연표와 그래프에서 안 보이는데?" — 서버·화면 규칙은 다
맞았고(파일을 JS 파이프라인에 넣으면 친구 셋이 연표·그래프에 선다), **화면이
읽는 자료가 달랐다.** 부팅은 계정 → 브라우저 순인데, 계정에는 09-07 23:32 에
단추로 올린 **옛 그래프**(친구 없음)가, 브라우저에는 뒤에 분석한 새 그래프가
있었다. 계정이 이겨서 옛 것이 떴다.

- 로그인한 사람의 분석 결과는 끝나는 대로 `auth.life.save` 로 계정에 올린다
  (`LifeView.watchJob`, 팝업 '내 계정에 저장했습니다'). 09-07 의 '단추로만 올린다'
  결정은 접는다 — 오늘 사용자가 "로그인 해서 직접 입력" 을 정했고 계정이 부팅의
  첫 자리라 늘 최신이어야 한다. 로그인 안 된 자리는 전처럼 브라우저에만.
- 이 컴퓨터의 `data/accounts.sqlite` 의 옛 문서는 새 그래프(다듬은 `data/life/나.json`)
  로 바꿔 두었다 (바꾸기 전 것은 스크래치에 복사).

## 2026-09-08 — 개인 역사, 만난 사람은 만난 곳과 주인공에 잇는다 (`life.refine`)

사용자: "고등학교에서 만났다고 하면 내가 입학했다고 말한 고등학교와 연결 시켜줘야
하는거야. 지금 그걸 못하고 있어. 연표에 추가도 안되고 있고." 실측(무료 모델의 답):
생년은 주인공이 아니라 '출생' `Time` 노드에, 친구 셋은 설명에만 '잠실고등학교
1학년 때 만난' 이라 적히고 **주인공·학교와 아무 관계가 없었다.** 연표 항목은
있었지만 나이만 있고 생년을 몰라 해가 비어 화면이 건너뛰었다.

`life.refine(payload)` — 검증 끝·더하기 끝·`POST /api/life/refine`(화면이 부팅 때
브라우저·계정의 옛 자료를 보낸다, 모델 없음)에서 돈다:

1. 생년: 주인공 start_date → born_in 으로 이어진 '출생' 노드 → 설명의 '1982년 … 태어난'.
2. 해: 학교 노드는 입학 사건의 해를 받고, 사람 노드는 설명의 '…1학년 때 만난' 을
   입학 해에서 센다. **입학 해를 찾을 때 사람 노드는 안 본다** — 주인공 설명의
   '1997년 잠실고등학교 입학' 이 주인공의 해(생년 1982)와 짝지어져 고1 이 1982 가 됐다.
3. 연표: 빈 해는 날짜 글 → 나이+생년 → 노드의 해. 해 순으로 다시 세운다.
4. 잇기: 사람의 설명이 이야기 속 학교·회사 이름을 부르면 `studied_at`·`worked_at`·
   `member_of` 로, 주인공과 관계가 없는 '만난' 사람은 `met` 으로 (confidence 0.8).
   두 번 돌려도 늘지 않는다.

프롬프트에도 같은 말을 넣었다 (만난 사람은 met·만난 곳과 잇고, 생년월일은 주인공
노드에). 지시문은 그대로.

## 2026-09-08 — 개인 역사, 학년은 해다 — 앞뒤에서 셈한다

사용자: "1997년 고등학교 입학 했다고 했고 1학년때 누굴 만나고 2학년때 누굴
만났다고 하면 연도는 말 안해도 1997년에 만났고 1998년에 만났다는걸 유추해서
알 수 있지 않나? 지금 우리 모델은 그걸 모르네?" 두 겹으로 고쳤다:

- **프롬프트** (`life.build_user`): 해를 말하지 않은 일도 앞뒤에서 셈해서
  `start_date` 에 적으라고 한다 — 학년·'입사 이듬해'·'그해 겨울'·'졸업 직후'.
  셈한 해는 confidence 0.7~0.9. 지시문(`life_prompt.md`)은 그대로다.
- **검증** (`life.validate`): 모델이 그래도 '고등학교 2학년'·'고2' 를 날짜 칸에
  그대로 적으면 우리가 센다. 순서는 **입학 해 → 생년**. 입학 해는 이야기의
  입학 사건('고등학교 입학')이나 학교 노드(`School`·`University` 의 시작 해)에서
  (`school_entries`), 없으면 생년에서 어림(초1 = 생년+7, 중1 = +13, 고1 = +16,
  대1 = +19 — `parse_when`, 화면에 '어림'). 연표 항목의 `date_text` 도 같다.
  JS `parseWhen` 은 생년 쪽만 안다 (서버를 안 거친 자료용).

## 2026-09-08 — 개인 역사, 더하는 이야기는 있는 그래프에 더한다 (`life.merge`)

사용자: "사용자가 자기 역사를 나중에 추가해서 입력 하면 원래 있던 역사에 추가
시켜줘. 기존것을 지우고 새로 만들지 말고". 화면이 그래프를 쥐고 있으면 '입력' 이
그것을 `base` 로 같이 보내고(`POST /api/life/analyze`), 서버가 세 자리에서 잇는다:

- **모델에게 있는 노드를 보인다** (`life.build_user(existing=)` — id·타입·이름·해와
  주인공 id·생년). 같은 것은 그 id 를 쓰고, 있는 것을 다시 만들지 말라고 한다.
- **검증은 옛 주인공을 믿는다** (`validate(subject=)`) — 새 답의 첫 인물이 주인공
  이라는 짐작이 더하는 이야기에서는 틀린다(새로 나온 아버지). 나이는 옛 생년으로 푼다.
- **`life.merge(base, add)`** — 노드는 id → 주인공 이름('사용자'·'나') → (타입, 이름)
  으로 겹침을 잡아 옛 것을 남기고 빈 칸만 채운다. 관계·연표·역사 연결·전환점·영향·
  패턴·순위·가족은 없던 것만 붙는다. 연표는 해 순으로 다시 서고 앞뒤를 다시 잇는다.
  물음은 새 답의 것. 옛 그래프는 복사본에 더한다.

결과 상태에 `added`(더한 수)가 실리고 화면이 "있는 역사에 더했습니다 · 새 사건과
사람 N · 새 관계 M" 으로 적는다. 글 칸은 끝나면 비운다 — 다음 글은 더하는 글이라
옛 글이 남아 있으면 같은 것을 두 번 보낸다. 원문 `.txt` 는 서버가 뒤에 잇는다.
CLI 는 `histgraph life 더.txt --base data/life/나.json`.

## 2026-09-08 — 개인 역사, 기본 자료 없음 — 로그인해 직접 적는다

사용자: "이제 디폴트로 넣었던 내 역사 데이터는 지워. 사용자가 로그인 해서
직접 입력 할거야". 화면이 아무것도 안 적은 사람에게 **남의(지어낸) 연표를
기본으로 세우던 길 셋**을 뺐다:

- 서버가 `data/life/*.json` 중 가장 최근 것을 골라 주던 `GET /api/life`
  (`GraphAPI.life`·`life.find`). 화면 부팅이 그것을 먼저 읽어 `예시.json` 이
  늘 떠 있었다. 이제 부팅은 **계정에 올려 둔 것 → 이 브라우저에 남긴 것 → 빈
  화면** 이다. 분석 결과(`/api/life/job` 의 payload)는 브라우저(`life-json`)에
  남긴다 — 서버가 파일을 다시 주지 않으므로 새로고침 뒤에도 보이려면 거기
  있어야 한다. 계정에 두는 것은 여전히 '내 계정에 저장' 단추로만.
- 빈 화면의 '예시로 먼저 보기' 와 `web/public/life-sample.json`. 지어낸 인물의
  그래프는 `web/tests/fixtures/life.json` 으로 옮겨 **시험용 붙박이**로만 남는다
  (배치·그래프 테스트가 그 사건 id 로 잰다). Vercel 빌드에서 그 파일을 지우던
  `strip-life` 플러그인도 뺐다 — 지울 것이 없다.
- `data/life/예시.txt`·`예시.json` (저장소 밖 파일) 을 지웠다.

이어서 입력 상자의 보기글(`STORY_EXAMPLE`)도 바꿨다 — 앞에 '예:' 를 달고 색을
더 흐리게(`opacity .55`) 두어 예문임이 보이게. 지명은 실제(익산·김제·부평·원주),
학교·회사·가게 이름은 지어낸 것 — 처음 지은 다섯 중 셋이 검색하니 실재하는
회사·사진관이라 다시 지었다 (노을결사진관·샛별뫼초등학교·온새미전자·
바람결대학교·별무리정밀 — 검색해 없는 것을 확인).

`histgraph life 이야기.txt` 와 서버 분석은 여전히 `data/life/{이름}.json` 에
**쓴다** (원문 `.txt` 와 함께 — 고쳐 쓰고 다시 돌리는 길). 화면이 그것을
저절로 읽지 않을 뿐이다.

## 2026-09-07 — 개인 역사 (`life` · `/life.html`) — 왼쪽 왕·대통령 · 가운데 한국사 · 오른쪽 내 역사

사용자가 "HistGraph Personal History Engine" 지시문(역할·노드 타입·관계·연표·
가족 뿌리·역사 연결·전환점·인과·가상 역사·패턴·영향 순위·물음 — JSON 만 출력)을
주고 "개인 역사 타임라인은 왼쪽은 왕/대통령 타임라인, 가운데 역사 타임라인,
오른쪽 개인역사 타임라인. 이렇게 그려줘".

만든 것:

- **엔진** `src/histgraph/life.py` + `life_prompt.md` (지시문 그대로, 고치지 않았다).
  `SCHEMA` 가 지시문의 출력 형식을 MLX 가 강제할 수 있는 스키마로 옮긴 것.
  `validate` 가 모르는 타입·관계를 버리고 날짜(`'2000년대 초반'`·`'20대 초반'`)를
  해로 풀고(`parse_when` — 생년 없이 나이는 못 푼다, 지어내지 않는다),
  `link` 가 모델이 이름으로 부른 역사 사건을 그래프 노드에 잇는다 (이름과 해가
  맞아야). `build_user` 가 **그래프의 사건 이름을 모델에게 보인다** — 모델은
  '1997년 외환 위기'라 부르고 그래프는 '대한민국의 IMF 구제금융 요청'이라 불러
  안 이어지던 것.
- **명령** `uv run histgraph life 이야기.txt` (MLX 기본, 35GB) ·
  `--json 받은.json` (Claude 채팅에 지시문을 넣어 받은 JSON 을 같은 관문으로) ·
  `--dry-run` (프롬프트만) · `--json 받은.json --counterfactuals` ('만약 없었다면'
  의 빈 답만 모델에 물어 채운다 — 그래프는 안 건드린다). 결과는 `data/life/` —
  **.gitignore** (개인 자료).
- **서버** `/api/context?from&to` (그 구간의 재위 띠 + `_anchors` 와 같은 규칙의
  큰 사건). 저장된 개인 그래프를 폴더에서 골라 주던 `/api/life` 는 2026-09-08 에
  뺐다 (아래 '기본 자료 없음'). 배포(Vercel)에는 개인 자료가 없으므로 화면이
  붙여 넣은 JSON 을 브라우저(localStorage)에 뒀다 (2026-09-08 에 그 단추를 뺐다 — 아래).
- **화면** `web/life.html` → `LifeView.jsx` + `lib/life.js`. 세 열이 **자
  하나**(`buildScale`)를 쓴다 — 같은 해는 같은 높이. 한 해의 높이는 두 열 중
  더 몰린 쪽이 정한다. 왼쪽 띠는 시대 연표의 `reignBand` 를 **함수로 꺼내** 같이
  쓴다. 오른쪽 끝에 인생 단계 띠(출생·중학교·창업…). 역사 → 개인 연결선은 홈을
  건너 개인 열 왼쪽 가장자리까지(직접 실선·간접 점선), 개인 사건의 인과는 시대
  연표와 같은 ㄷ 자 선. 태어나기 전의 역사(할아버지의 6·25)는 축이 아니라 캔버스
  머리 '태어나기 전'에 이어진 것만 선다. 주소 `#사건id` 가 고른 사건. 오른쪽
  패널: 사건(원인·결과·그 무렵의 한국사·만약 없었다면) · 분석(가족 뿌리·전환점·
  역사가 준 영향·패턴·영향 순위·물음) · 사람·문화.
- **'만약 없었다면' 은 물음을 눌러 편다** (2026-09-09 사용자: "질문만 있고 답변이
  없어. 질문을 클릭하면 답을 볼수 있게 답안도 작성해줘"). 전에는 물음 아래에 갈렸을
  길(`possibilities`)만 서 있었는데 그것은 '한국에서 대학을 계속 다녔을 가능성' 같은
  구절이라 답으로 읽히지 않았다. 이제 항목마다 문단 하나(`answer`)가 따라오고
  (지시문·`SCHEMA` 를 같이 고쳤다), 화면은 `Counterfactual` 이 접어 두었다가 누르면
  편다. 옛 문서는 `histgraph life --json … --counterfactuals` 가 빈 답만 채운다.
  답에 한글이 없거나 한자·가나가 섞이면 버린다 (`life.korean_line` — 실측: 무료
  모델이 '더 오래続했을' 을 냈다).
- **한글 관문**: 지시문의 식별자(노드 38·관계 54·영향 3)에 전부 한국어 이름표
  (`NODE_TYPE_KO`·`EDGE_TYPE_KO`·`IMPACT_KO`, 파이썬·JS 같은 표). 테스트가
  지시문을 읽어 빠진 것을 잡는다. 화면 글자 덩어리마다 한글이 있어야 한다.
- 예시(지어낸 인물)는 2026-09-08 에 화면에서 뺐다 — `web/tests/fixtures/life.json`
  에 시험용 붙박이로만 남는다. 실제 모델 실행은 이 세션에서 안 했다 — MLX 35GB.

- **그래프도 있다** (같은 날 사용자: "개인사도 그래프를 만들어줘. 역사 그래프랑
  똑같이", 이어 "연표와 그래프를 동시에 보여줘"). 역사 화면과 같은 배치 — 왼쪽
  세 열 연표(`boardWidth`, 2026-09-08 부터 접지 않는다) · 가운데 그래프 ·
  오른쪽 상세. 연표에서 고르든 그래프에서 고르든 같은 노드다. 캔버스
  (`GraphView`)와 설정 상자(`SidePanel`, `whole` 변형 — 펼침 깊이·최대 노드·연표
  스위치를 감춘다)를 그대로 쓰고, 자료만 `life.graphPayload` 가 캔버스 꼴로 옮긴다.
  지시문의 타입은 캔버스의 색 타입 여덟에 대응한다 (`GRAPH_TYPE` — 가족·친구는
  인물 파랑, 책·영화·음악·게임은 작품 연빨강, 학교·회사는 단체 크림). 그래프는
  통째로 싣고(수십 노드) 고른 노드가 중심이다. 실선은 본인이 말한 것, 점선은 미룬
  것(confidence < 1). 범례·관계 필터는 자료에서 센다 (`graphMeta`).
  헤드리스 검증은 `--screenshot` 만으로 안 되면 CDP 로 `window.__histgraphView`
  를 읽는다 — 이번에 효과 하나가 편집에서 빠진 것을 그렇게 잡았다 (노드 0).
  **개발 서버(5173)에서만 안 보이던 것**: StrictMode 가 캔버스를 떼었다 다시
  붙이는데 자료는 첫 캔버스에만 실렸다. `GraphCanvas` 의 `onReady(view)` 가
  캔버스를 만들 때마다 불리고, `LifeView.loadGraph` 가 거기서 다시 싣는다.
  빌드본(8100)만 보고 "된다"고 하지 말 것 — 사용자는 5173 을 본다.

**아직 배포하지 않는다** (같은 날 사용자: "이건 아직 배포 하면 안돼. 로컬에서
개발하고 테스트 해야 함"). 스위치는 `web/vite.config.js` `LIFE_PAGE = !VERCEL`
하나 — Vercel 빌드에서 `life.html`·예시·머리의 링크가 빠진다 (`life.test.mjs`
가 두 빌드를 잰다). 사용자가 넣으라고 할 때 그 줄을 푼다. main 에 합쳐도 화면에는
안 나간다. → **2026-09-09 에 풀었다** (아래 '개인 역사를 배포한다').

남는 것: 모바일 배치 없음.

### 2026-09-08 — '내 역사 입력하기' (화면에서 바로 이야기를 넣는다)

사용자: "여기 메뉴를 [예시] 보기 삭제하고 '내 인생 입력하기' 버튼을 만들어서
입력 박스를 보여주는데 placeholder 로 인생 입력의 예시를 보여줘." 터미널에서
`histgraph life 이야기.txt` 를 돌리는 것 말고 **화면에서 글을 적어 그대로**
그래프가 되게 했다.

- 머리의 `예시 보기` 를 뺐다. (같은 날 저녁, 빈 화면의 '예시로 먼저 보기' 도
  뺐다 — 아래 '기본 자료 없음'.)
- `내 역사 입력하기` → `StoryBox` (`LifeView.jsx`). 단추 이름은 머리의 '내 역사' 와
  같은 말로 부른다 (같은 날 사용자가 '내 인생' 을 '내 역사' 로 바꿨다).
  보기글(placeholder)이 무엇을
  적을지 대신 말한다 (`STORY_EXAMPLE` — 해와 곳·가족·이사·학교·일·만남·그때의
  마음). 빈 칸에 '자유롭게 적으세요' 라고 쓰면 아무도 첫 줄을 못 적는다. 적다 만
  글은 브라우저에 남긴다 (`life-story`).
- **답을 기다리는 요청에 매달지 않는다.** MLX 는 35GB 를 잡고 몇 분을 돈다 —
  `POST /api/life/analyze` 는 스레드에 맡기고 띄웠다는 것만 알리고, 화면이 2초
  마다 `GET /api/life/job` 을 묻는다 (`server.LifeAnalysis`, 한 번에 하나).
  창을 닫았다 다시 열어도 돌던 것을 다시 붙잡는다. 지나는 길은 CLI 와 같다
  (`analyze → validate → link → save`) — 결과는 `data/life/{이름}.json`, 이야기
  원문도 `.txt` 로 옆에 둔다.
- POST 는 **로컬 서버에만** 있다 (서버리스 함수는 `do_GET` 뿐). 배포에서는
  `/api/life/job` 이 늘 `idle` 이고 화면이 '로컬 서버에 닿지 못했습니다' 로
  물러난다 — 개인 자료도 모델도 이 컴퓨터 밖으로 나가지 않는다.
- 실측: 화면에서 글을 넣고 '세우기' 를 눌러 끝까지 돌렸다 (CDP, 5173). 3분 남짓에
  상자가 닫히고 연표·그래프가 새 자료로 섰다 — 노드 22 · 관계 21, 모델이 '외환위기'
  라 부른 것이 그래프의 `대한민국의 IMF 구제금융 요청` 노드에 이어졌다.

**머리에는 그 단추 하나만 남겼다** (같은 날 사용자: "'JSON 파일열기',
'붙여넣기', '연표접기' 버튼 모두 삭제해줘"). 붙여 넣기·파일 열기는 사람이 모델과
따로 대화해 받은 JSON 을 넣던 길인데 이제 화면이 직접 물어본다 (`PasteBox`·
`onFile`·`onPaste` 를 지웠다). 연표는 늘 펴 둔다 (`railOpen`·`life-rail` 을
지웠다 — 세 열을 좁게 잡은 뒤로 접을 이유가 없다). 브라우저에 남은 옛 자료
(`life-json`)는 부팅 때 그대로 읽는다.

### 2026-09-08 — 해석할 모델을 밖에서 빌린다 (OpenRouter 무료 모델)

사용자: "사용자가 역사를 입력하면 해석할 모델을 openrouter 로부터 받아서 해석
할거야 … 일단 무료 모델을 사용." 이야기를 읽는 것이 로컬 MLX 하나뿐이면 35GB
가 없는 자리에서는 화면이 아무것도 못 한다.

- **무엇을 골랐나.** OpenRouter 모델 428개 중 값이 0인 것은 19개. 그 열아홉에
  같은 일(이야기 → `life.SCHEMA`)을 그대로 시켜 봤다 (2026-09-08 실측):

  | 모델 | 결과 |
  |---|---|
  | **nvidia/nemotron-3-super-120b-a12b:free** | 한국어·스키마 다 통과. 예시 이야기에 노드 32 · 관계 27 · 연표 9 (54초) |
  | dots-studio/dots-3-note-preview:free | 라벨을 **중국어**로 냈다(`入学`·`搬家`) · 관계 양끝을 이름으로 적는다 |
  | liquid/lfm-2.5-2.6b:free | 한국어는 되는데 없는 날짜를 지어낸다 · 사고를 끌 수 없다 |
  | google/gemma-4-*:free | 상류(Google AI Studio)가 늘 429 — 사실상 못 쓴다 |
  | nemotron-3.5-lightning · inkling · north-mini-code · ling-3.0 | 스키마를 안 받는다 (영어 사고과정·제 마음대로 만든 형식) |
  | openrouter/free | 부를 때마다 다른 모델 — 재현이 안 된다 |

  갈린 기준은 하나다: `response_format.json_schema` 를 **진짜로 지키는가**.
  안 지키는 모델은 ollama 와 똑같이 파싱이 무너진다.
- **함정 넷을 백엔드가 막는다** (`backends.OpenRouterBackend`):
  상류 오류가 **HTTP 200 의 몸**으로 온다(`{"error": …}`, `choices` 없음) ·
  **사고를 끄지 않으면** 12,000 토큰 중 10,615 를 사고에 쓰고 잘린다(끄면 같은
  이야기를 5,476 토큰에 냈다. 못 끄는 모델은 400 으로 답하니 켜고 다시 부른다) ·
  무료는 **분당 20회**라 호출 사이를 띄운다 · 스키마를 거절하면 strict → 느슨 →
  JSON 모드로 한 칸씩 물러난다.
- **어느 쪽으로 갈지는 `.env` 가 정한다.** `OPENROUTER_API_KEY` 가 있으면
  OpenRouter, 없으면 예전처럼 로컬 MLX (`backends.default_life_backend`).
  화면은 묻지 않는다 — 기다리는 줄만 달라진다(로컬은 '모델을 올리는 중').
  모델 이름은 `OPENROUTER_MODEL`, CLI 는 `--backend`·`--model` 로 덮는다.
- **답을 받는 쪽도 같이 고쳤다.** 무료 모델이 실제로 낸 어긋남 둘인데, 관계는
  참인데 부르는 법만 다른 것이라 버리면 화면에서 없었던 일이 된다.
  (1) 관계의 양끝을 id 대신 **이름**으로 적는다 → 이름으로 되짚어 잇는다
  (같은 이름이 둘이면 손대지 않는다). (2) 우리가 보여 준 목록을 괄호째 베껴
  `대한민국의 IMF 구제금융 요청(1997)` 이라 적는다 → 뒤에 붙은 해를 떼고 잇는다
  (**이것 때문에 이은 것이 0 이었다**. 떼자 2/2). 어느 모델이 쓴 그래프인지도
  이제 `_model` 로 남는다 (`validate` 가 흘리고 있었다).
- 실측: `uv run histgraph life data/life/예시.txt` 가 54초에 끝났다 (MLX 는 몇 분).
  테스트 1,207 통과 — OpenRouter 는 네트워크 없이 몸통만 본다(가짜 `urlopen`).

## 2026-09-07 — "정도전은 제1차 왕자의 난을 지휘했다" (역할 전수 조사 · `roles --table`)

사용자 지적: 정도전은 그 난의 피해자인데 화면이 지휘자라 했다. 모든 노드를
전수 조사해 잘못된 정보를 삭제·수정하라는 요청. 이 세션은 **인물→사건
참여 관계의 역할**을 전수 조사했다 (다른 관계 종류·노드 자체의 오류는
아직 — 아래 '남는 것').

원인 (CLAUDE.md §1-6): 인포박스 `지휘관N` 은 양편의 사령관이고, 이름 뒤의
표식(†·{{KIA}}·☠·‡)을 파서가 버렸고, 화면은 라벨 있는 소스를 골라 읽었다.

만든 것:

- `data/roles.tsv` 1,113줄 — 정변·난·사화·옥사·사건 154건의 인물 참여를
  Claude 가 국편·민백·위키백과 근거로 직접 읽어 판정 (피해 206 · 언급 250 ·
  가담 197 · 대항 156 · 주도 148 · 표적 17 · 수습 21 · 삭제 118). 넷째 묶음은
  생몰년이 사건 연대와 어긋난 참여 33건 (사육신 문서의 송준길·허적 → 언급,
  1899년생 정기원의 신미양요 → 삭제).
- `roles --table` 명령 (`roles.load_table`·`apply_table`·`unjudged`) —
  편집 계층에 적고, `roles.CONTESTED` 사건에 역할 없는 참여가 남으면 종료
  코드 1. 표의 판정은 모델이 `--redo` 로도 못 덮는다. 모델 판정도 이제
  편집 계층에 적는다 (origin `roles-model`) — 재수집이 12·12 의 판정 16건을
  지웠던 것을 조사 파일에서 되살렸다.
- 인포박스 파서: `props.fate`·`props.side_name` (`infobox.link_fates`·
  `side_names`·`parse_infobox_marks`), 있는 엣지에 채우는 `infobox --marks`.
  화면 DB 표식 139 · 편 이름 1,278.
- 화면 문장 (`relations.js` `roleSentence`·`FATE_TAIL`): "옥포 해전에서 조선
  측을 지휘했다", "지휘하다 전사했다", "제1차 왕자의 난에서 살해되었다",
  "가담했고 처형되었다". 서버가 `fate`·`side_name` 을 넘긴다.
- 인과 4건 지움 (`chronology.tsv` drop): 태종 → 조선어학회 사건, 민영환 →
  광주 학생 항일 운동, 김일성 → 6·15 (근거가 7·4 공동성명), 정도전 → 제1차
  왕자의 난 (말뭉치가 '이방원 측 기록의 주장'이라 밝힌 것). 이몽학·서장옥의
  생년(몰년과 같거나 뒤)을 비웠다.
- 같은 날 지적 "정도전을 아예 지워 버렸네": 피해로 옮긴 관계가 '관련' 더미에
  묻혔다. 역할이 묶음 머리('피해 · 3')이자 그래프 선 이름이 되고, 참여 바로
  뒤에 선다 (`server.ROLE_HEADS`·`relHead`·`pages._head`). 상세 패널·/n/ 장·
  그래프 셋이 같은 규칙.
- **같은 문제 전수 조사** (2026-09-07 요청): 화면 DB 의 (타입, 라벨) 짝을 전부
  렌더링 규칙과 맞춰 봤다. 셋이 더 나왔다 — `다음`(27)·`이 기사의 대상`(36)·
  `소속`/`직위`(5)가 '관련' 더미에 묻혔고, `set_in`(105)·`adapted_from`(5)·
  `about`(1)은 문장 규칙이 없어 화살표로 섰다. `server.LABEL_HEADS` 한 표를
  화면·정적 페이지·연표가 같이 읽는다. 새 타입에 문장이 없으면 테스트가 잡는다.
- **지운 것 되살리기**: 내가 '삭제'로 적은 118 중 114 가 두 노드를 완전히
  끊었고, 그중 43 은 관계가 참이었다. 배경 인물 25 를 `언급`으로 낮춰 되살리고
  (`roles --table` 이 표의 근거로 관계를 세운다), 중복 표기 9 는
  `data/duplicates.tsv` 병합으로 옮기고(이름은 별칭으로 남는다), 단체를 인물로
  세운 노드 10 은 `단체` 판정으로 타입을 고쳐 참여를 되살렸다.
- `overrides` 가 노드 `type` 도 지킨다 — `reclassify` 가 SQL 로만 고쳐서 수집이
  되돌리고 있었다. 이제 거기서도 편집 계층에 적는다.
- **인과 사슬에서도 같은 일**: 정도전 → 제1차 왕자의 난 인과를 `chronology`
  `drop` 으로 지워 사슬에서 사라졌었다. 실록의 '음모' 서술이 한쪽 관점인 것이지
  인과가 거짓은 아니다 — 민백의 「진법훈련이 강화되자 사병에 대한 통수권이
  해체될 단계에 이르러 양파의 대립은 극에 달하였다」로 다시 적었다. 사람이 읽고
  적은 인과를 넣는 문 `causes --answers`(JSON, `data/causes_answers.json`)를
  달았다 — 모델 답과 같은 관문을 지난다.
- 테스트 1,109 / 화면 227. `check_korean` 통과.

남는 것:

- 이 조사는 인물→사건 참여만 봤다. 인물 사이(부모·배우자·사제)·인물→직위·
  사건→장소·인과 엣지 전체와 노드 자체(라벨·설명·날짜)의 오류는 같은 방식
  (기계 관문으로 거르고 걸린 것만 읽기)으로 아직 안 했다.
- 전투의 `지휘관` 은 그대로 두었다 — "인조는 병자호란에서 조선 측을
  지휘했다"는 인포박스 그대로지만 어색하다. 왕은 '통치'로 읽을지 정하지 않았다.
- 생몰년 어긋남 중 사건 날짜 쪽이 거친 것(양만춘·서희·효종의 대동법·광종의
  칭제건원·공민왕의 도첩제·목종의 전시과)은 두었다 — 날짜를 고칠 자리다.

## 2026-09-06 — "세종 인과 사슬에 어째서 한글창제가 빠진거지?" (뿌리 셋)

사슬이 덜 만들어져서가 아니었다. 모델은 세종 문서에서 `세종 → 훈민정음`
을 다섯 번 답했고 **판정 단계에서 셋이 겹쳐 버려졌다.** 셋 다 세종 한 건이
아니라 구조였다 — 땜질(별칭 하나·문서 하나 떼기) 대신 뿌리를 고쳤다
(사용자: "근본적인 해결법으로 고쳐줘").

| 뿌리 | 실측 | 고친 곳 |
|---|---|---|
| 민백 항목이 **동명이인**에 붙는다 — 이름·타입만 보고 별칭(휘 '이도')까지 이름으로 썼다 | 인물 문서 3,435건 중 284건이 딴사람 글 (1971년생 야구 선수에 1573년 문신) | `aks.consistent` — 시대 칸·정의의 시대 말·정의/본문의 **연도**로 잰다. 연도가 있으면 연도가 이긴다(사전의 시대 칸이 틀린 항목이 있다). 국가유산은 날짜가 지정일이라 안 잰다. `match_nodes`·`ingest`(받은 본문으로 한 번 더)·`fill_descriptions` 가 다 지난다. 되돌아보는 것은 `corpus --audit` — 문서 284건을 고아 `aks:E…` 로 옮기고 설명 8건을 비웠다 |
| 근거 대조가 **한자 괄호**에 진다 — 원문 `훈민정음(訓民正音)의`, 모델 인용 `훈민정음의` | 정본 문서 60건의 답 333개 중 못 찾은 26개의 절반. 세종 문서는 12개 중 9개 | `extract._compact_index` 가 한자 괄호를 양쪽에서 뺀다 (원문 좌표는 지킨다) |
| 해소기가 **그 자리에 못 서는 타입**을 고른다 — '훈민정음' = 국보 해례본(유물) → '타입 안 맞음' | 200개 문서 재판정에서 유물·장소로 풀린 결과 19건 (대부분은 사람 — 그건 설계) | `causes.resolve(allowed=…)` 가 자리에 맞는 타입만 고른다. 그래도 '훈민정음'→'한글'은 글자가 달라 코드가 못 잇는다 — `data/aliases.tsv` (`노드 id<TAB>별칭<TAB>근거`), `relabel` 이 적용하고 편집 계층 `alias:<별칭>` 칸에 남는다 |

돌린 순서: `corpus --audit`(원본·파생본) → `relabel`(원본·파생본) →
`causes --reresolve` → `causes --sync-only --sync-to data/korea.sqlite`.
모델은 안 썼다.

남은 것:

- 딴사람 글로 물었던 문서의 답(`causes_answers`)은 그대로다 — 그 글의
  근거는 이제 안 맞아 `근거 없음`으로 떨어지므로 틀린 엣지는 안 남지만,
  **맞는 글로 다시 물으면** 더 나올 것이다 (MLX, `causes --redo` 는 답이
  없는 것만 다시 묻는 규약이라 그 노드들의 답을 지워야 다시 묻는다).
- 실록 기사 노드(`훈민정음을 창제하다` 등)가 `about` 이 가리키는 국보의
  설명("국보 제30호…")을 그대로 받는다 — 기사 설명으로는 틀리다.

## 2026-09-06 — 결과 자리에 선 사람은 버리지 않고 참여로 돌린다

사용자 지적: "'이 인물을 만든 것'은 실제로 있긴 있잖아."

실측: 모델 답 7,398건 중 39건이 결과로 사람을 지목했고 관문이 전부 '타입
안 맞음'으로 떨어뜨렸다. 그런데 **39건이 전부 '영향'**이고 내용은 하나같이
'그 사건이 그 사람에게 무슨 일을 했나'다 — 안악 사건 → 김구(잡혔다),
강상인의 옥 → 심온(사사됐다), 해유령 전투 → 신각(처형됐다). '이 사건이
이 사람을 낳았다'는 답은 한 건도 없다.

그러니 온톨로지가 사람을 결과로 막은 것은 옳다. 다만 통째로 버리면 참인
사실이 사라졌다 — 김구와 안악 사건 사이에 엣지가 하나도 없었다. 이제
방향을 돌려 **사람 → 사건의 참여**로 적고 `roles` 가 무엇을 했는지
판정한다. 받는 조건은 좁다: 원인이 사건 · 결과가 사람 · 종류가 '영향' ·
**그때 살아 있었을 것**.

마지막 조건은 판정이 알려 줬다. 김종직(1431~1492)이 무오사화(1498)를
'주도'로 판정됐다 — 연산군이 사화의 명분으로 그를 지목했을 뿐이고 그는
부관참시된 쪽이다. 사명대사(1543~1610)도 심하전투(1619)에 붙었다.
`causes.alive_at` 이 막는다 (생몰을 모르면 막지 않는다).

- `roles --sources causes` — 소스를 골라 판정한다. 판정 안 된 참여가
  3,665건이라 한 번에 다 물으면 며칠이 걸린다. 새로 들어온 것부터 묻는다.
- 화면은 손대지 않았다. 관계 묶음이 이미 역할 이름으로 갈라 세우고
  문장까지 적는다 ('김구는 안악 사건의 피해자다', `relations.ROLE_SENTENCE`).

## 2026-09-06 — 사슬 머리에 방향을 적는다

'이 인물 관련' → **'이 인물에서 비롯된 일'** (사용자 선택). '관련'은 방향을
말하지 않는데 이 나무는 이 노드에서 뻗어 나간 쪽이다. 단체·개념도 같은
꼴이고 사건은 '이 일이 부른 것' 그대로다.

'만든 것'을 그대로 쓰지 않은 이유 둘: 만들다는 제작에 가깝고, **원인 쪽은
인물에게 아예 뜨지 않는다**(사람은 원인만 될 수 있다). '일으킨 일'은 네
걸음 밖까지 한 사람이 일으켰다고 단정하게 된다.

## 2026-09-06 — "이 시기 왕들때는 아무런 일이 없었어?" (덕종~헌종의 빈 연표)

연표에서 덕종(1031~1034)부터 헌종(1094~1095)까지 65년이 통째로 빈 칸이었다.
이유가 둘이고 성격이 다르다.

**하나는 우리 버그.** `server.ANCHOR_POOL = 400` 이 사건을 차수 상위
400개로 끊고 있었다. 조선만 있던 때 걸어 둔 문턱이라 그때는 아무것도 안
잘렸는데(연도 있는 사건 73개), 묶음이 고려~대한민국이 되며 사건이 568개가
됐고 **연도를 아는 사건 119건이 조용히 잘려 나갔다.** 차수가 낮은 쪽부터
잘리므로 자료가 얇은 시대가 먼저 사라진다 — 잘린 것 중 고려가 18건(왕규의
난·귀주성 전투·안북부 전투·명주 민란·조일신의 난 …)이고, 그 65년 창에 있던
두 건(1056·1067 흥왕사)도 함께 없어져 빈 칸이 됐다. `_anchors` 의 주석은
이미 "솎지 않는다"고 적고 있었다 — LIMIT 을 뗐다. 연표 마크 385 → 505.

**하나는 자료.** 그 시기에 전쟁이 없던 것은 사실이다 (거란 3차 침입 1019 →
무신정변 1170). 실측:

| 재는 것 | 수 |
|---|---|
| 고려 474년의 사건 노드 | 57건 (조선 505년은 214건) |
| 민백의 고려 '사건' 항목 95건 중 **1019~1135 사이** | **0건** |
| `wikipedia.EVENT_SEEDS["고려"]` 37개 | 전부 난·전쟁 |
| ko-wiki 의 그 시기 사건 문서 | 없다 (9재 학당·사학 12도·고려 천리장성 다 없음) |

그 시기의 역사는 사건이 아니라 **제도와 문화**로 적혀 있다. 사용자 결정:
**민백 표제 + 고려사 날짜**로 세운다 (`uv run histgraph founding`,
`aks.founding_events`). 이름·설명은 민백(표제 + 정의 한 문장), 날짜는
《고려사》·《고려사절요》 기사. 화면에 서는 것은 '국자감시'이지 '국자감시를
설치하다'가 아니다 — 기사 제목을 그대로 세우면 §1-3 에 걸린다.

관문 다섯 (하나라도 못 넘으면 안 세운다): 표제가 제목에 **낱말로** 있고
(조사는 붙어도 되지만 조사 아닌 한글이 이어지면 더 긴 이름이다 —
'국자감시를'의 '국자감'은 아니다) · 표제 **바로 뒤**에 그 유형에 맞는 술어가
오고('비서성에서 간행한'의 간행은 책이 나온 것이다, '방물을 바치다'·'설치를
건의하다'는 선 것이 아니다) · 날짜가 **양력**이고 · 걸린 기사가 다섯 건
이하이며 · 기사의 해가 항목 연대와 맞는다(`aks.consistent` — 1286년 경전이
1101년 기사를 받던 것을 막는다).

**세운 것 21건** (1009 중대성 · 1031 국자감시 · 1098 태자부 · 1101 서적포 ·
1106 해동비록 · 1237 강화외성 · 1304 대성전 · 1309 각염법 · 1372 자제위 …).
고려 사건 57 → 78. 이름이 이미 있는 둘(삼국사기·쌍성총관부)은 안 세우고
보고만 한다 — 합치는 것은 `dedupe` 쪽이다.

**남은 것** — 1031~1095 창은 여전히 얇다 (국자감시 · 흥왕사 둘). 《고려사》에
그 창의 양력 기사가 2,421건 있지만 제도의 설치를 적은 문장은 대개 지(志)에
있고 **지에는 날짜가 없다**. 세가의 날짜 있는 기사는 조정 일과가 대부분이라
(`동여진인들이 토물을 바치다`) 제목을 그대로 세울 수 없다. 천리장성(1033~
1044 북변 관성 축조) · 초조대장경(1087 대장경 완성 경축) · 구재학당(1055) 은
정본에 날짜가 있는데 표제가 기사 제목과 달라 못 이었다 — `data/aliases.tsv`
에 별칭을 적어 잇는 것이 다음 자리다.

## 2026-09-06 — 고려사를 붙였다 (사용자 요청)

"고려사도 조선·근현대사처럼 붙여줘. 조선왕과 이름이 겹치는 부분(숙종
같은)은 고려 숙종·조선 숙종 나눠서 만들어줘."

`korea` 묶음이 **고려~대한민국**이 됐다. 맨 앞 시대가 그래프의 중심이고
연표의 바닥이라 화면이 고려(918)에서 열리고 첫 눈금이 '고려건국'이다.
조선만 있을 때 맥락으로만 떠 있던 위화도 회군(1388)·전시과(976)가 제
시대에 선다.

**사용자가 받아 준 정본** (공공데이터포털, `data/raw/`):

| 파일 | 쓰는 곳 |
|---|---|
| 《고려사》 원문 XML (31,204 기사 · 양력 22,602) | 고려 사건의 날짜 |
| 《고려사절요》 원문 XML (11,222 기사) | 같음 |
| 한국학중앙연구원 역대인명정보 XML (27,035명) | 인물 생몰년 |

국편 색인(`data/raw/nikh/sillok.sqlite`)이 이제 세 책을 담고 `book` 으로
가른다 — **책을 안 가르면 조선 정종의 기사가 고려 사건의 날짜가 된다**
(`nikh.BOOKS_BY_ERA_DIGIT`). 실록과 다른 점 둘: 기사가 `level5` 가 아니라
**더 깊은 층이 없는 단위**이고(고려사는 level4, 절요는 level4·5 가 섞임),
날짜가 음력·양력 두 벌이라 양력을 고른다. 고려 사건 36건이 정본 날짜를
받았다 (무신정변 1170-08-30 · 이자겸의 난 1126-03 · 전시과 976-11 …).

**이름 겹침** — 묘호가 조선과 겹치는 임금이 열여섯이다 (태조·정종·문종·
예종·숙종·인종·명종·고종·원종·덕종·현종·경종·성종·헌종·세조·목종).

- 왕 34명은 `wikipedia.MONARCH_SEEDS` 가 **라벨까지 정해서** 받는다.
  위키백과 문서명은 '숙종 (고려)'인데 우리 관례는 '고려 숙종'이다. 고려
  안에서도 정종이 둘이라 '고려 정종 (3대)'·'고려 정종 (10대)'로 가른다.
- **인물 생몰년은 시대 창을 못 넘는다** (`nikh.person_years`). 이 검사가
  없어서 고려 원종(1219~1274)이 조선 원종(정원군, 1580~1619)의 생몰년을
  달고 있었다 — 실록 인물 CSV 는 조선 사람만 담는데 이름·한자가 같다는
  이유로 씌워졌고, 화면에는 '고려 원종이 1270년 삼별초 항쟁에 참여'가
  310년 어긋난 것으로 떴다. 정본 날짜는 `data/chronology.tsv` 에 적었다.

**재위 띠** — 고려 임금은 Wikidata 에 재위 문장이 거의 없다(34명 중 다섯).
한국어 위키백과 인포박스에 34명 전부 양력으로 있어서 왕을 받는 김에 읽는다
(`infobox.reign_spans`). `재위N` 은 `작위N` 과 짝이라, 짝을 안 보면 태자
시절이 재위가 된다 (실측 16명).

- 자리 노드 '왕(王)'(Q12087706)이 **단체**로 앉아 설명이 없다는 이유로
  파생본에서 빠졌고 재위 엣지 34건이 함께 사라졌다. 직위로 바로잡았다 —
  고구려 임금 8명도 같은 이유로 빠져 있었다. **설명 없는 org 자리 노드가
  아직 11개 더 있다** (명의 주교 37 · 부서장 12 …). 직위인데 단체로 앉은
  것들이라 `reclassify` 가 손볼 자리다.
- 한 사람이 같은 때 두 자리에 앉지는 않는다 — 겹치는 구간은 한 띠로
  모은다. 잇달아 있는 것은 안 모은다 (고종의 조선 임금 → 대한제국 황제).

**남은 것**

- 인과 사슬이 돌고 있다 (`~/.cache/histgraph/causes_goryeo.sh`, 로그
  `causes_goryeo.log`). 안 물은 문서 40건 — 무신정변·위화도 회군·황산대첩·
  요동 정벌 등 고려 사건이 대부분이다. 죽었으면 같은 스크립트를 다시 돌린다.
  끝나면 `python3 tools/check_korean.py` 뒤 `data/korea.sqlite` 를 커밋한다.
- `chronology` 위반 29건이 남아 있다 — **고려 것은 없다**. 전부 조선·근현대이고
  09-06 인과 수집이 만든 것이다 (만주사변 → 심하전투, 조선어학회 → 한글 …).
- `homonyms` 가 보고하는 '연대가 어긋난 참여' 18건 중 고려 것 몇은 **날짜가
  거친 탓**이다 — 서희(942~998) → 거란의 고려 침입(1010): 서희는 993년
  1차 침입의 사람인데 노드가 절요에서 1010년(2차)을 받았다. 지우면 참인
  것이 먼저 사라지므로 보고만 한다 (CLAUDE.md §1-2).
- '고려'가 고구려(wd:Q28370)의 별칭이라 아직 남아 있다. 그것 때문에 안북부
  전투(1231)의 참여자가 고구려로 적혀 있었고(Wikidata P710) 편집 계층에
  지웠다. 별칭 자체는 `aliases` 가 다시 달므로 막을 자리가 없다.

## 2026-09-06 — "서울올림픽이 냉전체제에 영향을 줬다는 거 사실이 아니야" (`fact_check` 관문)

인과 사슬에 서울올림픽 → 냉전체제 (영향) 이 서 있었다. 모델이 결과를
'냉전체제의 해소'로 냈고 해소기가 그것을 냉전체제로 풀었으며, 근거는
"서울올림픽 **이후** 소련이 해체되고 … 냉전체제는 해소되었다" — 시간 순서를
인과로 읽은 논리 비약이다. 사용자: "이런 논리비약을 피하면서 인과 사슬을
만들어야 해. 팩트체크를 철저하게 해."

`causes.fact_check` 가 `accept` 의 다섯째 관문이다 (모델 답·Claude 답·재판정
모두 지난다). 배경지식이 아니라 **근거 문장에서** 잰다:

1. 결과가 'X의 해소·붕괴·폐지·멸망…' 꼴이면 X 가 아니라 X 의 끝이다 — 종류를
   '영향'으로 바꾸고, 근거에 인과 표현(때문·계기·초래·따라·이에·함께…)이
   없으면 버린다. 서울올림픽 건은 여기서 떨어진다.
2. 근거 앞뒤 250자에 양끝 이름이 있어야 한다 (문서 주인공은 주어가 생략되니
   예외; 괄호 안 이름·계유정난/정란·흥선대원군/대원군은 같은 것으로). 둘 다
   없으면 버리고, 한쪽만 없으면 확신도 0.5 상한 — 별칭·서술로 가리킨 참인
   인과('흥선대원군의 천주교 탄압' = 병인박해)를 지우지 않기 위해서다.
3. 상징·추정 표현(상징적·평가되기도·설도 있다…)은 확신도 0.5 상한.
4. '원인'·'영향'인데 근거가 원인을 "X 이후/뒤"로만 두면 '배경'으로 낮추고
   확신도 0.5 (삼포왜란 뒤 삼포 폐쇄처럼 참인 것이 있어 버리지는 않는다).

이미 적힌 엣지는 `uv run python tools/check_causes.py [--db …] [--apply]` 가
같은 관문에 다시 넣는다 — 원본 2,389건 중 지움 11 · 종류 32 · 확신도 245,
파생본 지움 7 · 종류 27 · 확신도 216. 지운 것과 고친 값은 편집 계층
(`overrides`, origin `factcheck`; 엣지 `confidence` 칸을 새로 허용)에 남아
수집·재판정이 되살리지 못한다. 처음 두 번의 시도(이름 한 문장 안 필수, 상징
표현 즉시 버림)는 참인 엣지 594·335건을 걸었다 — 관문은 문장 한 개가 아니라
문맥으로, 지우기보다 낮추기로 기울여야 한다.

## 2026-09-06 — 인과는 Claude 가 직접 읽어 적었고, 남은 것은 다시 MLX 로

사용자 결정 둘. 09-05 밤 "우리 llm 을 쓰지 말고 네가 직접 해석해서 만들어줘.
토큰 limit 이 걸릴 때까지" → 09-06 "이제 니가 직접 만들지 말고 저번처럼 우리
자체 llm 을 이용해서 만들어".

**Claude 가 직접 읽은 것** (2026-09-05~06, `causes_answers` 의 모델
`claude-fable-5-1`, 778 문서): 화면 노드 중 사건·단체·개념 문서 **전부**
(658건 — `dump_docs.py --next` 가 더 내놓지 않는다) 과 인물 문서 120건
(차수 순, 인물 → 사건·단체). 화면 DB 인과 엣지 1,215 → 1,989건. 답은
`~/.cache/histgraph/answers/bNNN.jsonl`·`pNNN.jsonl` 에 남아 있고
`~/.cache/histgraph/ingest.py` 가 `causes.ingest_answers` 로 같은 관문에
넣는다(근거 원문 검증·해소·연대·상하위). 읽을 문서를 찍는 것은
`~/.cache/histgraph/dump_docs.py --next N --types=… [--uncaused] [--grep=정규식]`.

읽으며 고친 것 — 인과 엣지를 만들다 보면 날짜·중복이 먼저 걸린다:

- `data/chronology.tsv` 에 날짜 40여 건 (예송 1666~1659 로 뒤집혀 있던 것,
  진보당 1950 → 1956-11-10, 냉전체제 1990 → 1947~1991, 석유파동 1980 →
  1973-10~, 연행사 1882 → 1637~1894, 인혁당 1965 → 1964-08-14 …).
- `data/duplicates.tsv` 에 merge 6 (심하전투=사르후 전투, 회사령 concept=
  event, 부차 전투=심하전투, 냉전=냉전체제, 5·10선거=제헌 국회의원 선거 …),
  keep 2 (6·3 항쟁/한일회담 반대운동, 광복단/대한광복단).
- `data/ko_labels.tsv`: `wd:Q11599108` 울릉도 전투 → **울릉도 쟁계**
  (안용복의 영유권 분쟁이지 전투가 아니다).
- 말뭉치 불일치 둘: `wd:Q493295` 금성 전투(1953)에 후삼국 나주 금성전투 문서가,
  `wd:Q482850` 조선 철종에 덕수 이씨 문신(이변) 문서가 붙어 있다 — 답은 비웠다.

배운 규칙 (관문이 알려 준 것): **인물은 결과가 될 수 없다** (`EFFECT_TYPES`
— "타입 안 맞음"); `대한민국 임시정부` 는 `HOME_POLITIES` 라 양끝 다 막힌다;
인물 이름은 접두·접미로 안 풀리므로 라벨 그대로 적어야 한다(묄렌도르프 →
`파울 게오르크 폰 묄렌도르프`); 동명이인(김석주 둘)은 원인으로 쓰지 않았다;
`6.25 전쟁` 은 가운뎃점이 아니라 마침표다.

**돌아가고 있는 것** (`~/.cache/histgraph/causes_chain2.sh`, 로그
`causes_chain2.log`, `start_new_session` 으로 띄워 세션이 끝나도 산다):
① 화면 인물 문서 2,564건 `--scope data/korea.sqlite --types person`
② 원본의 나머지 사건·단체·개념 1,096건 ③ `--reresolve` → `check_korean`.
문서당 약 1분이라 ①②만 60시간 남짓. 10분마다 `--sync-only` 로 화면 DB 에
옮기는 루프가 같이 돈다. Claude 가 읽은 문서는 `causes_model` 표식으로
건너뛴다. **죽었으면 같은 스크립트를 다시 돌린다.** 끝나면
`python3 tools/check_korean.py` 뒤 `data/korea.sqlite` 를 커밋한다.
돌리기 전에 `ollama stop` 으로 6GB 를 비웠다 (`histgraph-mlx-needs-free-memory`).

남는 것: 문서 없는 화면 사건·단체 35건(실록 기사 제목 대부분)은 물을 글이
없다 — 말뭉치부터 채워야 한다(`corpus`). `data/duplicates.tsv` 233행
`wd:Q70585589`(단종 복위 운동)은 어느 DB 에도 없어 죽은 행이다.

## 2026-09-06 — 노드 색을 사용자가 준 팔레트로 (Coolors "skafold")

여섯 색(초록 004F2D · 남색 003049 · 빨강 D62828 · 주황 FB6C13 · 노랑 F9C80E
· 크림 F5E0B7)으로 아홉 타입을 칠했다. 주황(사건)·노랑(유물)·크림(단체)은
원색 그대로. **인물은 팔레트 밖의 옛 파랑 `#3d84f5`** — 빨강으로 씌웠더니
"인물 노드도 blue 로" (재위 띠와 같은 파랑). 빨강 원색은 쓰는 자리가 없다. **남색·초록은 원색 그대로 두면 바탕 `#1e1e1e` 위에서
대비가 1.2·1.7 이라 작은 원이 안 보였다** (PIL 로 그려서 봤다) — 같은 색상으로
밝혀 장소·영상에 쓰고, 어두운 값을 시대·직위(물러날 뼈대)에 뒀다. 검증기
`python3 tools/check_palette.py` 는 정상 10.7 · 2형 9.7 · 1형 7.3 으로 눈금을
넘긴다. 색이 사는 곳 넷을 다 고쳤다 — `graph-view.js` `TYPE_COLOR`·
`GROUP_COLOR`, `style.css` `--mark-*`·`--color-blue`, `pages.py` 문서 페이지의
`--actor·--event·--thing·--frame`, 그리고 `design.md` §3 표. 연표 '원인'의
파랑(`--color-blue`)은 사용자 결정(blue)이라 남겼고, 팔레트 남색을 글자가
읽히게 밝힌 `#4f93bf` 로 바꿨다. **왕·대통령의 재위 띠는 옛 파랑 `#3d84f5`
그대로** (사용자: "전처럼 blue 를 유지") — 띠가 `TYPE_COLOR.person` 을 읽고
있어 빨강이 됐던 것을 `REIGN_COLOR` 로 떼어냈다.

## 2026-09-06 — 노드를 누르면 인과 도면 (원인 왼쪽 · 결과 오른쪽) — **꺼 둠**

사용자 결정: "인과관계 그래프를 지우진 말고 기능 꺼두자." `App.jsx`
`CAUSAL_DIAGRAM = false`. 코드·테스트는 그대로 있고, 켜면 아래대로 움직인다.
꺼진 동안 클릭은 전처럼 주변 관계를 열고 인과 선은 굵은 회색이다.

세 번 고쳤다. "노드를 클릭하면 인과관계를 보여주는 그래프를" → 이웃 120
위에 파란 사슬을 얹음 → "처음부터 보여주지 말고(너무 복잡)" → 누른 뒤에만
얹고 나머지 흐림 → "너무 복잡하게 그려지고 있어서 아무런 정보값이 없어.
팔란티어나 다른 회사의 그래프 디자인을 검색해서 공부해서 수정해봐."

찾아본 것 (design.md §4 '인과 도면'): 팔란티어 Vertex 의 계층(좌→우) 배치,
Cambridge Intelligence 의 순차 배치, 사건 인과 시각화 논문(VAC2·DOMINO)의
시간축 흐름도·점진 탐색. 공통 — 인과는 층으로 세우고 다른 관계는 그리지
않는다. 그래서 **노드를 누르면 캔버스가 그 노드의 인과 도면이 된다**
(`GraphView.showCausal`·`causalLayout`·`_drawCausal`, `App.openCausal`):

- 두 걸음(`/api/chain?depth=2`)만. 원인은 왼쪽 열, 결과는 오른쪽 열, 열은
  수기야마 가장 긴 경로(모든 선이 좌→우), 열 안은 연도순, 이름 아래 연도.
- 배율이 아니라 화면 자로 잰다 (열 120~230px · 줄 40~58px). 크기가 바뀌면
  다시 잰다.
- 주변 관계 그래프는 접어 뒀다가 빈 곳을 누르면 그대로 돌아온다. 도면 안의
  노드를 누르면 그 노드의 도면으로. 처음 열릴 때는 도면이 아니다.

고친 김에 잡은 것: `/api/chain` 의 예산 60을 원인이 먼저 다 써서 **원인이
많은 사건의 결과가 0** 이었다(심하전투 — 연표엔 정묘호란·인조반정이 결과).
두 쪽이 나눠 쓰고 남은 것을 이어 쓴다 (`causes.chain`, 회귀 테스트). 그래프
API 는 인과 엣지의 라벨로 종류(배경·계기·영향)를 보낸다. **8100 서버는 다시
띄워야 이 둘이 반영된다.**

도면이 드러낸 자료 오류: 심하전투(1619)의 결과로 표전문제(1395)·
종계변무(1589). `chronology` 몫.

검증: 헤드리스 크롬 CDP 스크립트(스크래치 `cdp.mjs`)가 `window.__histgraphView`
에서 노드 자리를 읽어 누르고 찍는다. **크롬이 옛 index.html 을 캐시해 옛
번들을 돌린 적이 있다** — `Page.reload({ignoreCache:true})` 로 연다.

## 2026-09-06 — 인과 사슬의 트리 선이 부모의 점과 떨어져 있었다

"심하전투에 후금이 영향을 줬으면 저 트리선이 심하전투에 완전히 연결시켜
인과 관계를 확실하게 보여줘." 자식 단의 세로선이 **자식 줄에서만** 시작해
부모의 점·설명 글 아래에 빈틈이 있었고, 선도 점의 왼쪽 가장자리에 서 있었다.
고친 것 둘 (`ChainPanel.jsx`·`relations.js` `chainGuides`·`style.css`):
자식이 있는 줄은 점 바로 아래에서 줄 바닥까지 줄기(`.chain-stem`)를 내려
다음 줄의 꺾인 선과 잇고, 모든 안내선을 점의 한가운데(STEP 배수 + 반지름
4px)에 세운다. 헤드리스 크롬으로 그려 이어지는 것을 확인했다.

## 2026-09-05 — "원인 사건이 먼저 발생해야 하는거 아닌가?" (`chronology` 연대 관문)

연표에 '원인' 딱지를 파랗게 달고 오른쪽 여백의 꺾인 선으로 고른 사건과
이었더니, 공석신주사건(1636) **아래**에 원인인 병자호란(1636-12-09)이 섰다.
엣지는 참이고 날짜가 틀린 것이었다 — 탄핵은 1638년 1월인데 배경이 된 해가
적혀 있었다. 사용자 결정: 사람이 판정하지 말고 직접 조사해 정확한 날짜를
적는다.

실측 (화면 DB 인과 중 사건이 결과인 것): 원인이 결과보다 뒤 19건, 같은
해·달 안에서 결과 날짜가 거칠어 원인을 품는 것 42건. 정본(민백·국편
말뭉치)과 위키백과를 읽고 `data/chronology.tsv` 에 54줄을 적었다:

- **날짜 32건** — 임신약조 1510→1512, 대동법 1624→1608, 해유령 전투
  1562→1592, 단발령·옥포 해전처럼 음력을 양력처럼 적은 것, 사도세자
  사건 윤5월 → 양력 7월 4일 등. 날짜는 양력으로 통일했다 (Wikidata 노드가
  그레고리력이라 국편의 음력을 그대로 두면 같은 해 안에서 뒤집힌다).
- **지움 20건** — 5·18 → 서울의 봄, 이괄의 난 → 대동법(폐지), 병인양요 →
  경복궁 중건(지연)처럼 원인이 결과를 **끝낸** 것. 시일야방성대곡 →
  을사조약처럼 반대 방향이 따로 있는 것.
- **뒤집음 2건** — 위화도 회군 → 요동 정벌, 춘생문 사건 → 을미사변.

만든 것:

- `chronology` 모듈·명령. 원인 날짜가 결과보다 뒤이면 종료 코드 1. 둘 중
  거친 쪽 정밀도로 비교하므로 '같은 해 안'은 잡지 않는다 — 그것은 화면이
  맡는다.
- 편집 계층에 엣지 `deleted` 칸. `causes` 가 같은 문서에서 지운 인과를
  다시 뽑아도 저장소가 다시 지운다. 회귀 테스트 17건.
- 연표 `sortMarks`: 원인을 품는 거친 날짜는 원인 바로 뒤에 선다. 나가는
  인과의 딱지는 '결과'(서버가 '원인'으로만 주던 것). 연표 테스트 5건.
- 그래프의 강조 선을 보라에서 밝은 회색으로 (사용자 요청).

새 인과를 추출한 뒤엔 원본·파생본에 `chronology --apply` 를 한 번씩 돌린다
(CLAUDE.md §1-5). 실측: 이 작업 중에도 다른 세션의 추출이 인과를 보태서
관문을 돌릴 때마다 2~3건씩 새로 걸렸다 (옥포·적진포·동래성 — 1592년 해전·
전투는 음력을 양력처럼 적은 것이 더 남아 있을 수 있다. `precision` 이
연도만 자르는 것이지 역법을 옮기지는 않는다).

## 2026-09-05 — "아직도 인과 관계가 빠진 노드들이 너무 많아" (전부 묻는다)

사용자 요청: "전부 찾아서 인과 관계를 만들어줘". 실측(화면 DB): 사건 503 중
인과 없는 것 260, 단체 226 중 103. 빠진 이유가 셋이었다.

1. **말뭉치 문서가 없어 묻지도 못한 노드** — 원본 사건·단체·개념 1,888개
   (화면 195). 위키백과 전문을 받았다 (`corpus.build`, 스크래치
   `fetch_docs.py`) — kowiki 문서 483 → 1,700여 건.
2. **모델 답을 이름 못 풀어 버린 것** — 두 실행에서 답 5,000여 건 중
   3,237건. 있는 노드를 다른 표기로 부른 것이 적잖았다 ('대한민국임시정부'·
   '새마을운동'·'6·29 선언'·'중앙정보부'). `causes.LooseIndex` — 띄어쓰기·
   가운뎃점·마침표·괄호 한정어를 지운 열쇠로, 정확한 표기가 없을 때만 한
   번 더 푼다. 인물은 접미로 풀지 않고(이황 ≠ 퇴계 이황) 자국 왕조는
   여전히 막는다.
3. **답을 버려서 다시 물어야만 했던 것** — 이제 `causes_answers` 표에 남긴다.
   `causes --reresolve` 가 모델 없이 다시 판정한다 (노드가 생기거나 해소기가
   좋아졌을 때).

새 옵션: `--scope data/korea.sqlite` (화면에 선 노드부터), `--reresolve`,
`--sync-to` 는 열 문서마다 화면 DB 로 옮긴다. 테스트 966/966.

**1단계 실측 (14:25 끝).** 문서 185건 → 인과 엣지 186건. 화면 DB 인과 엣지
572 → 699, 인과 있는 사건 243 → 311 / 503, 단체 123 → 140 / 226 (커밋
4984a4d). 버린 것 — 이름 못 풂 515 · 타입 안 맞음 161 — 은 저장된 답으로
다시 보니 거의 설계대로다: '왕권 강화'·'열강 세력의 이권 침탈' 같은 주어
없는 서술구, 인물이 결과인 것, '진주성 함락'이 장소로 풀리는 것.
`임진왜란 --to 병자호란` 경로는 아직 없다 (2단계가 정묘호란·후금을 다시 묻는다).

**돌아가고 있는 것** (스크래치 `causes_chain.sh`, 로그 `causes_chain.log`):
다른 세션의 `paraphrase`(750/6,643) 를 멈추고 ① 화면 노드 중 안 물은 문서
② 화면 노드 전부 `--redo` (500건, 답을 저장하고 새 해소기로) ③ 원본 나머지
(차수 순) ④ `--reresolve` → `check_korean` → **paraphrase 를 같은 명령으로
되살린다** (해시로 이어 쓰므로 잃는 것은 문서 한 건). 문서당 약 1분이라
①~③ 합쳐 하루 넘게 걸린다. 중단됐으면 같은 스크립트를 다시 돌리면 된다 —
①③은 `causes_model` 표식으로 이어지고 ②는 처음부터 다시 묻는다.

남는 것: 못 푼 이름 중 **없는 노드**(국가안전기획부 · 제1차 미소공동위원회 ·
이란-이라크 전쟁 · 제1차 석유파동 · 사스) 는 노드 후보다 — 만들지 않았다
(설계: `causes` 는 노드를 만들지 않는다). 실록 기사 사건(`sillok:` 28)은
문서가 한 줄이라 물을 것이 없다. 인물 문서는 묻지 않았다 (`--types person`).

## 2026-09-05 — 편집 계층 (`overrides`) — 팔란티어 설계에서 가져온 것

사용자 요청: "팔란티어의 온톨로지 그래프와 우리의 온톨로지 그래프의 차이점을
분석해봐" → "우리가 팔란티어 설계에서 배울게 있나?" → "1,2,3번 순서대로
진행 하자". 배울 것 셋 — (1) 고친 값은 원천 위에 겹쳐 두고 읽을 때 합친다,
(2) 엣지 타입에 카디널리티를 선언한다, (3) `related_to` 를 갈라 낸다.

**(3) 끝 — 모델 대신 표.** 새 타입 `taught`(사제, 스승 → 제자), `histgraph
untangle` (`src/histgraph/untangle.py`). 규칙 127 · 겹침 194 뒤, 남은 추출
후보 1,369건은 사용자 결정("로컬 llm 을 쓰지 말고 너가 직접")으로 Claude 가
근거 문장을 읽어 `data/untangle.tsv` 에 판정했다 — 139건 타입(사제 38 · 소속
32 · 참여 32 · 자녀 23 · 소재 7 …), 나머지 `none`(형제·정적·동료·후손·배우
명단이라 목록에 없는 관계). 파생본 `related_to` 1,417 → 1,009 · 원본 2,285
→ 1,710. `untangle` 은 표가 있으면 모델을 띄우지 않는다. README
"`related_to` 갈라 내기" 절.

**갈라 내며 본 것 — 고쳤다 (2026-09-05, 두 DB)**:
- 예종(휘 이황)에게 간 이황의 관계: `homonyms` 가 이황 문서의 22건을 옮기고,
  다른 여섯 문서(남명·이익·김굉필·박세채·안방준·선조)의 '이황' 언급 6건은
  손으로 퇴계(wd:Q486291)에게 옮겼다. 남은 것은 정인지 문서의 진짜 예종 1건.
- '조선 원종'(정원군)에 씌워진 고려 원종의 국편 정본: 설명·표식을 고려 원종
  (wd:Q493978)에게, 엣지 5건(개경 환도·삼별초·몽골·강화도·고려 왕)도 그쪽으로,
  말뭉치 문서(kc_n203300)도 그쪽으로. 정원군은 위키데이터 한 줄로 되돌렸다.
  재발 방지: `nikh.pick_target` 의 정체 낱말 관문 (README "국편 정본" 절).
- 같은 사람 두 노드: `data/duplicates.tsv` 에 merge 10줄 — 혜암=현문, 만공스님,
  정암=조광조, 고재우=곽재우(추출 오타), 시마즈·시마즈 가·가문·군=시마즈 요시히로,
  한중록(사건→작품), 요=요나라. 회령시 둘은 이미 keep 이 있었다.
- 이원경: 회평군의 초명인데 현대 인물 이원경(1922~2007)에게 갔다 — 별칭을
  회평군에 달고 엣지를 지웠다.
- 타입: 동인·요나라·거란 → org, 강문회 → person, 경제문감 → heritage.
  조선인·국유지·소작농(집합명사)은 지웠다.
- **서술구 사건 (2026-09-06, 끝)**: `extract.is_abstract_event` — 사건 표지도
  숫자도 없는 이름은 사건 노드를 만들지 않는다. `promote.retype` 가 기존
  고아 사건에 같은 규칙: 같은 이름의 실제 노드에 흡수 22 · 단체로 22 ·
  지움 134 (원본). 파생본은 2건. `ex:event:` 451 → 273. README "타입 교정" 절.

**(2) 끝.** `ontology.MAX_TARGETS` (출생지 1 · 사망지 1 · 발생 시기 1 · 부모 2),
`_persist` 의 쓰기 전 경고, `histgraph cardinality [--fetch-places] [--list]`
(`src/histgraph/cardinality.py`). 충돌과 해상도 차이를 가르고 보고만 한다.
파생본 실측: 넘는 노드 172 — 충돌 73 · 해상도 차이 99. `--fetch-places` 로
장소 `located_in` 을 원본 568 · 파생본 212건 더했다. README "카디널리티" 절.
**남은 일**: 충돌 73건은 사람이 보는 목록이다 — 동명이인 문서가 섞인 것은
`homonyms`·`dedupe` 쪽, 소스가 틀린 것은 `ko_labels.tsv` 처럼 표로.

**(1) 끝.** `src/histgraph/overrides.py` — `overrides` 표와 `reapply`.
`GraphStore.upsert_nodes`·`upsert_edges` 가 끝나면서 방금 쓴 것에 걸린 줄을
다시 씌운다. `relabel`·`redescribe`·`describe`·`nikh`·`precision`·`reigns`·
`merge_node` 가 적는다. `histgraph overrides --seed` 로 두 DB 의 옛 값을
되짚어 적었다 (원본 10,833칸 · 파생본 10,874칸). 테스트 16건 (`[편집 계층]`).
README "편집 계층" 절, CLAUDE.md §2.

## 2026-09-05 — 화면을 Obsidian 스타일로

사용자 요청: "우리 그래프 디자인을 obsidian 디자인 스타일로 변경해줘. 그리고
design.md 파일을 만들어서 디자인 내용을 기록해줘."

[design.md](design.md) 를 다시 썼다 (09-04 판의 캔버스·연표·상세·상호작용 절은
그대로 두고 값과 Obsidian 절을 얹었다). 바뀐 것:

- 토큰을 Obsidian 이름·값으로 (`--background-primary #1e1e1e`, 강조 보라
  `hsl(254 80% 68%)`). `web/style.css`·`web/doc.css`·`pages.py STYLE`·
  `index.html` noscript 네 벌이 같은 값.
- 캔버스: 선은 회색 한 가지, 가리킨 노드의 선만 강조색. 노드 팔레트를
  Obsidian 색으로 다시 골랐고 `check_palette.py` 정상 10.9 · 2형 6.8 · 1형 5.5.
- 설정(시작점·범례·표시)은 왼쪽 사이드바에서 **캔버스 오른쪽 위에 뜨는
  상자**로 (Obsidian graph-controls). 기본 접힘. 노드·엣지 수는 상태 줄로.
- 상세 패널은 문서 한 장처럼 — 본문은 상자 없이, 관계는 카드 대신 줄.

테스트 `npm test` 48/48 (관계 수 검사는 칩 `.flair` 를 건너뛰게 고침),
`test_pipeline.py` 897/897. 아직 커밋 안 함.

같은 날 두 번째 요청: "obsidian-better-graph-view 를 참고해서 다시 반영".
그 플러그인은 테마가 아니라 Obsidian 그래프 내부를 후킹해 **관계 종류로
선을 거르는** 것이 핵심이다. 옮긴 것 — 설정 상자를 Obsidian 자리(왼쪽 위)로,
절을 Obsidian 그래프 설정과 같은 필터·범례·표시·힘으로. 필터에 관계 종류
체크 목록(`setEdgeFilter`), 표시에 화살촉·이름표 흐림 문턱·노드 크기·선 굵기
슬라이더(`setDisplay`, `labelAlpha`), 힘에 중심·반발·링크 거리 배율
(`layout.js` `forces`, 계수는 그대로고 곱만 한다). design.md §4·§5.

## 2026-09-05 — 연표는 조선 건국에서 시작한다

사용자 요청: "타임라인 시작을 항상 조선에서 시작해줘. 지금 조선 시작이 그냥
조선이라고 써있는데 '조선건국'이라고 명명해줘. 모든 타임라인에 나라 건국을
알려줄 때 항상 '건국'이라는 단어를 추가해줘."

- **축이 1097년부터 늘어져 있었다.** 조선 그래프에 고려 사건이 맥락으로 남아
  (1100년 '삼사', 위화도 회군 1388) 뼈대에 들어왔고, 왼쪽 띠엔 고려 공양왕이
  섰다. `server.timeline` 이 맨 앞 나라의 건국 해(`_polities()[0]`)를 바닥으로
  삼아 그 앞의 뼈대·이웃·재위를 뺀다 → 축 1389~. **고른 노드 자신이 건국보다
  앞서면**(태조 1335년생, 위화도 회군) 그 해까지만 연다.
- **나라는 첫 해에 '건국'을 단다.** 서버가 표식(`founded`)을 주고 화면
  `markName` 이 `조선건국`·`대한제국건국` 으로 적는다 (붙여 쓴다 — 사용자 표기) — 인물의 '탄생'과 같은
  꼴. 어느 노드가 나라인지는 `scope.Era.state`(일제강점기는 False) 와
  `successor_states`(조선 → 대한제국 Q28233) 가 정한다. 대한민국(Q884)은 장소로
  앉아 있고 날짜가 없어 서지 않는다 — 1948년은 '대한민국 정부 수립' 사건이 맡는다.
- 다음 세션이 확인할 것: 화면에서 대한제국 건국(1897)이 광무개혁과 같은 해에
  서는데 라벨이 겹치지 않는지 (한 해는 라벨 수만큼 늘어나므로 겹치지 않아야 한다).

## 2026-09-05 — 애드센스 '주의 필요' (`/n/` 장은 요약까지만)

애드센스 사이트 목록에 "Needs attention" 이 떴다
(도움말 12176698). 기계적 관문은 다 통과하고 있었다 — 광고 코드 세 장에
있음, `robots.txt` 전부 허용, `ads.txt` 200, 사이트맵 200. **문제는 콘텐츠였다.**

`/n/<id>` 장이 설명 칸을 통째로 뿌리고 있었다: 세조 25,093자 · 태조 18,787자,
`== 생애 ==` 위키 문법까지 그대로 (배포본에 1,077장). 로봇에게 그것은
남의 백과사전을 긁어 온 페이지다. 반대쪽 끝에서는 색인 6,810장 중 4,709장이
설명 300자 미만이고, '택견'·'배우'·'화가' 한 낱말짜리도 색인에 올라
있었다. 긁어 온 긴 글 1,000장 + 한 줄짜리 목록 5,000장 = 심사자가 본 사이트.

고친 것 (`pages.py`):

- **`summarize`** — 첫 절 제목 앞까지가 도입부다. `== 생애 ==` 도, 민족문화
  대백과·국편 글이 본문 사이에 세우는 마침표 없는 짧은 줄('출생과 성장')도
  제목이다. 문장 단위로 `SUMMARY_MAX`(360자)까지. 요약 중앙값 164자.
- **이 사이트의 말이 먼저** (`_lead`) — "조선 세종은 조선의 인물입니다. 자녀 14 ·
  소재로 다룸 12 · … 모두 57건과 이어져 있습니다." 원문이 아니라 관계망이
  아는 것. 그 뒤에 원문 요약, 그 뒤에 이어진 것 목록.
- **색인 문턱** (`indexable`) — 요약 120자 이상 **그리고** 관계 3건 이상.
  노드 장의 `noindex`·사이트맵·목록 장이 같은 함수를 본다. 6,810 → 2,300장.
- 상세 패널이 받는 전문(`api.node().description`)에서도 위키 문법을 걷는다
  (`plain_description`). DB 는 손대지 않았다 — 전문은 추출·말뭉치가 쓴다.

같은 날 두 번째 ("1,2번 모두 하자"):

- **상세 패널도 요약만 받는다.** `api.node().description` 이 `summarize` 를
  거친다. '전문 보기'·`LONG_DESC` 는 없앴다. 전문은 DB 에 그대로다.
- **출처 한 줄** — §1 의 유일한 예외 (사용자 결정). `provenance.desc_origin`
  이 판정하고 `/n/` 장(`.src`)과 상세 패널(`.d-desc-origin`)이 설명 바로
  아래에 적는다: `한국어 위키백과 문서를 줄인 글입니다 · 크리에이티브 커먼즈
  저작자표시-동일조건변경허락 4.0`. 이름은 문서로, 라이선스는 조문으로
  이어진다. 배포본 판정: 위키백과 5,244 · 국가유산청 960 · 우리역사넷 595 ·
  민백 168 · 위키데이터 27 · 나무위키 15 · **모름 1,368** (표식 없는 `wd`
  노드 — 위키백과 글처럼 보이지만 안 적는다. 틀린 출처보다 낫다).
- 국편이 설명을 덮어쓰면서 `desc_source: kowiki` 를 남기던 것을 고쳤다
  (`nikh` 가 제 표식을 찍고, 판정도 `canon: nikh` 가 먼저다). 배포본에 21건.

같은 날 세 번째 ("정본 말고 나무위키나 위키피디아에서 만든 글은 요약해서
보여주면 되잖아") — **`paraphrase`** (`summaries.py`):

- 정본(국편·민백·국가유산청·위키데이터)은 도입부 그대로. 위키백과·나무위키·
  출처 모름은 MLX 모델이 **우리 말로 새로 쓴 두세 문장**으로 바꾼다.
- 새 글은 `summaries` 표(node_id·text·model·src_hash)에 둔다. `description`
  은 안 건드린다 — 수집이 덮어써도 우리 글은 남고, 원문 해시가 달라지면
  저절로 무효(화면은 도입부로 물러남). `scope` 가 표를 같이 옮기고
  `--sync-to` 로도 옮긴다. 화면은 출처 줄에 '문서를 바탕으로 새로 쓴 글'.
- 받아들이는 기준(`accept`): 한국어·영어 없음·60~420자·원문과 30자 넘게
  겹치지 않음·'위키/문서/요약' 언급 없음·경어체 아님. 떨어지면 이유를 붙여
  한 번 다시 묻고, 그래도 떨어지면 저장 안 함.
- 4B 시험 모델 실측(6건): 새로 씀 2 · 떨어짐 4, 한 건 5초 안팎. 본 실행은
  기본 35B 모델로 — 파생본 후보 6,609건.

```
uv run histgraph paraphrase --scope data/korea.sqlite --sync-to data/korea.sqlite
# --scope 없이 원본 전체를 쓰면 후보 31,876건(며칠). 화면에 있는 6,609건부터.
```

남은 것:

- **`paraphrase` 본 실행이 아직이다.** 다른 세션의 `causes`(MLX 35GB)가 돌고
  있어 같이 못 띄운다. 끝나면 돌리고 파생본을 커밋·배포한다.
- 재심사 요청은 사람이 한다: 애드센스 → 사이트 → 검토 요청. 며칠~2·4주.
- **나무위키 글 15건은 비영리 조건(CC BY-NC-SA)이다.** 광고가 걸린 화면과
  어긋난다 — 출처는 적었지만 그것으로 풀리는 문제가 아니다. 뺄지 결정 필요.
- 출처 모름 1,368건은 `enrich --refresh` 로 다시 받으면 표식이 찍힌다
  (네트워크 필요, 한 번에 20건).

## 2026-09-04 — 그래프가 '왜'를 말하지 않았다 (`causes`·`chain` — 인과 사슬)

사용자 지적: "우리는 단순 그래프가 아니라 온톨로지 그래프이기 때문에 모든
노드들의 인과관계를 보여줘야 해. 'A 사건이 수백 년 뒤 B 사건에 어떻게
영향을 주었는가'를 그래프 탐색으로 추적할 수 있어야 한다 — 임진왜란 →
명나라의 쇠퇴 → 여진족의 성장 → 병자호란". 설계 근거는 README "그래프가
'왜'를 말하지 않았다", 규칙은 CLAUDE.md §2.

**한 것.**

1. 온톨로지에 `caused` 엣지 타입 (원인 → 결과, 라벨 = 원인·배경·계기·영향,
   `props.how`·`evidence`·`cause_as`/`effect_as`). Wikidata P828/P1542 는
   이제 `related_to '원인'` 이 아니라 `caused` 로 온다 (`links`), 옛 것은
   `causes.migrate` 가 옮겼다 (22건).
2. `src/histgraph/causes.py` — 말뭉치 사건 문서를 MLX 에 주고 인과를 뽑는다.
   양끝이 있는 노드로 풀려야 하고(노드를 만들지 않는다), 서술구는 주어로
   풀고(`heads`), 자국 왕조는 막고(`HOME_POLITIES`), 연대가 순방향이어야
   하고(`backwards`), 근거는 문장 단위로 원문에 있어야 한다.
3. 사슬 읽기 — `chain`(원인·결과 나무)·`paths`(최단 경로) · `/api/chain`·
   `/api/path` · `histgraph chain <이름> [--to <이름>]`.
4. 화면 — 상세 패널 맨 위 '인과 사슬'(`ChainPanel.jsx`)과 "…에서 어디까지
   이어졌나" 경로 찾기. 인과 카드에 종류 딱지와 '어떻게'. 캔버스의 인과
   선은 굵다. 범례에 한 줄.
5. `causes --sync-to data/korea.sqlite` 가 파생본으로 옮긴다 (양끝이 있는
   것만). `scope` 를 다시 밟지 않아도 된다.

첫 3건(임진왜란·병자호란·정묘호란) 실측 — 문서당 약 1분, 답 12건 중
엣지 1~4건. 버려지는 것의 대부분이 '조선의 여론'·'척화의지 고조' 같은
주어 없는 서술구 또는 자국 왕조라 **설계대로 버려진 것**이다.

```
uv run histgraph --db data/korea.sqlite chain 병자호란
병자호란 (1636)
원인:
  ← [원인] 정묘호란 (1627) — 정묘호란 이후 후금이 조선에 강압적인 태도를 강화하여 …
    ← [계기] 인조반정 (1623) — 인조반정으로 친명배금 정책이 추진되어 후금이 위협을 느끼고 …
    ← [계기] 이괄의 난 (1624) — 이괄의 난 잔당이 후금과 내통하며 침공을 주장하는 계기가 되었다.
결과:
  → [원인] 청나라 (1636) (청의 연호 사용 강요) — 병자호란 이후 청은 조선에게 청의 연호(숭덕) 사용을 강요했다.
```

**사건 문서 478건 결과 (2026-09-05 아침, 약 9시간).** 답 3,400여 건 중
인과 엣지 490건이 남았다. 버린 이유: 이름 못 풂 2,011 · 타입 안 맞음 568 ·
근거 없음 149 · 자기 자신 90 · 연대 역행 46. 못 푼 이름의 윗자리는
조선민주주의인민공화국·대한민국·북한(설계대로 막은 것)이고, 그 다음이
**노드 후보**다 — 과거제의 도입, 사스, 한일수교, 도널드 트럼프, 6·29
민주화 선언(`6.29 선언`이 있는데 표기가 달라 못 이었다 → 별칭), 민청학련
사건, 제1차 석유파동, 이란-이라크 전쟁. 화면 그래프 사건 488개 중 226개에
인과 엣지가 붙었다 (계기 121 · 원인 102 · 배경 93 · 영향 36).

돌고 나서 고친 것: **전투가 자기가 속한 전쟁의 원인으로 18건 적혔다**
(북관대첩 → 임진왜란, 한산도 전투 → 임진왜란). 같은 해라 연대로 못 잡는다.
상하위(`part_of`)가 있는 짝은 인과로 적지 않고(`part_of_each_other`), 이미
적힌 것은 `sync` 가 지운다(`prune_part_of`).

읽을 만한 사슬: `chain "3·1 운동"` — 세도정치·삼정문란 → 동학 농민 혁명 →
3·1 운동 → 문화정치·청산리 전투 → 자유시 참변. `chain 임진왜란` — 을묘왜변
→ 임진왜란 → 후금 → 심하전투, 기유약조, 금난전권.

**돌아가고 있는 것.** 단체·개념 문서 259건을 `uv run histgraph causes
--types org,concept --sync-to data/korea.sqlite` 로 돌리는 중이다
(2026-09-05 07:40 시작, 문서당 약 1분). 10분마다 화면 DB 로 옮기는 루프가
같이 돈다. **`임진왜란 --to 병자호란` 경로는 아직 없다** — 임진왜란 → 후금은
있는데 후금 → 정묘호란/병자호란이 없어서다. 후금·청나라 문서가 그 사슬을
말할 것이다. 로그는 세션 스크래치에 있고, DB 에는 문서마다 커밋된다.
**중단됐으면 같은 명령을 다시 돌리면 된다** — `props.causes_model` 이 없는
문서만 묻는다. 끝나면:

```
uv run histgraph causes --sync-only --sync-to data/korea.sqlite   # 화면 DB 로
python3 tools/check_korean.py                                      # 관문
git add data/korea.sqlite && git commit                            # 파생본은 배포된다
```

MLX 를 돌리기 전에 `ollama stop <모델>` 로 자리를 비웠다 (ollama 가
Qwen3.5-9B 를 6.8GB 물고 있었다). 이 실행이 도는 동안 다른 `extract`·
`roles` 는 띄우지 말 것.

**다음.**

- 단체·개념 문서가 다 돌고도 `임진왜란 --to 병자호란` 이 비면, 정묘호란
  문서를 `--redo` 로 다시 묻거나 후금 → 정묘호란을 손으로 본다.
- 못 푼 이름 목록(위)이 노드 후보다. '6·29 민주화 선언' 같은 표기 차이는
  별칭으로 푼다.
- `이순신 (이순신의 명량 해전 승리) →[원인] 임진왜란` 처럼 **참전자가 전쟁의
  원인으로** 적힌 것이 있다. 결과가 '정유재란의 대세'인데 정유재란이
  임진왜란의 별칭이라 그리 풀렸다. 서술구를 남겨 화면은 읽히지만, 참여자
  → 그 사건 인과를 걸러야 할지는 실측 뒤 정한다 (도요토미 히데요시 →
  임진왜란은 참이다).
- `dedupe` 가 노드를 합치면 인과 엣지도 옮겨지는지 확인 (`duplicates.py`
  는 엣지를 통째로 옮기므로 될 것이나 실측은 안 했다).

## 2026-09-05 — 사건 밖의 중복까지 (`dedupe --type`)

사용자: "더 많은 중복이 있으면 제거 해줘". 규칙은 CLAUDE.md §1-4, 설계
근거는 README "사건 밖 — 이름 말고 무엇이 같은가".

**사건처럼 손으로 판정할 수 없었다.** 같은 규칙을 인물에 돌리니 후보가
5,302쌍이었고 대부분 동명이인이었다. 그래서 이름 말고 **다른 증거**로
기계가 판정하게 했다 — 한자·생몰년·설명 첫 문장·소재지·표기 차이.
증거가 없는 것만 표로 넘긴다.

**만들면서 거짓 병합 넷을 잡았다.** 규칙을 처음 쓴 대로 뒀으면 1969년생
배드민턴 선수 '이 명희'가 1950년생 한진 회장 '이명희'와, 1984년생 음악가
'이 재현'이 1983년생 성우 '이재현'과 한 노드가 될 뻔했다. 사람 이름은
성과 이름을 띄기도 해서 그 공백이 표기 차이가 아니라 관례다 — **첫 마디가
한 글자면** 표기 차이로 보지 않게 고쳤다.

**손으로 적은 사건 56줄로 기계를 검증했다.** 기계가 판정한 31건이 전부
사람과 일치하고 어긋난 것이 0건이다. 처음엔 4건이 어긋났는데 원인이
자료였다 — 국편 연대기가 6·10 만세 운동을 1920년, 여수·순천 사건을
2021년(특별법 제정 연도)으로 적어 두었고, 임진왜란 노드에 '정유재란'이
한자 별칭으로 붙어 있었다. 그래서 **표기 차이를 연대보다 앞에** 두고,
한자는 `props.hanja`(정본)만 '다르다'의 근거로 쓴다.

```
전체 그래프  409쌍을 합쳤다 — 유물 308 · 연표 눈금 68 · 인물 24 · 직위 6 · 단체 2 · 개념 1
화면 그래프  47쌍 — 노드 10,020 → 9,973
판정 표      172줄 (합친다 43 · 다르다 129)
```

**유물이 가장 크게 갈렸다.** 국가유산은 지정 건마다 관리번호와 소재지를
갖는다. 관리번호가 같은 275쌍은 수집이 같은 건을 두 시도코드로 두 번 받아
온 것이고(한쪽은 빈 줄), 소재지가 같고 종목만 다른 19쌍은 보물에서 국보로
승격된 것이다. 반대로 이름도 한자도 같은 '동의보감' 셋은 소장처가 달라
**서로 다른 보물**이다 — 이름만 보고 합쳤으면 국보 셋이 하나가 됐다.

**장소는 중복이 아니라 이름 겹침이었다.** 후보 37쌍(same_as 로 이어 둔
30쌍 제외)이 하나도 중복이 아니다 — 대구·대전·인천의 '서구', 남북의
'강원도'. 인물도 화면 그래프의 59쌍 중 넷만 진짜 중복이었고(서재필·
이동녕·계봉우·이경선) 나머지는 동명이인이거나 후궁 봉작명이었다.

**다음.** 전체 그래프에 인물 1,712쌍이 '모름'으로 남아 있다. 연대가 없는
근현대 동명이인이라 자료가 더 오기 전에는 사람도 못 가른다 — 화면 그래프
(`data/korea.sqlite`)는 **모든 타입에서 모름 0**이다. 곁가지로 나온 것:
'전상우' 노드에 전두환의 설명이 붙어 있다(`enrich` 가 잘못 붙인 것),
'숙의 나씨'·'진주성 전투'는 동음이의 문서라 노드로 설 것이 아니다.

## 2026-09-04 — 한 사건이 두 노드로 서 있었다 (`dedupe` 관문)

사용자 지적: "사도세자 사건과, 임오화변은 같은거야. 둘중 하나로 통일
시켜야해. 이처럼 중복된 사건이 있는지 전수 조사해봐". 규칙은 CLAUDE.md
§1-4, 설계 근거는 README "같은 사건이 두 노드로".

원인은 소스마다 표제를 다르게 다는 것이다. 국편은 '사도세자 사건',
위키백과는 '임오화변'이고 '3·1운동'/'3·1 운동'처럼 띄어쓰기만 다른 것도
많았다. `nikh` 는 이름이 같은 노드를 찾아 정본을 씌우지만 띄어쓰기가
다르거나 이름이 아예 다르면 못 찾는다.

**한 것.**

1. `src/histgraph/duplicates.py` — 후보를 찾는 규칙 다섯(라벨·별칭·이칭·
   설명 첫 문장·핵심어)과 표 적용. 사건 1,636개에서 후보 55쌍이
   나왔고, 합치고 나서 별칭이 옮겨오며 하나가 더 나왔다.
2. `data/duplicates.tsv` — 사람이 적은 판정 56줄 (합친다 33 · 다르다 23).
   표에 없는 후보가 남으면 `histgraph dedupe` 가 종료 코드 1 로 묻는다.
3. 두 DB 에 적용. `ko_labels.tsv` 로 `wd:Q85985974` 를 '정주 전투'로
   고쳤다 (1950년 정주 전투가 '청주 전투'로 들어와 1592년 것과 겹쳤다).
   `wd:Q122846639`(임진왜란)의 끝 연도 1593 을 1598-12-16 로 고쳤다 —
   합쳐진 노드가 갖고 있던 값이다.

```
전체 그래프  사건 33쌍을 합쳤다 (사건 1,636 → 1,605)
화면 그래프  그 중 17쌍이 여기 있었다 (사건 504 → 488, 나머지는 범위 밖)
사도세자 사건  엣지 10 + 9 → 19 ('임오화변'·'임오옥'은 별칭으로 남는다)
임진왜란       엣지 137 + 17 → 152 (별칭 9개가 옮겨왔다)
```

(수치는 다른 세션이 동시에 쓰고 있어 조금씩 움직인다 — 합친 짝 수가
확실한 값이다.)

**합치지 않은 것 23쌍**이 절반 가까이였다 — 차수가 다른 것(제1차/제2차
왕자의 난), 이름만 같은 것(1592년 임진강 전투와 1951년 설마리 전투),
간지가 같은 것(을사사화/을사조약). 후보가 다른 오류를 가리키기도 했다:
`wd:Q15878954`(진주성 전투)는 사건이 아니라 **동음이의 문서**다.

**다음.** 사람·유물에도 같은 규칙을 돌리면 후보가 많다 (화면 그래프에서
인물 192쌍 · 유물 167쌍 · 장소 36쌍 · 연표 눈금 32쌍). 인물은 동명이인과
진짜 중복이 섞여 있어(`안견` wd/nikh 는 정본이 못 씌워진 중복으로 보이고
`숙원 이씨`는 다른 사람일 수 있다) 사건보다 판정이 까다롭다. 남은 것:
`조일동맹조약`/`조일맹약`(설명이 조일 잠정 합동조관과 뒤섞여 판정 보류),
`임진왜란`에 붙은 '정유재란' 별칭.

## 2026-09-04 — 설명 없는 노드를 화면에서 뺐다 (`describe` · `scope` 관문)

사용자가 연표에서 **'백성들이 종이를 바치는 일의 폐단에 대해 선혜청에서
아뢰다'** (실록 기사 제목, `sillok:woa_10004029_001`) 를 보고 말했다 —
"설명도 없어. 안 보여주는 게 더 좋을 것 같은데. 설명이 없는 노드는
삭제해서 타임라인에도 안 보이게 해줘. 만약 설명을 채울 수 있으면 채워".
규칙은 CLAUDE.md §1-3.

**먼저 잰 것이 중요했다.** 빈 설명이 3,297개였는데 그게 '자료가 없다'는
뜻이 아니었다:

| 어디서 | 빈 설명 | 채울 수 있던 것 |
|---|---|---|
| `wd:` | 573 | **415 (72%)** — 한국어 위키백과 문서 402 · Wikidata 한 줄 설명 220 |
| `ex:` | 1,351 | 민족문화대백과 정의 한 문장 304 |
| `time:` · `kr:period` | 1,195 | 뼈대라 채울 것이 없다 |

원인은 `enrich` 를 파생 그래프 범위로 한 번도 안 돌린 것이었다 —
`props.no_kowiki` 표식이 44개뿐인 게 증거다. 나머지 529개는 위키백과에
물어본 적조차 없었다.

**한 것.**

1. `enrich --scope data/korea.sqlite` — 노드 2,183개의 설명을 채웠다.
2. `histgraph describe` (새 명령) — 민족문화대백과 항목 CSV 의 '정의 한
   문장'으로 435개를 채웠다 (화면 그래프에 남은 것 188). 네트워크 불필요. `aks.DESC_KIND_TO_TYPES` 로
   유형 표를 여기서만 넓혔다 — 목민심서는 사전에서 '문헌/고서'고 영의정은
   '제도/관직'이라 `KIND_TO_TYPE` 으로는 통째로 빠졌다.
3. `histgraph promote` — 고아 243개를 실제 노드로 올렸다. 자유시
   참변·2·8 독립 선언·중일 전쟁이 이 길로 살아났다. `간도참변`·`종로경찰서
   폭탄투척 사건`은 제대로 된 `wd:` 노드가 이미 있어서, 이름만 다른 고아가
   빠진 것이 오히려 정리다.
4. `scope` 가 파생본을 만들 때 설명이 빈 **내용 노드**를 뺀다
   (`UNDESCRIBED_DROP_TYPES`). `redescribe` 가 설명을 비운 뒤를 위해
   `sweep_undescribed` 로 한 번 더 빗질한다.

**뺀 것과 안 뺀 것.** 사건·인물·단체·유물·작품·개념은 이름만으로 무엇인지
모르니 뺀다. 장소·직위·연표 눈금은 **이름이 곧 뜻이라** 남긴다 — '서울
종로구'에 해설을 붙일 일이 없는데 빼면 그 구에 있는 유물 86건이 '어디
있는지'를 잃는다.

결과: 화면 그래프 11,052 → 10,033 노드. 사건 785 → 504 (빠진 것은 대부분
실록 기사 제목과 `ex:` 중복이다). 빈 설명은 뼈대(연표 눈금 1,205 ·
장소 214 · 직위 209)만 남았다.

### 그 다음: "채울 수 없다는 거야?" — 아니었다, 버그 셋이었다

첫 보고에서 "진짜인데 빠진 사건이 대여섯 있다 (조미수호통상조약 · 단종
복위 운동 · 국공 내전 · 조선 건국 · 경복궁 점령 사건) — 채울 자료가 없다"
고 적었는데, 사용자가 되물어서 하나씩 캐 보니 **셋은 우리 코드가 막고
있었다.** 셋 다 고쳤다.

1. **Wikidata 의 사건은 뿌리가 둘이다.** `promote.CATEGORY_TO_TYPE` 에
   `Q1656682`(event) 만 있었는데, 국공 내전(P31=내전 Q8465) · 조미 수호
   통상 조약(P31=조약 Q131569) · 자유시 참변(P31=학살 Q3199915) 은
   **`Q1190554`(사건, 일어난 일)** 로 올라간다. 그래서 문서가 멀쩡히
   있는데도 '클래스 확인 실패'로 떨어졌다 — 승격 보고의 `unverified`
   180건이 이것이다. `Q1190554` 를 표에 넣자 40건이 더 붙었다 (간도
   참변 · 제물포 조약 · 조일통상장정 · 태평천국의 난 · 이봉창 의거 ·
   일장기 말소 사건 · 한일 병합 조약 …).
2. **사건의 넘겨주기를 통째로 막고 있었다.** 막는 이유는 옳다 —
   '곽산 학살 사건' → '3·1 운동' 같은 흡수를 피한다. 하지만 띄어쓰기만
   다른 것은 흡수가 아니라 표기 차이다. `fetch_qids(spacing_only=True)`
   로 **띄어쓰기를 뗐을 때 이름이 같은 넘겨주기만** 따라간다.
   '조미수호통상조약' → '조미 수호 통상 조약' 은 통과하고 '단종 복위
   운동' → '세조찬위' 는 그대로 걸러진다.
3. **`local_matches` 가 라벨을 글자 그대로 비교했다.** `ex:event:단종
   복위운동` 과 `wd:Q70585589 단종 복위 운동` 이 남남으로 남았고, 그
   둘이 같은 이름으로 서 있으니 `describe` 도 '같은 이름의 노드가 둘'
   이라며 건너뛰었다 — 사전에 정의가 멀쩡히 있는데 설명 없이 지워진
   것이다. `label_nospace` 단계를 넣어 9건을 합쳤다 (기묘 사화 ·
   제 1차 왕자의 난 · 백제 부흥 운동 · 통일 신라시대 …).

덤으로 `repair_facts` 가 **두 번째 실행에서 죽는** 결함도 나왔다 — 옮겨
갈 자리에 같은 엣지가 이미 있으면 `UNIQUE constraint failed` 다. 한 번만
돌리는 동안은 안 보였다. 이제 부딪히면 옮기는 대신 지운다 (관계는 이미
제 사람에게 붙어 있다).

**정말로 채울 자료가 없는 것.** 조선 건국 · 경복궁 점령 사건 — 한국어
위키백과에도 민족문화대백과에도 항목이 없다. `ex:` 고아 2,251개 중
1,805개가 이 쪽이다 (이름 조각).

**남은 것.**

- `promote` 의 마지막 단계(승격 QID 의 Wikidata 관계 보강)가 **다른 세션의
  `histgraph roles` 와 잠금 충돌로 죽어서 아직 안 돌았다.** 승격 자체는
  커밋됐다. DB 가 한가할 때 `uv run histgraph promote` 를 한 번 더 돌리면
  된다 (그 세션이 roles.py 를 판정마다 커밋하도록 고쳐 뒀으므로 이제
  덜 부딪힌다).

## 2026-09-04 — "뜬금없이 어떤 문장을 보여주고 있는데" (`nikh` 실록 참여 관문)

사용자 지적: '규장각에서 고사 절목을 올리다' 상세의 '참여 · 14' 머리에
"정조 사후 김조순·김재찬·서유구가 19세기의 주도적 인물로 성장하였다"
같은 문장이 서 있었다. 설계 근거는 README "참여자는 이름(漢字) 표기로".

- 문장은 민족문화대백과 「규장각」 항목의 본문이다. `nikh` 가 단체·유물
  항목의 본문 언급(`이름(漢字)`)을, 그 항목이 닿은 실록 기사 사건에
  **살아 있기만 하면** 전부 참여로 이었다. 화면은 같은 근거를 둘 이상이
  나누면 묶음 머리로 올리므로 이름보다 문장이 먼저 보였다.
- 이제 실록 기사에는 **그 기사의 인명 색인·원문에 한자가 있는 사람만**
  잇는다 (`SillokIndex.article_people` · `named_in_article`). 관청·건물
  꼬리 한자는 사람이 아니다 (`NOT_A_PERSON`, 이문원(摛文院)). 다시 돌리면
  묵은 실록 참여 엣지를 지운다 (`drop_sillok_participation`).
- 실측: 실록 기사 참여 253 → 43건 (기사 26 → 18). 걸러낸 205건. 파생본에서
  인물 17명이 빠졌다 — 그 엣지 하나로만 범위에 들어와 있던 사람들이다
  (서명응·정초·탁신…). 서명응처럼 규장각 자체와 얽힌 사람은 항목 노드
  쪽 관계로 다시 들어와야 한다 — 아직 안 했다.
- 남은 것: 실록 기사 노드의 **설명**은 아직 항목 요약을 복사한 것이다
  ('규장각' 설명이 절목 기사의 설명). 화면은 '문서에서 넘겨받은 글'로
  표시한다.

## 2026-09-04 — "왕 이름이 겹친다" (`homonyms`)

조선 연표에 **'여진 정벌'(1107)** 이 서 있는 것을 보고 사용자가 물었다 —
"여진 정벌은 고려때 일이야 왜 조선과 연결된지 모르겠어. 아마 왕이름이
겹쳐서 그럴거야. 조선왕과 고려왕 이름이 같을때가 있는데 구분을 반드시
해야해". 규칙은 CLAUDE.md §1-2.

**날짜의 원인은 정본 자료 쪽이었다.** 연대기 kc_i304300 은 조선 항목인데
(본문이 태종~선조대의 파저강·모련위 정벌) '설명' 칸에 고려 예종의 1107년
정벌이 적혀 있다. 연도 짐작이 첫 후보를 그대로 써서 조선 사건이 1107년
자리에 섰다. 짐작한 해는 이제 시대 창 안에 들어야 하고(`nikh.era_window`),
시대 밖의 해만 말하는 **사건**의 설명 칸은 딴 항목의 것으로 보고 버린다
(`summary_is_alien` — 914 항목 중 하나뿐).

**이름의 피해는 더 컸다.** 조선 예종의 휘가 이황(李晄)이라 별칭에 '이황'이
있다. 퇴계 이황(1501~1570) 문서에서 나온 관계 22건이 전부 예종
(1450~1469)에게 가 있었다 — 32년 차이라 생몰 검사(여유 40년)를 통과하고
차수는 예종이 컸다. `extract.pick_candidate` 가 이제 **출처 문서 자신을
먼저 본다**.

새 것: `src/histgraph/homonyms.py` · `uv run histgraph homonyms` ·
`scope` 의 동명이인 관문. 짐작이 없는 하나(문서의 주인공이 남에게 간
엣지)만 고치고, 연대 충돌과 겹치는 별칭은 **세어서 보여만 준다** — 지우면
참인 것이 먼저 사라진다(양만춘의 생년이 Wikidata 에 700년, 프랑스에
프랑크 왕국의 연대).

지금 남은 것 (전부 보고만 되고 있다):

```
연대가 100년 넘게 어긋난 참여   전체 17건 · korea 5건
   프랑스(481~561) --참여--> 6.25 전쟁     프랑스 노드에 프랑크 왕국 연대
   정인석(1978~)·정성근(1955~) --참여--> 갑자사화(1504)   현대 동명이인
   안향(1243~1306) --참여--> 백운동서원 건립(1543)        추숭이지 참여가 아니다
다른 노드의 라벨이기도 한 별칭   전체 122건 · korea 51건
   '고려' 가 고구려(wd:Q28370)의 별칭이다 — 아직 안 고쳤다
```

**고려의 여진 정벌에는 노드가 없다.** 연대기에 항목이 없고, 윤관(~1111)의
참여 엣지는 이름이 같은 추출 고아가 조선 항목에 합쳐지며 딸려 온 것이라
지웠다. 윤관의 9성은 따로 세워야 한다.

## 2026-09-04 — 말뭉치에 정본이 들어왔다 (`corpus --source aks|nikh`)

말뭉치를 위키백과 하나로 채웠다고 하자 사용자가 "필요한 사이트의 문서를
다운로드 받고 싶으면 나에게 요청해"라 했고, 공공데이터포털에서 받은
한국학중앙연구원 CSV 둘을 `data/raw/` 에 두며 **"rag용도로 사용하고 저
문서들이 정본이야"**. 설계는 README "말뭉치의 정본" 절.

한 것:

- `corpus.py`: 문서 표 열쇠가 (노드, 소스). 옛 파일은 `open_corpus` 가
  열 때 옮긴다. `SOURCE_PRIORITY` aks → nikh → kowiki — `mentions`(역할
  판정이 쓰는 대조)는 정본 문단을 앞에, `search` 는 점수 같으면 정본을
  앞에. `has_doc(conn, nid, source)`, `drop_doc`, `build_nikh`(연대기 본문,
  네트워크 없음).
- `sources/aks.py`: 항목 CSV → 노드 잇기(정규화 이름·타입 일치 + 양쪽 유일,
  별칭 포함) → 문서 페이지를 절 단위로 읽어 `== 절 ==` 글로. 못 이은
  항목은 `aks:E00…`. 근현대 사건부터 받는다.
- `corpus --source all|aks|nikh|kowiki --kinds 사건` (기본 all).
- 시험 711/711 (다른 세션의 nikh 시험도 이제 같이 돈다).

실측 (2026-09-04, 받기 끝):

```
민족문화대백과  항목 75,352 · 노드에 이은 것 6,760 (인물 4,823 · 유물유적 1,043 · 지명 353 · 사건 195 …)
                받음 7,424 쪽 (빈 문서 0) · 그중 못 이은 고아 aks:E… 647 · 문단 28,887
                12.3 내란은 2025-08 색인에도 없다 (그 사건의 근거는 아직 위키백과)
한국사연대기    914 항목 전부 노드에 이어짐 (props.nikh_id) · 문단 6,168
말뭉치 전체     문서 8,821 · 문단 42,625 · 2,451만 자 (aks 7,424 · nikh 914 · kowiki 483)
roles --dry-run 후보 385 · 근거 문단 있음 302 · 없음 83 (위키백과만일 때 358 / 274 / 84)
```

받기는 이어받기가 된다 (`has_doc` + `data/cache`). 끊겼으면 그냥 다시:

```sh
uv run histgraph corpus --source aks        # 남은 쪽만
uv run histgraph corpus --source nikh       # 연대기 (몇 초)
uv run histgraph roles --dry-run            # 근거 문단이 있는 후보 수 — 정본이 들어오면 늘어야 한다
```

### 모델 판정 (2026-09-05, MLX Qwen3.6-35B-A3B-8bit)

```
후보 382 · 근거 문단 있음 300 · 없음 82
판정  가담 128 · 주도 109 · 근거 없음 86 · 대항 22 · 피해 17 · 수습 10 · 언급 8 · 표적 2
12.3 내란  윤석열·박안수·여인형 주도 / 김용현·곽종근·이진우 가담 / 이재명·한동훈·조국·우원식 대항
           정진팔·노상원·문상호·이준석·윤종오·용혜인은 근거 문단이 없어 '근거 없음'(related_to)
```

첫 판에서 '주도'가 셋 틀렸다 — 우원식이 12.3 내란을, 박정희가 6·3 항쟁을,
전두환이 6월 민주 항쟁을 '주도'. 사건의 표적이거나 맞선 사람을 '중심
인물'이라는 이유로 주도로 읽는다. 프롬프트에 규칙 5 를 더하고(항쟁이
반대한 정권은 표적, 계엄을 막은 사람은 대항, 정의 문장은 일으킨 쪽에게만
근거) `roles --redo-roles 주도` 로 109건만 다시 물었다: 우원식 → 대항,
전두환 → 표적. **박정희 / 6·3 항쟁은 두 번 물어도 '주도'** — 근거가
"박정희 정권의 한일협상에 반대하여 일으킨 운동" 인데도. 손으로 고치지
않고 남겼다 (규칙: 관계는 근거로만 적는다). 다음 프롬프트 손질 때 시험
사례로 쓸 것.

두 가지 더 배웠다:
- `roles.run` 이 끝에서 한 번 커밋했다 → 첫 판정부터 몇 시간 쓰기 잠금을
  쥐어 다른 세션의 `promote` 가 막힌다 (journal_mode delete). 판정마다
  커밋하도록 고쳤다.
- 인포박스 `props.side` 는 편이지 '일으킨 쪽/막은 쪽'이 아니다 — 6.25 의
  2편은 북한이다. 역할을 편 번호로 유도하지 말 것.

화면 DB 는 `scope korea` 로 다시 만들었고(노드 10,024) 한국어 관문 통과.
국편 출처 표시 의무는 README 와 `docs.source` 로 지고 화면에는 안 쓴다 —
사용자에게 물었고 답은 아직이다.

## 2026-09-04 — 세종 연표에 사건이 하나였다 → 국편 정본 (`nikh`)

세종 재위 1418~1450 의 연표에 사건이 삼포 개항 하나였다. 원인은 셋이고
전부 수집 쪽이다: Wikidata 가 조선 사건을 거의 모르고, 위키백과 시드
표는 난·전쟁·사화뿐이며, 추출 사건은 연도가 없다(248건 중 244건).

**결정: 국사편찬위원회 자료가 정본이다. 충돌하면 이쪽이 맞다.** 사용자가
공공데이터포털에서 받은 파일이 `data/raw/nikh/` 에 있다(저장소 밖, 815MB).
설계와 실측은 README "국사편찬위원회 정본" 절.

```
uv run histgraph nikh              # 처음엔 실록 색인(3분)부터 만든다. --dry-run 으로 미리 본다
uv run histgraph scope korea       # 파생본 → data/korea.sqlite
python3 tools/check_korean.py
```

결과: 연대기 항목 965 → 기존 노드 473 에 씌움 · 새 노드 441 · 참여 엣지
1,489. 세종 재위 사건 1 → 17, 세종의 사건 엣지 0 → 16, 파생본 사건 522 → 782.
연도 정답표 60건 중 53건.

다음에 할 일:
- **연대기 연도 7건이 틀린다** (4군 6진 개척 1428/1433, 대동법 1624/1608,
  자격루 1469/1434 …). `props.date_uncertain` 이 달린 것은 문장 규칙의
  짐작이다. 연대기 웹 페이지에는 연도 줄이 있는데 표에는 없다 — 국편에
  표 갱신을 청하거나, 실록 검색을 더 조인다.
- **실록 관직 CSV** (336,255건) 는 안 썼다. 인물↔직위↔날짜 엣지를 추출
  없이 준다. 인물 CSV 115,859명도 연대기 인물 33명의 생몰년에만 썼다.
- **실록 색인은 RAG 저장소다** (`data/raw/nikh/sillok.sqlite`, 기사 413,771
  건, 본문은 한문). 설명 없는 노드에 기사 제목 목록을 붙이는 경로가 없다.
- 이름이 여럿에 걸려 새 노드로 세운 33건(허준·주희·이황·영조…)은
  동명이인 노드와 나란히 있다. 화면에서 둘로 보인다.
- 출처 표시. 국편 저작권 정책은 "학술·연구 목적 + 출처 표시"를 요구한다.
  CLAUDE.md §1 은 항목마다 출처를 안 쓴다 — 소개 페이지 한 곳에 밝히는
  절충이 필요하다. `props.nikh_url`·`props.sillok_id` 로 근거는 남아 있다.

## 2026-09-04 — 작품 설명이 비어 있었다 (`enrich` 순서, 그리고 `namu`)

화면에서 '퐁당퐁당 Love' 를 열었더니 설명이 "아직 서사를 받아오지
않았습니다" 였다. 작품 660편 중 565편이 그랬다.

**원인은 실행 순서다.** `works` 는 제목·분류·QID 만 저장하고 본문은
`enrich` 가 채우는데, 실행 목록에서 `enrich` 가 `works` 보다 앞에 있어
작품이 생기기 전에 서사 수집이 끝났다. 위 156행의 `enrich --types media
--full` 은 적혀만 있고 실행되지 않았다. 돌리니 605편 중 604편이 채워졌다
(한국어 위키백과 문서 없음 1편).

**그래도 토막글이 남는다.** 604편 중 125편이 100자 미만이다 — '《태조
왕건》은 영화이다.' 가 전부인 문서. 그래서 나무위키 '개요'+'줄거리' 절을
받는 `sources/namu.py` 와 `histgraph namu` 명령을 만들었다. 결과: 127편
중 70편 채움, 57편은 나무위키에도 없다(1960~80년대 영화·드라마).

나무위키는 API 가 없고 제목 규칙이 다르다. `/w/제목` 은 서버가
렌더링해 주므로 curl 로 읽히고, robots.txt 도 `/w/`·`/Search` 를 허용한다.
Wikidata 의 나무위키 ID(P8885)는 토막글 126편 중 2편뿐이라 못 쓴다.
제목 후보(괄호 붙이기·갈래·연도)를 열어 보고, 다 없으면 제목 검색에서
같은 제목·작품 분류인 것을 고른다.

**문서가 열렸다고 그 작품이 아니다** — 첫 실행 76건 중 8건이 틀렸다:

- `궁녀 (1972년 영화)` 가 2007년 영화를 받았다. 날짜 칸이 비어 연도
  검사가 꺼진 것. **라벨 괄호의 해가 날짜 칸보다 먼저다** (`year_for`).
- `옥녀` 가 드라마 옥중화의 등장인물 문서를 받았다. 분류에 '드라마' 가
  있으면 작품으로 봤다. 등장인물·캐릭터·출생·사망 분류는 거른다.
- `창`(영화) 이 `창(만화)` 를 받았다. 만화 문서인데 분류에 '한국의 범죄
  영화' 가 있었다. **제목 괄호의 갈래를 분류보다 먼저 본다** (`paren_fits`).
- `대원군 (1972년 드라마)` 가 '자세한 내용은 대원군(1966) 문서를 참고'
  한 줄을 받았다. '제목이 같은 드라마' 목록 문서는 **우리 해의 절만**
  받는다 (`year_section`). 틀 문구('이 문서에 스포일러가…')도 걷는다.
- `사미인곡(동음이의어)` 가 통과했다. 분류만 봤는데 제목에도 있었다.

`enrich --refresh` 가 나무위키로 채운 긴 글을 위키백과 토막글로 되돌리지
않도록, `desc_source` 에 `namu` 가 든 노드는 새 글이 더 길 때만 바꾼다.

라이선스: 나무위키 본문은 CC BY-NC-SA 2.0 KR. `props.desc_source` 에
`namu`(교체) 또는 `kowiki+namu`(토막글 뒤에 덧붙임)로 남긴다. 화면에는
출처를 쓰지 않는다 (CLAUDE.md §1).

실행 순서 (works 뒤):

```
uv run histgraph enrich --types media --full   # 위키백과 본문
uv run histgraph namu                          # 토막글(100자 미만)만 나무위키로
uv run histgraph scope korea
```

## 2026-09-04 — "이재명은 12.3 내란에 참여했다" (말뭉치와 역할 판정)

대통령 띠를 세우고 나서 바로 지적받았다. 이재명이 12.3 내란의 참여자로
떴다 — 체포 명단에 오른 사람이다. 출처는 인포박스 `주요인물2`. 인포박스는
양편을 1·2 로 가르는데 파서가 편을 버리고 둘 다 participated_in 으로
만들었다. 편의 뜻(누가 계엄을 폈고 누가 막았나)은 구조 어디에도 없고
산문에만 있다. 그래서 "근현대사 문서를 내려받아 RAG 로 쓰자".

한 것:

- `infobox`: 칸 이름을 엣지 라벨로(지휘관·주요 인물·교전·가해), 편 번호를
  `props.side` 로. 화면은 라벨대로 읽는다("…의 주요 인물이다"). 12.3 내란
  16명이 이제 1편 9명(윤석열·김용현·…)과 2편 7명(우원식·이재명·…)으로
  갈려 있다 — 다만 어느 편이 무엇을 했는지는 아직 아니다.
- `corpus.py` — 말뭉치 저장·검색층. `data/corpus.sqlite`(gitignore),
  문단 단위, FTS5 trigram. `corpus`(내려받기)·`ask`(검색) 명령.
  1945년 뒤 사건 508 + 이웃 인물·단체 291 + 시드 26 = 825 노드가 대상.
- `roles.py` — 사건 참여 엣지마다 그 사람이 나오는 문단만 모아 역할을
  묻는다(주도·가담·대항 / 피해·표적·수습·언급). 근거 인용이 문단에 있어야
  받고, 참여가 아니면 related_to 로 옮긴다(`props.was`). `roles` 명령,
  `--dry-run` 은 모델 없이 근거 문단 유무만 센다.
- 화면 `relations.js`: 역할·칸 라벨을 문장으로 ("…에 맞섰다", "…에서
  표적이 되었다", "…의 주요 인물이다"). 서버가 `role_evidence` 를 근거로
  넘긴다.

결과 (2026-09-04):

```
말뭉치      문서 477건 · 문단 7,591개 · 434만 자 (대상 825 중 위키백과 문서가 없는 264는 못 받음)
ask "체포 명단 이재명"  → 이재명 문서 '윤석열 정부 비상계엄 정국' 문단 (담을 넘어 … 계엄 해제 요구 안건을 통과시켰다)
roles --dry-run          후보 358건 · 근거 문단 있음 274 · 없음 84
```

trigram 색인을 먼저 썼다가 물렸다 — 세 글자 미만은 못 찾는데 '체포'·
'명단'·'내란'이 다 두 글자다. unicode61 + 앞머리 일치(`"체포"*`)로 바꿨고,
옛 파일은 `open_corpus` 가 열 때 알아서 다시 짓는다.

**모델 판정은 아직 안 돌렸다.** MLX 를 다른 세션의 `extract` 가 잡고
있어서(35GB, 둘 띄우면 커널이 죽인다) `roles --dry-run` 까지만 했다.
MLX 가 비면:

```sh
uv run histgraph roles              # 후보 전부 (1945년 뒤)
uv run histgraph scope korea        # 화면 DB 로
python3 tools/check_korean.py
```

남은 것:

- 동명이인 오염 하나 더: 이광식 → 이재명 `related_to`(extract) 는 족보의
  다른 이재명이다. `promote` 의 별칭 해소가 근현대 인물의 흔한 이름을
  옛 인물에 붙인다 — 연대 관문을 related_to 에도 걸어야 한다.
- 말뭉치 소스는 지금 위키백과 하나다. 국편 한국사연대기·민족문화대백과는
  활용신청/로그인 뒤 파일로 받는 길이라(`histgraph-nikh-data-route`)
  `corpus.put_doc` 에 source 를 달리해 넣으면 된다.

## 2026-09-04 — 대통령이 왕의 띠에 섰다, 1945년 뒤가 사건을 얻었다

"이승만부터 이재명까지의 대통령 라인을 조선 왕 타임라인과 같은 줄에".
왕이 하던 일 — 사람이 시간을 읽는 눈금 — 을 1948년 뒤에는 대통령이 한다.

한 것:

- `reigns` 가 대통령 자리도 찾는다 (`wikidata.fetch_president_positions`).
  계층(P279* 대통령) + 관할(P1001 대한민국) + 권한대행 계열 제외. 표식
  `props.reign` 의 값이 `monarch`/`president` 가 됐다 (예전 `true` 는 군주).
- 황교안은 Wikidata 에 대행 표식 없이 대통령으로 적혀 있다. 이름을 박지
  않고 **남의 임기 안에서 시작하는 임기**를 뺀다(`drop_nested_terms`).
  대통령 자리에만 건다 — 군주 자리에 걸었더니 우왕·진덕여왕이 빠졌다.
- `server._reigns` 가 `kind` 와 `ongoing` 을 넘긴다. 재임 중(이재명)은
  오늘까지 긋는다. 화면(`timeline.js`)은 말만 가른다: 재위/재임, 칩은
  '왕 30 · 대통령 14'.
- `scope` 에 대한민국 시대(`daehan`). 국적으로 인물을 고르지 않고
  (`seed_by_polity=False`) 사건과 대통령 자리(`seed_positions`)에서만
  씨앗을 잡는다. `korea` 묶음이 조선~대한민국이 됐다.
- 시드 표에 대한민국 사건 90 · 단체 21 · 개념 8 — 문서명은 전부 조회로
  확인했다 (넘겨주기가 엉뚱한 데로 가는 것은 뺐다: '8·15 광복'→광복절,
  '김구 암살 사건'→국제공산당 자금사건). `spans --types event` 로 연대
  58건, `infobox --types event` 로 나머지.

결과:

```
korea.sqlite   노드 9,717 → 10,444 · 엣지 20,267 → 23,484
1945년 뒤 사건   12 → 106개 (연대 있음 · 흥남 철수 작전·6.29 선언은 어디에도 날짜가 없다)
띠             왕 30 · 대통령 14 (이승만 1948 ~ 이재명 2025~)
한글 아닌 글    3 → 0 (유엔군 공세·북진·철수, zh 라벨로 의역)
테스트         파이썬 635/635 · 화면 전부 통과
```

남은 것:

- 개념 씨앗은 엣지가 없어 `scope` 의 고립 정리에서 빠진다 (파생본의
  concept 4개). 개념은 `about`/`extract` 가 엣지를 붙여야 화면에 온다.
- 대한민국 정체 노드 Q884 가 장소라 이 시대는 `from_period` 가 없다.
  제1~6공화국(Q491559 …)을 시대 노드로 세우면 연표의 시대 눈금이 생긴다.
- 1945년 뒤 인물↔사건 엣지는 아직 인포박스 것뿐이다. `extract --types
  event` 가 이 90건을 지나야 참여 엣지가 붙는다.

## 2026-09-04 — 화면에 영어가 세 번째로 떴다 (한국어 관문)

`1923 Jogono Police Station bombing` 이 사건 이름으로 떴다. 09-03 에 두 번
지적받고 `relabel`·`redescribe` 를 만들어 둔 뒤였다. 표는 있었고, 화면
DB(`korea.sqlite`)에 안 돌렸을 뿐이다 — 세 번 다 같은 모양의 실패라
"돌려라"를 문서에 한 줄 더 적는 것으로는 안 된다고 판단했다.

한 것:

- `labels.foreign_text` — 라벨·설명에 한글이 한 자도 없는 노드를 센다.
  이 함수 하나가 관문이다.
- `scope` 가 파생본을 만들자마자 표와 사전을 돌리고, 남으면 **종료 코드 1**.
- `tools/check_korean.py` — 배포 진입점이 여는 DB 를 센다. `pre-push`
  훅과 CI(`test.yml`) 가 부른다. 파일 이름을 박지 않고 진입점에서 읽는다.
- 화면 DB 에 남아 있던 19개를 표에 적었다 (근거: ja/ru 라벨, kowiki 문서명,
  로마자 음역). 그중 `Q16435447` 은 라벨이 아예 없는 항목인데
  1880~1959 크질오르다 사망한 작가라 계봉우로 적었다 — 기존 계봉우
  노드(`Q12583989`)와 중복 의심, `relabel` 이 보고했다.
- 규칙은 [CLAUDE.md](CLAUDE.md) §1 에 옮겨 적었다. 세션이 바뀌어도 먼저 읽는다.

결과: `korea.sqlite` 한글 아닌 글 19 → **0** · 테스트 602/602.

남은 것: `joseon.sqlite` 에는 42개가 그대로다 — 전부 교황 요한 바오로 2세
주변(추기경 회의·사도 순방)이라 이름 문제가 아니라 **범위 문제**다.
누가 조선 그래프에 교황을 끌어들였는지 `scope` 의 씨앗 확장을 봐야 한다.
화면은 안 읽는 파일이라 이번에는 두었다.

## 2026-09-04 — 인포박스는 소재를 적지 않는다 (2단계 앞부분)

"산문 추출보다 인포박스가 빠르지 않나"에서 시작해 열어 봤다. 작품 틀에는
**소재·배경 칸이 없다.** 대신 발표일과 원작을 준다. 소재는 도입부의
위키링크에 있고, 링크는 QID 로 곧바로 풀리므로 LLM 없이 잇을 수 있다.

한 것:

- `works.harvest` — 작품 문서에서 발표일·원작·소재를 읽는다 (`works` 가
  기본으로 실행, `--no-harvest` 로 끔). LLM 을 쓰지 않는다.
- 소재 판정 관문 셋: 단서 어구 → 만든 사람 걸러내기 → 몰년.
  관문 없이 받으면 인트로 링크 인물의 절반 이상이 감독·배우다.
- 장소는 `depicts` 가 아니라 `set_in` 으로 잇는다.
- `adapted_from`(원작) 엣지 타입 추가.
- `enrich` 가 작품 산문에서 '등장 인물'·시청률·각주 절을 걷어낸다
  (`wikipedia.strip_sections`). '역사적 사실'은 남긴다.
- `enrich` 대상 선정을 `source='wd'` 에서 `id LIKE 'wd:%'` 로 바꿨다 —
  `works` 가 만든 작품은 id 가 `wd:` 인데 소스가 `kowiki` 라 통째로 빠졌다.
- `reclassify.repair_edges` — 노드를 안 바꿔도 엣지는 어긋날 수 있다.
  재수집이 새 `about` 을 이미 사건인 노드에 걸어 넣는다(실측 11건).

결과:

```
작품 문서 658건을 읽어
  발표일   109 → 382편
  원작      0 → 12건
  소재    184 → 284건
  배경    327 → 345건
표본 30건 정밀도 30/30 · 어긋난 엣지 13 → 2 · 테스트 568/568
```

**본문 전체까지 넓혀도 한 건도 안 늘었다** (작품 80편 기준 도입부 37개,
본문 전체 37개). 단서 어구는 도입부에만 있고, 줄거리는 관계를 단서 없이
적는다. **규칙으로 캘 수 있는 것은 여기까지다.**

2단계 판정 현황:

| 기준 | 지금 |
|---|---|
| 작품 중 사실층 엣지 보유 ≥ 70% | **63%** (418/660) |
| `depicts` 총계 ≥ 600 | **284** |
| 표본 30건 정밀도 ≥ 0.9 | **30/30 ✓** |

남은 두 기준은 산문 추출(MLX)이 있어야 한다. **작업 시점에 다른 세션이
`extract` 로 MLX 를 잡고 있어서 돌리지 못했다.** 순서는 이렇다:

```sh
uv run histgraph enrich --types media --full   # 작품 산문 (등장인물 절은 자동으로 걷힌다)
uv run histgraph extract --types media         # MLX 가 비어 있을 때
uv run histgraph promote --local-only
```

## 2026-09-04 — 작품 명단은 분류에 있었다 (`works`)

Wikidata 는 작품에 관계를 주지 않는다. 위키백과 분류에서 긁어 온 592편에
대해 P840(서사 장소) 0건, P144(원작) 0건, P921(주제) 3건, P136(장르) 5건.
**같은 작품들의 분류 이름이 관계를 그대로 말한다** — '이순신을 소재로 한',
'조선을 배경으로 한', '조선 역사 드라마'.

한 것:

- `sources/works.py` — 뿌리 분류 하나(`분류:한국의 역사를 소재로 한 작품`)를
  훑어 작품 노드와 `depicts`·`set_in` 엣지를 만든다. LLM 을 쓰지 않는다.
- `histgraph works` 명령 (`--dry-run` 있음).
- `ontology.FORMS` — 매체 구분. `media` 노드는 `props.form` 없이 만들어지지
  않는다 (`Node.__post_init__` 이 막는다).
- `fetch_media` 도 form 을 채운다. 이미 있던 작품 79개는 P31 로 메웠다.
- `set_in` 도착에 `org` 허용 (from_period 와 같은 이유 — 시대 구분은 왕조다).

결과:

```
분류 115개 · 문서 599건 (QID 98%)
작품 노드 96 → 638개  — 영화 303 · 드라마 298 · 책 31 · 무대 3 · 게임 3
매체 구분(form)  100%
소재(depicts)   21 → 183건 · 작품 152편
배경(set_in)     0 → 327건 · 작품 302편
테스트 492/492
```

**1단계 판정 기준 중 하나는 못 채웠다.** `media` ≥ 300 (638 ✓),
`form` 100% (✓), **`set_in` ≥ 80% (47% ✗)**. 80% 는 재기 전에 적어 둔
숫자였다. 배경을 말하는 분류가 나무의 절반쯤에만 있고, Wikidata 는 이
작품들에 아무것도 주지 않는다(P840 0건). 소재의 시대에서 배경을 유도해
봐도 22편밖에 늘지 않는다. **나머지는 2단계(산문)에서 와야 한다** —
문서 인트로가 "고구려 말기부터 발해 건국까지"라고 말해 준다.

이어서 볼 것:

- **못 이은 이름 9개** — '임진왜란'은 노드가 둘이다(`wd:Q122846639` 차수
  102, `wd:Q576338` 차수 14). 병합하면 작품 22편이 소재를 얻는다.
  '홍길동'·'광개토대왕'은 그래프에 아예 없다.
- **매체를 못 읽어 만들지 않은 40건** — 대부분 작품이 아니지만(사람·국보·
  병풍·동상), '계축일기'·'칼의 노래'·'만화 허준 동의보감'처럼 진짜 작품도
  섞여 있다. 제목 전체에서 매체어를 읽으면 잡히지만 오탐이 늘어난다.
- **`from_period` 가 어긋난 엣지 68건** — 일제강점기(`wd:Q503585`)와
  고려(`wd:Q28208`)가 `place` 로 앉아 있는데 `from_period` 의 도착은
  시대·정체만 허용한다. 이 노드들은 다른 세션이 만지고 있던 것이라
  건드리지 않았다. `reclassify` 의 사다리를 시대 쪽으로 늘리면 잡힌다.

## 2026-09-04 — 개념이 사건 행세를 하고 있었다 (`reclassify`)

작품이 역사에 붙어 있는 것처럼 보였다. `depicts` 157건. **그중 실제 역사
사건은 6.25 전쟁 6건뿐이었고**, 나머지는 '조직범죄'(20)·'보복'(11)·
'연쇄살인범'(7)·'자살'(5) 같은 주제어가 **사건 노드로 앉아 있던 것**이다.
원인은 `fetch_media` 가 P921(주제)을 묻지 않고 사건으로 가정한 한 줄.

한 것:

- 온톨로지에 `concept`(개념·주제) 타입과 `about`(주제) 엣지. `depicts` 는
  실체(인물·사건·장소·단체)만 가리킨다. `set_in`(배경)도 타입만 세워 뒀다.
- `reclassify` 명령 — Wikidata 에 물어 사건과 개념을 가른다. 지우지 않고
  옮긴다. `--dry-run` 으로 계획만 볼 수 있다.
- `fetch_media` 가 인물 여부를 같은 질의에서 함께 묻고, 판정이 안 서면
  **개념으로** 떨어뜨린다 (사건이 아니라).
- `server.TYPE_GROUP` 에 `concept` → `frame`. 색은 늘리지 않았다.

결과:

```
노드 77개 재분류 (개념 71 · 인물 4 · 단체 1 · 시대 1) · 보류 11
depicts  157 → 21건  (전부 실체: 사건 13 · 인물 7 · 단체 1)
about      0 → 135건
어긋난 엣지 0건 · 테스트 474/474
```

인물로 간 넷은 이봉창·윤봉길·이춘재·김헌창이다. 사람이 사건 노드로 앉아
있었다.

판정 사다리와 그것을 만들며 틀린 것들(P279 단독 판정, 사건 계층 신뢰,
조직 뿌리, 매핑표 앞자리)은 README "개념이 사건 행세를 하고 있었다" 에
적어 뒀다. **되풀이하지 말 것.**

남은 보류 11건은 손대지 않았다. 특히:

- **정미의병** — Wikidata 가 클래스도 시점도 주지 않아 '보복'과 구별되지
  않는다. 사람이 판단해야 한다.
- **진주성 전투(wd:Q15878954)** — 실은 **위키미디어 동음이의어 문서**다.
  개념이 아니라 노드로 있으면 안 되는 것이라 지우는 판단을 넘겨 둔다.
- 임오화변·원격현장감 — Wikidata 에 P31 이 아예 없다.

**주의: `data/histgraph.sqlite` 를 파일 복사로 되돌리지 말 것.** 이 작업 중에
백업으로 되돌렸는데, 그때 다른 세션의 `extract` 가 같은 DB 에 쓰는 중이었고
그쪽이 02:27~02:36 사이에 쓴 배치 2건가량이 지워졌다. 그 문서들은 '이미
추출함' 표시도 함께 사라졌으므로 다음 `extract` 가 다시 처리한다(영구 손실은
아니다). 되돌릴 일이 있으면 파일이 아니라 SQL 로 할 것.

## 2026-09-03 — 설명이 빈 노드 (`enrich`)

화면의 `사량진왜변` 에 설명이 없었다. **위키백과에 문서가 멀쩡히 있었다.**
빈 칸의 원인은 자료가 아니라 우리 쪽 세 가지였다.

1. `--limit` 기본값이 500 이라 **차수 1짜리는 순번이 오지 않았다.**
   설명 있는 인물의 평균 차수 8.4, 없는 인물 2.1.
2. 대상 타입이 `person`·`event` 뿐이라 **장소·단체·직위는 한 번도
   조회된 적이 없다.**
3. `redirects=1` 인데 결과를 요청한 문서명으로만 찾아서, **넘겨주기가
   걸린 문서는 본문을 받아놓고 버렸다** (`신숙공주` → `순숙공주`).

```
조선 그래프 wd 노드의 빈 설명   2,796 →  27   (3,832개 중 99.3%)
전체 그래프 wd 노드의 빈 설명  29,532 → 826   (31,205개 중 97.4%)

설명 출처 (전체 wd 노드)   kowiki 25,820 · wd:en 2,222 · wd:ko 902 · 없음 2,261
   ※ wd:en 2,222건은 그 뒤 `redescribe` 로 한국어화(1,913) / 비움(309).
     아래 "영어로 뜨는 설명" 참고.
넘겨주기로 되찾은 것        45건 (desc_via 로 표시)
```

남은 826개 중 801개는 한국어 위키백과 문서도 Wikidata 한 줄 설명도 없다.
나머지 25개는 문서는 있는데 본문이 비어 있는 토막 문서다.

`--limit` 기본값을 없앴다(남은 전부). 타입 기본값은 wd 노드 전체
(`ALL_WD_TYPES`).

**못 채운 것을 못 채웠다고 적는다.**

- `props.no_kowiki` — 한국어 위키백과에 문서가 없는 노드. 다음 실행이
  헛수고를 반복하지 않는다. `--refresh` 는 이 표시도 무시한다.
- **쿼리가 죽은 구간은 표시하지 않는다.** sitelink 조회는 공개
  엔드포인트라 502·429 로 곧잘 죽는데, 그 구간까지 '문서 없음'으로
  못 박으면 타임아웃 한 번에 200개가 영구히 건너뛰어진다.
  `fetch_titles(unresolved=...)` 가 둘을 가른다. 전체 그래프 실행 중에
  실제로 502·429 가 났다 — 이 가드가 없었으면 조용히 망가졌을 자리다.
- `props.desc_source` — `kowiki` / `wd:ko` / `사전`(영어 한 줄을 옮긴 것).
- `props.desc_via` — 넘겨주기로 다른 문서에서 가져온 글이면 그 문서명.

**화면은 셋을 다르게 그린다** (`descHtml`·`whyEmpty` in `web/app.js`).
산문, `'의금부' 문서에서 넘겨받은 글입니다`, 그리고 빈 칸에는
`설명 없음 — <왜 비었는지>`. **자료 출처는 적지 않는다** — 예전에는
`Wikidata · 영어 한 줄 설명` 을 붙였는데 뺐다. 빈 칸을 그냥 두면 '원래
설명이 없는 노드'로 읽히는데, 실제로는 소스마다 이유가 다르다 —
`timeline` 은 연표의 해, `extract` 는 이름만 추출된 노드, `khs` 는
해설문이 없는 유산, `wd` 는 문서가 없거나 아직 안 받았거나.

**전체 그래프도 돌려 뒀다.** `--interval 0.4` 로는 SPARQL 이 429·502 로
자주 죽는다 (실제로 600개가 '조회 실패' 로 남아 한 번 더 돌려야 했다).
다음에는 **`--interval 1.5` 이상**을 쓸 것.

**넘겨주기에는 함정이 하나 더 있었다.** 넘겨주기로 온 글은 다른 개체의
문서다 — `무관랑` 의 설명은 `사다함` 문서이고, `이유` 는 `엠파이어 (음악
그룹)` 문서다. 화면에서는 출처를 밝히면 되지만 **추출은 다르다.** 그 글을
그 노드의 것으로 읽으면 사다함의 관계가 무관랑에게 붙는데, 근거 구절
검증으로는 못 막는다(구절은 원문에 실제로 있다). `load_documents` 가
`desc_via` 붙은 노드를 뺀다.

또 **정규화를 넘겨주기로 세면 안 된다** — 밑줄·공백만 손질된 것까지
세면 같은 문서를 두고 "다른 문서에서 넘겨받았다"고 적게 된다
(`김만기 (조선)` 1건이 그랬고, 데이터에서도 지웠다).
`extract`(970)·`timeline`(646)·`khs`(670) 노드의 빈 설명은 이 경로로는
안 채워진다 — 위키백과 sitelink 가 없는 노드들이다. `khs` 유물의 9.3%는
국가유산청이 `content` 를 빈 값으로 준다(API 응답 직접 확인). `same_as`
를 타고 빌려오는 길은 **일부러 막아 뒀다** — 상대가 같은 것을 가리키지
않는다(README 참조).

**덤으로 나온 결함 (아직 안 고침).** `resolve` 의 `place_label_exact` 가
구·시를 상위 시·도에 `same_as` 로 붙여 놨다. 조선 그래프 92건 중 15건:

```
서울 종로구 → 서울특별시 · 부산 서구 → 부산광역시 · 경기 광주시 → 경기도
```

`same_as` 가 아니라 `part_of` 다. 이번 작업에서는 손대지 않았다 —
설명 결손과는 별개의 문제이고, `resolve` 의 매칭 규칙을 건드려야 한다.

## 2026-09-03 — 고립 사건 전수조사

'왕자의 난'이 홀로 선 것이 한 건짜리 사고인지 확인하려고 조선 그래프의
사건 385개를 훑었다. **엣지가 하나도 없는 사건 19개 · 시간 엣지만 있는
사건 8개**(화면에 '연결된 관계가 없습니다'로 뜬다). 원인은 셋 다
'안 물어본 것'이었다.

1. 사건 쪽 속성 여섯 갈래 — 아래 `links` 절.
2. `props.polity` 만 있고 `from_period` 엣지가 없는 사건 198건(조선 105건).
   `resolve.link_event_periods` 가 세운다. 위키백과 사건은 엣지로 붙는데
   Wikidata 사건은 칸에만 있어서 화면에서 갈렸다.
3. `http._fetch` 의 재시도가 요청 본문을 오류 HTML 로 덮어쓰고 있었다
   (`body = err.read()`). WDQS 가 503 을 한 번 뱉으면 그 구간이 통째로
   비고 캐시에 그대로 남는다 — 사건 관계 98건이 그렇게 사라졌다가
   재실행에서 되살아났다. **POST 를 쓰는 모든 SPARQL 수집에 해당한다.**

지금 조선 그래프: 엣지 없는 사건 **0개**, 시간 엣지만 있는 사건 22개
(Wikidata 에도 관계가 없는 것들 — `extract` 가 할 일).

이름 가족(`제N차 X` ↔ `X`)에 남은 어긋남 4건은 전부 승격 안 된 추출
고아(`ex:제1차 요동 정벌` 등)다. 이을 일이 아니라 합칠 일.

## 2026-09-03 — 사건이 자기 쪽에 적어 둔 관계 (`links`)

연표에 '왕자의 난'·'제1차 왕자의 난'·'제2차 왕자의 난'이 나란히 섰는데
**셋 사이에 엣지가 하나도 없었다**('왕자의 난' 연결 0건). 수집이 물어본
사건 관계는 인물↔사건(P1344)뿐이라 사건끼리는 서로를 모른다.

- `histgraph links` — 여섯 갈래를 한 번에: P361/P527(상하위)→`part_of`,
  P710(참가자)→`participated_in`, P276(장소)→`occurred_at`,
  P155/P156(전후)·P828/P1542(인과)→`related_to`('다음'/'원인').
  전체 새 엣지 241건 · 조선 141건. 양쪽에 적힌 같은 사실은 한 방향으로
  모은다(하위→상위, 앞→뒤, 원인→결과).
- **옥포 해전에 이순신이 없었고, 원인·결과 엣지는 그래프에 한 건도
  없었다** (병인박해→병인양요, 제너럴셔먼호 사건→신미양요).
- 양끝 타입이 스키마에 안 맞는 46건은 버린다 — Wikidata 는 해전 참가자로
  '일본'(우리 그래프에선 place)을 적는다.
- 반대쪽 개체가 그래프에 없는 관계 1,496건은 **건너뛴다** — 여기서 노드를
  만들면 콘클라베 같은 무관한 사건이 딸려 들어온다. 필요하면 events·
  enrich 로 먼저 사건을 들여올 것.
- **임진왜란이 두 노드다.** Q122846639(1592~1593 침입)가 Q576338(1592~1598
  전쟁)의 일부라고 Wikidata 가 적어 두었는데 우리 라벨은 둘 다 '임진왜란'
  이다. 이름이 같은 두 노드는 잇지 않고 보고만 한다 — 합칠 일이다.
  (`relabel` 이 보고한 중복 24건과 같은 성격의 숙제)

## 2026-09-04 — 연표가 재위 띠와 어긋났다 (몰린 해 · 날짜 자릿수)

선조를 고르면 왼쪽 재위 띠와 오른쪽 연도가 통째로 어긋나 있었다. 인종
(1544~1545) 막대 옆에 1592년 전투들이, 효종(1649~1659) 막대 옆에 1597년
전투들이 섰다.

**원인은 둘이 다른 자를 쓴 것이다.** 띠는 `yOf(연도)` 로 제 해에 서고,
라벨은 겹침을 피해 밀린 자리에 섰다. 임진왜란 한 해에 사건이 38건이라
라벨이 1140px 를 먹고, 밀린 만큼이 그대로 어긋남이 됐다 (실측: 1545년
을사사화 -1064px, 1598년 -1738px — 그 눈금으로 각각 97년·73년치).

- **양보하는 쪽을 바꿨다** (`web/src/lib/timeline.js`, `buildScale`).
  라벨을 미는 대신 **그 해가 늘어난다**. 한 해가 내주는 높이는
  `max(비례몫, 그 해의 라벨 수 x GAP)` 이고, 축·점·재위 띠·라벨이 모두
  이 자 하나(`pos` 표)를 쓴다. 밀어낼 것이 없으니 어긋남은 0 이다.
  사건 없는 해는 그대로 비례하므로(중앙값 11px/년) 100년이 1년처럼 보이는
  일은 없다. 밀린 자리를 잇던 안내선은 걷어냈다 — 이을 것이 없다.
- **`yearAt` 은 이제 나누기가 아니라 표를 되짚는다** (눈금이 고르지 않다).
- **늘어난 해는 그 안의 차례가 시간 순으로 읽힌다.** 그래서 가나다순이던
  정렬을 날짜순으로 바꿨고(`sortMarks`), `/api/timeline` 이 마크마다
  `date` 를 함께 준다.
- 그러자 날짜가 거짓말인 것이 드러났다 → `histgraph precision` (아래).

## 2026-09-04 — 연도만 아는 날이 1월 1일 행세를 했다 (`precision`)

Wikidata 는 '1592년'을 `1592-01-01` 로 준다. 자릿수(`wikibase:timePrecision`)
는 값이 아니라 **문장**에 붙어 있어 `wdt:` 수집이 못 가져왔다. 전체
그래프에서 시작일이 1월 1일인 노드 7,117개.

- `histgraph precision` — `p:`/`psv:` 로 값 마디까지 내려가 자릿수를 읽고
  그만큼 잘라 부분 날짜로 되돌린다 (`1592`, `1592-09`). 실측: 원본 7,512건 ·
  korea 3,452건 · joseon 2,490건. **수집 뒤에 다시 돌릴 것** —
  `upsert_nodes` 가 날짜 칸을 새 값으로 덮어쓴다.
- 날짜만 보고 자를 수는 없다. 3·1 운동(1919-03-01)은 진짜 3월 1일이다.
- 화면은 늘어난 해의 첫 줄에만 해를 적고 나머지 줄에는 달을 적는다.
  달을 모르는 줄은 비운다 — 빈 칸이 '1월'이 아니라 '모른다'는 뜻이다.
- 1592년이 이제 실제 순서로 선다: 4월 동래성 → 4월 탄금대 → 5월 옥포 →
  5월 부산진 → 6월 상주 → 7월 한산도 → 9월 진주대첩.

**남은 것**
- 자릿수를 못 받은 노드 62개(원본). 삭제·병합된 QID 로 보인다.
- 부분 날짜를 쓰는 곳이 늘었으니, 날짜를 문자열로 비교하는 코드가 더 없는지
  훑어볼 것. 지금 확인한 곳은 전부 앞 네 자리만 읽는다.

## 2026-09-03 — 왕의 재위 띠 (`reigns`)

연표가 사건을 세워 놓고 **누구 때의 일인지**는 말하지 못했다. 임금도
'조선 임금' 자리도 그래프에 있는데 둘을 잇는 `held_position` 엣지 552개가
전부 날짜 없음이었다 — 재위는 P39 **문장의 한정어**(`pq:P580`/`pq:P582`)
에만 있고 우리 수집은 `wdt:` 로만 긁었다.

- `histgraph reigns` — 군주 자리(P279* 군주, 왕비·황후는 제외)에 앉은
  인물의 재위를 엣지 날짜로 옮겨 적는다. 전체 45명 · 조선 26명.
  props 에 `reign` 표식을 남긴다(수집이 props 를 덮어쓰므로 그때는 다시).
- `/api/timeline` 이 `reigns` 를 함께 준다. 고른 노드와 무관하게 늘 같은
  띠다 — 축을 재는 자이기 때문이다.
- 화면: 연표 왼쪽에 재위 막대(집권 시기) · 몰년 동그라미 · 그 사이 점선
  (물러난 뒤 산 기간). 고른 노드의 해를 품은 임금이 밝아진다.
  머리의 `왕 14/26` 은 이름을 세운 임금 / 전체다 (좁으면 막대만 남고
  줌인하면 나온다). 칩을 누르면 띠를 끈다(패널 356→252px).

**남은 것**
- 재위를 못 채운 8명: 명승·궁예·문무왕·무령왕·산상왕·지마 이사금·
  발해 고왕·조선 원종. 원종은 추존이라 정상이고, 나머지는 Wikidata 에
  한정어가 없다. **추정으로 채우지 말 것.**
- 왕대 환산표(아래 '다음에 할 일 3')의 재료가 이걸로 생겼다. 다만
  `신라 진흥왕대` 같은 표기를 연도로 옮기는 배선은 아직 없다.

## 2026-09-03 — 영어로 뜨는 노드 (`relabel`)

조선 그래프의 '왕자의 난' 옆에 `Sayuksin assassination plot` 이 떠 있었다.
수집 쿼리가 라벨을 `"ko,en"` 으로 물어보기 때문에, 한국어 라벨이 없는
개체는 영어 이름이 그대로 노드 이름이 된다 (전체 1,490개 · 조선 32개).

**다시 수집하는 것으로는 안 고쳐진다.** 영문 노드 1,480개에 대해
`rdfs:label(ko)`·`skos:altLabel(ko)`·한국어 위키백과 사이트링크를 다시
물어봤더니 새 이름을 얻을 수 있는 건 2개뿐이었다.

- `data/ko_labels.tsv` — 손으로 적은 이름 435개. `QID⇥한국어⇥근거` 형식이고
  **근거 칸을 반드시 채운다**(`ja=趙秉昌`, `zh=金喆煥`, `로마자 Kim Won-ju`).
  의심스러운 이름은 그 칸부터 볼 것.
- `histgraph relabel` — 표를 덮어쓰고, 옛 영어 이름은 별칭으로 남긴다
  (`Sayuksin` 으로도 계속 검색된다). 멱등.
- **수집·scope 뒤마다 다시 돌릴 것.** `upsert_nodes` 가
  `label = excluded.label` 로 덮어쓰므로 영어로 되돌아간다.

**부수 효과 — 중복이 드러난다.** `Cho Sok`→조속으로 고치자 생몰(1595~1668)
이 같은 `조속` 노드가 이미 있었다. 이정보·남병길·미마지·해구·해수도 같다.
`relabel` 은 보고만 하고 합치지 않는다(합치는 규칙은 `promote` 쪽).
**24건이 보고돼 있고 아직 아무도 확인하지 않았다.**

**남은 1,055개는 적지 않았다.** 한자 없이 로마자만 있는 근현대 남북한
인물이라(`Pak Chung-il`) 음절 복원이 갈린다. 조선 그래프는 0개이므로
화면에서는 보이지 않는다. 다음에 손대려면 `relabel --list-remaining`.

## 2026-09-03 — 연표 줌: 멀리서는 큰 사건만, 가까이 가면 전부

`web/timeline.js`. 서버는 그대로다(뼈대 156개를 다 보낸다) — 무엇을
세울지는 화면이 단계마다 정한다.

- **0단계 = 시대 전체가 한 화면.** 단계마다 눈금 두 배, 다섯 단계.
  마지막 단계는 밀도 상한을 풀고 전부 세운다.
  조선: 13 → 22 → 36 → 48 → 156.
- **누가 서는가.** 고른 노드·이웃·왕조는 늘. 뼈대는 차수 높은 것부터
  자리를 잡고, 이미 선 것과 30px 안에 겹치면 그 단계에선 물러난다.
  임계값이 없다 — 눈금이 정한다. 빈 구간에서는 차수 1짜리도 0단계부터
  선다(삼포 개항·나주괘서사건). 지도가 빈 땅에 작은 마을을 그리는 것과
  같아서 그대로 뒀다.
- 머리에 `큰 사건 13 / 156 · 더 보려면 +` 를 늘 적는다. 빠진 사건이
  없는 셈이 되면 안 된다.
- 줌 축: 휠·핀치(⌘/Ctrl+wheel)는 커서 아래 해를, 단추는 고른 노드를
  고정한다. 맨 휠은 스크롤 그대로. 단계는 `localStorage('tl-zoom')` 에
  남고 노드를 바꿔도 유지된다.

이전의 "8px = 1년 고정"은 이 안에 녹아 있다 — 같은 단계면 같은 눈금이다.

## 2026-09-03 — 별칭이 화면까지 오지 않았다

연표에 한일병합은 섰는데 **'경술국치'로는 못 찾았다.** 두 겹이었다.

```
전체 그래프의 별칭 5,064건 · 조선 그래프의 별칭 0건
```

- **`scope` 가 `aliases` 표를 안 옮겼다.** 노드·엣지·`same_as` 는 옮기면서
  별칭만 빠져 있었다. 화면이 읽는 것은 시대 그래프이므로 **별칭 검색이
  화면에서는 한 번도 동작한 적이 없다** — '이방원'으로 태종 찾기도 전체
  그래프에서만 되던 일이었다. 지금은 3,153건이 함께 간다.
- **`wikidata.fetch_aliases` 는 쓰이지 않는 코드였다.** 정의만 있고 부르는
  데가 없어서, 있던 5,064건은 전부 국가유산청·위키백과가 노드에 얹어 준
  것이었다. Wikidata 에는 한일병합의 한국어 별칭이 13개 있다(경술국치·
  국권피탈·일제강점…). `histgraph aliases` 명령을 붙이고 사건·조직·장소·
  직위·매체 2,244개에 대해 돌렸다 (+2,184건).

```
'경술국치' → 한일병합   ·   '국권피탈' → 한일병합   ·   '이방원' → 태종
```

**안 돌린 것.** 인물 28,961명 (`aliases --types person`, 배치 145회
≈ 4분). 별칭 표는 (노드, 별칭)이 자연키라 여러 번 돌려도 쌓이지 않는다.
돌린 뒤에는 `scope joseon` 을 다시 해야 화면에 반영된다.

## 2026-09-03 — 물어보고 답을 버린 것 (사건의 정체)

연표에 **경술국치가 없어서** 따라갔다. 전체 그래프에는 한일병합
(`wd:Q3277083`) 이 1910-08-29 로 정확히 있는데 조선 그래프에는 없었고,
같은 이유로 조선 연대(1392~1910) 안에서만 94건이 빠져 있었다 —
진주민란·갑오개혁·신임사화·경신 대기근·임진왜란 전투 30여 건.

`fetch_events` 의 쿼리는 `?e wdt:P17 wd:{polity}` 로 **정체를 물어 놓고
답을 버리고 있었다.** 인물은 처음부터 `props.polity` 를 적고
`scope.select_seeds` 가 그걸로 씨앗을 고르는데, 사건에는 그 칸이 비어
있어서 `props.seed_era`(손으로 적은 시드 목록)나 왕조 노드와 직접 연결된
사건만 살아남았다.

빠진 94건의 P17: **조선 73 · 대한제국 14** · 그밖 5 · 없음 2.

고친 것 셋:

- `fetch_events` 가 `props.polity` 를 적는다. 이미 쌓인 노드는
  `tools/backfill_event_polity.py --apply` 가 `json_set` 으로 그 칸만
  채운다(전체 그래프 242개 기록). **다시 수집(`ingest wikidata`)하면 안
  된다** — `upsert_nodes` 가 `props = excluded.props` 라서 `enrich` 의
  kowiki_url 과 `promote` 의 merged_from 이 날아간다.
- `Era.successor_events` — 조선 그래프가 **대한제국 사건**까지 담는다
  (사용자 판단). 1897년에서 자르면 조선이 어떻게 끝났나가 통째로 빠지고,
  이미 3·1 운동·청산리 전투가 인물을 타고 들어와 있어 자르는 쪽이 실제와
  안 맞았다. **인물에는 적용하지 않는다** — 대한제국 국적까지 씨앗으로
  삼으면 근현대로 끌려간다.
- `scope` 가 **연대를 아는 사건은 고립이어도 남긴다**. 한일병합은 엣지가
  0개다. 고립을 버리는 이유는 화면에 점으로만 뜨기 때문인데, 연표에서는
  '언제' 하나로 제 몫을 한다. 이번 재생성에서 32개가 그렇게 살아남았다.
- 연표는 **라벨이 QID 그대로인 노드는 안 세운다** (`wd:Q85881723` →
  'Q85881723'). 읽는 사람에게 아무 말도 하지 않는 줄이다. 조선 그래프에
  한 건 있다.

`scope joseon` 을 다시 돌렸다 (백업: 재생성 전 파일은 남기지 않았다 —
전체 그래프에서 다시 파생되므로 언제든 같은 명령으로 만든다).

```
노드 6,954 → 7,094 · 엣지 13,076 → 13,224
사건 297 → 385 · 그중 연대를 아는 것 76 → 160
연표 뼈대 71 → 157 · 축 높이 4,666 → 4,822px
```

**남은 구멍.** 을사조약(`wd:Q233254`, 1905-11-17)은 Wikidata 에 P17 이
아예 없어서 여전히 안 들어온다. `한일 병합 조약`·`한일 합방`·`한일 강제
병합` 같은 `ex:` 추출 고아가 아홉 개 있는데 전부 날짜가 없다 — 이것들을
`wd:` 사건에 붙이는 것은 `promote` 관문 쪽 일이다.

## 2026-09-03 — 조직에는 날짜를 물어본 적이 없었다 (`spans`)

화면에서 **조선을 고르면 연표가 '연도 미상'** 이라고 말했다. 없는
데이터가 아니라 **안 물어본 데이터**였다.

```
조선 그래프의 org 80개 → 날짜 있는 것 0개 (전체 그래프도 342개 중 0개)
Wikidata: wd:Q28179 P571=1392-08-13 · P576=1897-10-12
```

인물은 P569/P570, 사건은 P580/P582/P585 를 처음부터 가져왔는데 조직은
다른 노드의 엣지 상대(국적·직위·소속)로만 들어와 라벨과 URL 뿐이었다.
`wikidata.fetch_spans` + `histgraph spans` 를 넣었다. P571/P576 을 먼저
보고 없으면 P580/P582 로 물러나며, **끝이 시작보다 앞서면 통째로 버린다**
(Wikidata 날짜는 지저분하다 — 아래 절 참고). 이미 적혀 있는 날짜는
건드리지 않는다.

두 DB 모두 돌렸다: 조선 33/80 · 전체 158/342. 나머지는 Wikidata 에도 없다.

**날짜만 채운다고 화면에 나오지는 않았다.** 연표의 뼈대는 `type='event'`
만 세우는데 조선은 `org` 라서, 값이 들어온 뒤에도 1392년 자리가 비어
있었다(위화도 회군 1388 다음이 곧장 제1차 왕자의 난 1398). 왕조 노드를
`kind: "era"` 로 따로 세우고 존속 기간 막대는 축 옆에 비켜 그린다.
그래프에 `ex:event:조선 건국` 이 있지만 날짜 없는 엣지 둘짜리 고아라
거기에 P571 을 옮겨 적지는 않았다 — 그건 추측이다.

**아직 안 한 것.** `timeline` 을 다시 돌리지 않아서 새로 생긴 날짜가 아직
`dated_to` 엣지로는 안 걸려 있다 (화면 연표는 `start_date` 를 직접 읽으므로
이미 보인다). "1392년에 무슨 일이" 질의에 조선이 걸리게 하려면
`uv run histgraph timeline` 을 다시 돌려야 한다.
place 323개·role 33개도 여전히 날짜가 없다 — `spans --types place role` 로
같은 방법이 되지만 장소의 '설립'은 연표에서 뜻이 흐려 손대지 않았다.

## 2026-09-03 — 연표: 노드를 시간 위에 세운다

`### 4. 화면에서 더 할 것` 의 '연표 보기'를 했다. 노드를 고르면 왼쪽에
세로 연표가 서서 **몇 년쯤이고 무엇 뒤 무엇 앞인지**를 말한다.
`GET /api/timeline?id=…` · `web/timeline.js` · README `### 연표`.

만들면서 데이터에서 걸린 것:

- **사건에 날짜가 없다.** 조선 그래프 사건 297개 중 `start_date` 를 가진
  것은 73개다. 갑자사화·기묘사화처럼 `from_period` 로 `time:YYYY` 에만
  붙어 있는 것들이 있어 그 경로를 함께 본다. 그렇게 알아낸 값은 화면에
  `연결된 연도` 라고 적는다 — 노드가 스스로 적고 있는 날짜와 섞지 않는다.
- **사람을 점으로 찍으면 화면이 거짓말을 한다.** 갑자사화 이웃에 윤필상
  (1427년생)을 '참여'로 달아 1427년에 세우면 "1427년에 참여했다"로 읽힌다.
  연표의 점은 `event·artwork·media` 뿐이다. 인물의 생몰은 **그 인물을
  골랐을 때** 자기 자리에서 구간 막대로만 말한다.
- **축을 늘리는 건 대개 오염이다.** 갑자사화(1504) 참여자에 1955년생
  정성근이 있고(동명이인), 정축하성은 1637~1895(258년)이며, 몰년을 모르는
  인물 열두 명의 `end_date` 가 2000-01-01 이다. 각각 150년 창 · 구간 상한
  (인물 110·사건 60) · 생년=몰년 무시로 막는다. `server._span` 은
  `promote.life_of` 와 같은 규칙이다(둘을 합치지 않은 이유는 `promote` 가
  `extract`·`http` 를 끌고 오기 때문 — 서버는 표준 라이브러리만 쓴다).
- **창이 한 해로 좁아지는 경우.** 이웃이 전부 인물이면 창에 아무것도 남지
  않는다. 창 밖 앞뒤로 여섯씩 데려와야 "무오사화 뒤, 중종반정 앞"을 말하고
  앞뒤 한 세기가 딸려 온다.
- **자 눈금을 고정했다(8px=1년).** 창에 맞춰 축을 늘였다 줄였다 하면 같은
  1cm 가 갑자사화에서는 5년, 이순신에서는 17년이 되어 노드끼리 견줄 수
  없었다. 넘치는 만큼은 스크롤이 받고, 고른 자리로 자동으로 맞춰진다
  (머리의 연도를 누르면 되돌아온다). 연도를 모르는 개체만 예외로 시대
  전체를 한 화면에 담는다 — 견줄 자리가 없는데 4,500px 를 스크롤시킬
  이유가 없다.
- **축은 그래프가 담은 처음 해부터 끝 해까지 통으로 간다** (조선
  1356~1929년, 4,822px). 노드 언저리만 잘라 보내면 훑어도 시대의 양 끝에
  닿지 못하고, 노드마다 축이 달라 자리를 견줄 수도 없다.
  **연대를 아는 사건은 다 세운다**(조선 71개). 한때 십년마다 둘로 솎았는데
  — 1590년대에만 20건이 몰려 있어서 — 연표에서 빠진 사건은 그 시대에
  없었던 일이 되므로 되돌렸다. 몰린 곳의 라벨 밀림은 안내선으로 갚는다.
  뼈대에서 빼는 것은 둘뿐이다: **엣지 하나뿐인 `ex:` 고아** ('1908년
  복권'·'1963년 문집 간행'이 축을 1963년까지 늘려 놓았다. 차수 2 이상은
  남긴다 — 진산사건 1791, 차수 3 이 1728~1791년의 빈 구간을 메운다),
  그리고 **이름도 해도 같은 중복**.
- **머리의 띠를 걷어내고 스크롤 손잡이로 바꿨다.** 처음엔 '시대 전체에서
  이 자리'를 보여주는 띠였는데 아무 기능이 없었고, 눈금(1359~1926)도
  왕조의 존속 기간이 아니라 **차수 상위 사건의 최소·최대**여서 사실은
  조선을 가리키지도 않았다. 왕조 노드(`wd:Q28179`)에 날짜가 없어 진짜
  존속 기간을 댈 방법도 없다. 지금은 연표 자신의 범위를 눈금으로 삼아
  창틀(보이는 구간)과 고른 노드 자리를 표시하고, 누르거나 끌면 옮긴다.

테스트 326 → 356.

**남은 데이터 문제.** 임진왜란이 두 노드다 — `wd:Q122846639`(차수 37,
1592~1593)와 `wd:Q576338`(차수 1, 1592~1598). 둘 다 Wikidata 항목이고
한국어 라벨이 같다. 연표는 **화면에서만** 차수 높은 쪽을 세우고 노드는
그대로 둔다. 제대로 갈라내려면 엔티티 해소 쪽 일이다 — 라벨 유사도로
합치면 제1차/제2차 요동 정벌이 한 노드가 된다.

**안 한 것.** 왕대 표기('태조 7년')는 여전히 연도로 못 읽는다 — `### 3.
왕대 환산표` 가 그대로 남아 있다. 연표에서 이 라벨들은 그냥 빠진다.
연표는 시대(`era`) 띠의 범위를 **차수 상위 사건의 최소·최대**로 잡는데
(조선 그래프에서 1359~1926) 이건 왕조의 실제 존속 기간이 아니다.

## 2026-09-03 — 근거가 통과시키는 오류 세 갈래

화면에서 눈에 걸린 것을 따라가니 전부 **근거 검증이 구조적으로 못 잡는**
유형이었다. 근거 구절은 원문에 실제로 있고 낱개로 보면 멀쩡한데, 관계가
틀렸거나 근거가 그 대상을 말하고 있지 않다.

- **참여 오독** — 황진이(1506~1567)가 병자호란(1636)에 '참여'로 붙었다.
  근거는 `임진왜란과 병자호란 등으로 인해 대부분 실전되었고` — 작품이
  소실됐다는 문장이다. `loss_context` 가 원인 문형+소실 어휘를 함께 볼
  때만 버린다(어휘만 보면 원균의 해전 참여 7건이 날아간다).
  `lifespan_conflict` 에 `participated_in` 을 넣어 사후 참여도 막는다.
  사건 날짜가 없으면 헛도므로 Wikidata **P585(시점)** 폴백을 넣었다
  (병자호란이 P580/P582 없이 P585만 가져서 무연대였다). 정리 96건:
  `tools/sweep_participation.py`.
  **끝자리 괄호만 보다가 76건을 놓쳤다.** 처음 `label_year` 는 `(YYYY년)`
  꼬리만 봐서 `황진이 (2006년)` 은 잡고 `2021년~2022년 KBS 1TV 드라마
  《태종 이방원》`, `왕과 비 출연` 은 통과시켰다. 지금은 라벨 어디서든
  네 자리 연도를 찾고, 라벨에 없으면 **근거의 가장 이른 연도**를 쓴다.
  근거연도는 **판정에만 쓰고 저장하지 않는다** — 정답을 아는 사건 57개로
  재보니 ±1년 안이 88%이고 신유박해를 1760년(정답 1801)으로 읽었다.
  화면에 사건 연대로 뜰 값을 그 정확도로 적을 수는 없다.
- **가제티어 덤프** — `무오사화 --from_period-->` 39건의 대상이 전부
  프롬프트의 '알려진 개체' period 상위 150개였다. 모델이 원문 대신 보기
  목록을 쏟아내고 근거 칸에 문서 첫 문장을 붙인 것. **낱개로는 못 잡고**
  (주어, 관계, 근거) 묶음의 **지목률**로만 갈린다 — 정상 열거문 88~100%,
  덤프 0~20%. `gazetteer_dump` 가 묶음 5건 이상 + 지목 못한 것이 과반일
  때 그 못한 것만 버린다. 정리 76건: `tools/sweep_dumps.py`.
  **지목 검사를 낱개로 person 밖에 넓히지 말 것** — period 41%·event
  28%·place 34%가 걸리는데 대부분 정상이다.
- **작품 표기 변이** — `등만월대회고`(登滿月臺懷古)와 `만월대 회고시` 가
  같은 시인데 두 노드여서 황진이 작품이 9편으로 부풀었다.
  `promote.title_variant_matches` 가 **작품 타입·같은 창작자·갈래 접두
  (登·詠·題)와 접미(詩·歌)만 벗겨 정확히 같아질 때**만 잇는다.
  유사도로 넓히면 절반이 틀린다 — 후보 26쌍에 `제1차/제2차 요동 정벌`,
  `단종 복위 사건 (1456년)/(1457년)`, `부산항/원산항의 개항` 이 섞여 있다.
- **화면** — 열거문 하나가 관계 여럿을 낳으면 카드마다 같은 인용이 찍혀
  "다른 작품인데 내용이 같은" 것처럼 보였다. 여럿이 공유하는 근거는 묶음
  머리에 한 번만 둔다(`web/app.js` `showDetail`). 왼쪽 패널은 기본 접힘 +
  헤더 ☰ 토글.

테스트 272 → 297.

**아직 안 본 구간 (다음 세션용).** 위 정리는 각 규칙 안에서는 전수지만
"근거가 대상을 지목하지 않는" 엣지 전체를 훑은 것은 아니다. 조선 그래프에
**548건**이 남아 있고(participated_in 312·related_to 83·from_period 70·
child_of 42…) 표본 12건을 보니 대부분은 정상이었다 — 사건 문서의 문장이
그 사건 이름을 다시 적지 않는 것이 자연스럽기 때문이다. 다만 그 안에
`조식 --related_to--> 조선 예종`(근거는 이황 이야기) 같은 진짜 오류가
섞여 있다. **낱개 이름 검사로 넓히지 말 것** — period 41%·event 28%·
place 34%가 걸리는데 대부분 멀쩡하다. 가르려면 다른 신호가 필요하다.

## 2026-09-03 — 그래프의 떨림

배치가 식는 내내 노드가 떨었다. 원인은 **반발력의 근거리 불안정**이다.
반발력이 `C/d²` 라 가까워지면 강성(`2C/d³`)이 폭발하는데, 감쇠 0.82 의
준음함수 오일러가 견디는 한계는 3.6 근처다. 큰 노드 둘이 18px 안으로
들어가면 5.5 를 넘겨 서로 밀고 되돌아오는 진동에 갇힌다. 다체 합력이
노드를 수시로 그 안으로 밀어 넣으니 배치 내내 떨었다.

실제 데이터(180노드 그래프 6개)로 `GraphView` 를 Node 에서 돌려 화면
좌표의 프레임별 역방향 이동량을 쟀다:

| | 60Hz 떨림 | 120Hz 떨림 | 카메라 떨림 | 겹친 노드 쌍 |
|---|---|---|---|---|
| 전 | 1.96 | 0.98 | 1.309 | 4.0 |
| 후 | **0.17** | **0.00** | **0.152** | **1.3** |

넷을 고쳤는데 **감소의 사실상 전부는 첫 번째**다(빼면 2.03 으로 복귀):

1. `MIN_REPEL_DIST = 32` — 그보다 가깝게는 보지 않는다. 아래로는 힘이
   일정해져 여전히 밀어내되 강성이 0 이라 진동하지 않는다.
2. **고정 시간간격**(초당 60틱). 힘 계수가 전부 '한 틱당'인데 프레임마다
   돌리고 있었다. 120Hz 화면에서는 alpha 가 벽시계 기준 두 배로 식어
   배치가 **덜 풀린 채 3초 만에 굳었다**(조용한 게 아니라 일찍 얼어붙은
   것). 이제 주사율과 무관하다 — 대신 120Hz 에서 배치가 도는 시간이
   3초 → 4.5초로 늘어난다.
3. 미세 이동 정지(0.1px)·카메라 데드존. 기여는 작다(0.02, 카메라 7%).
4. 라벨 깜빡임 이력 — 한 번 뜬 이름은 3px 더 겹쳐야 물러난다. 위 지표에
   안 잡히는 종류라 수치로는 확인하지 못했다.

퍼짐과 평균 엣지 길이는 그대로다(815k→798k px², 207→209px). 떨림만
걷어냈지 배치 성격은 안 건드렸다.

## 2026-09-03 — 영어로 뜨는 설명 (`redescribe`)

`relabel` 이 이름에 한 일을 설명에 한다. 배경과 수치는
[README.md](README.md) "영어로 뜨는 설명" 에 있다. 요점만:

- 사전·규칙(`src/histgraph/koreanize.py`). **모르는 말은 지어내지 않고
  비운다.** 원문은 `props.desc_en` 에 남아 다시 살릴 수 있다.
- 관문은 `ontology.Node.__post_init__` 하나. 예외는
  `wikipedia._fill_from_wikidata`(SQL 직접 쓰기)라 거기만 따로 막았다.
- **화면에서 자료 출처 표기를 뺐다.** `Wikidata · 영어 한 줄 설명` 줄이
  사라졌다. `desc_via` 는 출처가 아니라 경고라 남겼다.
- 두 그래프 모두 한글 없는 설명 **0건**.

**남은 것:** 아직 영어인 *이름* 1,055개(전체 그래프). `relabel` 의 수작업
표 영역이고 화면이 읽는 조선 그래프는 0개다.

## 2026-09-03 — 선택하면 그 노드가 뿌리다

그래프나 연표에서 노드를 고를 때마다 직전 노드가 `←` 로 상위에 붙었다.
서로 아무 관계도 없는 두 노드가 부모-자식처럼 보인다 — 세종을 보다 저쪽
끝 황진이를 누르면 `← 세종` 이 붙는 식이다.

자취(`trail`)가 쌓이는 자리는 `showDetail` 한 곳인데 **모든 경로**가
쌓고 있었다. 이제 `nest` 를 준 걸음만 쌓인다:

| 어디서 골랐나 | 후 |
|---|---|
| 그래프·연표 | **뿌리가 된다** — 쌓인 자취까지 지운다 |
| 오른쪽 패널의 관계 | 그 밑으로 들어간다 (서브메뉴) |
| `←` 되짚기 | 쌓지도 지우지도 않는다 |

playwright 로 실제 페이지를 띄워 확인했다(앱 코드에 시험용 전역을 심지
않고, 화면에 보이는 `←` 단추와 제목만 보고 판정). 수정 전 코드로 돌리면
정확히 세 건이 깨진다. 시험 스크립트는 저장소에 넣지 않았다 — playwright
의존이 생겨서다.

**남은 것:** 검색으로 옮기면 그래프 중심만 바뀌고 **오른쪽 패널은 옛
노드를 붙들고 있다**(`← 노량 해전` 이 그대로 남는다). 원래부터 그랬고
이번에 망가진 것은 아니지만, 위 규칙에 어긋나는 마지막 자리다.

## 2026-09-04 — 정여립의 난에 정여립이 없었다 (`alias_exact`)

화면에서 `정여립의 난` 을 열면 설명도 없고 관계가 **하나뿐**이었다.
정여립 본인이 없다. 그런데 그래프에는 정여립도, 그 사건도 다 있었다 —
`기축옥사` 라는 **다른 이름으로** 옆에 따로 서 있었다.

```
ex:event:정여립의 난   관계 1   이산해 ← related_to        설명 없음
wd:Q7836645 기축옥사   관계 3   정여립 ← participated_in   설명 있음
                              별칭: 기축사화 · 정여립의 난 · 정여립의 옥사
```

**원인은 자료가 아니라 순서다.**

| 시각 (KST) | 일어난 일 |
|---|---|
| 08-31 05:25 | 이산해 문서 추출 → `정여립의 난` 을 못 찾아 `ex:` 노드를 만듦 |
| 09-03 12:47 | `aliases` 를 처음 만들어 돌림 → 그 이름이 기축옥사의 별칭이 됨 |
| — | **아무도 다시 해소하지 않았다** |

추출의 이름 해소(`extract.resolve`)는 별칭을 **본다.** 다만 그건 추출이
돌던 그 순간의 별칭표다. `fetch_aliases` 는 8월 13일부터 있었지만
파이프라인에서 부르는 곳이 없는 죽은 코드였고, 그걸 부르는 `aliases`
명령은 9월 3일에 생겼다. **사흘 차이로 화면이 틀렸다.**

승격에도 이 이름을 붙일 길이 없었다. 두 문이 다 닫혀 있었다.

- `local_matches` 는 **라벨만** 봤다 — 별칭은 한 번도 안 봤다.
- `kowiki_matches` 는 사건에 대해 넘겨주기를 **일부러** 안 따라간다
  (하위 항목이 상위 항목에 흡수된다). 정여립의 난은 위키백과에서
  기축옥사로 넘겨주기다.

`local_matches` 에 `alias_exact` 단계를 넣었다 — `promote` 가 곧 **오늘의
별칭표로 다시 해소하는** 단계다. 조건은 점수가 아니라 규칙이다: 그
이름으로 불리는 **같은 타입 노드가 딱 하나**일 때만, 라벨 일치가 앞서고,
연대가 어긋나면 안 잇는다.

실측 18건(전체 12 · 조선 6). **손으로 다 확인했고 오탐 0.**

```
정여립의 난 → 기축옥사        갑오경장 → 갑오개혁       한일 합방 → 한일병합
진산사건 → 신해박해           남원성 전투 → 남원 전투    경덕궁 → 경희궁
제1차 진주성 전투 → 진주대첩   광주학생운동 → 광주 학생 항일 운동
남북협상 → 전조선 제정당사회단체 대표자 연석회의        황주·화순 → 황주군·화순군
```

`정여립의 난` 으로 검색하면 별칭을 타고 기축옥사가 나오고, 그 안에
정여립(참여)·조선(시대)·이산해(관련)가 다 있다.

**교훈은 이 한 건보다 넓다.** 해소는 한 번 일어나고 끝난다. 뒤늦게 온
별칭·노드는 이미 만들어진 고아를 되돌리지 않는다. 새 소스를 채운 뒤에는
`promote --local-only` 를 다시 돌려야 한다.

## 2026-09-04 — 이름이 별명 취급을 받고 있었다 (`co_names`)

기축옥사를 열면 **'정여립의 난' 이 어디에도 안 보였다.** 별칭 더미 맨 아래
'다른 이름' 에 처박혀 있었다. 셋이 한 더미에 있는데 무게가 다르다.

```
기축사화 · 정여립의 옥사    위키데이터가 곁다리로 적어 둔 표기 변형
정여립의 난               이 사건을 부르는 **또 하나의 이름**
```

그 이름으로 이 사건을 아는 사람이 더 많은데 화면이 별명으로 그렸다.

**가르는 기준은 점수가 아니라 출신이다.** `props.merged_from` 에 있는
이름은 우리 그래프에서 **자기 노드를 갖고 있던** 이름이다 — 어떤 소스가
그 대상의 이름으로 그렇게 적었다는 뜻이라 altLabel 과 격이 다르다. 이미
기록하고 있었고 화면이 안 쓰고 있었을 뿐이다.

한쪽이 다른 쪽에 통째로 들어 있으면 뺀다 — `조선 세조 · 세조` 를 두 번
쓸 이유가 없다. 이 규칙으로 두 이름을 갖는 노드는 **전체 62개 · 조선 47개**.

```
조선 태조 · 태조 이성계    인목왕후 · 인목대비    헌경왕후 · 혜경궁 홍씨
신해박해 · 진산사건        진주대첩 · 제1차 진주성 전투    무안대군 · 이방번
```

`co_names()` 가 판정하고 `names` 를 **검색·그래프·상세에 함께 싣는다**.
승격된 이름은 `aliases` 에서 빠진다. 그래프 라벨은 옆에 이어 붙이면 한
줄이 길어져 이웃을 밀어내므로 **아랫줄에 작게** 쓴다 (`labelBox` 가 그
높이를 같이 본다).

**규칙이 잘못된 병합을 하나 드러냈다** — `문하시중 · 문하시랑` 이 한
노드다. 다른 직위인데 합쳐져 있었고, 이름이 제목에 나란히 서면서 눈에
띄었다. 아직 안 고쳤다.

## 2026-09-04 — 사건 산문을 도입부에서 본문 전체로

화면의 사건에 참여자가 없었다. 원인은 **추출이 읽는 글이 도입부뿐**이었던
것이다 — `enrich` 가 사건에 `exintro=1` 로 받아 둔 글을 그대로 원문으로
쓴다.

```
기축옥사   도입부 1,036자  →  본문 전체 9,883자
도입부에는 정여립만 나온다. 본문에는 정철·정언신·송익필·정개청·최영경·김장생이 있다.
```

`enrich --types event --full --refresh` 로 사건 문서 **329건의 본문
전체**를 받았다.

| 사건 산문 길이 | 전 | 후 |
|---|---|---|
| <400자 | 192 | 45 |
| 1.5k–6k | 48 | 120 |
| 6k+ | 39 | 80 |

참여자 수가 본문 길이를 그대로 따라간다 — 본문 전체를 가진 사건은
고구려-당 전쟁 42명·갑신정변 43·갑자사화 39, 도입부만 가진 사건은 0~1명.

**구조화 소스는 이미 바닥이다.** 확인했다: Wikidata 가 기축옥사에 대해
아는 것은 P710 을 포함해 **정여립 한 명뿐**이고 신해박해는 0건이다.
인포박스 `지휘관/주요인물` 은 전투 문서의 필드라 옥사·사화에는 없다
(사건 52건에만 걸린다). 남은 길은 산문 추출뿐이다.

### 대상을 좁히는 두 플래그

조각당 수 분이 드는 로컬 추출에서 **대상 축소가 유일한 비용 조절**이다.

```sh
histgraph extract --types event --max-participants 1 --min-chars 1500
```

```
본문 1,500자 미만 제외   326건 → 198건
참여자 1명 초과 제외     198건 → 126건   ← 병자호란(65명)·3·1 운동(45) 제외
                        → 173조각
```

**`--scope` 를 쓰면 안 된다.** 그건 대상 문서와 **가제티어를 같이** 좁힌다
— 사건 128개만 넣으면 아는 개체가 127개로 줄어(600 → 127) 인물 이름이
전부 `ex:` 고아가 된다. 새 플래그는 대상만 좁힌다.

## 2026-09-04 — 같은 틀을 열어놓고 그 줄만 안 읽었다 (`infobox`)

임오화변이 연표에 없었다. `start_date` 가 비어서다. Wikidata 에
`P580/P582/P585` 가 없는 사건이 **238건**이고 임오화변이 그중 하나다.

날짜를 세우는 코드가 여섯 군데인데 **전부 구조화 필드만 본다.**

```
timeline.py:66      연도 노드 자신
promote.py:805      Wikidata 생몰년
extract.py:582      label_year(name)   ← 라벨의 괄호 연도만
wikidata.py:289/543/632    P569/P570/P580/P582/P585
```

`description` 을 읽는 자리가 한 곳도 없다. `parse_years()` 도 인자가
`label` 이다 — 이름에 숫자가 없는 사건은 그냥 지나간다.

**그런데 답은 산문이 아니라 인포박스에 적혀 있었다.**

```
{{역사적 사건 정보
| 별칭   = 임오옥, 사도세자사건
| 참가자 = [[영조]]·[[노론]]·[[구선복]]<br/>[[정조|세손 산]], [[이석문]] …
| 날짜   = [[1762년]] (영조 38) [[7월 5일]]
}}
```

**우리 파서는 이 틀을 이미 열고 있었다.** `장소` 를 읽으려고. 그런데
필드 표에 `날짜`·`참가자`·`별칭` 이 없어서 그 줄들을 지나쳤다.

원인은 **필드 표를 전투 문서만 보고 짠 것**이다. `지휘관`·`교전국` 은
`전쟁 정보` 틀의 이름이라 옥사·사화·정변에는 아예 없다. 그쪽은 `역사적
사건 정보` 틀을 쓰고 참가자를 `참가자` 한 칸에 몰아 적는다. "옥사에는
인포박스 필드가 없다"고 결론 냈던 것이 틀렸다 — **필드가 없는 게 아니라
이름이 달랐다.**

고친 것:

- `EVENT_FIELDS` 에 `참가자` 추가 (person·org 양쪽 허용).
- `EVENT_VALUE_FIELDS` 신설 — **링크가 아니라 값 원문**을 읽는 경로.
  날짜·별칭은 엣지가 아니라 노드 자신의 속성이라 기존 경로를 못 탄다.
- `infobox_date()` — 실측한 네 가지 서식을 읽는다. **괄호 안은 절대 안
  본다**: `(영조 38)` 의 38 을 연도로 집으면 안 되는데 재위 연차가 거의
  모든 사건에 붙어 있다. 범위는 시작만 취한다.
- `infobox_aliases()` — 쉼표·가운뎃점으로 가르고 서식을 벗긴다.
- `apply_event_attrs()` — **빈 날짜만 채운다.** Wikidata P585 는 사람이
  손본 값이고 인포박스는 자유 기술이라, 어느 쪽이 옳은지 모를 때 이미
  있는 것을 밀어내지 않는다 (`--refresh` 면 덮는다).

```
임오화변   start_date  (없음) → 1762-07-05        연표에 선다
          별칭        임오옥 · 사도세자사건
          참여자      0명 → 영조·정조·노론·구선복·이석문·장조

조선 그래프   participated_in  1,048 → 1,647 (+599) · occurred_at 171 → 268
전체 그래프   participated_in  1,953 → 2,137 (+184) · occurred_at 427 → 484
```

**날짜 수확은 7건뿐이다** (전체 4 · 조선 3). 사건 312건을 훑었는데
인포박스를 가진 것이 70건이고, 날짜 없는 사건 230건 중 kowiki 문서가
있는 것이 97건뿐이다. 인포박스로 얻을 것은 여기까지가 거의 전부다.

**남은 구멍은 산문 정의문이다.** 날짜 없고 설명 있는 사건 143건 중
**79건**이 도입부 200자 안에 연도를 적어 두고 있다 (갑자사화 1504 ·
요동 정벌 1388 · 송유진의 난 1594 · 영남 만인소 사건 1792).

다만 **첫 연도를 그냥 집으면 안 된다.** 같은 목록에 `신라의 대외 관계
676` · `고려의 역사 474` 가 섞여 있는데 이건 사건이 난 해가 아니라 시대
서술의 첫 연도다. 규칙은 "첫 연도"가 아니라 **"정의문의 연도"** 여야 한다
— 라벨 뒤 `은/는` 다음, 서술어가 `사건이다/일이다/변란이다` 로 닫히는
문장. 라벨에 간지(`임오`)가 있으면 연도와 **대조까지 할 수 있다** —
1762년은 임오년이 맞다. 아직 안 만들었다.

## 이전 세션에서 끝낸 것

- **프론트엔드** (`serve` + `web/`) — 왕조에서 시작해 이웃을 펼쳐 나가는
  그래프 탐색 화면. 표준 라이브러리 HTTP 서버 + 순수 JS 캔버스, 의존성
  없음. 상세 패널이 관계마다 **출처와 근거 구절**을 띄운다.
- **유도 부분그래프** — `store.neighbors` 가 프론티어에 닿는 엣지만
  돌려줘서 이웃끼리의 관계가 통째로 빠져 있었다. 화면이 바큇살만 뻗은
  별 모양이 됐던 원인.
- **자기순환 제거** — 1,258건. `timeline` 과 추출 양쪽에서 새고 있었다.
- **이름 오연결 전수 조사** (`promote.audit_links`) — 추출 엣지 끝점
  1,944개를 훑어 21건을 옳은 노드로 옮겼다. 판정 근거는 **출처 문서의
  시대**다. 새로 생기지 않도록 `to_graph` 도 라벨·별칭 후보 중 차수가
  큰 쪽을 고르게 바꿨다.
- **중복 표시 제거** — 같은 사실을 두 소스가 말한 81건이 화면에 두 번
  나왔다 (행주대첩 → 행주산성). 저장은 소스별로 두고 화면에서 합친다.
  `ex:` 연도 노드 56개도 `same_as` 를 근거로 정규 연도 노드에 병합.
- **왕을 시대 씨앗에 포함** — `조선 정종`처럼 국적 태그가 없는 인물이
  3,390명이고 거기에 왕들이 있었다. 라벨 접두로 씨앗에 넣으니 조선
  그래프가 노드 5,622 → 5,817, 엣지 9,017 → 9,593 이 됐다.
- **신규 인물 승격** (`promote`) — ex 고아 96개를 실제 노드로 병합,
  신규 `wd:` 노드 69개 생성 후 Wikidata 관계 189건 보강. 신규식·유관순·
  신채호·길선주가 이제 그래프에 얽혀 있다.
- **타입 오분류 74건 교정** — `조선총독부`→org, `병조판서`→role.
- **중간 저장** — `cmd_extract` 가 문서 단위로 저장한다. 4시간짜리 작업이
  죽어도 마지막 노드 하나만 잃는다.
- **고립 ex 노드 154개 제거** (`--prune-orphans`).

## 환경

- **포트는 8100 고정.** 이 기계에서 8000 은 다른 프로젝트(next 백엔드)가,
  8010 도 과거에 쓰였다. `serve` 기본값을 8100 으로 바꿨으므로
  (`server.py`·`cli.py`) 인자 없이 띄우면 된다. 다른 포트로 옮기지 말 것.
- **메모리가 빠듯하다.** 64GB 기계에서 MLX 모델이 35GB 를 잡는데,
  ollama 가 `keep_alive: Forever` 로 llama-server 를 7.7GB 물고 있고
  (`ollama ps` 로 확인) 스왑이 15.3GB 중 13.6GB 차 있다. **추출을 돌리기
  전에 `ollama stop <모델>` 로 자리를 비울 것.**
- **ANTHROPIC_API_KEY 없음.** `ant` CLI 미설치. 추출은 로컬 MLX 로만 돌아간다.
- 모델 `mlx-community/Qwen3.6-35B-A3B-8bit` 다운로드 완료 (35GB).
- **4bit 전환은 사용자가 명시적으로 거부함.** 속도 개선은 대상 축소로만.
- ollama 는 JSON 스키마를 무시하므로 쓰지 않는다 (`backends.py` 상단 참조).

## 다음에 할 일 (우선순위)

### 0. 인물 문서 923건 추출 — '조선에만 매달린' 인물들 (2026-08-14 추가)

안방준(`wd:Q709174`)처럼 위키백과 본문은 통째로 있는데 엣지는
`related_to 조선` 하나뿐인 인물이 대부분이다. 원인: 지난 추출이
`--types event org` 로 돌아서 **인물 문서 본문은 한 번도 추출되지 않았다.**
서사 있는 인물 1,327명 중 자기 문서가 추출된 사람은 157명뿐.

소량 검증(안방준)으로 확인한 것:
- 품질은 목표를 맞춘다 — participated_in 임진왜란·정묘호란·병자호란,
  related_to 성혼·김집·김육, 관직 16건, 저서 29건이 근거와 함께 나온다.
- **족보 목록이 모델을 망가뜨렸다** — `증손부 : 창녕조씨` 꼴 목록에서
  방계 인물을 본인의 배우자로 붙이고, 같은 블록을 4번 반복하는 루프로
  조각당 6분을 태웠다. `extract.strip_kinship_lists` 가 이제 선별 전에
  자동 제거한다. 근거 검증은 이걸 못 잡는다 — 근거가 원문에 실제로
  있기 때문이다.
- **친족 호칭을 열거하려다 두 번 실패했다.** 60개를 적어 뒀는데
  `종조부`·`재종숙` 이 새어 나와 방계 9명이 안방준의 부모가 됐다. 실측
  46,370줄에서 2,265줄을 놓쳤고 놓친 라벨 상위 30개가 전부 친족어였다.
  지금은 어휘가 아니라 **구조**(짧은 한글 라벨 + 콜론)로 잡는다.
- **소유격 오독** — `처는 정승복의 딸이다` 에서 `spouse_of 정승복` 이
  나왔다. 한 세대를 건너뛴 것이다. `possessive_mismatch` 가 버린다.
- 속도: 서사 밀집 조각은 185~250초/조각 (관계 40건 생성 시). 1,162조각
  전체는 수십 시간이므로 **--limit 으로 끊어서 여러 밤에 나눠 돌릴 것.**
- **상한 포화에 주의.** `OUTPUT_SCHEMA` 의 `maxItems: 40` 에 조각마다
  정확히 40건으로 붙는다. 관계를 놓치고 있을 수 있다. 다만 상한을 올리면
  JSON 이 `max_tokens` 에서 잘려 **0건**이 됐던 전례가 있으므로(주석 참조)
  올리려면 `max_tokens` 를 같이 올리고 소량으로 검증할 것.

실행 (끝나면 promote → timeline → scope 순서 유지):

```sh
uv run histgraph extract --scope data/joseon.sqlite \
    --types person --limit 100
```

**주의 — 메모리를 비우고 돌릴 것.** 2026-08-14 세션에서 MLX 실행이 두 번
연속 죽었는데, 계측해 보니 원인은 **메모리 고갈**이었다. 모델 로드 중
10초 간격 실측:

```
23:24:31  free 4.4GB · compressed  2.3GB · swap 13.6/15.3GB
23:24:41  free 0.0GB · compressed 10.1GB · swap 13.6/15.3GB
23:24:51  free 0.0GB · compressed 40.5GB · swap 16.7GB (여유 748MB)
23:25:01  free 5.3GB · compressed 35.2GB · swap 30.7GB (여유 0)   ← 스왑 고갈
```

MLX 는 실측 **wired 45.7GB** 를 잡는다 (모델 35GB + KV 캐시). wired 는
페이지아웃이 안 되므로 64GB 기계에서 **나머지 전부가 18GB 안에** 들어가야
한다. 다른 앱이 25GB 를 쓰고 있으면 자리가 없고, 커널이 가장 큰
프로세스(= MLX)를 죽인다. `ps -o rss=` 로는 1~2GB 로만 보이니
(통합 메모리) `vm_stat` 의 **wired** 를 봐야 한다. 실행 전에 확인할 것:

```sh
ollama ps                    # keep_alive Forever 로 물고 있는 모델이 있다
ollama stop <모델>           # 7.7GB 회수
vm_stat | head -4            # free 가 한 자리 GB 면 아직 부족하다
```

계측 스크립트는 세션 스크래치패드에 있다 (`run_with_memlog.sh`) — 다시
죽으면 추측하지 말고 같은 방식으로 재보면 된다.

**먼저 인포박스를 돌릴 것 — 공짜다.** (2026-08-14) 전에는
`type = 'event'` 로 하드코딩돼 있어 인물 인포박스를 안 읽었다. 이제
아버지·어머니·배우자·자녀·스승·제자·출생지·사망지를 **LLM 없이**
가져온다. 산문의 족보 목록과 달리 인포박스는 필드 주인이 명확해 안전하다.

실측: **인물 25건 → 관계 65건** (문서당 2.6건, 타입 불일치 17건 제외).
`kowiki_url` 을 가진 인물이 1,603명이므로 대략 4,000건 규모다. LLM 추출이
조각당 200초인 것에 비하면 사실상 공짜이므로 **인물 추출보다 먼저**
돌리는 것이 맞다.

```sh
uv run histgraph infobox --types person --limit 50   # 먼저 소량으로
uv run histgraph infobox --types person              # 확인되면 전체
```

방향이 필드마다 다르다는 점이 핵심이다: `아버지 = [[안중관]]` 은
주인이 출발점이지만 `자녀 = [[안후지]]` 는 반대다. `PERSON_FIELDS` 의
세 번째 값(`OUT`/`IN`)이 그것이고, 테스트가 온톨로지와 대조한다.

### 1. 남은 ex 노드 288개 — 승격 가능한 것은 이미 다 했다
`promote` 를 다시 돌려도 0건이다. 남은 후보 293개(period 제외)의 정체는
이렇다.

```
no_article    248  위키백과에 문서가 없음 (‘내관 최숙’, ‘여종 존이’)
ambiguous      42  동음이의 문서 (‘선조’ 같은 목록 문서)
unverified     34  Wikidata 클래스 확인 실패
type_mismatch   6  타입이 어긋남
anachronism     3  연대가 어긋남 (동명이인)
```
**여기서 더 짜내려 하지 말 것.** 대부분은 문서가 존재하지 않는 단역이다.
늘리려면 승격이 아니라 **새 추출**(다른 시대·다른 코퍼스)이 답이다.

### 2. 남은 동명이인 보류 8건
전수 조사(`promote` 가 매번 돌린다)에서 **규칙으로 못 가른 것들**이다.
끝점 1,944개 중 21개는 옮겼고, 아래 8개는 그대로 뒀다.

```
이범석(8) ↔ 이범석(11), 이범석(2)      김구(6) ↔ 김구(7), 김구(5), 김구(1)
이서(6) ↔ 덕원군(11), 이서(2)          김성일(3) ↔ 김성일(4), 김성일(1)
```

**차수가 엇비슷한 후보를 자동으로 고르면 절반은 틀린다.** 김구가 셋인
것은 데이터가 틀린 게 아니라 실제로 사람이 셋인 것이다. 가르려면 문서
안의 다른 단서(생몰년·관직·동시 등장 인물)가 필요하다.

또 `임진왜란` 노드가 둘이다 — 라벨이 같고 QID 가 다르다.

```
wd:Q576338     엣지  1  · 위키백과 본문 13,454자
wd:Q122846639  엣지 20  · 설명 없음   ← 추출 엣지가 이쪽에 붙었다
```

병합 기계(`promote.merge_node`)는 이미 있으므로 **어느 QID 가 정본인지
정하는 규칙**만 세우면 된다. 설명이 있는 쪽? 차수가 큰 쪽? 이 사례는
둘이 엇갈린다.

### 3. 왕대 환산표
`신라 진흥왕대`·`조선시대 초기 15세기` 같은 표기 330건이 연표에 못 붙어
있다. **재료는 생겼다** — `histgraph reigns` 가 왕 45명의 재위를
held_position 엣지에 채웠으므로(2026-09-03), 남은 것은 `timeline.py` 가
'진흥왕대'를 그 임금의 재위 구간으로 옮기는 배선이다. 재위를 모르는 8명
(궁예·문무왕·무령왕·산상왕·지마 이사금·발해 고왕 등)은 **추정으로 채우지
말 것** — 틀린 연표가 만들어진다.

### 4. 화면에서 더 할 것 (급하지 않음)
- ~~**연표 보기**~~ — 2026-09-03 완료. 위 절 참고.
- **경로 찾기** — "숭례문에서 세종까지". `same_as` 를 건너는 경로가 실제로
  있다는 것이 이 프로젝트의 주장이므로 보여줄 값이 있다.
- 화면은 `data/{era}.sqlite` 를 읽는다. 파이프라인을 다시 돌렸으면
  `scope` 로 다시 뽑아야 반영된다.

### 5. 다른 시대 확장
`uv run histgraph scope goryeo --out data/goryeo.sqlite` 로 같은
파이프라인이 돈다. 다만 `wikipedia.EVENT_SEEDS` 가 조선에 치우쳐 있어
시대별 시드 보강이 먼저다.

### 6. 승격 관문 되돌리기 도구
`merged_from` 에 무엇을 어떤 근거로 합쳤는지 남아 있지만, 잘못 합친 것을
되돌리는 명령은 없다. 지금은 병합 정확도가 관문 네 개로 방어되고 있어
급하지 않다.

## 대규모 추출 전 반드시 확인할 것

과거 두 번 다 **첫 실행에서 배선 누락**이 드러났다. "기능을 만들었다"와
"그 경로를 실제로 탄다"는 다른 문제다.

```sh
# 1. 근거 검증이 켜져 있는가 (doc_text 가 넘어가는가)
grep -n "to_graph" src/histgraph/cli.py        # doc_text= 가 있어야 함

# 2. 가제티어가 범위 한정되는가
grep -n "build_gazetteer" src/histgraph/cli.py # scope_ids= 가 있어야 함

# 3. 소량 검증 (3문서, 약 10분)
uv run tools/check_extraction.py 3 mlx data/joseon.sqlite
```

네 지표를 본다: 관계 유형 분포(`participated_in` 이 나오는가) ·
가제티어 연결률 · 근거 충실도 · 조각당 속도.

## 파이프라인 실행 순서

```sh
uv run histgraph doctor                      # 소스 접근 진단
uv run histgraph ingest heritage --kinds 11 12
uv run histgraph ingest wikidata
uv run histgraph events                      # 핵심 사건 직접 수집
uv run histgraph enrich --types event --full # 사건은 본문 전체 (도입부로는 참여자가 안 나온다)
uv run histgraph enrich --limit 1200         # 나머지 타입의 위키백과 서사
uv run histgraph prune                       # 스포츠 제거 (필수)
uv run histgraph reclassify                  # 개념을 사건에서 갈라냄 (필수)
uv run histgraph works                       # 역사를 다룬 작품 명단 (분류 순회)
uv run histgraph enrich --types media --full # 작품 본문 — works 뒤에! (앞의 enrich 는 작품이 생기기 전이다)
uv run histgraph namu                        # 그래도 100자 미만인 작품은 나무위키 개요·줄거리로
uv run histgraph aliases                     # 한국어 별칭 (인포박스·승격 **앞에**)
uv run histgraph infobox                     # 사건 인포박스 — 날짜·별칭·참가자 (LLM 불필요)
uv run histgraph infobox --types person      # 인물 인포박스 (LLM 불필요)
uv run histgraph resolve                     # 소스 간 연결
uv run histgraph promote --prune-orphans     # 추출 고아를 실제 노드로
# 남은 것만 LLM 으로. 인포박스가 채운 사건은 --max-participants 가 걸러낸다
uv run histgraph extract --types event --max-participants 1 --min-chars 1500
uv run histgraph extract --scope data/joseon.sqlite --types person --limit 100
uv run histgraph promote --local-only        # 추출이 만든 고아를 다시 붙인다
uv run histgraph spans                       # 조직·왕조 존속 기간 (P571/P576)
uv run tools/backfill_event_polity.py --apply   # 사건의 정체 (P17)
uv run histgraph timeline                    # 연도 정규화 (승격 뒤에)
uv run histgraph scope joseon                # 시대별 서브그래프 (맨 끝)
uv run histgraph reigns                      # 왕의 재위 (P39 한정어 → 엣지 날짜)
uv run histgraph links                       # 사건끼리의 상하위·전후 (P361/P527/P155/P156)
uv run histgraph relabel                     # 영문 라벨 → 한국어 (표 덮어쓰기)
uv run histgraph redescribe                  # 영문 설명 → 한국어 (사전, 모르면 비움)
uv run histgraph --db data/joseon.sqlite relabel     # 시대 그래프에도 한 번 더
uv run histgraph --db data/joseon.sqlite redescribe  # 시대 그래프에도 한 번 더
uv run histgraph --db data/joseon.sqlite reigns      # (scope 를 다시 돌렸으면 불필요)
uv run histgraph serve                       # 화면 (조선 그래프를 읽는다)
```

**싼 것을 먼저, 비싼 것을 뒤에.** `enrich` → `infobox` → `extract` 순서가
그것이다. 셋 다 같은 자리를 채우려 하는데 비용이 세 자릿수로 다르다.

| 단계 | 비용 | 얻는 것 |
|---|---|---|
| `enrich --full` | 요청 1건/문서 | 산문. **이게 없으면 뒤의 둘이 읽을 게 없다** |
| `infobox` | 요청 1건/문서 | 날짜·별칭·참가자 — 구조에서 직접, 오독 위험 없음 |
| `extract` | **조각당 수 분** (로컬 MLX) | 산문에 흩어진 관계 |

**`infobox` 는 반드시 `enrich` 뒤에 둘 것.** 인포박스는 `props.kowiki_url`
이 있는 노드만 보는데 그 값을 채우는 것이 `enrich` 다. 앞에 두면 대상이
그만큼 적다 — 실측: `enrich --full` 로 kowiki_url 이 늘어난 뒤에 돌리니
사건 312건이 대상이 됐고, participated_in 이 조선 그래프에서 1,048 →
1,647 로 늘었다.

`promote` → `timeline` → `scope` 순서를 지킬 것. 승격이 노드를 만들고
지우므로 연표와 서브그래프는 그 뒤에 다시 만들어야 반영된다.

**`aliases` 는 `promote` 앞에 둘 것.** 승격이 이름으로 노드를 찾을 때
별칭을 보므로, 별칭이 없는 상태로 승격하면 붙을 수 있는 것이 고아로
남는다 (`정여립의 난` 이 그렇게 남았다 — 아래 절 참조). **별칭을 나중에
채웠으면 `promote --local-only` 를 한 번 더 돌린다** — 그것 말고는
이미 만들어진 고아를 되돌리는 것이 없다.

**인물 추출(`--types person`)은 메모리를 비우고 돌릴 것.** 위 `환경` 절
참조 — MLX 가 wired 45.7GB 를 잡아서 자리가 없으면 커널이 죽인다.

## Wikidata 날짜는 지저분하다 (2026-08-16 정리 완료)

`nodes.start_date` 에 날짜가 아닌 값이 섞여 있었다. 지금은 0건이지만
**다시 수집하면 되돌아온다** — `_iso_date` 를 고쳐 뒀으니 그 검증을 지우지
말 것.

- **blank node URL 100건** — Wikidata 는 '값 불명'을
  `http://www.wikidata.org/.well-known/genid/…` 로 준다. `T` 로만 잘라
  담고 있어서 날짜 칸에 URL 이 들어앉아 있었다 (침류왕·고국원왕·왕인…).
- **생년 > 몰년 15건** — `아신왕 0450 ~ 0405` 처럼 start 가 '몇 세기쯤'
  어림값이다. end 가 실제 몰년이므로 end 만 남겼다.

연대는 이 프로젝트에서 동명이인을 가르는 유일한 근거인데(`plausible_period`,
`lifespan_conflict`) 날짜 칸이 오염되면 **에러 없이 '연대를 모름'으로
처리되어 관문이 조용히 통과시킨다.**

**날짜를 문자열로 비교하지 말 것.** 기원전은 `-0057` 이라 `lstrip('-')`
후 비교하면 BC 57 이 BC 18 보다 뒤가 된다. 이 실수로 동명성왕·혁거세 등
6명의 정상 날짜를 지웠다가 되살렸다. `timeline._year_of` 로 **부호 있는
정수**를 얻어 비교할 것 — 프로덕션 코드는 이미 그렇게 한다.

## 되풀이하지 말 것

- **총계만 보고 판단하기.** `participated_in` 5,791건이 90.5% 올림픽이었다.
  엣지 수가 아니라 대상 노드의 라벨 분포를 봐야 한다.
- **가진 데이터를 확인하지 않고 말하기.** "산문에서 추출하면 된다"고 했지만
  가진 산문의 27%가 판본 치수였다. "신규식은 전체 그래프에 있을 것"이라고
  했지만 597개 중 0개였다.
- **이름이 같으면 같은 사람이라고 믿기.** 무오사화의 `한유` 를 문서명만
  보고 승격하면 당나라 문인 韓愈(768~824)에 붙는다. 연대를 대조할 것.
- **이름으로 노드를 찾을 때 아무거나 집기.** `LIMIT 1` 은 순서를 우연에
  맡긴다. 라벨·별칭 후보를 모두 모아 차수로 고르고, 시대가 갈리면 출처
  문서의 시대를 본다.
- **같은 조건을 두 겹으로 쌓고 걸러진다고 착각하기.** `worth_extracting` 이
  `min_score` 와 같은 조건을 검사해 404→404 로 하나도 안 줄었다.
- **정규식과 SQL LIKE 목록을 나란히 두기.** 반드시 갈라진다.
- **문자열 구간을 잘라 통째로 교체하기.** `to_graph` 를 고치다 그 사이의
  `narrative_score` 등 4개를 같이 지웠다.
- **데이터를 지우기 전에 대조하지 않기.** 날짜 정리 스크립트가 1,353건을
  바꿨는데 예상은 125건이었다. 다행히 668건은 진짜 손상이었고 잘못 지운
  것은 15건이라 되살렸지만, 확인한 방법은 **정리 전 스냅샷
  (`data/joseon.sqlite`)과 대조** 한 것뿐이었다. 지우는 스크립트는
  `--apply` 없이 계획부터 찍고, 바뀐 건수가 예상과 다르면 멈출 것.
- **정리 단계를 중간에 끼우기.** `relax_invalid_edges` 를 보강보다 앞에
  두면 그 뒤에 들어온 불일치 63건이 다음 실행까지 남는다.
- **중복을 데이터에서 지우기.** 같은 사실을 두 소스가 말하는 것은 오류가
  아니라 교차검증이다. 지울 곳은 저장소가 아니라 화면이다.
- **`ingest_log.ran_at` 을 현지 시각으로 읽기.** SQLite `datetime('now')`
  는 **UTC** 다. git 커밋 시각은 KST(+9)라 둘을 나란히 적으면 아홉 시간이
  어긋난다. 정여립의 난 표를 처음에 그렇게 적어 하루가 밀렸다
  (`08-30 20:25 UTC` = `08-31 05:25 KST`). 시각을 대조할 때는
  `datetime(ran_at,'+9 hours')` 로 맞출 것.
- **해소가 한 번 일어나고 끝난다고 두기.** 자료는 순서대로 오지 않는다.
  `정여립의 난` 은 추출이 만들 때 그 이름을 아는 노드가 없었고, 사흘 뒤
  별칭이 들어왔지만 아무도 다시 해소하지 않아 화면의 사건에 **주인공이
  없었다.** 별칭·노드를 새로 채웠으면 `promote --local-only` 를 다시 돌릴 것.
- **한 소스만 보고 "못 붙일 이름"이라 결론짓기.** 사건에 넘겨주기를 안
  따라가는 것은 옳은 규칙이지만, 그것 하나로 막히면 남는 길이 있는지
  봐야 한다 — 기축옥사는 별칭표가 이미 답을 갖고 있었다.
- **탐색이 모은 엣지를 그대로 서브그래프라고 부르기.** 그건 프론티어에
  닿는 엣지일 뿐이다. 노드 집합이 정해지면 **그 안의 모든 엣지**를 다시
  물어봐야 이웃끼리의 관계가 나온다. 안 그러면 화면이 별 모양이 된다.

# 화면 설계 — Obsidian 스타일

지금 코드가 실제로 하고 있는 것을 적는다. **바라는 모습이 아니라 현재
상태**다. 값이 바뀌면 이 문서도 같이 고친다 — 여기 적힌 숫자는 전부
파일에서 읽어 온 것이고, 색 수치는 `python3 tools/check_palette.py` 가
그때그때 다시 잰다.

2026-09-05 사용자 요청으로 화면을 **Obsidian 의 디자인 스타일**로 옮겼다
("우리 그래프 디자인을 obsidian 디자인 스타일로 변경해줘"). 그 전 화면
(따뜻한 검정 `#141413` 바탕, 파란 강조, 카드형 패널, 왼쪽 설정 사이드바)의
기록은 git 의 `494b94d` 판 이 문서에 있다.

한 줄로: **크롬은 회색 사다리, 색은 노드와 강조(보라) 둘뿐, 상자 대신 줄.**

두 번째 요청(같은 날)으로 [obsidian-better-graph-view](https://github.com/cr7pt0gr4ph7/obsidian-better-graph-view)
를 참고해 그래프 쪽을 다시 손봤다. 그 플러그인은 겉모습이 아니라 Obsidian
그래프의 **내부를 후킹해 빠진 기능을 채우는 것**이다 — 관계 종류로 선을
거르는 `edge:` 연산자, 문서의 `title` 속성을 이름표로, "지금 문서를
그래프에서 찾기". 우리 그래프는 관계에 종류가 있고 이름표는 이미 라벨이라,
옮긴 것은 **관계 종류 필터**와 Obsidian 그래프 설정의 네 절(필터·묶음·표시·
힘) 전부다 (§5).

설계의 배경과 실패 기록은 [README.md](README.md) "화면(`serve`)" 절에 있다.
여기는 **지금 화면이 어떤 규칙 위에 서 있는가**만 모아 둔다.

파일:

```
web/index.html            껍데기 (lang="ko", #root 하나, noscript 안내)
web/style.css             토큰과 모든 CSS (단일 파일)
web/doc.css               방침·약관 페이지 (읽기 모드)
src/histgraph/pages.py    /n/ 문서 페이지의 STYLE (읽기 모드, 서버 렌더링)
web/src/App.jsx           상태와 배선
web/src/components/       Search · SidePanel(그래프 설정 상자) · TimelinePanel · GraphCanvas · DetailPanel · Glyph · ChainPanel
web/src/lib/graph-view.js 3D 렌더러 + 팔레트 (React 를 모른다 — 3d-force-graph · three)
web/src/lib/layout.js     d3-force-3d 배치 계수
web/src/lib/timeline.js   연표 렌더러 (React 를 모른다)
web/src/lib/relations.js  관계를 한국어 문장으로
```

---

## 1. 전제

**그래프가 주인공이다.** 화면 전체가 어둡고 조용하다. 크롬(머리·패널·
테두리)은 전부 무채색 회색 사다리이고, 색은 노드와 강조색 둘뿐이다.

**왜 Obsidian 인가.** Obsidian 의 그래프 뷰는 "노트가 점, 링크가 선"인
화면이고 우리도 "개체가 점, 관계가 선"이다. 같은 문제를 푼 디자인이라
옮길 것이 많다:

- 바탕은 어둡고 무채색, 선은 가늘고 회색, 색은 점에만.
- 가리키면 이웃만 남고 나머지는 거의 사라진다. 가리킨 점의 선은 더 밝은 회색
  (Obsidian 은 강조색이지만 2026-09-05 사용자 결정으로 선은 회색만 쓴다).
- 설정은 캔버스 **위에 떠 있는 상자**(graph-controls)다. 사이드바가 아니다.
- 오른쪽 사이드바는 문서 한 장처럼 읽힌다 — 제목, 본문, 그 아래 백링크.
- 아래에 상태 줄이 있고 글자 수 같은 숫자는 거기 오른쪽 끝에 선다.

Obsidian 은 한 화면에 색 계열을 둘만 둔다(회색 사다리 + 강조색). 우리는
노드 타입 색이 아홉이라 그보다 시끄럽다. 그래서 나머지를 더 조용하게
눌렀다 — 선에서 색을 걷어냈고, 카드 상자를 없앴고, 강조색은 초점·링크·
누를 수 있는 것에만 쓴다.

**색만으로 뜻을 나르는 자리를 만들지 않는다.** 노드 색은 9종이지만 색약
시야에서 최악 쌍이 ΔE 5.5 다 — "다르다"이지 "나란히 놓지 않아도 읽힌다"가
아니다. 범례는 색 견본 옆에 타입 이름을 글자로 적고, 노드를 고르면 상세
패널이 타입을 글자로 말한다.

**화면에 영어와 자료 출처를 쓰지 않는다** (CLAUDE.md §1). 사람이 읽는 글자는
전부 한국어다. 유일한 예외는 설명 바로 아래 출처 한 줄(라이선스 의무).
`web/tests/render.test.mjs` 가 서버 렌더링 결과에서 영어가 새는지 실제로 잰다.

---

## 2. 토큰

`style.css` `:root`. Obsidian 기본 다크 테마(app.css)의 **이름과 값을
그대로** 쓴다. 이름을 새로 짓지 않은 이유: Obsidian 테마 문서를 열어 놓고
그 이름으로 찾으면 된다. **다크 전용**이다 (`color-scheme: dark`).

### 회색 사다리

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--color-base-00` | `#1e1e1e` | `--background-primary` — 캔버스·본문·입력칸·검색 결과 |
| `--color-base-10` | `#242424` | `--background-primary-alt` — 되돌아가기 줄 hover |
| `--color-base-20` | `#262626` | `--background-secondary` — 사이드바·머리 줄·상태 줄·설정 상자 |
| `--color-base-25` | `#2a2a2a` | (옛 `--surface-3` 다리) |
| `--color-base-30` | `#363636` | `--background-modifier-border` — 모든 1px 선, 훑기 막대 바탕 |
| `--color-base-35` | `#3f3f3f` | `--graph-line`, 테두리 hover |
| `--color-base-40` | `#555555` | 입력칸 초점 테두리, 인용·콜아웃 왼쪽 줄 |
| `--color-base-50` | `#666666` | `--text-faint` — 연도·개수·힌트·출처 줄 |
| `--color-base-70` | `#b3b3b3` | `--text-muted` — 보조 글자 |
| `--color-base-100` | `#dadada` | `--text-normal` — 본문 |

hover 는 색을 바꾸지 않고 **흰색을 7.5% 얹는다** (`--background-modifier-hover:
rgba(255,255,255,.075)`). 어느 바탕 위에서든 같은 만큼 밝아진다.

### 강조색

`hsl(254 80% 68%)` = `#8a6cef`, Obsidian 의 보라. 뜻은 하나다 — **"지금
이것"(초점) 또는 "누를 수 있다"**. 타입·종류·상태를 강조색으로 가르지 않는다.

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--color-accent` | `hsl(254 80% 68%)` | 링크 hover, 고른 노드 테두리, 재위 띠의 '지금 임금', 훑기 막대 표식, 체크박스 |
| `--color-accent-2` | `hsl(254 80% 78%)` | 가리킨(hover) 노드 테두리 |
| `--background-modifier-active-hover` | 강조 15% | 타고 들어온 관계 상자(`.d-via`), 눌린 아이콘 단추 |
| `--tag-background` / `--tag-border` | 강조 10% / 15% | 인과 종류 딱지(`.chain-kind`), 재위 띠 켬(`.tl-kings`) |

노드 팔레트(§3)는 파랑·크림·주황~노랑과 남색·초록이라 강조색(보라)과
겹치는 타입이 없다. 2026-09-06 전에는 단체가 연보라 `#c2a4ff` 라 고른
단체 노드가 보라 점에 보라 테두리였는데, 팔레트를 바꾸며 그 약점이 없어졌다.

### 글꼴·크기

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--font-interface` | `-apple-system, BlinkMacSystemFont, "Segoe UI", Inter, "Apple SD Gothic Neo", "Noto Sans KR", "Malgun Gothic", system-ui` | 전부. 캔버스도 같은 순서(`graph-view.js` `FONT`) |
| `--font-ui-smaller` | 11px | 개수·연도·출처 줄·힌트·범례 머리 |
| `--font-ui-small` | 13px | 기본 UI 글자, 검색, 관계 이름, 절 머리 |
| `--font-ui-medium` | 15px | 연표 머리의 연도 |
| `--font-ui-larger` | 20px | 상세 제목 |
| `--font-text-size` | 14px | 상세 본문. 문서 페이지는 16px |
| `--line-height-normal` | 1.5 | |
| 10.5px | | 연도·재위 연도·관계 이름표 (tabular-nums) |

Obsidian 은 Inter 를 싣는다. 우리는 싣지 않는다 — 한글은 어차피 시스템
글꼴이 맡고, 라틴·숫자는 시스템 글꼴(SF·Segoe)이 Inter 와 같은 결이다.
글꼴 파일 하나를 더 받게 할 만큼의 차이가 아니다.

### 모서리·그림자

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--radius-s` | 4px | 줄(hover 바탕), 칩, 콜아웃, 아이콘 단추, 훑기 막대 |
| `--radius-m` | 8px | 설정 상자, 캔버스 위 안내 칩 |
| `--radius-l` | 12px | 검색 결과 상자, 스크롤바 잡이 |
| `--input-radius` | 5px | 입력칸·셀렉트·단추 |
| `--pill-radius` | 2em | 타입 딱지, 다른 이름, 인과 종류, 연결된 연도, 재위 띠 단추 |
| `--shadow-s` / `--shadow-l` | Obsidian 값 그대로 | 설정 상자 / 검색 결과 |

**라이트 테마 (2026-09-06)**. 진실은 `<html data-theme>` 하나다. `public/theme-boot.js`
가 CSS 보다 먼저 저장값(`localStorage.theme`)이나 운영체제 설정을 읽어 박고 —
React 뒤에 바꾸면 어두운 화면이 한 번 번쩍인다 — 머리 줄의 `.theme-toggle`
(`src/lib/theme.js`)이 바꾼다. CSS 는 `:root[data-theme="light"]` 에서 **회색
사다리와 반투명 값만** 뒤집는다. 뜻을 가진 이름은 사다리를 가리키므로 저절로
따라온다. **값은 Obsidian 라이트가 아니다** — 그대로 옮겼더니 "시인성이 너무
떨어진다"고 지적받았다 (옅은 글자 #ababab 대비 2.3, 테두리 #e0e0e0 대비 1.3).
GitHub Primer·Radix gray·Tailwind 를 실측해 셋의 공통 수준으로 맞췄다: 본문
`#1f1f1f`, 보조 `#555555`, 가장 옅은 글자도 `#767676`(4.5:1), 테두리 `#d0d0d0`,
사이드바 `#f2f2f2`, 강조 hsl(258 80% 56%). 3D 그래프는 CSS 변수를 못 읽어
`graph-view.js` 의 `DARK`·`LIGHT` 표가 같은 값을 들고(선 `#9a9a9a`, 가리킨 선은
글자 무게), 노드 색은 `TYPE_COLOR_LIGHT` — **색상은 그대로, 밝기만 뒤집었다**
(크림 → 황갈, 노랑 → 어두운 노랑, 연빨강 → 원색 D62828, 남색 → 원색 003049,
뼈대(시대·직위)는 옅어져 물러나되 2.5:1 은 지킨다). 인물 파랑·사건 주황·재위 띠
파랑은 두 바탕에서 다 읽혀 그대로다. 방침·약관(`doc.css`)도 같은 선택자로 따라온다.

그림자는 **떠 있는 것**에만 쓴다. 사이드바와 목록에는 없다 — Obsidian 은
층을 그림자가 아니라 바탕색 한 단(secondary)과 1px 선으로 가른다.

스크롤바는 Obsidian 처럼 12px 폭에 트랙 없이 잡이만 (흰색 10%, hover 20%,
투명 테두리 3px 로 가늘게).

### 옛 이름 다리

`timeline.js` 가 `var(--line)`·`var(--surface-2)` 를 SVG 문자열 안에서
직접 쓴다. `:root` 끝에 `--surface·--surface-2·--surface-3·--line·--text·
--text-2·--text-3·--accent·--font` 를 새 토큰에 이어 두었다. 새 코드는
Obsidian 이름을 쓴다.

---

## 3. 노드 색 — 색이 타입을 말한다

모양은 하나(원)로 두고 **색 아홉이 타입을 말한다.** Obsidian 의 "그룹"
색칠에 해당한다. 한때는 색을 갈래 넷으로만 쓰고 세부 타입을 모양(육각형·
마름모·사각형…)으로 갈랐는데, 모양을 걷어내고 색이 그 짐을 다 지도록 바꿨다.

색은 2026-09-06 사용자가 준 여섯 색 팔레트(Coolors "skafold")에서 가져온다:
초록 `004F2D` · 남색 `003049` · 빨강 `D62828` · 주황 `FB6C13` · 노랑 `F9C80E`
· 크림 `F5E0B7`. 여섯으로 아홉 타입을 다 못 덮으므로 **색상은 팔레트에서만
가져오고 밝기로 갈랐다.** 주황·노랑·크림은 원색 그대로다. **인물만 예외로
팔레트 밖의 파랑 `#3d84f5`** 다 — 팔레트를 씌운 뒤 사용자가 "인물 노드도
blue 로" 라고 했다 (2026-09-06). 연표의 재위 띠와 같은 파랑이다. 빨강
`D62828` 은 원색으로 쓰이는 자리가 없고 작품(연빨강)에 밝힌 값으로만 남는다. 남색과
초록은 원색 그대로 두면 바탕 `#1e1e1e` 위에서 대비가 1.2·1.7 이라 작은 원이
보이지 않았다 (그려서 확인했다). 그래서 같은 색상으로 밝힌 값을 장소·영상에
쓰고, 원색에 가까운 어두운 값을 물러나 있어야 할 뼈대(시대·직위)에 뒀다.
색약 검증기는 눈금 5.0 을 넘긴다 (정상 10.7 · 2형 9.7 · 1형 7.3).

그 전(2026-09-05)에는 Obsidian 의 색 이름(blue·purple·orange·green·yellow·
pink·cyan)에 맞춰 골랐었다.

`web/src/lib/graph-view.js` `TYPE_COLOR`:

| 타입 | 화면 이름 | 색 | 갈래 |
|---|---|---|---|
| `person` | 인물 | `#3d84f5` 파랑 (팔레트 밖, 사용자 결정) | 인물·단체 |
| `org` | 단체·국가·왕조 | `#f5e0b7` 크림 (원색) | 인물·단체 |
| `event` | 사건 | `#fb6c13` 주황 (원색) | 사건 |
| `place` | 장소 | `#2e9e5e` 초록 (`004F2D` 를 밝힘) | 장소·유물 |
| `heritage` | 유물·문화재 | `#f9c80e` 노랑 (원색) | 장소·유물 |
| `artwork` | 예술작품 | `#f2a0a0` 연빨강 (`D62828` 을 밝힘) | 장소·유물 |
| `media` | 영화·드라마 | `#3a7ca5` 남색 (`003049` 을 밝힘) | 장소·유물 |
| `period` | 시대 | `#2a5d78` 어두운 남색 | 시대·직위 |
| `role` | 직위·칭호 | `#1e6b45` 어두운 초록 | 시대·직위 |

`concept`(개념·주제)는 **색을 따로 갖지 않는다.** `GROUP_COLOR.frame`
(`#2a5d78`)으로 물러난다 — 아홉 색도 흩어진 작은 원에서는 구별이 빠듯해서
열 번째를 만들지 않았다.

### 타입 안의 갈래 — 씨족의 파는 색을 받지 않는다 (2026-09-10)

전주 이씨·김해 김씨의 **본관과 파**를 세웠다 (`clans.py`, 파 248개). 새 타입을
만들지 않았으므로 **새 색도 없다** — `org` 크림 그대로다. 표식은
`props.kind == "clan"` 과 `props.clan_level`(`본관`·`파`)이다.

색을 뽑지 않은 까닭은 팔레트가 좁아서가 **아니다.** 재 보면 자리는 있다 —
열 번째 색을 가장 잘 골랐을 때(바탕 대비 2.5:1 이상에서 훑어 `#4040f0`) 기존
아홉과의 최악 거리가 ΔE **14.1** 이라 눈금 5.0 은 물론 지금 최악 쌍(1형 7.3)도
안 건드린다. 그런데도 안 준다: **색상은 타입이 쥔 부호이고 파는 타입이 아니다.**
색을 주면 화면에만 열 번째 타입이 생겨 "조선총독부와 덕천군파는 다른 종류"라고
말하게 되는데, 온톨로지는 둘 다 `org` 라고 답한다. `media` 의 `form`
(영화·드라마·책·게임)이 이미 같은 판정을 받았고 색을 못 받았다. 그리고 그
가장 좋은 자리는 파랑–보라 사이라 이미 뜻이 있다 (파랑 = 인물·재위·원인,
보라 = 지금 이것·누를 수 있음).

대신 **갈래를 옮겼다.** 갈래(`group`)가 나르는 것은 색이 아니라 '얼마나 앞에
세우나'다 (팔란티어의 prominent / normal / hidden). 파는 날짜가 없고 사건에
참여하지 않으며 하는 일이 '누구를 담는가' 뿐이라 시대·직위와 같은 틀이다 —
`server._group()` 이 `frame` 을 준다. 화면에서 달라지는 것은 둘:

| | 여느 단체 (조선총독부) | 본관·파 (덕천군파) |
|---|---|---|
| 그래프 색 | 크림 `#f5e0b7` | **같은 크림** |
| 그래프 밑동 반지름 | 6 (+√차수×1.7) | **4** — 차수 1인 파가 5.7 |
| 타입 딱지 | `단체·국가·왕조` | **`분파` · `본관`** (`ontology.type_label`) |
| `/n/` 장의 색 점 | `--actor` | `--frame` (그 장은 늘 갈래 넷으로 그린다) |
| 범례 | `단체·국가·왕조` 한 줄에 함께 센다 | |

**범례는 늘리지 않았다** — 범례는 타입의 표이고(색 견본 하나에 줄 하나), 파는
타입이 아니다. 같은 크림 점이 두 줄로 서면 색 견본이 무엇을 가리키는지가
흐려진다.

**관계는 라벨이 이름을 준다.** `clans.py` 가 엣지에 적어 두는 라벨 셋이 화면
문장의 재료다 (`server.LABEL_HEADS`·`LABEL_DIR_HEAD` = `relations.js` 의 같은 표
= `pages._head`):

| 엣지 | 라벨 | 묶음 머리 (나감 / 들어옴) | 문장 |
|---|---|---|---|
| `member_of` 파조 → 파 | `파조` | 파조 / 파조 | 덕천군은 덕천군파의 파조다 |
| `part_of` 파 → 상위 | `분파` | 속한 문중 / 갈라진 파 | 덕천군파는 전주 이씨에서 갈라져 나온 파다 |
| `related_to` 본관 → 지명 | `본관` | 본관 지명 / 이곳을 본관으로 하는 씨족 | 전주 이씨의 본관은 전주시다 |

라벨이 타입 이름을 이기는 자리에서 **타입 제한을 걷었다.** 전에는 참여·관련만
라벨로 불렀는데, 이기는 것은 타입이 아니라 표에 적힌 라벨이다 — '파조'는
'소속'보다, '분파'는 '상위'보다 정확하다. 표에 없는 라벨('언급'·'원인'·'출생')은
그대로 지나간다.

**족보에 오른 자손 수(`props.members`)는 화면에 세우지 않는다.** 이미 설명
문장이 한 번 말했고(“…자손이 68,936명이다”), 관계 줄에 수를 붙이면 그 수가 '이
관계의 세기'로 읽힌다. 세어서 나온 수라 자격은 있으므로 **차례에만** 쓴다 —
전주 이씨 상세의 '갈라진 파 · 111' 은 완풍군파(124,001명)부터 선다. 무게를
차례에만 쓰는 것과 같은 자리다 (§10).

**파는 연표에 서지 않는다** — 날짜가 없다. 연표는 `연도를 알 수 없습니다` 라고
적고 그 시대의 큰 사건만 세운다 (이미 있던 길).

### 만든 방식이 관계의 이름이다 — `created` 의 여섯 라벨 (2026-09-10)

작품과 만든 사람을 잇는 `created` 엣지를 세웠다 (`creators.py` · 화면 DB 250건 ·
원본 504건). **새 타입도 새 색도 없다** — 이미 있던 엣지 타입이고, 는 것은
라벨과 문장뿐이다. 라벨은 여섯이고 판정은 사람이 표에 적는다
(`data/creators.tsv` — 초상의 주인공도, 간행을 명한 임금도 만든 사람이 아니다).

| 라벨 | 묶음 머리 (사람 쪽 / 작품 쪽) | 문장 | 실측 |
|---|---|---|---|
| `그림` | 그린 것 / 그린 사람 | 정선이 인왕제색도를 그렸다 | 29 |
| `글씨` | 글씨를 쓴 것 / 글씨를 쓴 사람 | 김정희가 세한도의 글씨를 썼다 | 48 |
| `저술` | 지은 것 / 지은 사람 | 허준이 동의보감을 지었다 | 78 |
| `편찬` | 엮은 것 / 엮은 사람 | 신숙주가 동국정운을 엮었다 | 67 |
| `제작` | 만든 것 / 만든 사람 | 장영실이 자격루를 만들었다 | 15 |
| `발원` | 만들게 한 것 / 만들게 한 사람 | 문정왕후가 회암사명 약사여래삼존도를 만들게 했다 | 13 |

전에는 여섯이 다 "X가 Y를 만들었다" 한 문장이었다 — 타입 이름 '제작' 하나로
읽었기 때문이다. **그린 것과 쓴 것과 지은 것은 다른 일이다**: '정선이
인왕제색도를 만들었다'는 틀린 말에 가깝고, 발원은 손으로 만든 사람이 아예
아니다. 라벨이 있으면 라벨이 타입 이름을 이긴다는 규칙(씨족의 파조·분파·본관과
같은 자리)을 그대로 쓴다.

**방향으로 갈라 부른다.** 사람 상세에서는 그가 만든 것들의 목록이고, 작품
상세에서는 만든 사람이다 — `server.CREATOR_DIR_HEAD` 가 `LABEL_DIR_HEAD` 에
합쳐지고 `LABEL_HEADS` 는 그 표의 열쇠를 그대로 쓴다 (손으로 두 번 적지 않는다).
같은 표가 세 벌 있다: `server.py` · `pages.py` · `web/src/lib/relations.js`.
한 군데만 고치면 나머지가 '제작'·'관련'으로 물러난다.

- 그래프의 선 이름: `제작` → **`그림`·`글씨`·`저술`·`편찬`·`제작`·`발원`**
  (`server.graph` 가 `LABEL_HEADS` 에 있는 라벨을 선 이름으로 쓴다).
- 연표 딱지: `_rel_name` 이 방향으로 갈라 `specific` 을 달아 보낸다
  (`글씨` → 사람 쪽에서 '글씨를 쓴 것').
- 상세 묶음: `relations.js relHead` · `pages._head` — 김정희의 상세에 '글씨를
  쓴 것 · 12' 와 '지은 것 · 3' 이 따로 선다.
- 차례: `RELATION_ORDER` 는 그대로다. `created` 는 이미 다섯째라 작품 상세에서
  만든 사람이 첫 묶음으로 선다 (실측: 목민심서·역대병요에서 index 0).
- 라벨 없는 옛 엣지(산문 추출 17건)는 '제작'·"만들었다"로 남는다.

관문은 셋이다 — 만든 방식마다 방향별 이름이 있는가(`creators.ROLES` ==
`server.CREATOR_DIR_HEAD`) · 문장 규칙이 있는가(`relations.CREATED_SENTENCE`) ·
세 벌의 `LABEL_HEADS` 가 같은가 (`tests/test_pipeline.py`).

갈래는 **색상 계열**로 남는다. 인물·단체는 파랑·크림, 사건은 주황,
장소·유물·작품은 초록~노랑~연빨강~남색, 시대·직위는 어두운 남색·초록. 화면이
먼저 네 덩어리로 읽히고 그 안에서 타입이 갈린다.

로고 표식(`style.css` `--mark-*`)과 문서 페이지의 `--actor·--event·--thing`
은 이 표의 person·event·place 를 베낀 것이다 — **색을 바꾸면 네 곳**
(graph-view.js · style.css · doc.css · pages.py)과 `render.test.mjs` 의
색값 두 곳을 같이 바꾼다.

### 색 거리는 재서 안다

`python3 tools/check_palette.py` 가 `graph-view.js` 에서 팔레트를 **직접
읽어** 잰다 (OKLab ΔE ×100, 색약 시뮬레이션은 Viénot 1999). 눈금은 5.0.
2026-09-05 실측:

| | 정상 시야 | 2형(deutan) | 1형(protan) |
|---|---|---|---|
| 타입 9색 | 10.9 (org↔artwork) | **6.8** (event↔place) | **5.5** (event↔place) |
| 갈래 4색 | 23.4 | 6.8 (event↔thing) | 5.5 |

옛 팔레트(10.7 · 5.9 · 5.7)와 같은 급이다. 3형(tritan)은 도구가 함께 찍지만
**판정에 쓰지 않는다** — Viénot 의 방법은 1·2형에만 유효하다.

**색을 고치면 이 검증기를 다시 돌린다.** 위 숫자는 그때의 기록일 뿐이고
실제 값은 검증기가 말한다.

---

## 4. 그래프 — 3D (`graph-view.js` · `3d-force-graph`)

**2026-09-11 사용자 결정으로 2D 캔버스 렌더러를 걷어내고 3D 로 갈아탔다**
("3d-force-graph를 이용해서 우리 그래프를 3d로 만들어줘"). 전환 단추는 두지
않는다 — 3D 하나다. 한국사 그래프와 내 역사 그래프가 **같은 `GraphView`** 를
쓰므로 한 곳을 고치면 둘 다 바뀐다.

**뜻은 그대로고 표현만 옮겼다.** 색상 = 타입 · 크기 = 차수 · 굵기 = 인과 ·
선 무늬 = 출처의 확실성 · 링 = 상태 · 조명 = 가리킨 노드와 직접 이웃 ·
화살촉 = 방향. 옮기며 다시 정한 것 셋은 아래에 이유를 적었다 (그리는 규칙의
정본은 `.claude/agents/graph-drawer.md` §3~§5·§12.23~25).

React 가 그리지 않는다 — React 는 자리를 잡아주고 `destroy()` 로 수명만
관리한다. **엔진은 늦게 싣는다**: `3d-force-graph` 는 Node 에서 import 가
터지는데(window 가 없다) 배치 검증과 서버 렌더링 검증이 이 파일을 Node 에서
그대로 읽는다. 그래서 생성자가 `import('3d-force-graph')` 로 미루고, 그
사이에 들어온 `setData`·`select`·`setDisplay` 는 상태로 남았다가 엔진이
오면(`_attach`) 통째로 실린다.

three 는 CSS 변수를 못 읽으므로 값을 상수로 한 번 더 들고 있다 —
**`style.css` 를 고치면 여기도 고친다.**

| 상수 | 값 | = 토큰 |
|---|---|---|
| `surface` | `#1e1e1e` | `--background-primary` (바탕·이름표 외곽선) |
| `edgeBase` | `#4a4a4a` | `--graph-line` 보다 한 단 밝다 |
| `edgeSoft` | `rgba(74,74,74,.35)` | 조명 밖으로 물러난 선 |
| `edgeSame` | `#3f3f3f` | same_as — 관계가 아니라 이음이라 더 어둡다 |
| `edgeLit` / `edgeLitSame` | `#a8a8a8` / `#7a7a7a` | 가리킨·고른 노드에 붙은 선. 보라를 쓰지 않는다 (2026-09-05 사용자 결정) |
| `accent` / `accentSoft` | `#8a6cef` / `#af9af4` | `--color-accent` / `--color-accent-2` — 상태 링에만 |
| `causeLit` | `#8cc4ea` | `--color-blue` 계열 — 인과 도면의 선 |
| `text` / `textDim` | `#dadada` / `#9a9a9a` | `--text-normal` / `--text-muted` 무게 |

**자는 월드 단위다.** 노드 반지름 4~17, 링크 거리 112. 2D 의 픽셀 값을 그대로
쓰면 선이 통나무가 되므로 아래 값으로 옮겼다.

| 것 | 값 | 왜 |
|---|---|---|
| 노드 반지름 | `base(frame 4 · 나머지 6) + min(11, √차수 × 1.7)` × nodeScale | 라이브러리가 `∛val × nodeRelSize` 를 반지름으로 쓰므로 `nodeVal = r³`, `nodeRelSize = 1` |
| 선 굵기 | 0.7 (same_as 0.5, 인과 +0.8, 도면 1.15) × lineScale, 하한 0.2 | 0 이면 관이 실(THREE.Line)이 되어 굵기도 무늬도 못 싣는다 |
| 파선 마디 | 추출 7 · same_as 16 | 2D 의 `5 4` / `2 4` 를 마디 밀도로 옮긴 것 |
| 화살촉 | 길이 5.5, 도착 노드 표면(`linkDirectionalArrowRelPos(1)`) | 라이브러리가 반지름을 빼고 세운다 — 2D 의 `r + 3` 자리 |
| 상태 링 | 반지름 + 4.5 의 스프라이트 | 고리는 옆에서 보면 사라진다. 스프라이트는 늘 카메라를 마주 본다 |
| 이름표 | **화면에서 늘 초점 12.5px · 나머지 11px · 둘째 이름 9.5px** (2D 값 그대로), 바탕색 외곽선 | `three-spritetext`. 스프라이트는 멀어지면 작아지므로 거리마다 다시 키운다 (2D 의 `/k`) |
| 조명 밖 | α 0.16 | 2D 의 `globalAlpha` 그대로 |
| `focusOn` 거리 | 240 | 맞춤이 도는 중이면 손대지 않는다 |
| 맞춤 여백 | 90 | 20틱째 첫 맞춤(즉시), 그 뒤 60틱마다 0.7초 전환, 멈추면 한 번 더 |
| 맞춤 거리 | 우리가 잰다 (`_fit`) | 노드 자리+반지름만 세고 네 번 다듬는다. 실측(노드 120·반지름 650 구름·1084×737): 라이브러리의 `zoomToFit` 2308 → **1686**, 세로 채움 87% → **100%** |

| 상태 | 그림 |
|---|---|
| 고른 노드 | `accent` 링 |
| 그래프 중심 | 흰색 45% 링 |
| 가리킨 노드 | `accentSoft` 링 |
| 조명 밖 | 알파 0.16 (색상은 그대로 — 타입을 잃지 않는다) |

**3D 로 옮기며 다시 정한 셋.**

1. **확실성(파선)은 관에 무늬를 입혀 지킨다.** 3D 의 선은 원기둥이라
   `setLineDash` 가 없다. 다른 부호로 옮길 자리도 없었다 — 색상은 타입이,
   굵기는 인과가, 투명도는 조명이 이미 쥐고 있다. 그래서 원기둥의 uv 가 길이
   방향으로 0→1 인 것을 이용해 **줄무늬 알파맵**을 되풀이해 마디를 낸다
   (`_dashMaterial`, 재질은 색 × 마디 수로 캐시). 굵기가 그대로 사는 것이
   중요하다 — 실측 2026-09-11 인과 엣지 2,604건 중 **2,575건이 `conf < 1`**
   이라, 파선을 가는 실로 그렸으면 인과의 굵기가 화면에서 사라졌을 것이다.
2. **굵기는 인과 하나만 진다.** 2D 에서는 가리킨 선도 굵어졌지만(1.1 → 1.8px),
   3D 에서 조명은 **밝기**가 말한다. 한 부호에 뜻 하나.
3. **깊이에는 아무 뜻도 싣지 않는다.** 배치가 3차원으로 퍼지는 것 자체가
   관계의 가까움이다. 앞뒤에 시간·인과·중요도를 실으면 **돌려보는 순간 그 뜻이
   사라진다** — 카메라를 사용자가 쥐고 있는 화면에서 카메라에 기대는 부호는
   부호가 아니다. 인과 도면도 그래서 평면(z=0)에 세운다.
4. **이름표는 화면 크기를 지킨다.** 스프라이트는 멀어지면 작아지는데, 노드
   120개짜리 그래프가 화면에 꽉 찬 거리(≈1700)에서 8.5 짜리 글자는 4px 이
   되어 아무도 못 읽는다 (2026-09-11 지적: "첫 화면에 이름표가 하나도 없다").
   그래서 거리마다 다시 키워 늘 11px 이게 한다 — 2D 가 `/k` 로 하던 일이고,
   무엇을 세울지는 그 다음에 겹침이 정한다.

**이름표는 자리가 있으면 붙이고 없으면 만다** (2D 와 같은 규칙, 자만 3D 가
되었다). 노드를 카메라로 화면 좌표에 옮겨 글자 상자가 겹치는지 **픽셀로** 잰다
(3프레임에 한 번). 우선순위 `(초점 3 · 중심 2) + min(차수/40, 1)`, 초점·중심은
겹쳐도 무조건, 한 번 뜬 이름은 3px 더 겹쳐야 물러난다(깜빡임 방지), 카메라
뒤로 넘어간 노드는 세지 않는다. 실측(노드 120 · 1084×737): 이름표 61개.

**멀면 흐려진다** — `labelAlpha(거리, textFade, 꽉 차는 거리)`. 2D 의 배율
자리에 카메라 거리가 들어왔는데, **자는 절대 거리가 아니라 그 그래프가 화면에
꽉 차는 거리**(`_fitDist`)다. 그래프마다 크기가 다르고 처음 열리는 거리도 그만큼
다르다 — 절대 거리(1000)를 걸었더니 노드 120개짜리 첫 화면이 2308 에서 열려
이름표가 하나도 안 떴다 (2026-09-11 지적). 꽉 차는 거리를 1 로 놓으면 fade 0 은
1.35배까지, 기본 0.3 은 1.1배까지 또렷하고 1.6배에서 사라지며, 1 이면 절반
거리까지 다가가야 뜬다. **첫 화면에는 늘 이름이 있다.**

**카메라 맞춤도 직접 잰다** (`_fit`). 라이브러리의 `zoomToFit` 은 노드에 붙인
이름표·상태 링까지 상자에 넣고 상자의 긴 변을 `atan` 으로 나눠 늘 멀찍이
선다 — 실측 2026-09-11: 반지름 650 짜리 구름에 카메라가 2308 에 서서 그래프가
1084×737 화면의 가로 350px 만 썼다(노드가 2~6px 이라 '크기 = 차수'가 안 읽혔다).
우리는 **노드의 자리와 반지름만** 세고, 지금 보고 있는 쪽을 지킨 채 거리만
고친다: 노드 하나가 화면에 들려면 `거리 ≥ 깊이 + 어긋남/tan(시야각 절반)` 이고
그중 가장 먼 것이 답이다. 그러고 **네 번 다듬는다** — 원근에서는 가까운 노드가
더 크게 벌어져 구름의 그림자가 한가운데에 안 오므로(다듬기 전에는 아래가 여백에
딱 닿고 위가 73px 남아 세로를 87%만 썼다), 실제로 찍어 보고 어긋난 만큼 바라보는
곳을 옮기고 거리를 곱한다. 결과: 거리 1686, 세로 채움 100%.

**관계 이름은 그리지 않는다.** 2D 는 가리켰을 때 선 중점에 적었는데, 3D 에서
선 위의 글자는 카메라가 돌면 선을 벗어나 어느 선의 이름인지 사라진다. 그 말은
상세 패널이 문장으로 한다 (§7).

**라이브러리가 내는 영어를 끈다** (CLAUDE.md §1): 안내 글 `showNavInfo(false)`,
툴팁 `nodeLabel(() => '')`·`linkLabel(() => '')`. 이름은 노드에 붙은 이름표가
한국어로 말한다.

**설정이 렌더러에 닿는 길.** `display = { nodeScale, lineScale, textFade,
arrows }` · `hiddenEdgeTypes` · `forces = { center, repel, link }`. 각각
`setDisplay`·`setEdgeFilter`·`setForces` 로 들어온다. 손잡이는 3D 에서도 다 뜻을
갖는다 — 흐림 문턱만 배율이 아니라 거리에 걸린다. **다시 거는 자리를 갈라
두었다**: 조명이 만지는 것(`_relight` — 색·화살촉·상태 링)과 크기가 만지는 것
(`_resizeNodes` — `nodeVal`·`linkWidth`)이다. 뒤엣것은 라이브러리가 노드·선
객체를 통째로 **다시 만들므로** 슬라이더가 움직일 때만 지난다.

**조명.** 가리키는 동안은 hover 가, 손을 떼면 골라 둔 노드가 이어받는다. 빈
곳을 누르면 꺼진다. 조명이 켜지면 그 노드와 직접 이웃만 밝고, 나머지 선은
`edgeSoft` 로 물러난다.

**몸짓.** 궤도 회전·확대는 라이브러리(OrbitControls). 노드 한 번 누르기 =
고르기, **320ms 안에 두 번 = 펼치기**(3D 라이브러리에는 더블클릭 신호가 없다),
끌기 = 못박기(`fx/fy/fz` — 라이브러리는 드래그가 끝나면 못을 뽑으므로
`onNodeDragEnd` 에서 다시 박는다), 빈 곳 누르기 = 조명 끄기.

**노드를 누르면 캔버스가 인과 도면이 된다** (`showCausal` · `causalLayout`).
**지금은 꺼져 있다** — `App.jsx` `CAUSAL_DIAGRAM = false` (2026-09-06 사용자:
"인과관계 그래프를 지우진 말고 기능 꺼두자"). 꺼진 동안 클릭은 전처럼 주변
관계를 열고, 이웃 그래프의 인과 선은 굵은 회색이며 범례도 글자색이다. 켜면
아래대로 움직인다. 2026-09-06 사용자 요청의 자취: "노드를 클릭하면 인과관계를
보여주는 그래프를" → 이웃 위에 파란 사슬을 얹었더니 "처음부터 보여주지
말고(그럼 너무 복잡해 보임)" → 누른 뒤에만 얹고 나머지를 흐렸더니 "너무
복잡하게 그려지고 있어서 아무런 정보값이 없어. 팔란티어나 다른 회사의
그래프 디자인을 검색해서 공부해서 수정해봐". 찾아본 셋이 같은 말을 했다:

- 팔란티어 Vertex 는 그래프 배치에 **계층(좌→우)** 을 따로 두고 뿌리 노드를
  골라 엣지 방향대로 층을 세운다 (Foundry 문서 "Graphs display options").
- Cambridge Intelligence(KeyLines)는 힘 배치가 전체 모양만 보여주므로 흐름이
  있는 자료는 **순차 배치(sequential layout)** 로 층을 나눠야 얽힘이 준다.
- 사건 인과 시각화 논문들(VAC2·DOMINO)은 인과 나무를 **시간축을 따라 원인 →
  결과의 흐름도**로 그리고, 고른 사건에서 출발해 그 사건으로 이어지는 경로만
  **점진적으로** 펼친다.

공통은 하나다 — 인과는 방향이 뜻이므로 **층으로 세우고, 그 밖의 관계는
그리지 않는다.** 그래서:

- **처음 열릴 때**(부팅·검색·주소·시작점)는 주변 관계 그래프 그대로. 인과도
  회색이다.
- **캔버스에서 노드를 누르거나 상세·연표에서 고르면** `App.openCausal` 이
  `/api/chain` 을 **두 걸음**만 받아(`depth=2` — 네 걸음은 심하전투 원인 31개가
  한 화면에 겹쳤다) `GraphView.showCausal` 로 도면을 세운다. 주변 관계
  그래프는 접어 두고(`_saved`) 빈 곳을 누르면 그대로 돌아온다(`exitCausal`).
  인과가 없는 노드(대부분의 인물)는 전처럼 주변 관계를 편다. 도면 안의 노드를
  누르면 그 노드의 도면으로 이어 간다 — 이것이 점진적 탐색이다. 더블클릭은
  도면을 접고 그 노드의 이웃을 편다. Esc 도 접는다.
- **배치**(`causalLayout`): 가운데 0열이 고른 노드, 왼쪽이 원인, 오른쪽이
  결과. 열은 수기야마(Sugiyama) 층 매기기의 **가장 긴 경로**다 — 한 노드가
  여러 가지에 나와도 한 번만 서고, 모든 선이 왼쪽에서 오른쪽으로만 간다
  (가까운 열에 두면 후금 → 정묘호란이 같은 열 안에 서고 방향이 사라진다).
  열 안은 연도순, 모르면 뒤. 순환은 되돌아오는 선을 층에서 뺀다.
- **3D 에서도 도면은 평면이다** (2026-09-11): 열 간격 230 · 줄 간격 58 의
  격자를 `z = 0` 평면에 세우고 노드를 **못박아**(`fx/fy/fz`) 힘이 못 건드리게
  한 뒤 카메라를 정면에 둔다. 돌려보면 평면인 것이 보이고, 그것이 참이다 —
  층 배치가 말하는 것은 걸음 수뿐이라 깊이에 실을 뜻이 없다. 화면 크기로
  간격을 다시 재던 2D 의 셈은 없앴다: 카메라가 거리를 맞춘다.
- **그리기**: 선은 파랑 `causeLit`, 추출은 파선 무늬, 화살촉은 도착 노드 앞.
  노드를 가리키면 그 노드를 지나는 경로만 밝다(`causalReach`). 열 머리
  ('원인'·'원인의 원인'·'더 앞선 원인' / '결과'·…)를 도면 위에 흐리게 띄워
  자리만으로 뜻을 나르지 않는다. 이름은 전부 쓰고 아랫줄에 연도(`names[1]`).
- 안내 칩: `심하전투의 인과 · 원인 16 · 결과 11 · 노드를 누르면 그 노드의
  인과로, 빈 곳을 누르면 주변 관계로`.

**도면이 자료 오류를 드러낸다** — 심하전투(1619)의 결과로 표전문제(1395)·
종계변무(1589)가 섰다. 연대 관문(`chronology`, CLAUDE.md §1-5)의 몫이다.

검증용 손잡이: `GraphCanvas` 가 `window.__histgraphView` 에 뷰를 건다. 화면
코드는 쓰지 않는다 — CDP 스크립트가 노드를 눌러 보는 데 쓴다. 3D 에서는
`x·y` 만으로 화면 자리를 알 수 없으므로(카메라가 정한다) **`screenAt(id)`**
가 컨테이너 안의 픽셀을 준다 (카메라 뒤로 넘어갔으면 `null`).

**엣지는 회색 한 가지다.** 전에는 출발 노드의 타입 색을 입혔다("사람이 건
관계인지 사건이 건 관계인지가 한눈에 갈린다"). 걷어낸 이유: 색이 점과 선에
다 있으면 화면이 아홉 색으로 뒤덮여 점이 안 읽힌다. 방향은 화살촉이 말한다
(설정에서 끌 수 있다 — Obsidian 의 '화살표'). 조명 안(가리킨·고른 노드에
붙은 선)만 **밝은 회색**(`edgeLit`), 나머지는 `edgeSoft` 로 물러난다. 선에는
강조색(보라)을 쓰지 않는다 — 2026-09-05 "에지 컬러가 보라색인데 회색으로".
파랑은 위의 인과 도면에서만 나온다.

| 선 | 뜻 | 범례 문구 |
|---|---|---|
| 민 관 | `conf = 1`, 구조화 소스 | 구조화 소스 (확실) |
| 파선 무늬 (마디 7) | `conf < 1`, 산문에서 추출 | 산문에서 추출 (근거 있음) |
| 촘촘한 파선 (마디 16) | `same_as` (`edgeSame`, 강조 시 `edgeLitSame`) | 동일 실체 (same_as) |
| +0.8 굵게 | `caused` 인과 | 인과 (원인 → 결과) |

어디서 온 사실인지 화면에서 구분되지 않으면 둘 다 못 믿게 된다. (범례의
견본은 SVG 라 2D 의 실선·파선 그대로다 — 3D 의 관 무늬가 그 뜻을 그대로 진다.)

**배치**(`layout.js`)는 d3-force-3d. 계수는 손으로 짠 옛 시뮬레이션에서 옮겨
온 그대로고 **차원만 늘었다**: 반발 `300 + r×24` (800 컷) · 링크 거리
`112 + r + r`(강도 0.12) · `same_as` 34(강도 0.35) · 겹침 방지 `r + 6`(0.7) ·
원점 끌기 중심 0.06 / 그 외 0.016 · `velocityDecay` 0.18 · `alphaDecay` 0.015 ·
`alphaMin` 0.005. 설정의 '힘' 절은 이 계수를 바꾸지 않고 **배율만 곱한다**
(`forces = { center, repel, link }`, 기본 1) — 그래서 배치 검증은 배율 1 로
그대로 돈다.

**끌어당기는 자리는 원점(0,0,0)이다.** 3D 에서 '화면 가운데'는 카메라가
바라보는 원점이라 창 크기와 무관하다 — 2D 의 `retarget`(창이 바뀌면 중심을
옮긴다)이 없어졌고, **창 크기가 바뀌어도 배치를 데우지 않는다**(2D 에서
배치가 영영 안 식던 병의 자리였다).

**틱은 엔진이 돌린다** (2D 는 우리가 60틱/초로 돌렸다). 멈추는 자리는
`alphaMin` 하나이고 `cooldownTicks`·`cooldownTime` 은 무한 — 틱·시간으로
자르면 큰 그래프가 식기 전에 굳는다. **되데우기는 한 단계뿐이다**
(`d3ReheatSimulation` = alpha 1): 3D 엔진은 alpha 를 밖으로 안 내주므로 데우는
계기를 **새 자료와 힘 설정 둘**로 줄였다. 이미 식은 뒤에 오는 '멈춤' 신호는
헛것이라 무시한다 — 설정이 하나라도 바뀌면 라이브러리가 시뮬레이션을 켰다
바로 끄므로, 그때마다 카메라를 맞추면 노드를 가리킬 때마다 화면이 튄다.

**배치가 서기 전에는 되데우지 않는다.** `d3ReheatSimulation` 은 그 자리에서
엔진을 '도는 중'으로 세우는데(`resetCountdown`) 정작 배치(`state.layout`)는
자료 소화가 1ms 뒤에 만든다. 그 사이 프레임이 한 번 지나가면 라이브러리가 없는
배치를 짚어 터지고 — **터진 렌더 고리는 다음 프레임을 걸지 않아 화면이 통째로
죽는다** (`resumeAnimation()` 도 안 먹는다: 요청 번호가 남아 있어 새 고리를 안
건다). 자료가 엔진보다 먼저 오는 화면에서만 났다 — 내 역사(`onReady` 가
만들자마자 `setData`)가 늘 그랬고 한국사는 자료가 늦게 와서 살았다 (2026-09-11
실측: `/life.html` 의 `renderer.info.render.frame` 이 1 에서 멎었다). 그래서
첫 틱을 본 뒤부터만 되데우고, 그 전에는 자료를 싣는 것만으로 충분하다
(라이브러리가 스스로 alpha 를 1 로 되돌린다). 그러고도 죽는지 **1.5초마다
잰다** — `renderer.info.render.frame` 이 안 늘면 `pauseAnimation()` →
`resumeAnimation()` 로 새 고리를 건다.

배치가 화면 밖으로 달아나지 않게 셋을 건다: 시작 위치를 **공 껍질 위의
황금각 나선**(방위각 2.399963 rad, 극각은 황금비의 소수부)에 고르게 놓고,
`forceCollide` 로 겹침을 막고, 식을 때까지 카메라가 따라간다(`_fit`).
사용자가 화면을 돌리면 그때부터 손을 뗀다.

## 5. 배치

```
┌ .top  머리 줄 (secondary, 52px) ────────────────────────────────────┐
│ ● histgraph  [고려~대한민국]        [검색 ─────────────]         [☾] │
├───────────┬──────────────────────────────────────┬──────────────────┤
│ .timeline │ .stage-wrap                          │ .detail          │
│ 252px     │ [⚙]┌ 설정 ─┐  .stage 3D 그래프 (primary)│ 348px          │
│ 선택 시   │    │ 시작점 │                        │ 노드 고를 때     │
│           │    │ 필터   │                        │ = 문서 한 장     │
│           │    │ 범례   │                        │                  │
│           │    │ 표시   │                        │                  │
│           │    │ 힘     │                        │                  │
│           │ [조선 주변 · 노드 120]               │                  │
├───────────┴──────────────────────────────────────┴──────────────────┤
│ .foot 상태 줄 (24px)  © 2026 histgraph   방침 · 약관   노드 · 엣지   │
└─────────────────────────────────────────────────────────────────────┘
```

| 자리 | Obsidian 의 무엇 | 클래스 | 정한 것 |
|---|---|---|---|
| 머리 줄 | 탭 머리 줄 | `.top` | 52px, secondary 바탕. 세 칸 격자(1fr · 640px · 1fr)라 검색이 화면 한가운데 온다. 시대 이름은 열린 문서 탭처럼 보이는 단추(`.era`) — 누르면 중심으로 |
| 테마 단추 | 설정의 외형(라이트/다크) | `.theme-toggle` | 격자 셋째 칸 오른쪽 끝, `.clickable-icon`. 아이콘은 **누르면 되는 것**(어두운 화면엔 해, 밝은 화면엔 달). 이름표도 '밝은 화면으로'·'어두운 화면으로' |
| 검색 | 빠른 전환(quick switcher) | `.search`, `.results` | 입력칸은 form-field 바탕·5px·38px 높이·15px 글자 (09-06: 작아서 있는 줄 모른다는 지적). 결과는 primary 바탕·12px·`--shadow-l`. 고른 줄은 hover 바탕 + 강조색 글자 |
| 연표 | 왼쪽 사이드바의 파일 트리 | `.timeline` | §6 |
| 그래프 | 그래프 뷰 | `.stage`, `#canvas` | §4. **`#canvas` 는 `<canvas>` 가 아니라 빈 상자(`<div>`)다** — 3D 엔진이 그 안에 제 캔버스를 만들어 넣는다(들어갈 때 `innerHTML` 을 비운다). 왼쪽 아래 안내 칩(`.stage-note`), 비었으면 가운데 안내 |
| 설정 상자 | graph-controls | `.graph-controls`, `.graph-controls-toggle` | 캔버스 **왼쪽 위**(Obsidian 과 같은 자리), 240px, `--shadow-s`. **기본 접힘** — 조절 단추(lucide sliders)만 남는다. 절은 `<details>` 라 따로 접힌다. 아래 표 |
| 상세 | 오른쪽 사이드바의 문서 + 백링크 창 | `.detail` | §7 |
| 상태 줄 | status bar | `.foot` | 24px, 11px 글자. 노드·엣지 수는 오른쪽 끝(`.counts`) |

### 설정 상자의 절 (Obsidian 그래프 설정과 같은 순서)

| 절 | Obsidian | 안에 있는 것 | 닿는 곳 |
|---|---|---|---|
| 시작점 | (없음 — 우리 것) | **무게** 상위 12개 (`central.py` — 타입 가중 PageRank). 누르면 그 주변을 편다 | `load(id)` |
| 필터 | Filters | 펼침 깊이 · 최대 노드 · 연도·시대 노드 포함 · **관계 종류** 체크 목록(`meta.edge_types`, 개수 순) | 앞 셋은 그래프를 다시 받고, 관계 종류는 `setEdgeFilter` (다시 받지 않는다) |
| 범례 | Groups | 타입 색 견본 + 이름 + 개수, 선 종류 | 읽기만 |
| 표시 | Display | 화살촉 · 이름표 보이기 · 왼쪽 연표 보이기 · 슬라이더: 이름표 흐림 문턱(0~1, 3D 에서는 **카메라 거리**에 걸린다) · 노드 크기(0.5~2) · 선 굵기(0.5~3) | `setDisplay` |
| 힘 | Forces | 슬라이더: 중심 힘(0~2) · 반발 힘(0.2~3) · 링크 거리(0.4~2.5) | `setForces` |

기본으로 시작점·범례·표시가 열려 있고 필터·힘은 접혀 있다. 슬라이더는
값을 오른쪽에 늘 적는다 (Obsidian 은 툴팁으로 보여 주지만 늘 보이는 편이
되돌리기 쉽다). 설정은 `App.settings` 한 객체에 있고 `GraphCanvas` 가
절마다 효과를 따로 두어 슬라이더 하나가 움직일 때 다른 절이 배치를 다시
데우지 않는다.

`#root` 가 `body` 의 세로 배치를 이어받는다 — 이걸 빼면 `.layout` 의
`flex:1` 이 기댈 곳을 잃어 연표 스크롤과 그래프 상자의 크기가 한꺼번에 죽는다
(3D 엔진은 크기 0 인 상자에 아무것도 안 그린다). `render.test.mjs` 가 이
규칙과 `#canvas` id 를 잰다.

반응형은 **한 단계뿐**이다 (`max-width: 1100px` → 연표 208 / 상세 300px).
모바일 배치는 없다.

---

## 6. 연표 (`timeline.js`)

그래프는 무엇이 무엇과 이어져 있는지만 말하고 **언제인지는 말하지 않는다.**
그 자리를 이 막대가 맡는다. 겉모습은 Obsidian 의 파일 트리다 — 줄은 hover
에 흰색 7.5%, 고른 노드는 `--text-normal` 600, 나머지는 muted/faint.

**자리는 연도에 비례한다.** 목록으로 늘어놓으면 1400년과 1401년 사이가
1400년과 1500년 사이만큼 벌어져 연표라고 부를 수 없다. 축은 언제나 그래프가
담은 처음 해부터 끝 해까지다 — 어느 노드를 골라도 같은 축 위에 서므로
옮겨 다니며 자리를 견줄 수 있다. 조선 그래프는 조선 건국(1392)에서 시작한다.

치수 (`timeline.js` 상단 상수):

```
LANE_W 104   재위 띠 칸 (끄면 0)      BAR_X 11 · BAR_W 6 · DEATH_X 19
AXIS_X  46   세로축                   DOT_X 58  라벨 시작
PAD_TOP 18 · PAD_BOTTOM 64            GAP 30   라벨 최소 간격
LANE_GAP 26  재위 라벨 최소 간격 (이름+연도 두 줄)
MAX_LEVEL 4 · MAX_HEIGHT 40000px
FOLD_H 120   접은 구간의 높이 (몇 년을 접었든 같다) · FOLD_INSET 10  파선이 물러서는 만큼
문턱  빈 구간이 화면 절반(`bodyH − 18 − 64` 의 1/2)을 넘으면 접는다
```

**빈 구간은 접는다 — 솎는 것이 아니다** (2026-09-11, `buildScale` 의 `fold`).
`korea` 묶음에 고대가 들어오며 축이 918년에서 기원전 2333년으로 내려갔다
(−2335 ~ 2029, 4,364년). 기원전 2332~221년에는 표시도 재위 띠도 하나가 없어,
비례를 곧이곧대로 지키면 첫 표시와 둘째 표시가 **6,713px — 화면 일곱 장 —**
떨어진다 (실측: 연표 맨 위를 열면 점 하나만 보이고 그 아래가 전부 비었다).

- **표시는 하나도 빼지 않는다** (CLAUDE.md §1-3 뒷절). 제 몫을 내놓는 것은
  비어 있는 해뿐이고, 접을 수 있는 것은 **표시도 재위 띠도 없는 구간**이다 —
  띠가 걸친 해를 접으면 띠가 뭉개진다. 띠를 켜고 끄는 것과는 무관하다.
- **문턱은 화면 절반이다.** 그래야 표시와 표시 사이에서 빈 채로 굴리는 거리가
  한 화면을 넘지 않는다. 문턱을 한 화면으로 두면 다음 표시가 화면 밖에 남아
  '여기서 끝났나'로 읽힌다 (실측 797px → 433px).
- **접은 구간의 높이는 늘 `FOLD_H` 다.** 높이가 길이를 말하면 그것도 비례인
  척하는 셈이다 — 길이는 글자가 말한다 (`세울 것이 없는 2,112년`, 도구말에
  `기원전 2332년부터 기원전 221년까지 … 접었습니다`).
- **접었다는 것이 보인다**: 축을 끊고(`axisLine`), 위아래를 파선으로 막고
  (`.tl-fold`, 구간 안쪽으로 10px 물러서 긋는다 — 끝에 딱 맞추면 그 자리
  표시의 글자를 가로지른다), 몇 년인지 적는다. 바탕은 비워 둔다 — 그 사이를
  지나는 왕조 막대(고조선)가 가려지면 안 된다.
- **접은 해는 자를 나눠 갖지 않는다.** 남은 해가 `base` 를 나눠 가지므로
  고대를 넣었다고 조선의 눈금이 4분의 1로 줄지 않는다 (실측 3.0 → 6.1px/년).
  자가 촘촘해지면 접을 구간이 더 생길 수 있어 자리가 굳을 때까지 되짚는다.
- 자는 접힌 구간 **안에서도** 이어진다 (`FOLD_H` 를 해 수만큼 나눈다) —
  픽셀을 해로 되짚는 길(`yearAt`)과 훑기 막대가 끊기지 않는다.
- 실측 (고조선~대한민국, 표시 591개, 판 902px): 접힌 곳 둘
  (기원전 2332~221년 2,112년 · 기원전 193~110년 84년), 첫 표시와 둘째 표시
  사이 **433px**, 전체 높이 29,302px. 개인 역사(`life.js`)는 접지 않는다 —
  `fold` 를 주는 쪽만 접는다.

**줌은 자리가 아니라 밀도를 바꾼다.** 0단계는 시대 전체가 한 화면에
들어오고 한 단계마다 눈금이 두 배가 된다. 무엇이 '큰' 사건인지는 그래프가
정한다 — 많이 연결된 사건부터 자리를 잡고, 30px 안에서 겹치는 사건은 다음
단계로 물러난다. **빠진 것이 없는 셈이 되면 안 되므로** 머리에
`큰 사건 13 / 156` 를 늘 적는다.

**그리는 규칙**:

| 것 | 그림 |
|---|---|
| 고른 노드 | 점 r 4 + 구간 막대 4px(85%), 굵은 글자 (`.k-self`) |
| 이웃·뼈대 | 점 r 2.6, `--text-muted` (`.k-anchor` 는 `--text-faint`) |
| 왕조 | 축 **옆**(−6px)에 2px 막대(50%) — 축 위에 겹치면 사건 점을 다 덮는다 |
| 재위 막대 | `TYPE_COLOR.person` 6px 알약, 한 칸 걸러 78%/50%, 해당 임금 100%, 그 이름은 강조색 |
| 몰년 | `--surface-2` 로 채운 r 2.8 원 + `2 3` 점선 |

**재위·재임 띠는 취임(즉위)한 날에 앉는다.** 자의 눈금은 한 해라
(`buildScale` 의 `pos`) 해로만 앉히면 12월에 취임한 대통령의 막대와 이름이 그
해 1월에 서고, 권한대행이 앉았던 빈 데가 화면에서 사라진다 (2026-09-09 사용자
지적: 박정희 1963-12-17 · 최규하 1979-12-21 · 전두환 1980-09-01 · 이재명
2025-06-04). 자리를 잡는 길이 둘이다 (`dateRuler`):

- **그 해에 사건이 서 있으면 그 사건들 사이에.** 몰린 해는 사건이 날짜 순으로
  한 칸(`GAP 30`)씩 서므로(`placeMarks`) 해를 고르게 나누면 어긋난다 — 2025년에
  사건 아홉이 선 자리에서 6월 취임을 해의 42% 지점에 앉히면 4월 사건 옆이다.
  앞뒤 사건의 **가운데**에 앉힌다. 한 사건의 칸에 딱 맞추면 그 사건이 곧 취임인
  것처럼 읽힌다 (이재명의 취임 2025-06-04 은 제21대 대통령 선거 06-03 다음이고
  한반도 집중호우 07-16 앞이다).
- **사건이 없는 해는 소수로 나눈다.** 서버가 해 안의 자리를 함께 준다
  (`server._year_at`: `1963-12-17` → `1963.96`, 달까지만 알면 그 달의 첫날).
  그것도 모르면 예전대로 그 해의 첫날이다.

개인 역사(`life.js`)도 같은 자를 쓴다 — 눈금이 되는 것은 **역사 열**이다.
개인 열은 사람마다 달라 자가 될 수 없다.

**사람은 점으로 찍지 않는다.** 사건·작품만 연표의 점이 된다. 인물·장소·조직은
이어지는 것이라 한 점에 찍으면 거짓을 말한다. 사람이 언제 살았는지는 그
사람을 골랐을 때 생몰 구간(막대)으로 말한다.

**원인은 파랗고, 오른쪽 여백의 꺾인 선이 고른 노드와 잇는다** (2026-09-05
사용자 요청: "'원인' 표시를 blue 로, 타임라인 오른쪽으로 꺾인 모양의 라인으로
원인 사건과 연결"). `caused` 엣지의 들어오는 쪽(`rel.dir === 'in'`)만 원인이다
(`isCause`). 딱지 `.tl-rel.is-cause` 는 `--color-blue`(`#4f93bf`, 노드
팔레트의 남색을 글자가 읽히게 밝힌 것). 왼쪽 칸의 왕·대통령 재위 띠도
파랑(`timeline.js` `REIGN_COLOR` `#3d84f5`)이다 — 2026-09-06 노드 팔레트를
바꾼 뒤 사용자 결정으로 옛 파랑을 그대로 둔다. 노드 색과 잇지 않는다.
선(`causeWire`)은 원인 줄에서 오른쪽으로 12px 나가 캔버스 오른쪽
12px 안쪽의 세로 선을 타고 고른 노드 줄로 돌아오며, 끝의 화살촉이 고른 노드를
가리킨다 — 축 위에 그리면 점·막대와 겹치고 라벨 사이를 지나면 글자를 가른다.
선이 있는 연표만 줄을 24px 접는다(`.tl-canvas.has-cause .tl-mark`). 결과(out)
는 잇지 않고 딱지만 `결과` 로 단다 (`DIR_HEAD.caused`) — 서버는 인과 이름을
'원인'으로만 주므로 방향을 여기서 가른다. 양쪽을 다 그리면 선이 어느 쪽으로
읽히는지 흐려진다.

**원인은 결과보다 위에 선다.** 같은 해 안의 차례가 곧 시간 순으로 읽히는데
날짜만으로는 그 차례가 안 나온다 — 같은 날인 인과(한일병합·무단통치
1910-08-29), 결과의 날짜가 본래 거친 것(을사조약 1905-11-17 → 애국계몽운동
1905), 날짜를 아예 모르는 것(왕자의 난 1398-08-26 → 함흥차사), 단체의 창립이
곧 결과인 것(3·1 운동 → 북로군정서 1919)이 다 걸린다. 거친 날짜도 빈 날짜도
'그 해 어딘가'이지 '그 해 첫날'이 아니므로, **결과의 날짜가 원인의 날짜를
품으면**(`dateContains`, 같은 날·빈 날짜 포함) 결과를 원인 뒤에 세운다.

`sortMarks(marks, causes)` 는 해마다 위상 정렬(칸)을 돌린다 — 세울 수 있는
것 중 날짜(같으면 가나다, 그래도 같으면 들어온 순)가 앞선 것을 고르므로,
인과가 없는 마크의 차례는 날짜순 그대로다. **순환이 있어도 멈추지 않는다**
(진주농민봉기 ↔ 임술민란이 서로를 원인으로 문다) — 아무도 앞설 수 없으면 남은
것 중 날짜가 앞선 것을 세우고 마저 푼다. 인과는 서버가 `causes`(같은 해에 함께
선 마크들 사이의 `[원인 id, 결과 id]`, `_mark_causes`)로 주고, 고른 노드에 달린
`rel` 도 같은 길로 든다. 2026-09-08 이전에는 뒤엣것뿐이라 **둘 다 뼈대인 쌍은
인과를 아예 몰랐다** (전수 조사: 화면에 뒤집혀 서던 32쌍 중 24쌍이 이것).

날짜가 서로를 품지 않으면(원인 12월, 결과 3월) 날짜가 이긴다 — 화면은 자료의
날짜를 뒤집지 않는다. 남은 8쌍이 그것이고(헌정연구회 1905-05, 청나라 1636),
단체의 그 자리가 창립이라 결과가 아닌 것이다. 날짜 자체가 틀린 것은 화면이
아니라 `chronology` 표가 고친다 (CLAUDE.md §1-5).

**넘겨받은 연도는 같은 얼굴로 내놓지 않는다.** 노드가 스스로 적고 있는
날짜가 아니라 이어진 연도 노드에서 가져온 값이면 머리에 `연결된 연도`
알약(`.tl-via`)을 붙인다. 연도도 없고 연도를 아는 이웃도 없으면 자리를
지어내지 않고 "연도를 알 수 없습니다"라고 적는다.

**머리의 훑기 막대**(`.tl-map`)는 연표가 화면보다 길 때만 나온다. 창틀
(테두리만, 칠하지 않음)이 지금 보이는 구간, 보라 표식이 고른 노드의 자리다.
칠해 버리면 둘 다 '막대'로 보여 무엇이 무엇인지 갈리지 않는다.

배율과 재위 띠 여부는 `localStorage`(`tl-zoom`·`tl-reigns`)에 남는다.

---

## 7. 상세 패널 (`DetailPanel.jsx`) — 문서 한 장

위에서부터 Obsidian 문서를 읽는 순서다:

1. **`← 되돌아갈 노드`** `.d-back` — sticky. 관계 목록이 길어 스크롤을 내려도
   되돌아갈 자리가 사라지면 안 된다. 되돌아갈 곳을 **이름째** 들고 있는다.
2. **타고 들어온 관계** `.d-via` — 강조색 15% 바탕. 이 패널에서 강조색
   바탕을 쓰는 유일한 자리. 조선시대 중기에는 개체가 37개 달려 있어 방금
   누른 것을 목록에서 다시 찾아야 했다.
3. **타입 딱지** `.d-type` — 색 견본 + 타입 이름, 회색 알약. 색만으로
   읽히지 않게 늘 같이 둔다.
4. **제목** `.d-title` — 인라인 제목. 20px 700, -0.02em.
5. **기간** `.d-dates` — 속성 줄처럼 muted, tabular-nums.
6. **본문** `.d-desc` — **상자에 넣지 않는다.** 14px, 1.65. 문서 본문은 그냥
   글이다. 서버가 주는 것은 요약이고 전문 보기는 없다 (애드센스 스크랩 판정).
   설명이 없으면 콜아웃(`.d-nodesc`, 왼쪽 3px 회색 줄)으로 왜 없는지 적는다 —
   빈 칸을 그냥 두면 '원래 설명이 없는 노드'로 읽힌다. 넘겨받은 글은
   그렇다고 적는다(`.d-desc-src`) — 출처 표기가 아니라 경고다.
7. **출처 한 줄** `.d-desc-origin` — 11px faint, 밑줄 링크. CLAUDE.md §1 의
   유일한 예외.
8. **절 머리** `.d-section-title` — 백링크 창 머리처럼 13px 600 + 위 1px 선.
   개수는 칩(`.flair`)에 든다.
9. **다른 이름** `.d-aliases` — 속성 창의 알약(2em).
10. **인과 사슬** (`ChainPanel`) — 관계 목록보다 먼저. 종류 딱지(`.chain-kind`)
    는 강조색 10% 알약, 들여쓰기가 걸음 수.
11. **관계** `.rel` — 백링크 목록. **카드가 아니라 줄이다.** 테두리·바탕 없이
    hover 만 밝아지고 이름이 강조색이 된다. 근거는 인용(`.rel-ev`, 왼쪽 2px
    회색 줄). 여러 관계가 함께 딛고 선 근거는 묶음 머리 아래 한 번만.

**자취는 상세 안에서만 쌓인다.** 그래프나 연표에서 고른 노드는 그 자체가
뿌리다 — 캔버스에서 아무 노드나 누를 때마다 직전 노드가 상위로 붙으면
아무 관계 없는 둘이 부모-자식처럼 보인다. 최대 50칸. 관계 문장은 한국어
조사를 받침으로 가려 붙인다 (`relations.js` `pt`).

---

## 8. 문서 페이지 (`/n/`, 방침·약관)

Obsidian 의 읽기 모드: 한 줄 너비 `--file-line-width: 700px`, 본문 16px,
줄 간격 1.5, h1 1.8em 700, h2 1.25em 600, 링크는 강조색에 밑줄. 목록 줄은
hover 에 흰색 7.5%. 콜아웃(`.note`)은 강조색 8% 바탕에 왼쪽 3px 강조색 줄.

세 벌이다 — `web/doc.css`(방침·약관), `src/histgraph/pages.py` 의 `STYLE`
(`/n/`), `web/index.html` 의 noscript 인라인 색. 토큰 값은 셋이 같다.

---

## 9. 상호작용

| 몸짓 | 하는 일 |
|---|---|
| 노드 클릭 | 상세 열기 + 그 주변을 **얹어서** 펼치기 |
| 노드 더블클릭 (320ms 안에 두 번) | 자리를 지킨 채 이웃만 얹기 |
| 노드 드래그 | 그 자리에 못박기 (`fx/fy/fz`) |
| 빈 곳 클릭 | 조명 끄기 |
| 빈 곳 드래그 | 그래프 돌려보기 (3D 궤도 회전) |
| 휠 | 그래프 확대·축소 (카메라가 다가간다) |
| 왼쪽 위 조절 단추 | 설정 상자 열기·닫기 |
| 필터의 관계 종류 | 그 종류의 선과, 선이 안 남은 노드를 숨긴다 |
| 표시·힘 슬라이더 | 그 자리에서 바로 반영 (노드 크기·힘은 배치를 다시 데운다) |
| `/` | 검색창으로 |
| `↑`·`↓`·`Enter` | 검색 결과 오르내리기·고르기 |
| `Esc` | 상세 닫기 (한 칸씩이 아니라 통째로) |
| `Alt`/`⌘` + `←` | 상세 안에서 한 칸 되돌아가기 |
| `⌘`/`Ctrl` + 휠, 핀치 | 연표 줌 (그냥 휠은 스크롤이어야 한다) |

주소(`location.hash`)에 지금 보는 노드를 남긴다 — 새로고침해도 자리를 잃지
않고, 링크로 넘길 수 있다. 검색은 140ms 쉬고 묻는다.

**화면을 열면 왕조에서 시작한다.** 차수 1위 노드로 열면 그때그때 병자호란이
중심이 되는데, 그건 우연이고 "이게 무엇의 그래프인가"를 말해주지 못한다.
머리의 시대 단추가 언제든 중심으로 되돌린다.

---

## 10. 지키는 것 · 바꿀 때

- **한글만.** 이 디자인이 들여온 영어는 없다. 아이콘의 `aria-label` 도 한국어.
- **색만으로 뜻을 나르지 않는다.** 범례에 타입 이름, 상세에 타입 딱지.
- **상자보다 줄.** 새 요소를 그릴 때 테두리·바탕을 먼저 두지 않는다. 줄로
  두고 hover 에 흰색 7.5% 를 얹는다. 떠 있는 것(설정 상자·검색 결과)만
  바탕·선·그림자를 갖는다.
- **강조색은 "지금 이것"과 "누를 수 있다"에만.**

```bash
python3 tools/check_palette.py     # 노드 색을 만졌으면 — 2형·1형 5.0 이상
cd web && npm test && npm run build
```

| 파일 | 무엇을 |
|---|---|
| `web/tests/layout.test.mjs` | 배치가 식는가 · 겹치지 않는가 · 중심이 **원점**을 지키는가 · 엔진이 늦게 와도 자료를 잃지 않는가 · **배치가 서기 전에 되데우지 않는가**(렌더 고리가 죽는 자리) · **카메라가 노드 구름을 여백 90px 안에 꽉 채우는가** · **첫 화면에 이름표가 서고 화면 크기가 11px 인가** · 진짜 라이브러리에 우리가 부르는 이름이 다 있는가 |
| `web/tests/relations.test.mjs` | 조사·문장·카드 합치기 |
| `web/tests/render.test.mjs` | 실제 조립 · 타입 색값 · **화면에 영어가 새지 않는가** |

- 노드 색: graph-view.js `TYPE_COLOR` → style.css `--mark-*` → doc.css →
  pages.py → `render.test.mjs` 의 색값 두 곳.
- 바탕·글자 색: style.css `:root` → graph-view.js 상수 → doc.css → pages.py
  → index.html noscript.

---

## 11. 안 옮긴 것 · 어긋나 있는 자리

- **노드를 회색 한 가지로.** Obsidian 의 기본은 모든 노트가 `--text-muted`
  회색 점이다. 우리 점은 타입을 말해야 하므로 색을 남겼다 (§3).
- **Inter 글꼴** (§2).
- **모바일 배치가 없다.** 1100px 한 단계에서 폭만 줄이고, 그 아래로는
  그대로 눌린다.
- **3D 가 무겁다.** 엔진(`3d-force-graph` + `three`)은 늦게 실리는 덩어리로
  1,562 kB(gzip 421 kB)다 — 첫 그림이 뜬 뒤에 받으므로 첫 화면은 그대로지만,
  느린 회선에서는 그래프가 한 박자 늦게 선다. 그동안 화면은 빈 상자다.

---

## 12. 개인 역사 (`life.html` · `lib/life.js`)

세 열이 **자 하나**(`buildScale`) 위에 선다 — 같은 해는 세 열에서 같은 높이다.
왼쪽은 시대 연표와 같은 재위 띠(`reignBand`, 파랑 `REIGN_COLOR`), 가운데는
그 무렵의 큰 사건(`/api/context`, `_anchors` 와 같은 규칙), 오른쪽은 이 사람의
사건. 맨 오른쪽에 인생 단계 띠(강조색) — 개인 열 왼쪽에 두면 역사 → 개인
연결선이 띠의 이름을 가로지른다.

```
COLS  lane 104 · history 300 · gutter 44 · personal 320 (오른쪽 24 은 인과 선) · stage 84
AXIS_X 46 · PAD_TOP 18 · PAD_BOTTOM 64 · ZOOM 5
```

| 것 | 그림 |
|---|---|
| 역사 사건 | r 2.6 `--text-faint`; 이어진 것은 r 3.4 에 파란 테두리, 이름이 `--color-blue` |
| 그래프에 없는 사건 | 속이 빈 점 + 딱지 '그래프에 없는 사건' — 모델이 부른 이름을 그래프인 척 세우지 않는다 |
| 개인 사건 | 파랑 점, **r 3.4 고정** (역사 열의 '이어진 것'과 같은 크기); 구간이 있으면 막대; 고른 것은 링과 진하기로 |
| 모델이 매긴 1~10 | **눈금으로 그리지 않는다** (2026-09-09 사용자 결정 셋) — 중요도(점 크기도 상세의 눈금도) · 영향 순위(`influence_score`) · 전환점(`turning_point_score`). 재는 식이 없어 막대가 잰 값처럼 읽힌다. **차례**는 그 묶음의 뜻이라 남는다. 남은 눈금은 '역사가 준 영향'의 `strength` 하나. **고치는 칸으로도 두지 않는다** (상세의 '편집') — 안 그리는 값을 고치라고 내밀면 그 눈금이 칸으로 되돌아온다 |
| 감정 (`emotional_impact`) | **그리지 않는다** (2026-09-09 사용자: "노드의 '감정' 메뉴도 삭제해 필요 없어") — 모델이 사건마다 붙인 한 낱말('불안'·'설렘')이라 이야기가 그렇게 말했다는 근거가 없다. 수집은 그대로, 화면에서만 뺀다 |
| 확신도 | 백분율(`확실함 … 90%`)은 **그리지 않는다** (2026-09-09 사용자 결정) — 코드가 만드는 엣지가 죄다 0.8 상수라 잰 값이 아니다. '말한 것 / 미룬 것'은 확인되는 이진 사실이라 관계 목록의 딱지와 그래프의 점선으로 남는다 |
| 역사 → 개인 | 홈을 건너는 S 곡선, 직접 실선 · 간접/가능성 점선 `3 3`, 개인 열 왼쪽 가장자리에서 화살촉 |
| 개인 인과 | 시대 연표와 같은 ㄷ 자 `causeWire`, 선마다 4px 안으로 |
| 태어나기 전 | 축이 아니라 캔버스 머리의 점선 상자 — 이어진 것만, 이유와 함께 |

**상세는 그 자리에서 고친다** (2026-09-09 사용자: '삭제' 옆의 '편집'). 칸의 차례는
상세의 차례와 같다 — 종류·이름·날짜·장소·설명, 연표(날짜 글·시절), 전환점, 함께,
그 무렵의 한국사, 만약 없었다면. **관계는 칸에 없다** (같은 날 사용자: "편집 메뉴에서
관계를 빼라고") — 화면에 선 선 가운데는 문서에 없는 것이 있어(섬 잇기·참여자 풀기·
가족 호칭을 코드가 긋는다) 칸으로 내밀면 사람이 고른 것과 코드가 그은 것이 섞인다.
화면이 안 그리는 값(감정·1~10 점수)도 칸으로 두지 않는다.

**같은 해의 차례는 연표의 차례다.** 날짜 문자열로 세우면 '1998'(전학)이
'1998-03'(이사)보다 앞에 서서 결과가 원인 위에 온다 (§6 의 같은 함정).

**영어는 안 낸다.** 지시문의 식별자는 `NODE_TYPE_KO`·`EDGE_TYPE_KO`·`IMPACT_KO`
로만 화면에 오고, 테스트가 지시문을 읽어 빠진 것을 잡는다. 자료의 고유명
(IMF·AI)은 한글과 함께 있으면 통과 — 기준은 시대 연표와 같이 '덩어리에 한글이
한 자도 없는가'다.

**그래프는 연표 옆에 같이 선다** (역사 화면과 같은 배치: 왼쪽 연표 864px · 가운데
그래프 · 오른쪽 상세 348px; 연표는 머리의 '연표 접기'로 접는다). 그래프(§4, **3D**)·
설정 상자(§5)는 역사 그래프의 것이다 — 같은 `GraphView` 라 한 곳을 고치면 둘 다 바뀐다.
자료만 다르다 — 지시문의 타입을 색 타입 여덟에 대응시켜(`GRAPH_TYPE`) 색이 곧
갈래가 되게 한다. 그래프를 통째로 싣고 고른 노드가 중심이며, 조명·이름표·화살촉
규칙은 §4 그대로. 선 범례는 '본인이 말한 것 (실선)'·'말한 것에서 미룬 것 (점선)'.

**한국사는 이 화면 안에서 편다** (2026-09-10 사용자: "한국사 사건을 연표에서
클릭하면 한국사 페이지로 이동하는데 그러지 말고 한국사 그래프와 노드 정보를 그
페이지에서 바로 보여줘 이동하지 말고"). 가운데 열의 사건(그리고 '태어나기 전'의 줄,
상세의 '그 무렵의 한국사')을 누르면 **캔버스가 그 사건의 주변 관계**(`/api/graph`)가
되고 **오른쪽이 한국사 상세**(`DetailPanel` — 한국사 장이 쓰는 그 부품)가 된다.
같은 노드를 두 화면에서 다르게 적지 않으려고 여기서 다시 그리지 않고 그 부품을 부른다.

| 것 | 그림 |
|---|---|
| 지금 보는 한국사 사건 | 연표에서 굵어진다 (`life-h.k-self` — 내 사건의 `k-self` 와 같은 문법) |
| 캔버스 안내 줄 | `{이름} 주변 · 노드 n · 관계 m` 뒤에 **'내 역사로 돌아가기'**(`.stage-back`) |
| 설정 상자 | 범례·시작점·펼침 깊이가 **지금 캔버스에 선 그래프의 것**으로 바뀐다 (한국사를 보는데 내 그래프의 타입 수가 적혀 있으면 그 수는 거짓말이다) |
| 왼쪽 세 열 | 그대로 있다 — 화면을 떠나지 않는 것이 이 일의 전부다 |

돌아오는 길은 셋이다: 안내 줄의 단추 · 상세의 닫기 · **내 사건을 고르는 것**(한
캔버스가 둘을 같이 그릴 수는 없다). 링크(`href="/#id"`)는 남겨 두어 cmd·ctrl·가운데
단추로 한국사 장을 새 탭에 여는 길은 그대로다 — 옮겨가는 것은 사람이 그렇게 눌렀을
때뿐이다. 그래프에 없는 사건(`kind: 'extra'`)은 `<span>` 이라 열 자리가 없다.


# 화면 설계 — Obsidian 스타일

지금 코드가 실제로 하고 있는 것을 적는다. **바라는 모습이 아니라 현재
상태**다. 값이 바뀌면 이 문서도 같이 고친다 — 여기 적힌 숫자는 전부
파일에서 읽어 온 것이고, 색 수치는 `python3 tools/check_palette.py` 가
그때그때 다시 잰다.

2026-09-05 사용자 요청으로 화면을 **Obsidian 의 디자인 스타일**로 옮겼다
("우리 그래프 디자인을 obsidian 디자인 스타일로 변경해줘"). 그 전 화면
(따뜻한 검정 `#141413` 바탕, 파란 강조, 카드형 패널, 왼쪽 설정 사이드바)의
기록은 git 의 `494b94d` 판 이 문서에 있다.

한 줄로: **크롬은 회색 사다리, 색은 노드와 강조(보라) 둘뿐, 상자 대신 줄.**

두 번째 요청(같은 날)으로 [obsidian-better-graph-view](https://github.com/cr7pt0gr4ph7/obsidian-better-graph-view)
를 참고해 그래프 쪽을 다시 손봤다. 그 플러그인은 겉모습이 아니라 Obsidian
그래프의 **내부를 후킹해 빠진 기능을 채우는 것**이다 — 관계 종류로 선을
거르는 `edge:` 연산자, 문서의 `title` 속성을 이름표로, "지금 문서를
그래프에서 찾기". 우리 그래프는 관계에 종류가 있고 이름표는 이미 라벨이라,
옮긴 것은 **관계 종류 필터**와 Obsidian 그래프 설정의 네 절(필터·묶음·표시·
힘) 전부다 (§5).

설계의 배경과 실패 기록은 [README.md](README.md) "화면(`serve`)" 절에 있다.
여기는 **지금 화면이 어떤 규칙 위에 서 있는가**만 모아 둔다.

파일:

```
web/index.html            껍데기 (lang="ko", #root 하나, noscript 안내)
web/style.css             토큰과 모든 CSS (단일 파일)
web/doc.css               방침·약관 페이지 (읽기 모드)
src/histgraph/pages.py    /n/ 문서 페이지의 STYLE (읽기 모드, 서버 렌더링)
web/src/App.jsx           상태와 배선
web/src/components/       Search · SidePanel(그래프 설정 상자) · TimelinePanel · GraphCanvas · DetailPanel · Glyph · ChainPanel
web/src/lib/graph-view.js 캔버스 렌더러 + 팔레트 (React 를 모른다)
web/src/lib/layout.js     d3-force 배치 계수
web/src/lib/timeline.js   연표 렌더러 (React 를 모른다)
web/src/lib/relations.js  관계를 한국어 문장으로
```

---

## 1. 전제

**그래프가 주인공이다.** 화면 전체가 어둡고 조용하다. 크롬(머리·패널·
테두리)은 전부 무채색 회색 사다리이고, 색은 노드와 강조색 둘뿐이다.

**왜 Obsidian 인가.** Obsidian 의 그래프 뷰는 "노트가 점, 링크가 선"인
화면이고 우리도 "개체가 점, 관계가 선"이다. 같은 문제를 푼 디자인이라
옮길 것이 많다:

- 바탕은 어둡고 무채색, 선은 가늘고 회색, 색은 점에만.
- 가리키면 이웃만 남고 나머지는 거의 사라진다. 가리킨 점의 선은 더 밝은 회색
  (Obsidian 은 강조색이지만 2026-09-05 사용자 결정으로 선은 회색만 쓴다).
- 설정은 캔버스 **위에 떠 있는 상자**(graph-controls)다. 사이드바가 아니다.
- 오른쪽 사이드바는 문서 한 장처럼 읽힌다 — 제목, 본문, 그 아래 백링크.
- 아래에 상태 줄이 있고 글자 수 같은 숫자는 거기 오른쪽 끝에 선다.

Obsidian 은 한 화면에 색 계열을 둘만 둔다(회색 사다리 + 강조색). 우리는
노드 타입 색이 아홉이라 그보다 시끄럽다. 그래서 나머지를 더 조용하게
눌렀다 — 선에서 색을 걷어냈고, 카드 상자를 없앴고, 강조색은 초점·링크·
누를 수 있는 것에만 쓴다.

**색만으로 뜻을 나르는 자리를 만들지 않는다.** 노드 색은 9종이지만 색약
시야에서 최악 쌍이 ΔE 5.5 다 — "다르다"이지 "나란히 놓지 않아도 읽힌다"가
아니다. 범례는 색 견본 옆에 타입 이름을 글자로 적고, 노드를 고르면 상세
패널이 타입을 글자로 말한다.

**화면에 영어와 자료 출처를 쓰지 않는다** (CLAUDE.md §1). 사람이 읽는 글자는
전부 한국어다. 유일한 예외는 설명 바로 아래 출처 한 줄(라이선스 의무).
`web/tests/render.test.mjs` 가 서버 렌더링 결과에서 영어가 새는지 실제로 잰다.

---

## 2. 토큰

`style.css` `:root`. Obsidian 기본 다크 테마(app.css)의 **이름과 값을
그대로** 쓴다. 이름을 새로 짓지 않은 이유: Obsidian 테마 문서를 열어 놓고
그 이름으로 찾으면 된다. **다크 전용**이다 (`color-scheme: dark`).

### 회색 사다리

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--color-base-00` | `#1e1e1e` | `--background-primary` — 캔버스·본문·입력칸·검색 결과 |
| `--color-base-10` | `#242424` | `--background-primary-alt` — 되돌아가기 줄 hover |
| `--color-base-20` | `#262626` | `--background-secondary` — 사이드바·머리 줄·상태 줄·설정 상자 |
| `--color-base-25` | `#2a2a2a` | (옛 `--surface-3` 다리) |
| `--color-base-30` | `#363636` | `--background-modifier-border` — 모든 1px 선, 훑기 막대 바탕 |
| `--color-base-35` | `#3f3f3f` | `--graph-line`, 테두리 hover |
| `--color-base-40` | `#555555` | 입력칸 초점 테두리, 인용·콜아웃 왼쪽 줄 |
| `--color-base-50` | `#666666` | `--text-faint` — 연도·개수·힌트·출처 줄 |
| `--color-base-70` | `#b3b3b3` | `--text-muted` — 보조 글자 |
| `--color-base-100` | `#dadada` | `--text-normal` — 본문 |

hover 는 색을 바꾸지 않고 **흰색을 7.5% 얹는다** (`--background-modifier-hover:
rgba(255,255,255,.075)`). 어느 바탕 위에서든 같은 만큼 밝아진다.

### 강조색

`hsl(254 80% 68%)` = `#8a6cef`, Obsidian 의 보라. 뜻은 하나다 — **"지금
이것"(초점) 또는 "누를 수 있다"**. 타입·종류·상태를 강조색으로 가르지 않는다.

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--color-accent` | `hsl(254 80% 68%)` | 링크 hover, 고른 노드 테두리, 재위 띠의 '지금 임금', 훑기 막대 표식, 체크박스 |
| `--color-accent-2` | `hsl(254 80% 78%)` | 가리킨(hover) 노드 테두리 |
| `--background-modifier-active-hover` | 강조 15% | 타고 들어온 관계 상자(`.d-via`), 눌린 아이콘 단추 |
| `--tag-background` / `--tag-border` | 강조 10% / 15% | 인과 종류 딱지(`.chain-kind`), 재위 띠 켬(`.tl-kings`) |

노드 팔레트(§3)는 파랑·크림·주황~노랑과 남색·초록이라 강조색(보라)과
겹치는 타입이 없다. 2026-09-06 전에는 단체가 연보라 `#c2a4ff` 라 고른
단체 노드가 보라 점에 보라 테두리였는데, 팔레트를 바꾸며 그 약점이 없어졌다.

### 글꼴·크기

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--font-interface` | `-apple-system, BlinkMacSystemFont, "Segoe UI", Inter, "Apple SD Gothic Neo", "Noto Sans KR", "Malgun Gothic", system-ui` | 전부. 캔버스도 같은 순서(`graph-view.js` `FONT`) |
| `--font-ui-smaller` | 11px | 개수·연도·출처 줄·힌트·범례 머리 |
| `--font-ui-small` | 13px | 기본 UI 글자, 검색, 관계 이름, 절 머리 |
| `--font-ui-medium` | 15px | 연표 머리의 연도 |
| `--font-ui-larger` | 20px | 상세 제목 |
| `--font-text-size` | 14px | 상세 본문. 문서 페이지는 16px |
| `--line-height-normal` | 1.5 | |
| 10.5px | | 연도·재위 연도·관계 이름표 (tabular-nums) |

Obsidian 은 Inter 를 싣는다. 우리는 싣지 않는다 — 한글은 어차피 시스템
글꼴이 맡고, 라틴·숫자는 시스템 글꼴(SF·Segoe)이 Inter 와 같은 결이다.
글꼴 파일 하나를 더 받게 할 만큼의 차이가 아니다.

### 모서리·그림자

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--radius-s` | 4px | 줄(hover 바탕), 칩, 콜아웃, 아이콘 단추, 훑기 막대 |
| `--radius-m` | 8px | 설정 상자, 캔버스 위 안내 칩 |
| `--radius-l` | 12px | 검색 결과 상자, 스크롤바 잡이 |
| `--input-radius` | 5px | 입력칸·셀렉트·단추 |
| `--pill-radius` | 2em | 타입 딱지, 다른 이름, 인과 종류, 연결된 연도, 재위 띠 단추 |
| `--shadow-s` / `--shadow-l` | Obsidian 값 그대로 | 설정 상자 / 검색 결과 |

**라이트 테마 (2026-09-06)**. 진실은 `<html data-theme>` 하나다. `public/theme-boot.js`
가 CSS 보다 먼저 저장값(`localStorage.theme`)이나 운영체제 설정을 읽어 박고 —
React 뒤에 바꾸면 어두운 화면이 한 번 번쩍인다 — 머리 줄의 `.theme-toggle`
(`src/lib/theme.js`)이 바꾼다. CSS 는 `:root[data-theme="light"]` 에서 **회색
사다리와 반투명 값만** 뒤집는다. 뜻을 가진 이름은 사다리를 가리키므로 저절로
따라온다. **값은 Obsidian 라이트가 아니다** — 그대로 옮겼더니 "시인성이 너무
떨어진다"고 지적받았다 (옅은 글자 #ababab 대비 2.3, 테두리 #e0e0e0 대비 1.3).
GitHub Primer·Radix gray·Tailwind 를 실측해 셋의 공통 수준으로 맞췄다: 본문
`#1f1f1f`, 보조 `#555555`, 가장 옅은 글자도 `#767676`(4.5:1), 테두리 `#d0d0d0`,
사이드바 `#f2f2f2`, 강조 hsl(258 80% 56%). 캔버스는 CSS 변수를 못 읽어
`graph-view.js` 의 `DARK`·`LIGHT` 표가 같은 값을 들고(선 `#9a9a9a`, 가리킨 선은
글자 무게), 노드 색은 `TYPE_COLOR_LIGHT` — **색상은 그대로, 밝기만 뒤집었다**
(크림 → 황갈, 노랑 → 어두운 노랑, 연빨강 → 원색 D62828, 남색 → 원색 003049,
뼈대(시대·직위)는 옅어져 물러나되 2.5:1 은 지킨다). 인물 파랑·사건 주황·재위 띠
파랑은 두 바탕에서 다 읽혀 그대로다. 방침·약관(`doc.css`)도 같은 선택자로 따라온다.

그림자는 **떠 있는 것**에만 쓴다. 사이드바와 목록에는 없다 — Obsidian 은
층을 그림자가 아니라 바탕색 한 단(secondary)과 1px 선으로 가른다.

스크롤바는 Obsidian 처럼 12px 폭에 트랙 없이 잡이만 (흰색 10%, hover 20%,
투명 테두리 3px 로 가늘게).

### 옛 이름 다리

`timeline.js` 가 `var(--line)`·`var(--surface-2)` 를 SVG 문자열 안에서
직접 쓴다. `:root` 끝에 `--surface·--surface-2·--surface-3·--line·--text·
--text-2·--text-3·--accent·--font` 를 새 토큰에 이어 두었다. 새 코드는
Obsidian 이름을 쓴다.

---

## 3. 노드 색 — 색이 타입을 말한다

모양은 하나(원)로 두고 **색 아홉이 타입을 말한다.** Obsidian 의 "그룹"
색칠에 해당한다. 한때는 색을 갈래 넷으로만 쓰고 세부 타입을 모양(육각형·
마름모·사각형…)으로 갈랐는데, 모양을 걷어내고 색이 그 짐을 다 지도록 바꿨다.

색은 2026-09-06 사용자가 준 여섯 색 팔레트(Coolors "skafold")에서 가져온다:
초록 `004F2D` · 남색 `003049` · 빨강 `D62828` · 주황 `FB6C13` · 노랑 `F9C80E`
· 크림 `F5E0B7`. 여섯으로 아홉 타입을 다 못 덮으므로 **색상은 팔레트에서만
가져오고 밝기로 갈랐다.** 주황·노랑·크림은 원색 그대로다. **인물만 예외로
팔레트 밖의 파랑 `#3d84f5`** 다 — 팔레트를 씌운 뒤 사용자가 "인물 노드도
blue 로" 라고 했다 (2026-09-06). 연표의 재위 띠와 같은 파랑이다. 빨강
`D62828` 은 원색으로 쓰이는 자리가 없고 작품(연빨강)에 밝힌 값으로만 남는다. 남색과
초록은 원색 그대로 두면 바탕 `#1e1e1e` 위에서 대비가 1.2·1.7 이라 작은 원이
보이지 않았다 (그려서 확인했다). 그래서 같은 색상으로 밝힌 값을 장소·영상에
쓰고, 원색에 가까운 어두운 값을 물러나 있어야 할 뼈대(시대·직위)에 뒀다.
색약 검증기는 눈금 5.0 을 넘긴다 (정상 10.7 · 2형 9.7 · 1형 7.3).

그 전(2026-09-05)에는 Obsidian 의 색 이름(blue·purple·orange·green·yellow·
pink·cyan)에 맞춰 골랐었다.

`web/src/lib/graph-view.js` `TYPE_COLOR`:

| 타입 | 화면 이름 | 색 | 갈래 |
|---|---|---|---|
| `person` | 인물 | `#3d84f5` 파랑 (팔레트 밖, 사용자 결정) | 인물·단체 |
| `org` | 단체·국가·왕조 | `#f5e0b7` 크림 (원색) | 인물·단체 |
| `event` | 사건 | `#fb6c13` 주황 (원색) | 사건 |
| `place` | 장소 | `#2e9e5e` 초록 (`004F2D` 를 밝힘) | 장소·유물 |
| `heritage` | 유물·문화재 | `#f9c80e` 노랑 (원색) | 장소·유물 |
| `artwork` | 예술작품 | `#f2a0a0` 연빨강 (`D62828` 을 밝힘) | 장소·유물 |
| `media` | 영화·드라마 | `#3a7ca5` 남색 (`003049` 을 밝힘) | 장소·유물 |
| `period` | 시대 | `#2a5d78` 어두운 남색 | 시대·직위 |
| `role` | 직위·칭호 | `#1e6b45` 어두운 초록 | 시대·직위 |

`concept`(개념·주제)는 **색을 따로 갖지 않는다.** `GROUP_COLOR.frame`
(`#2a5d78`)으로 물러난다 — 아홉 색도 흩어진 작은 원에서는 구별이 빠듯해서
열 번째를 만들지 않았다.

갈래는 **색상 계열**로 남는다. 인물·단체는 파랑·크림, 사건은 주황,
장소·유물·작품은 초록~노랑~연빨강~남색, 시대·직위는 어두운 남색·초록. 화면이
먼저 네 덩어리로 읽히고 그 안에서 타입이 갈린다.

로고 표식(`style.css` `--mark-*`)과 문서 페이지의 `--actor·--event·--thing`
은 이 표의 person·event·place 를 베낀 것이다 — **색을 바꾸면 네 곳**
(graph-view.js · style.css · doc.css · pages.py)과 `render.test.mjs` 의
색값 두 곳을 같이 바꾼다.

### 색 거리는 재서 안다

`python3 tools/check_palette.py` 가 `graph-view.js` 에서 팔레트를 **직접
읽어** 잰다 (OKLab ΔE ×100, 색약 시뮬레이션은 Viénot 1999). 눈금은 5.0.
2026-09-05 실측:

| | 정상 시야 | 2형(deutan) | 1형(protan) |
|---|---|---|---|
| 타입 9색 | 10.9 (org↔artwork) | **6.8** (event↔place) | **5.5** (event↔place) |
| 갈래 4색 | 23.4 | 6.8 (event↔thing) | 5.5 |

옛 팔레트(10.7 · 5.9 · 5.7)와 같은 급이다. 3형(tritan)은 도구가 함께 찍지만
**판정에 쓰지 않는다** — Viénot 의 방법은 1·2형에만 유효하다.

**색을 고치면 이 검증기를 다시 돌린다.** 위 숫자는 그때의 기록일 뿐이고
실제 값은 검증기가 말한다.

---

## 4. 캔버스 (`graph-view.js`)

React 가 그리지 않는다. 초당 60번 다시 그려지는 곳이라 가상 DOM 을 통과시킬
이유가 없다 — React 는 자리를 잡아주고 `destroy()` 로 수명만 관리한다.

캔버스는 CSS 변수를 못 읽으므로 값을 상수로 한 번 더 들고 있다 —
**`style.css` 를 고치면 여기도 고친다.**

| 상수 | 값 | = 토큰 |
|---|---|---|
| `SURFACE` | `#1e1e1e` | `--background-primary` (라벨 외곽선·노드 링) |
| `EDGE_BASE` | `#4a4a4a` | `--graph-line` 보다 한 단 밝다. 1px 선은 `#3f3f3f` 로는 안 보였다 |
| `EDGE_SOFT` | `rgba(74,74,74,.35)` | 조명 밖으로 물러난 선 |
| `EDGE_SAME` | `#3f3f3f` | same_as — 관계가 아니라 이음이라 더 어둡다 |
| `EDGE_LIT` / `EDGE_LIT_SAME` | `#a8a8a8` / `#7a7a7a` | 가리킨·고른 노드에 붙은 선. 보라를 쓰지 않는다 (2026-09-05 사용자 결정) |
| `ACCENT` / `ACCENT_SOFT` | `#8a6cef` / `#af9af4` | `--color-accent` / `--color-accent-2` — 노드 테두리에만 |
| `CAUSE_LIT` | `#8cc4ea` | `--color-blue` 계열 — 인과 도면의 선. 연표의 '원인'과 같은 파랑 |
| `TEXT` / `TEXT_DIM` | `#dadada` / 62% | `--text-normal` / `--text-muted` 무게 |

**노드.** 원 하나. 반지름 `base + min(11, √차수 × 1.7)`, `base` 는 뼈대(frame)
4 · 나머지 6 (`layout.js` `nodeRadius`). 바탕색 링(2px)을 둘러 노드가 겹쳐도
서로 먹히지 않게 한다. Obsidian 은 초점 노드를 강조색으로 **칠하지만** 우리
노드는 타입 색을 지고 있으므로 **테두리**로 두른다.

| 상태 | 그림 |
|---|---|
| 고른 노드 | `ACCENT` 2px 링, 반지름 +4.5 |
| 그래프 중심 | 흰색 45% 1.5px 링, 반지름 +4.5 |
| 가리킨 노드 | `ACCENT_SOFT` 1.5px 링, 반지름 +3.5 |
| 조명 밖 | `globalAlpha` 0.16 (Obsidian 은 거의 지운다) |

**설정이 렌더러에 닿는 길.** `display = { nodeScale, lineScale, textFade,
arrows }` · `hiddenEdgeTypes` · `forces = { center, repel, link }`. 각각
`setDisplay`·`setEdgeFilter`·`setForces` 로 들어온다. 노드 크기와 힘은 배치를
다시 데운다 (자리는 지킨 채 alpha 0.4 / 0.6). 관계 종류를 끄면 그 선을 안
그리고, 선이 하나도 안 남은 노드도 안 그린다 — 시뮬레이션에는 남아 있어
켜면 제자리로 돌아온다 (`nodeShown`·`edgeShown`).

**이름표는 멀리서 흐려진다** (`labelAlpha`). 문턱 `textFade` 0~1 이 "완전히
보이는 배율" 0.3~1.5 를 정하고, 그보다 0.35 배 아래까지 서서히 사라진다.
기본 0.3 이면 배율 0.66 에서 다 보이고 0.31 에서 없어진다. 중심과 초점의
이름은 늘 그린다. Obsidian 의 '텍스트 흐림 문턱'과 같은 몸짓이다.

**조명.** 가리키는 동안은 hover 가, 마우스를 떼면 클릭해 둔 노드가 조명을
이어받는다. 빈 곳을 누르면 꺼진다. 조명이 켜지면 그 노드와 직접 이웃만
밝고, 나머지 엣지는 `EDGE_SOFT` 1px 로 물러난다.

**노드를 누르면 캔버스가 인과 도면이 된다** (`showCausal` · `causalLayout` ·
`_drawCausal`). **지금은 꺼져 있다** — `App.jsx` `CAUSAL_DIAGRAM = false`
(2026-09-06 사용자: "인과관계 그래프를 지우진 말고 기능 꺼두자"). 꺼진 동안
클릭은 전처럼 주변 관계를 열고, 이웃 그래프의 인과 선은 굵은 회색(+1.2px)이며
범례도 글자색이다. 켜면 아래대로 움직이고 범례의 인과 줄을 `#4f93bf` 로 바꾼다. 2026-09-06 사용자 요청의 자취: "노드를 클릭하면 인과관계를
보여주는 그래프를" → 이웃 위에 파란 사슬을 얹었더니 "처음부터 보여주지
말고(그럼 너무 복잡해 보임)" → 누른 뒤에만 얹고 나머지를 흐렸더니 "너무
복잡하게 그려지고 있어서 아무런 정보값이 없어. 팔란티어나 다른 회사의
그래프 디자인을 검색해서 공부해서 수정해봐". 찾아본 셋이 같은 말을 했다:

- 팔란티어 Vertex 는 그래프 배치에 **계층(좌→우)** 을 따로 두고 뿌리 노드를
  골라 엣지 방향대로 층을 세운다 (Foundry 문서 "Graphs display options").
- Cambridge Intelligence(KeyLines)는 힘 배치가 전체 모양만 보여주므로 흐름이
  있는 자료는 **순차 배치(sequential layout)** 로 층을 나눠야 얽힘이 준다.
- 사건 인과 시각화 논문들(VAC2·DOMINO)은 인과 나무를 **시간축을 따라 원인 →
  결과의 흐름도**로 그리고, 고른 사건에서 출발해 그 사건으로 이어지는 경로만
  **점진적으로** 펼친다.

공통은 하나다 — 인과는 방향이 뜻이므로 **층으로 세우고, 그 밖의 관계는
그리지 않는다.** 그래서:

- **처음 열릴 때**(부팅·검색·주소·시작점)는 주변 관계 그래프 그대로다. 인과도
  회색이다.
- **캔버스에서 노드를 누르거나 상세·연표에서 고르면** `App.openCausal` 이
  `/api/chain` 을 **두 걸음**만 받아(`depth=2` — 네 걸음은 심하전투 원인 31개가
  한 화면에 겹쳤다) `GraphView.showCausal` 로 도면을 세운다. 주변 관계
  그래프는 자리·카메라째 접어 두고(`_saved`) 빈 곳을 누르면 그대로 돌아온다
  (`exitCausal`). 인과가 없는 노드(대부분의 인물)는 전처럼 주변 관계를 편다.
  도면 안의 노드를 누르면 그 노드의 도면으로 이어 간다 — 이것이 점진적
  탐색이다. 더블클릭은 도면을 접고 그 노드의 이웃을 편다. Esc 도 접는다.
- **배치**(`causalLayout`): 가운데 0열이 고른 노드, 왼쪽이 원인, 오른쪽이
  결과. 열은 수기야마(Sugiyama) 층 매기기의 **가장 긴 경로**다 — 한 노드가
  여러 가지에 나와도 한 번만 서고, 모든 선이 왼쪽에서 오른쪽으로만 간다
  (가까운 열에 두면 후금 → 정묘호란이 같은 열 안에 서고 방향이 사라진다).
  열 안은 연도순, 모르면 뒤. 순환은 되돌아오는 선을 층에서 뺀다.
- **화면 자로 잰다**(`_layoutCausal`): 이름표는 배율과 무관하게 11px 라
  줄 간격을 배율로 줄이면 겹친다. 열 간격은 화면 폭을 열 수로 나눈 것
  (120~230px), 줄 간격은 화면 높이를 가장 긴 열로 나눈 것(40~58px), 배율 1.
  폭이 넘치면 그만큼만 줄이되 줄 간격은 화면에서 40px 아래로 안 내려간다.
  캔버스 크기가 바뀌면(상세 패널이 열리면 폭이 준다) 다시 잰다.
- **그리기**: 선은 열 사이를 잇는 3차 곡선(`bezierCurveTo`, 접선 수평) —
  곧은 선은 중간 열의 점을 가로지른다. 파랑 `CAUSE_LIT` 2px, 추출(`conf<1`)은
  파선, 화살촉은 끝 접선을 따른다. 종류(배경·계기·영향·원인, 서버가 엣지
  라벨로 보낸다)는 선 한가운데에 — 선이 24 를 넘으면 가리킨 경로에만.
  이름은 전부 쓰고 아랫줄에 연도(`names[1]`). 열 머리('원인'·'원인의
  원인'·'더 앞선 원인' / '결과'·…)를 흐리게 적어 색만으로 뜻을 나르지 않는다.
  노드를 가리키면 그 노드를 지나는 경로만 밝다(`causalReach`).
- 안내 칩: `심하전투의 인과 · 원인 16 · 결과 11 · 노드를 누르면 그 노드의
  인과로, 빈 곳을 누르면 주변 관계로`.

헤드리스 크롬(CDP)으로 심하전투를 열고 눌러 확인했다. **도면이 자료 오류를
드러낸다** — 심하전투(1619)의 결과로 표전문제(1395)·종계변무(1589)가
섰다. 연대 관문(`chronology`, CLAUDE.md §1-5)의 몫이다.

검증용 손잡이: `GraphCanvas` 가 `window.__histgraphView` 에 뷰를 건다. 화면
코드는 쓰지 않는다 — CDP 스크립트가 노드의 자리를 읽어 누르는 데 쓴다.

**엣지는 회색 한 가지다.** 전에는 출발 노드의 타입 색을 입혔다("사람이 건
관계인지 사건이 건 관계인지가 한눈에 갈린다"). 걷어낸 이유: 색이 점과 선에
다 있으면 화면이 아홉 색으로 뒤덮여 점이 안 읽힌다. 방향은 화살촉이 말한다
(설정에서 끌 수 있다 — Obsidian 의 '화살표'). 조명 안(가리킨·고른 노드에
붙은 선)만 **밝은 회색**(`EDGE_LIT`) 1.8px, 조명 없을 때 1.1px, 여기에 '선 굵기'
배율. 선에는 강조색(보라)을 쓰지 않는다 — 2026-09-05 "에지 컬러가 보라색인데
회색으로". 파랑은 위의 인과 도면에서만 나온다.

| 선 | 뜻 | 범례 문구 |
|---|---|---|
| 실선 | `conf = 1`, 구조화 소스 | 구조화 소스 (확실) |
| `5 4` 파선 | `conf < 1`, 산문에서 추출 | 산문에서 추출 (근거 있음) |
| `2 4` 점선 | `same_as` (`EDGE_SAME`, 강조 시 `EDGE_LIT_SAME`) | 동일 실체 (same_as) |
| +1.2px 굵게 (도면을 켜면 파랑 곡선) | `caused` 인과 | 인과 (원인 → 결과) |

어디서 온 사실인지 화면에서 구분되지 않으면 둘 다 못 믿게 된다.

**화살촉.** 도착 노드 바로 앞(`r + 3`)에 5px 삼각형, 선과 같은 색. 방향이
없으면 '누가 누구에게'가 사라진다. `same_as` 에는 붙이지 않는다.

**관계 이름은 가리켰을 때만** 그린다. 이웃이 16개 이하이고 배율이 0.5 를
넘을 때. 늘 그리면 자녀가 12명인 세종 주위에 '자녀'가 12번 겹쳐 찍힌다.

**이름표는 자리가 있으면 붙이고 없으면 만다.** 겹치는지를 실제로 재서
정한다 — '차수 6 이상만' 같은 기준은 화면 사정과 무관해서, 왕조를 중심에
놓으면 왕후 수십 명이 이름 없는 파란 점이 된다. 우선순위는
`(초점 3 · 중심 2) + min(차수/40, 1)`, 중심과 초점은 무슨 일이 있어도 그린다.
한 번 뜬 이름은 3px 더 겹쳐야 물러난다 — 겹침 판정이 매 프레임 뒤집히면
이름이 초당 몇 번씩 깜빡인다. 글자는 초점 12.5px 600 · 나머지 11px, `TEXT`
/ `TEXT_DIM`, 바탕색 외곽선(3px)을 둘러 선 위에서도 읽히게 한다. 두 번째
이름(`names[1]`)은 9.5px 로 **아랫줄에** 쓴다 — 옆에 이어 붙이면 '기축옥사 ·
정여립의 난'이 한 줄로 길어져 이웃 라벨을 밀어낸다.

**배치**(`layout.js`)는 d3-force. 계수는 손으로 짠 옛 시뮬레이션에서 옮겨
왔다: 반발 `300 + r×24` (800px 컷) · 링크 거리 `112 + r + r`(강도 0.12) ·
`same_as` 34(강도 0.35) · 겹침 방지 `r + 6`(0.7) · 중심 끌기 0.06 / 그 외
0.016 · `velocityDecay` 0.18 · `alphaDecay` 0.015. 설정의 '힘' 절은 이
계수를 바꾸지 않고 **배율만 곱한다** (`forces = { center, repel, link }`,
기본 1) — 그래서 배치 검증은 배율 1 로 그대로 돈다.

**틱은 직접 돌린다** — 초당 60틱 고정. 힘 계수가 전부 "한 틱당"으로 잡혀
있어서 d3 타이머에 맡기면 120Hz 화면에서 같은 그래프에 힘이 두 배로 들어간다.

배치가 화면 밖으로 달아나지 않게 세 가지를 건다: 시작 위치를 **황금각
나선**(2.399963 rad)에 고르게 놓고, `forceCollide` 로 겹침을 막고, 식을
때까지(`alpha > 0.02`) 카메라가 8%씩 따라간다. 사용자가 화면을 움직이면
그때부터 손을 뗀다. 배율 한계는 맞춤 0.22~1.35 · 휠 0.25~3.5.

---

## 5. 배치

```
┌ .top  머리 줄 (secondary, 52px) ────────────────────────────────────┐
│ ● histgraph  [조선~대한민국]        [검색 ─────────────]         [☾] │
├───────────┬──────────────────────────────────────┬──────────────────┤
│ .timeline │ .stage-wrap                          │ .detail          │
│ 252px     │ [⚙]┌ 설정 ─┐  .stage 캔버스 (primary)│ 348px            │
│ 선택 시   │    │ 시작점 │                        │ 노드 고를 때     │
│           │    │ 필터   │                        │ = 문서 한 장     │
│           │    │ 범례   │                        │                  │
│           │    │ 표시   │                        │                  │
│           │    │ 힘     │                        │                  │
│           │ [조선 주변 · 노드 120]               │                  │
├───────────┴──────────────────────────────────────┴──────────────────┤
│ .foot 상태 줄 (24px)  © 2026 histgraph   방침 · 약관   노드 · 엣지   │
└─────────────────────────────────────────────────────────────────────┘
```

| 자리 | Obsidian 의 무엇 | 클래스 | 정한 것 |
|---|---|---|---|
| 머리 줄 | 탭 머리 줄 | `.top` | 52px, secondary 바탕. 세 칸 격자(1fr · 640px · 1fr)라 검색이 화면 한가운데 온다. 시대 이름은 열린 문서 탭처럼 보이는 단추(`.era`) — 누르면 중심으로 |
| 테마 단추 | 설정의 외형(라이트/다크) | `.theme-toggle` | 격자 셋째 칸 오른쪽 끝, `.clickable-icon`. 아이콘은 **누르면 되는 것**(어두운 화면엔 해, 밝은 화면엔 달). 이름표도 '밝은 화면으로'·'어두운 화면으로' |
| 검색 | 빠른 전환(quick switcher) | `.search`, `.results` | 입력칸은 form-field 바탕·5px·38px 높이·15px 글자 (09-06: 작아서 있는 줄 모른다는 지적). 결과는 primary 바탕·12px·`--shadow-l`. 고른 줄은 hover 바탕 + 강조색 글자 |
| 연표 | 왼쪽 사이드바의 파일 트리 | `.timeline` | §6 |
| 캔버스 | 그래프 뷰 | `.stage`, `#canvas` | §4. 왼쪽 아래 안내 칩(`.stage-note`), 비었으면 가운데 안내 |
| 설정 상자 | graph-controls | `.graph-controls`, `.graph-controls-toggle` | 캔버스 **왼쪽 위**(Obsidian 과 같은 자리), 240px, `--shadow-s`. **기본 접힘** — 조절 단추(lucide sliders)만 남는다. 절은 `<details>` 라 따로 접힌다. 아래 표 |
| 상세 | 오른쪽 사이드바의 문서 + 백링크 창 | `.detail` | §7 |
| 상태 줄 | status bar | `.foot` | 24px, 11px 글자. 노드·엣지 수는 오른쪽 끝(`.counts`) |

### 설정 상자의 절 (Obsidian 그래프 설정과 같은 순서)

| 절 | Obsidian | 안에 있는 것 | 닿는 곳 |
|---|---|---|---|
| 시작점 | (없음 — 우리 것) | 차수 상위 12개. 누르면 그 주변을 편다 | `load(id)` |
| 필터 | Filters | 펼침 깊이 · 최대 노드 · 연도·시대 노드 포함 · **관계 종류** 체크 목록(`meta.edge_types`, 개수 순) | 앞 셋은 그래프를 다시 받고, 관계 종류는 `setEdgeFilter` (다시 받지 않는다) |
| 범례 | Groups | 타입 색 견본 + 이름 + 개수, 선 종류 | 읽기만 |
| 표시 | Display | 화살촉 · 이름표 보이기 · 왼쪽 연표 보이기 · 슬라이더: 이름표 흐림 문턱(0~1) · 노드 크기(0.5~2) · 선 굵기(0.5~3) | `setDisplay` |
| 힘 | Forces | 슬라이더: 중심 힘(0~2) · 반발 힘(0.2~3) · 링크 거리(0.4~2.5) | `setForces` |

기본으로 시작점·범례·표시가 열려 있고 필터·힘은 접혀 있다. 슬라이더는
값을 오른쪽에 늘 적는다 (Obsidian 은 툴팁으로 보여 주지만 늘 보이는 편이
되돌리기 쉽다). 설정은 `App.settings` 한 객체에 있고 `GraphCanvas` 가
절마다 효과를 따로 두어 슬라이더 하나가 움직일 때 다른 절이 배치를 다시
데우지 않는다.

`#root` 가 `body` 의 세로 배치를 이어받는다 — 이걸 빼면 `.layout` 의
`flex:1` 이 기댈 곳을 잃어 연표 스크롤과 캔버스 크기가 한꺼번에 죽는다.
`render.test.mjs` 가 이 규칙과 `#canvas` id 를 잰다.

반응형은 **한 단계뿐**이다 (`max-width: 1100px` → 연표 208 / 상세 300px).
모바일 배치는 없다.

---

## 6. 연표 (`timeline.js`)

그래프는 무엇이 무엇과 이어져 있는지만 말하고 **언제인지는 말하지 않는다.**
그 자리를 이 막대가 맡는다. 겉모습은 Obsidian 의 파일 트리다 — 줄은 hover
에 흰색 7.5%, 고른 노드는 `--text-normal` 600, 나머지는 muted/faint.

**자리는 연도에 비례한다.** 목록으로 늘어놓으면 1400년과 1401년 사이가
1400년과 1500년 사이만큼 벌어져 연표라고 부를 수 없다. 축은 언제나 그래프가
담은 처음 해부터 끝 해까지다 — 어느 노드를 골라도 같은 축 위에 서므로
옮겨 다니며 자리를 견줄 수 있다. 조선 그래프는 조선 건국(1392)에서 시작한다.

치수 (`timeline.js` 상단 상수):

```
LANE_W 104   재위 띠 칸 (끄면 0)      BAR_X 11 · BAR_W 6 · DEATH_X 19
AXIS_X  46   세로축                   DOT_X 58  라벨 시작
PAD_TOP 18 · PAD_BOTTOM 64            GAP 30   라벨 최소 간격
LANE_GAP 26  재위 라벨 최소 간격 (이름+연도 두 줄)
MAX_LEVEL 4 · MAX_HEIGHT 40000px
```

**줌은 자리가 아니라 밀도를 바꾼다.** 0단계는 시대 전체가 한 화면에
들어오고 한 단계마다 눈금이 두 배가 된다. 무엇이 '큰' 사건인지는 그래프가
정한다 — 많이 연결된 사건부터 자리를 잡고, 30px 안에서 겹치는 사건은 다음
단계로 물러난다. **빠진 것이 없는 셈이 되면 안 되므로** 머리에
`큰 사건 13 / 156` 를 늘 적는다.

**그리는 규칙**:

| 것 | 그림 |
|---|---|
| 고른 노드 | 점 r 4 + 구간 막대 4px(85%), 굵은 글자 (`.k-self`) |
| 이웃·뼈대 | 점 r 2.6, `--text-muted` (`.k-anchor` 는 `--text-faint`) |
| 왕조 | 축 **옆**(−6px)에 2px 막대(50%) — 축 위에 겹치면 사건 점을 다 덮는다 |
| 재위 막대 | `TYPE_COLOR.person` 6px 알약, 한 칸 걸러 78%/50%, 해당 임금 100%, 그 이름은 강조색 |
| 몰년 | `--surface-2` 로 채운 r 2.8 원 + `2 3` 점선 |

**사람은 점으로 찍지 않는다.** 사건·작품만 연표의 점이 된다. 인물·장소·조직은
이어지는 것이라 한 점에 찍으면 거짓을 말한다. 사람이 언제 살았는지는 그
사람을 골랐을 때 생몰 구간(막대)으로 말한다.

**원인은 파랗고, 오른쪽 여백의 꺾인 선이 고른 노드와 잇는다** (2026-09-05
사용자 요청: "'원인' 표시를 blue 로, 타임라인 오른쪽으로 꺾인 모양의 라인으로
원인 사건과 연결"). `caused` 엣지의 들어오는 쪽(`rel.dir === 'in'`)만 원인이다
(`isCause`). 딱지 `.tl-rel.is-cause` 는 `--color-blue`(`#4f93bf`, 노드
팔레트의 남색을 글자가 읽히게 밝힌 것). 왼쪽 칸의 왕·대통령 재위 띠도
파랑(`timeline.js` `REIGN_COLOR` `#3d84f5`)이다 — 2026-09-06 노드 팔레트를
바꾼 뒤 사용자 결정으로 옛 파랑을 그대로 둔다. 노드 색과 잇지 않는다.
선(`causeWire`)은 원인 줄에서 오른쪽으로 12px 나가 캔버스 오른쪽
12px 안쪽의 세로 선을 타고 고른 노드 줄로 돌아오며, 끝의 화살촉이 고른 노드를
가리킨다 — 축 위에 그리면 점·막대와 겹치고 라벨 사이를 지나면 글자를 가른다.
선이 있는 연표만 줄을 24px 접는다(`.tl-canvas.has-cause .tl-mark`). 결과(out)
는 잇지 않고 딱지만 `결과` 로 단다 (`DIR_HEAD.caused`) — 서버는 인과 이름을
'원인'으로만 주므로 방향을 여기서 가른다. 양쪽을 다 그리면 선이 어느 쪽으로
읽히는지 흐려진다.

**원인은 결과보다 위에 선다.** 결과의 날짜가 거칠어(연도만) 원인의 날짜를
품으면 문자열 순으로는 결과가 먼저다 — 병자호란(12월 9일)이 공석신주사건
(1636) 아래에 섰다 (2026-09-05 지적). `sortMarks` 는 원인을 품는 거친 날짜를
원인 **바로 뒤**에 세운다 (같은 날도 원인이 먼저). 고른 노드와 이웃 사이의
인과만 안다. 날짜 자체가 틀린 것은 화면이 아니라 `chronology` 표가 고친다
(CLAUDE.md §1-5).

**넘겨받은 연도는 같은 얼굴로 내놓지 않는다.** 노드가 스스로 적고 있는
날짜가 아니라 이어진 연도 노드에서 가져온 값이면 머리에 `연결된 연도`
알약(`.tl-via`)을 붙인다. 연도도 없고 연도를 아는 이웃도 없으면 자리를
지어내지 않고 "연도를 알 수 없습니다"라고 적는다.

**머리의 훑기 막대**(`.tl-map`)는 연표가 화면보다 길 때만 나온다. 창틀
(테두리만, 칠하지 않음)이 지금 보이는 구간, 보라 표식이 고른 노드의 자리다.
칠해 버리면 둘 다 '막대'로 보여 무엇이 무엇인지 갈리지 않는다.

배율과 재위 띠 여부는 `localStorage`(`tl-zoom`·`tl-reigns`)에 남는다.

---

## 7. 상세 패널 (`DetailPanel.jsx`) — 문서 한 장

위에서부터 Obsidian 문서를 읽는 순서다:

1. **`← 되돌아갈 노드`** `.d-back` — sticky. 관계 목록이 길어 스크롤을 내려도
   되돌아갈 자리가 사라지면 안 된다. 되돌아갈 곳을 **이름째** 들고 있는다.
2. **타고 들어온 관계** `.d-via` — 강조색 15% 바탕. 이 패널에서 강조색
   바탕을 쓰는 유일한 자리. 조선시대 중기에는 개체가 37개 달려 있어 방금
   누른 것을 목록에서 다시 찾아야 했다.
3. **타입 딱지** `.d-type` — 색 견본 + 타입 이름, 회색 알약. 색만으로
   읽히지 않게 늘 같이 둔다.
4. **제목** `.d-title` — 인라인 제목. 20px 700, -0.02em.
5. **기간** `.d-dates` — 속성 줄처럼 muted, tabular-nums.
6. **본문** `.d-desc` — **상자에 넣지 않는다.** 14px, 1.65. 문서 본문은 그냥
   글이다. 서버가 주는 것은 요약이고 전문 보기는 없다 (애드센스 스크랩 판정).
   설명이 없으면 콜아웃(`.d-nodesc`, 왼쪽 3px 회색 줄)으로 왜 없는지 적는다 —
   빈 칸을 그냥 두면 '원래 설명이 없는 노드'로 읽힌다. 넘겨받은 글은
   그렇다고 적는다(`.d-desc-src`) — 출처 표기가 아니라 경고다.
7. **출처 한 줄** `.d-desc-origin` — 11px faint, 밑줄 링크. CLAUDE.md §1 의
   유일한 예외.
8. **절 머리** `.d-section-title` — 백링크 창 머리처럼 13px 600 + 위 1px 선.
   개수는 칩(`.flair`)에 든다.
9. **다른 이름** `.d-aliases` — 속성 창의 알약(2em).
10. **인과 사슬** (`ChainPanel`) — 관계 목록보다 먼저. 종류 딱지(`.chain-kind`)
    는 강조색 10% 알약, 들여쓰기가 걸음 수.
11. **관계** `.rel` — 백링크 목록. **카드가 아니라 줄이다.** 테두리·바탕 없이
    hover 만 밝아지고 이름이 강조색이 된다. 근거는 인용(`.rel-ev`, 왼쪽 2px
    회색 줄). 여러 관계가 함께 딛고 선 근거는 묶음 머리 아래 한 번만.

**자취는 상세 안에서만 쌓인다.** 그래프나 연표에서 고른 노드는 그 자체가
뿌리다 — 캔버스에서 아무 노드나 누를 때마다 직전 노드가 상위로 붙으면
아무 관계 없는 둘이 부모-자식처럼 보인다. 최대 50칸. 관계 문장은 한국어
조사를 받침으로 가려 붙인다 (`relations.js` `pt`).

---

## 8. 문서 페이지 (`/n/`, 방침·약관)

Obsidian 의 읽기 모드: 한 줄 너비 `--file-line-width: 700px`, 본문 16px,
줄 간격 1.5, h1 1.8em 700, h2 1.25em 600, 링크는 강조색에 밑줄. 목록 줄은
hover 에 흰색 7.5%. 콜아웃(`.note`)은 강조색 8% 바탕에 왼쪽 3px 강조색 줄.

세 벌이다 — `web/doc.css`(방침·약관), `src/histgraph/pages.py` 의 `STYLE`
(`/n/`), `web/index.html` 의 noscript 인라인 색. 토큰 값은 셋이 같다.

---

## 9. 상호작용

| 몸짓 | 하는 일 |
|---|---|
| 노드 클릭 | 상세 열기 + 그 주변을 **얹어서** 펼치기 |
| 노드 더블클릭 | 자리를 지킨 채 이웃만 얹기 |
| 노드 드래그 | 그 자리에 못박기 (끌지 않고 떼면 고르기) |
| 빈 곳 클릭 | 조명 끄기 |
| 휠 | 캔버스 확대 (커서 아래 지점 고정) |
| 왼쪽 위 조절 단추 | 설정 상자 열기·닫기 |
| 필터의 관계 종류 | 그 종류의 선과, 선이 안 남은 노드를 숨긴다 |
| 표시·힘 슬라이더 | 그 자리에서 바로 반영 (노드 크기·힘은 배치를 다시 데운다) |
| `/` | 검색창으로 |
| `↑`·`↓`·`Enter` | 검색 결과 오르내리기·고르기 |
| `Esc` | 상세 닫기 (한 칸씩이 아니라 통째로) |
| `Alt`/`⌘` + `←` | 상세 안에서 한 칸 되돌아가기 |
| `⌘`/`Ctrl` + 휠, 핀치 | 연표 줌 (그냥 휠은 스크롤이어야 한다) |

주소(`location.hash`)에 지금 보는 노드를 남긴다 — 새로고침해도 자리를 잃지
않고, 링크로 넘길 수 있다. 검색은 140ms 쉬고 묻는다.

**화면을 열면 왕조에서 시작한다.** 차수 1위 노드로 열면 그때그때 병자호란이
중심이 되는데, 그건 우연이고 "이게 무엇의 그래프인가"를 말해주지 못한다.
머리의 시대 단추가 언제든 중심으로 되돌린다.

---

## 10. 지키는 것 · 바꿀 때

- **한글만.** 이 디자인이 들여온 영어는 없다. 아이콘의 `aria-label` 도 한국어.
- **색만으로 뜻을 나르지 않는다.** 범례에 타입 이름, 상세에 타입 딱지.
- **상자보다 줄.** 새 요소를 그릴 때 테두리·바탕을 먼저 두지 않는다. 줄로
  두고 hover 에 흰색 7.5% 를 얹는다. 떠 있는 것(설정 상자·검색 결과)만
  바탕·선·그림자를 갖는다.
- **강조색은 "지금 이것"과 "누를 수 있다"에만.**

```bash
python3 tools/check_palette.py     # 노드 색을 만졌으면 — 2형·1형 5.0 이상
cd web && npm test && npm run build
```

| 파일 | 무엇을 |
|---|---|
| `web/tests/layout.test.mjs` | 배치가 식는가 · 겹치지 않는가 · 중심이 가운데를 지키는가 |
| `web/tests/relations.test.mjs` | 조사·문장·카드 합치기 |
| `web/tests/render.test.mjs` | 실제 조립 · 타입 색값 · **화면에 영어가 새지 않는가** |

- 노드 색: graph-view.js `TYPE_COLOR` → style.css `--mark-*` → doc.css →
  pages.py → `render.test.mjs` 의 색값 두 곳.
- 바탕·글자 색: style.css `:root` → graph-view.js 상수 → doc.css → pages.py
  → index.html noscript.

---

## 11. 안 옮긴 것 · 어긋나 있는 자리

- **라이트 테마.** Obsidian 은 둘 다 있지만 우리는 다크만이다. 캔버스가 색을
  상수로 들고 있어 테마를 바꾸려면 `getComputedStyle` 로 토큰을 읽게
  고쳐야 한다. 필요해지면 그때 — 값은 Obsidian 라이트(base-00 `#ffffff`,
  base-100 `#222222`, 강조는 같은 보라)를 쓰면 된다.
- **노드를 회색 한 가지로.** Obsidian 의 기본은 모든 노트가 `--text-muted`
  회색 점이다. 우리 점은 타입을 말해야 하므로 색을 남겼다 (§3).
- **Inter 글꼴** (§2).
- **모바일 배치가 없다.** 1100px 한 단계에서 폭만 줄이고, 그 아래로는
  그대로 눌린다.

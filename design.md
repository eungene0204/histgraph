# 화면 설계

지금 코드가 실제로 하고 있는 것을 적는다. **바라는 모습이 아니라 현재
상태**다. 값이 바뀌면 이 문서도 같이 고친다 — 여기 적힌 숫자는 전부
파일에서 읽어 온 것이고, 색 수치는 `uv run tools/check_palette.py` 가
그때그때 다시 잰다.

설계의 배경과 실패 기록은 [README.md](README.md) "화면(`serve`)" 절에 있다.
여기는 **지금 화면이 어떤 규칙 위에 서 있는가**만 모아 둔다.

파일:

```
web/index.html            껍데기 (lang="ko", #root 하나)
web/style.css             토큰과 모든 CSS (442줄, 단일 파일)
web/src/App.jsx           상태와 배선
web/src/components/       Search · SidePanel · TimelinePanel · GraphCanvas · DetailPanel · Glyph
web/src/lib/graph-view.js 캔버스 렌더러 + 팔레트 (React 를 모른다)
web/src/lib/layout.js     d3-force 배치 계수
web/src/lib/timeline.js   연표 렌더러 (React 를 모른다)
web/src/lib/relations.js  관계를 한국어 문장으로
```

---

## 1. 전제 세 가지

**그래프가 주인공이다.** 화면 전체가 어둡고 조용하다. 색은 노드에만 쓴다
(`style.css` 첫 줄). 크롬(머리·패널·테두리)은 전부 무채색이고, 유일한
예외는 누를 수 있는 것에 붙는 파랑(`--accent`)이다.

**색만으로 뜻을 나르는 자리를 만들지 않는다.** 노드 색은 9종이지만 색약
시야에서 최악 쌍이 ΔE 5.9 다 — "다르다"이지 "나란히 놓지 않아도 읽힌다"가
아니다. 그래서 범례는 색 견본 옆에 타입 이름을 글자로 적고, 노드를 고르면
상세 패널이 타입을 글자로 말한다. 캔버스만 보고 타입을 알아야 하는 상황을
남기지 않는 것이 이 팔레트가 기대고 있는 전제다.

**화면에 영어와 자료 출처를 쓰지 않는다.** 사람이 읽는 글자는 전부
한국어다. 영어로만 있는 설명은 사전으로 옮기고, 못 옮기면 비운다.
'Wikidata 한 줄 설명' 같은 딱지도 화면에 적지 않는다 — 읽는 사람이 묻지
않은 것을 답하면서 정작 궁금한 것을 밀어낸다. 수집 경로는 `props` 에 남는다.
`web/tests/render.test.mjs` 가 서버 렌더링 결과에서 영어가 새는지 실제로 잰다.

---

## 2. 토큰

`style.css` `:root`. **다크 전용**이다 (`color-scheme: dark`) — 라이트
테마도, `prefers-color-scheme` 분기도 없다.

| 토큰 | 값 | 쓰는 자리 |
|---|---|---|
| `--surface` | `#141413` | 바탕 · 캔버스 · 라벨 외곽선 |
| `--surface-2` | `#1a1a19` | 머리 · 좌우 패널 · 연표 |
| `--surface-3` | `#232321` | 입력칸 · 카드 · 검색 목록 |
| `--line` | `#2f2f2c` | 모든 테두리 |
| `--text` | `#f0efec` | 본문 |
| `--text-2` | `#c3c2b7` | 보조 |
| `--text-3` | `#8b8b84` | 물러난 글자 · 라벨 머리 · 수치 |
| `--accent` | `#3987e5` | 초점 · 링크 · 누를 수 있는 것 |
| `--mark-actor` | `#4a6ad8` | 로고 표식 |
| `--mark-event` | `#ec7e3e` | 로고 표식 |
| `--mark-thing` | `#3fb968` | 로고 표식 |

**색은 두 갈래뿐이다.** `--accent` 는 크롬이 쓰는 단 하나의 색 — 초점
테두리 · 링크 · 체크박스 · 누를 수 있는 것. `--mark-*` 는 로고 표식
(`.brand .mark`)이 쓰는 세 점이고, 값은 `graph-view.js` 의 `TYPE_COLOR`
person·event·place 를 **그대로 옮긴 것**이다 — 로고는 노드 팔레트의
축소판이라 같은 색을 두 벌 두지 않는다. 나머지는 전부 무채색이다.

글꼴: `"Apple SD Gothic Neo", "Noto Sans KR", "Malgun Gothic", system-ui`.
기본 14px. 캔버스도 같은 스택을 쓴다 (`graph-view.js` `labelFont`).

**크기 눈금** — 실제로 쓰이는 값만:

```
19px  상세 제목            13px  본문·검색 입력·관계 이름
16px  연표 머리의 해        12.5px 범례·설정
15px  로고                 12px  안내 · 셈
11px  섹션 머리 (대문자 간격 .08em, --text-3)
10.5px 연도·재위 연도 (tabular-nums)
```

모서리: 입력칸 8px · 카드 8px · 목록 항목 6~7px · 알약 999px.
그림자는 검색 결과 목록 하나뿐이다 (`0 18px 40px rgba(0,0,0,.55)`).

---

## 3. 노드 색 — 색이 타입을 말한다

모양은 하나(원)로 두고 **색 아홉이 타입을 말한다.** 한때는 색을 갈래
넷으로만 쓰고 세부 타입을 모양(육각형·마름모·사각형…)으로 갈랐는데,
모양을 걷어내고 색이 그 짐을 다 지도록 바꿨다.

`web/src/lib/graph-view.js` `TYPE_COLOR`:

| 타입 | 화면 이름 | 색 | 갈래 |
|---|---|---|---|
| `person` | 인물 | `#4a6ad8` 파랑 | 인물·단체 |
| `org` | 단체·국가·왕조 | `#b394f2` 연보라 | 인물·단체 |
| `event` | 사건 | `#ec7e3e` 주황 | 사건 |
| `place` | 장소 | `#3fb968` 초록 | 장소·유물 |
| `heritage` | 유물·문화재 | `#ffbb31` 금 | 장소·유물 |
| `artwork` | 예술작품 | `#cf7bb0` 분홍 | 장소·유물 |
| `media` | 영화·드라마 | `#7edde2` 옅은 청록 | 장소·유물 |
| `period` | 시대 | `#5f5f5c` 어두운 회색 | 시대·직위 |
| `role` | 직위·칭호 | `#9a8b6c` 흙빛 | 시대·직위 |

`concept`(개념·주제)는 **색을 따로 갖지 않는다.** `GROUP_COLOR.frame`
(`#5f5f5c`)으로 물러난다 — 아홉 색도 흩어진 작은 원에서는 구별이 빠듯해서
열 번째를 만들지 않았다. 개념은 뼈대 쪽이니 물러나 있는 것이 맞다.

갈래는 **색상 계열**로 남는다. 인물·단체는 파랑~보라, 사건은 주황,
장소·유물·작품은 초록~금~분홍~청록, 시대·직위는 무채색. 화면이 먼저 네
덩어리로 읽히고 그 안에서 타입이 갈린다. 뼈대(시대·직위·개념)는 어둡게
묶어 물러나 있다.

### 색 거리는 재서 안다

`uv run tools/check_palette.py` 가 `graph-view.js` 에서 팔레트를 **직접 읽어**
잰다 (OKLab ΔE ×100, 색약 시뮬레이션은 Viénot 1999). 눈금은 5.0.
2026-09-04 실측:

| | 정상 시야 | 2형(deutan) | 1형(protan) |
|---|---|---|---|
| 타입 9색 | 10.7 (org↔artwork) | **5.9** (place↔role) | **5.7** (event↔role) |
| 갈래 4색 | 19.2 | 6.4 (event↔thing) | 10.5 |

갈래 안에서 서로 뭉쳐 보이지 않는지도 함께 잰다 — 인물·단체 18.2 ·
장소·유물·작품 10.3 · 시대·직위 16.5 (2형 기준).

3형(tritan)은 도구가 함께 찍지만 **판정에 쓰지 않는다.** Viénot 의 방법은
저자가 1·2형에만 유효하다고 밝힌 것이라, 3형은 방식에 따라 같은 팔레트가
6 과 14 로 갈린다.

**색을 고치면 이 검증기를 다시 돌린다.** 위 숫자는 그때의 기록일 뿐이고
실제 값은 검증기가 말한다.

---

## 4. 캔버스 (`graph-view.js`)

React 가 그리지 않는다. 초당 60번 다시 그려지는 곳이라 가상 DOM 을 통과시킬
이유가 없다 — React 는 자리를 잡아주고 `destroy()` 로 수명만 관리한다.

**노드.** 원 하나. 반지름 `base + min(11, √차수 × 1.7)`, `base` 는 뼈대(frame)
4 · 나머지 6 (`layout.js` `nodeRadius`). 바탕색 링(2px)을 둘러 노드가 겹쳐도
서로 먹히지 않게 한다.

| 상태 | 그림 |
|---|---|
| 고른 노드 | `--text` 2px 링, 반지름 +4.5 |
| 그래프 중심 | 흰색 50% 1.5px 링, 반지름 +4.5 |
| 가리킨 노드 | 흰색 35% 1.5px 링, 반지름 +3.5 |
| 조명 밖 | `globalAlpha` 0.22 |

**조명.** 가리키는 동안은 hover 가, 마우스를 떼면 클릭해 둔 노드가 조명을
이어받는다. 빈 곳을 누르면 꺼진다. 조명이 켜지면 그 노드와 직접 이웃만
밝고, 나머지 엣지는 `rgba(198,196,186,0.16)` 1px 로 물러난다.

**엣지.** 선은 **출발 노드의 색**을 그대로 입는다 — 사람이 건 관계인지
사건이 건 관계인지가 한눈에 갈린다. 굵기는 조명 밖 1.3 · 조명 안 1.9.

| 선 | 뜻 | 범례 문구 |
|---|---|---|
| 실선 | `conf = 1`, 구조화 소스 | 구조화 소스 (확실) |
| `5 4` 파선 | `conf < 1`, 산문에서 추출 | 산문에서 추출 (근거 있음) |
| `2 4` 점선 | `same_as` (회색 고정) | 동일 실체 (same_as) |

어디서 온 사실인지 화면에서 구분되지 않으면 둘 다 못 믿게 된다.

**화살촉.** 도착 노드 바로 앞(`r + 3`)에 5px 삼각형. 방향이 없으면 '누가
누구에게'가 사라진다. `same_as` 에는 붙이지 않는다 (방향이 없다).

**관계 이름은 가리켰을 때만** 그린다. 이웃이 16개 이하이고 배율이 0.5 를
넘을 때. 늘 그리면 자녀가 12명인 세종 주위에 '자녀'가 12번 겹쳐 찍힌다.

**이름표는 자리가 있으면 붙이고 없으면 만다.** 겹치는지를 실제로 재서
정한다 — '차수 6 이상만' 같은 기준은 화면 사정과 무관해서, 왕조를 중심에
놓으면 왕후 수십 명이 이름 없는 파란 점이 된다. 우선순위는
`(초점 3 · 중심 2) + min(차수/40, 1)`, 중심과 초점은 무슨 일이 있어도 그린다.
한 번 뜬 이름은 3px 더 겹쳐야 물러난다 — 겹침 판정이 매 프레임 뒤집히면
이름이 초당 몇 번씩 깜빡인다. 글자에는 바탕색 외곽선(3px)을 둘러 선 위에서도
읽히게 한다. 두 번째 이름(`names[1]`)은 9.5px 로 **아랫줄에** 쓴다 — 옆에
이어 붙이면 '기축옥사 · 정여립의 난'이 한 줄로 길어져 이웃 라벨을 밀어낸다.

**배치**(`layout.js`)는 d3-force. 계수는 손으로 짠 옛 시뮬레이션에서 옮겨
왔다: 반발 `300 + r×24` (800px 컷) · 링크 거리 `112 + r + r`(강도 0.12) ·
`same_as` 34(강도 0.35) · 겹침 방지 `r + 6`(0.7) · 중심 끌기 0.06 / 그 외
0.016 · `velocityDecay` 0.18 · `alphaDecay` 0.015.

**틱은 직접 돌린다** — 초당 60틱 고정. 힘 계수가 전부 "한 틱당"으로 잡혀
있어서 d3 타이머에 맡기면 120Hz 화면에서 같은 그래프에 힘이 두 배로 들어간다.

배치가 화면 밖으로 달아나지 않게 세 가지를 건다: 시작 위치를 **황금각
나선**(2.399963 rad)에 고르게 놓고, `forceCollide` 로 겹침을 막고, 식을
때까지(`alpha > 0.02`) 카메라가 8%씩 따라간다. 사용자가 화면을 움직이면
그때부터 손을 뗀다. 배율 한계는 맞춤 0.22~1.35 · 휠 0.25~3.5.

---

## 5. 배치

```
┌──────────────────────────────────────────────────────────────┐
│ ☰  ● histgraph  (조선)     [검색            ]     노드 · 엣지 │  .top
├────────┬────────┬──────────────────────────┬─────────────────┤
│ .side  │.timeline│        .stage           │    .detail      │
│ 244px  │ 252px  │        (flex:1)          │    348px        │
│ 접힘   │  선택   │                          │   노드 고를 때   │
└────────┴────────┴──────────────────────────┴─────────────────┘
```

- **`.side`(244px)** — 기본 **접힘**. 그래프가 주인공이니 ☰ 를 눌러야 열린다.
  시작점 · 범례 · 표시 설정.
- **`.timeline`(252px)** — 노드를 고르면 왼쪽에 선다. 체크박스로 끌 수 있다
  (화면 폭을 250px 먹는다).
- **`.stage`** — 캔버스. 왼쪽 아래에 지금 무엇을 보고 있는지 한 줄
  (`.stage-note`), 아무것도 없으면 가운데에 안내.
- **`.detail`(348px)** — 노드를 고르면 오른쪽에 선다. Esc 로 닫는다.

`#root` 가 `body` 의 세로 배치를 이어받는다 — 이걸 빼면 `.layout` 의
`flex:1` 이 기댈 곳을 잃어 연표 스크롤과 캔버스 크기가 한꺼번에 죽는다.

반응형은 **한 단계뿐**이다 (`max-width: 1100px` → 200 / 208 / 300px).
모바일 배치는 없다. 좁은 화면에서는 네 칸이 그대로 눌린다.

---

## 6. 연표 (`timeline.js`)

그래프는 무엇이 무엇과 이어져 있는지만 말하고 **언제인지는 말하지 않는다.**
그 자리를 이 막대가 맡는다.

**자리는 연도에 비례한다.** 목록으로 늘어놓으면 1400년과 1401년 사이가
1400년과 1500년 사이만큼 벌어져 연표라고 부를 수 없다. 축은 언제나 그래프가
담은 처음 해부터 끝 해까지다 (조선 그래프는 1356~1929) — 어느 노드를 골라도
같은 축 위에 서므로 옮겨 다니며 자리를 견줄 수 있다.

치수 (`timeline.js` 상단 상수):

```
LANE_W 104   재위 띠 칸 (끄면 0)      BAR_X 11 · BAR_W 6 · DEATH_X 19
AXIS_X  46   세로축                   DOT_X 58  라벨 시작
PAD_TOP 18 · PAD_BOTTOM 64            GAP 30   라벨 최소 간격
LANE_GAP 26  재위 라벨 최소 간격 (이름+연도 두 줄)
MAX_LEVEL 4 · MAX_HEIGHT 40000px
```

**줌은 자리가 아니라 밀도를 바꾼다.** 지도가 멀리서는 큰 도시만 보여주듯,
0단계는 시대 전체가 한 화면에 들어오고 한 단계마다 눈금이 두 배가 된다.
마지막 단계는 밀도 상한을 풀고 전부 세운다. 무엇이 '큰' 사건인지는 그래프가
정한다 — 많이 연결된 사건부터 자리를 잡고, 30px 안에서 겹치는 사건은 다음
단계로 물러난다. **빠진 것이 없는 셈이 되면 안 되므로** 머리에
`큰 사건 13 / 156 · 더 보려면 +` 를 늘 적는다.

**그리는 규칙**:

| 것 | 그림 |
|---|---|
| 고른 노드 | 점 r 4 + 구간 막대 4px(85%), 굵은 흰 글자 (`.k-self`) |
| 이웃·뼈대 | 점 r 2.6, `--text-2` (`.k-anchor` 는 `--text-3` 로 더 물러남) |
| 왕조 | 축 **옆**(−6px)에 2px 막대(50%) — 축 위에 겹치면 사건 점을 다 덮는다 |
| 밀린 라벨 | `--text-3` 45% 1px **회색 곡선**. 밀렸을 때만 긋는다 |
| 재위 막대 | `TYPE_COLOR.person` 6px 알약, 한 칸 걸러 78%/50%, 해당 임금 100% |
| 몰년 | `--surface-2` 로 채운 r 2.8 원 + `2 3` 점선 |

안내선에 **노드 색을 쓰지 않는다.** 처음에는 노드 색 실선이었는데 1398년에
'왕자의 난'과 '제1차 왕자의 난'이 나란히 서서 한 줄이 밀리자 **둘이 이어진
것으로 읽혔다** — 그때 그래프에는 둘 사이에 엣지가 하나도 없었다. 갈리는
것은 선의 모양이 아니라 색이다.

**사람은 점으로 찍지 않는다.** 사건·작품만 연표의 점이 된다. 인물·장소·조직은
이어지는 것이라 한 점에 찍으면 거짓을 말한다. 사람이 언제 살았는지는 그
사람을 골랐을 때 생몰 구간(막대)으로 말한다.

**넘겨받은 연도는 같은 얼굴로 내놓지 않는다.** 노드가 스스로 적고 있는
날짜가 아니라 이어진 연도 노드에서 가져온 값이면 머리에 `연결된 연도`
알약(`.tl-via`)을 붙인다. 연도도 없고 연도를 아는 이웃도 없으면 자리를
지어내지 않고 "연도를 알 수 없습니다"라고 적는다.

**머리의 훑기 막대**(`.tl-map`)는 연표가 화면보다 길 때만 나온다. 창틀
(테두리만, 칠하지 않음)이 지금 보이는 구간, 파란 표식이 고른 노드의 자리다.
칠해 버리면 둘 다 '막대'로 보여 무엇이 무엇인지 갈리지 않는다.

배율과 재위 띠 여부는 `localStorage`(`tl-zoom`·`tl-reigns`)에 남는다.

---

## 7. 상세 패널 (`DetailPanel.jsx`)

머리부터: `← 되돌아갈 노드` → 타고 들어온 관계 한 줄 → 타입 알약 →
제목 → 연도 → 설명 → 다른 이름 → 관계 목록.

- **`.d-back` 은 sticky.** 관계 목록이 길어 스크롤을 내려도 되돌아갈 자리가
  사라지면 안 된다. 되돌아갈 곳을 **이름째** 들고 있는다.
- **자취는 상세 안에서만 쌓인다.** 그래프나 연표에서 고른 노드는 그 자체가
  뿌리다 — 캔버스에서 아무 노드나 누를 때마다 직전 노드가 상위로 붙으면
  아무 관계 없는 둘이 부모-자식처럼 보인다. 최대 50칸.
- **타고 들어온 관계는 맨 위에 한 번 더** 적는다(`.d-via`). 조선시대 중기에는
  개체가 37개 달려 있어 방금 누른 것을 목록에서 다시 찾아야 했다.
- **설명이 없으면 왜 없는지 적는다**(`.d-nodesc`). 빈 칸을 그냥 두면 '원래
  설명이 없는 노드'로 읽힌다.
- **넘겨받은 글은 그렇다고 적는다** — '판의금부사' → '의금부'. 이건 출처
  표기가 아니라 "이 글이 다른 문서의 글"이라는 경고다.
- 긴 설명은 168px 에서 접고 `전문 보기`. 접힌 높이를 넘길 만큼 길 때만
  단추를 낸다.
- 관계는 종류별로 묶이고 각 줄에 **근거 구절**이 인용부호로 붙는다. 여러
  관계가 함께 딛고 선 근거는 카드 밖에 한 번만 둔다.
- 관계 문장은 한국어 조사를 받침으로 가려 붙인다 (`relations.js` `pt`).

---

## 8. 상호작용

| 몸짓 | 하는 일 |
|---|---|
| 노드 클릭 | 상세 열기 + 그 주변을 **얹어서** 펼치기 |
| 노드 더블클릭 | 자리를 지킨 채 이웃만 얹기 |
| 노드 드래그 | 그 자리에 못박기 (끌지 않고 떼면 고르기) |
| 빈 곳 클릭 | 조명 끄기 |
| 휠 | 캔버스 확대 (커서 아래 지점 고정) |
| `/` | 검색창으로 |
| `↑`·`↓`·`Enter` | 검색 결과 오르내리기·고르기 |
| `Esc` | 상세 닫기 (한 칸씩이 아니라 통째로) |
| `Alt`/`⌘` + `←` | 상세 안에서 한 칸 되돌아가기 |
| `⌘`/`Ctrl` + 휠, 핀치 | 연표 줌 (그냥 휠은 스크롤이어야 한다) |

주소(`location.hash`)에 지금 보는 노드를 남긴다 — 새로고침해도 자리를 잃지
않고, 링크로 넘길 수 있다. 검색은 140ms 쉬고 묻는다.

**화면을 열면 왕조에서 시작한다.** 차수 1위 노드로 열면 그때그때 병자호란이
중심이 되는데, 그건 우연이고 "이게 무엇의 그래프인가"를 말해주지 못한다.
머리의 `조선` 알약이 언제든 중심으로 되돌린다.

---

## 9. 검증

```bash
npm test                      # 배치 · 관계 문장 · 서버 렌더링 (브라우저 없이)
uv run tools/check_palette.py # 색 거리 (정상 · 2형 · 1형)
```

| 파일 | 무엇을 |
|---|---|
| `web/tests/layout.test.mjs` | 배치가 식는가 · 겹치지 않는가 · 중심이 가운데를 지키는가 |
| `web/tests/relations.test.mjs` | 조사·문장·카드 합치기 |
| `web/tests/render.test.mjs` | 실제 조립 · **화면에 영어가 새지 않는가** |

색이나 노드 그리기를 고치면 `check_palette.py` 를, 사람이 읽는 글자를 고치면
`render.test.mjs` 를 반드시 다시 돌린다.

---

## 10. 지금 어긋나 있는 자리

- **모바일 배치가 없다.** 1100px 한 단계에서 폭만 줄이고, 그 아래로는
  네 칸이 그대로 눌린다.
- **라이트 테마가 없다.** `color-scheme: dark` 고정이다.

# histgraph — 세션마다 먼저 읽는 규칙

이 파일은 짧게 둔다. 설계 배경은 [README.md](README.md), 지금 상태와
최근 작업은 [HANDOFF.md](HANDOFF.md), 화면 디자인은 [design.md](design.md) 에 있다. 여기는 **어겨서 지적받은
것**만 적는다.

## 1. 화면에 한글 아닌 글을 띄우지 않는다

세 번 지적받았다 (2026-09-03 두 번, 09-04 한 번: `1923 Jogono Police
Station bombing` 이 사건 이름으로 떴다). 세 번 다 원인이 같았다 —
**표(`data/ko_labels.tsv`)와 사전(`koreanize.py`)은 있었는데, 그래프를
다시 만든 뒤 돌리지 않았거나 파생본(`data/korea.sqlite`)에 안 돌렸다.**

그래서 지금은 사람이 기억하는 대신 기계가 묻는다. 관문이 셋이다:

| 어디서 | 무엇을 | 걸리면 |
|---|---|---|
| `histgraph scope` | 파생본을 만들자마자 `relabel`·`redescribe` 를 돌리고 남은 것을 센다 | 종료 코드 1 · 남은 노드를 찍는다 |
| `tools/hooks/pre-push` | 배포될 DB 를 `tools/check_korean.py` 로 센다 | push 취소 |
| `.github/workflows/test.yml` | 같은 스크립트 | 빨간불 (`--no-verify` 로 넘긴 push 를 잡는다) |

재는 기준은 하나다: **라벨·설명에 한글이 한 자도 없으면 걸린다**
(`labels.foreign_text`). 자료 출처(Wikidata·위키백과)를 화면에 적는 것도
같은 규칙으로 금지다 — 읽는 사람이 묻지 않은 것이다. **예외는 둘뿐이다.**

1. 출처와 라이선스 (2026-09-05 사용자 결정): 설명 문단 **바로 아래 한 줄**에
   한국어로 적는다 (`provenance.desc_origin` → `/n/` 장의 `.src`, 상세 패널의
   `.d-desc-origin`). 남의 글을 옮겼으면 그렇다고 적는 것이 라이선스 의무다.
   그 한 줄 밖(제목·목록·검색·연표)에는 여전히 안 적는다.
2. 저작권 한 줄 (2026-09-09 사용자 결정, "원문 그대로"): `/n/` 노드·목록 장의
   footer 가운데에 `© 2026 histgraph. All rights reserved.` 를 영어로 세운다
   (`pages.COPYRIGHT` → `.foot .copy`). **여기 말고는 없다** — 방침·약관과
   그래프 화면에는 세우지 않는다. 문구를 늘리면 관문이 다시 잡도록, 봐주는
   글자는 `pages.COPYRIGHT` 상수 하나에서 온다.

걸렸을 때 하는 일:

- **이름**: `data/ko_labels.tsv` 에 `QID<TAB>한국어<TAB>근거` 를 적고
  `uv run histgraph --db data/korea.sqlite relabel`. 원본에도 한 번
  (`uv run histgraph relabel`). 근거 칸에 어디서 온 이름인지 적는다
  (`ja=鍾路警察署爆弾事件`, `로마자 Im Ho`). 근거가 약하면 **음역이라도 적고
  그렇게 밝힌다** — 화면에 영어를 두는 것보다 낫다. 동명이인은 기존 관례대로
  `이름 (1875년)`·`이름 (주교)` 처럼 괄호로 가른다.
- **설명**: `uv run histgraph --db data/korea.sqlite redescribe`. 사전에
  없는 말은 **지어내지 않고 비운다**. 화면이 '설명 없음 — 왜 없는지'를
  한국어로 그린다. 원문은 `props.desc_en` 에 남으므로 사전을 키우면
  되살아난다.
- 이름을 고쳤더니 같은 이름이 이미 있다고 나오면 중복이다. `relabel` 은
  보고만 한다. 합치는 규칙은 `promote` 쪽.
- **이름이 아예 다른 두 표기**('훈민정음'과 '한글')는 어떤 코드도 못 잇는다.
  `data/aliases.tsv` 에 `노드 id<TAB>별칭<TAB>근거` 를 적고 같은 `relabel` 을
  돌린다 (2026-09-06: 인과 추출이 `세종 → 훈민정음` 을 다섯 번 답하고도
  그 이름의 노드가 국보 해례본뿐이라 버려졌다). 편집 계층에 남는다.

하지 말 것:

- 커넥터마다 영어 검사를 따로 적지 않는다. 관문은 `ontology.Node.__post_init__`
  (설명) 과 위의 셋이다. **SQL 로 라벨·설명을 직접 쓰는 경로를 새로 만들면**
  그 경로는 Node 를 안 지나므로 `koreanize.to_korean` 을 거기서 한 번 더
  건다 (`wikipedia._fill_from_wikidata` 가 그 예).
- 화면 코드(`web/`)에 영어 문구·출처 딱지를 넣지 않는다. 검색·상세·연표에
  새 텍스트를 붙일 때 (1) 한글인가 (2) 자료 이름을 말하고 있지 않은가를 본다.
- 이 규칙을 "다음에 돌리자"로 미루지 않는다. 화면 DB 를 만졌으면 끝에
  `python3 tools/check_korean.py` 를 한 번 돌리고 끝낸다.

## 1-2. 이름이 겹치면 반드시 가른다

2026-09-04 지적: "조선왕과 고려왕 이름이 같을때가 있는데 구분을 반드시
해야해". 왕의 휘(諱)가 별칭으로 들어와 있어서 조선 예종(휘 이황 李晄)이
퇴계 이황의 관계 22건을 먹고 있었다.

재는 것은 `uv run histgraph homonyms` 다 (`scope` 도 같은 관문을 건다).
**고치는 것은 하나뿐이다** — 출처 문서의 주인공이 남에게 간 엣지. 나머지
둘(연대가 어긋나는 참여 · 다른 노드의 라벨이기도 한 별칭)은 **세어서
보여만 준다.** 지우면 참인 것이 먼저 사라진다: 양만춘의 생년이 Wikidata
에 700년으로 적혀 있어 안시성 전투(645)보다 늦고, 김종직이 무오사화에
얽힌 것은 부관참시라 연결 자체는 참이다. 그 목록은 **틀린 날짜를 찾는
창**으로 읽는다.

같은 이름의 사건·인물은 괄호로 가른다 — `여진 정벌 (조선)`,
`이름 (1875년)`, `이름 (주교)`.

**말뭉치도 같은 함정에 빠진다** (2026-09-06: 세종의 휘 '이도'가 조선 후기
의병장 이도의 민족문화대백과 글을 받았고, 그 글이 인과 추출에 정본으로
들어갔다. 실측 인물 문서 284건이 딴사람 글이었다). 항목을 노드에 이을 때
**연대를 잰다** (`aks.consistent` — 시대 칸·정의·본문의 연도). 검사가
없던 때 이어진 것은 `uv run histgraph corpus --audit` 가 고아 아이디로
옮기고, 어긋난 정의로 채운 설명은 비운다. 원본과 파생본에 한 번씩.

## 1-3. 이름 말고 할 말이 없으면 화면에 세우지 않는다

2026-09-04 지적: 연표에 `백성들이 종이를 바치는 일의 폐단에 대해
선혜청에서 아뢰다` (실록 기사 제목) 가 설명 없이 서 있었다 — "안
보여주는 게 더 좋을 것 같은데".

`scope` 가 파생본을 만들 때 **설명이 빈 내용 노드**를 뺀다
(`scope.UNDESCRIBED_DROP_TYPES` — 사건·인물·단체·유물·작품·개념).
**이름이 곧 뜻인 타입은 뺀 목록에 없다**: 연표의 눈금(`period`)·직위
(`role`)·장소(`place`). '영의정'과 '서울 종로구'는 해설이 없어도 빈 칸이
아니고, 장소를 빼면 그 구에 있는 유물 86건이 '어디 있는지'를 잃는다.

**빼기 전에 채운다.** 빈 설명은 대개 '자료가 없다'가 아니라 '아직 안
물어봤다'였다 — 실측: 빈 설명 `wd:` 노드 573개 중 415개가 위키백과나
Wikidata 한 줄 설명을 갖고 있었다. 그래서 순서가 있다:

```
uv run histgraph scope korea --keep-undescribed --out /tmp/full.sqlite
uv run histgraph enrich --scope /tmp/full.sqlite    # 위키백과·Wikidata
uv run histgraph describe                           # 민족문화대백과 정의 (네트워크 불필요)
uv run histgraph scope korea                        # 그러고도 빈 것만 뺀다
```

첫 줄이 왜 필요한가: `enrich --scope data/korea.sqlite` 로 주면 **이미
지워진 파생본**을 범위로 삼는다 — 정작 채워야 할 노드가 범위 밖이라 영영
안 채워진다. 채울 때 쓰는 범위는 `--keep-undescribed` 로 전부 담아 만든다.

`enrich`·`describe` 를 건너뛰고 `scope` 를 돌리면 **채울 수 있었던 노드가
지워진다.** 사건이 갑자기 수백 개 줄면 그것부터 의심한다.

### 거꾸로, 있는 것을 솎지도 않는다

2026-09-06 지적: 덕종~헌종(1031~1095)의 연표가 통째로 비어 있었다 — "이
시기 왕들때는 아무런 일이 없었어? 그럴리가 없잖아." 화면이 사건을 **차수
상위 400개**만 세우고 있었다 (조선만 있던 때 걸어 둔 문턱인데 묶음이
고려~대한민국이 되며 사건이 568개가 됐다). 차수 낮은 쪽부터 잘리니 **자료가
얇은 시대가 먼저 사라진다** — 연도를 아는 사건 119건이 잘렸고 그중 고려가
18건이었다. 연표에서 빠진 사건은 읽는 사람에게 그 시대에 없었던 일이다.
연표에 무엇을 세울지 고르는 자리(`server._anchors`)에 **수를 세는 문턱을
다시 놓지 않는다.** 몰린 해는 화면이 늘려 세운다.

그러고도 비어 있으면 자료가 없는 것이다. 그 시기(1019~1135)는 정본에도
사건 항목이 0건이고 **제도와 문화로 적혀 있다.** 민백의 제도·시설 항목을
정본 기사의 날짜로 세우는 것이 `uv run histgraph founding` 이다 (이름·설명은
민백, 날짜는 《고려사》). 위키백과 시드로는 못 채운다 — 그 시기 사건 문서가
아예 없다.

## 1-4. 같은 일이 두 이름으로 서 있으면 합친다

2026-09-04 지적: "사도세자 사건과, 임오화변은 같은거야. 둘중 하나로 통일
시켜야해." 소스마다 표제를 다르게 단다 — 국편은 '3·1운동'·'사도세자
사건', 위키백과는 '3·1 운동'·'임오화변'이다.

재는 것은 `uv run histgraph dedupe` 다 (`--type person` 처럼 타입을 준다).
**규칙은 후보를 찾을 뿐이고**(라벨·별칭·이칭·설명 첫 문장·핵심어 다섯),
판정은 **이름 말고 다른 증거**로 한다 — 한자·생몰년·설명 첫 문장·소재지.
증거가 없으면 `data/duplicates.tsv` 에 사람이 적고, **표에 없는 후보가
남으면 종료 코드 1** 로 묻는다. 표는 언제나 기계를 이긴다.

실측: 사건 후보 55쌍 중 32쌍만 같은 사건이었고, 인물 후보 5,302쌍은
대부분 동명이인이었다 (김지훈이 넷). **이름만으로는 아무것도 단정하지
않는다.**

```
merge<TAB>남길 id<TAB>없앨 id<TAB>근거      keep<TAB>id<TAB>id<TAB>왜 다른지
```

남길 쪽은 대개 엣지가 많은 노드지만 **연대가 맞는 쪽이 먼저다.** 없앤
노드의 날짜는 남는 쪽이 비어 있을 때만 옮긴다. 없어지는 이름은 별칭으로
남는다.

**수집 뒤마다 다시 돌린다.** `upsert_nodes` 는 id 로 노드를 다시 세우므로
다음 `ingest`·`nikh` 가 없앤 노드를 되살린다. 파생본에도 한 번 더
(`--db data/korea.sqlite`).

합치지 말 것:

- 차수가 다른 것 (`제1차`/`제2차 왕자의 난`), 이름만 같은 것 (1592년
  `임진강 전투`와 1951년 `설마리 전투`).
- **후궁 봉작명**. '숙의 김씨'는 단종·중종·효종·헌종·철종에게 각각 있다.
  품계와 성이지 이름이 아니다.
- **국가유산의 지정 건.** 이름도 한자도 같은 '동의보감'이 셋인데
  국립중앙도서관본·규장각본·한국학중앙연구원본이라 서로 다른 보물이다.
  같은 것은 관리번호가 같거나(수집이 두 번 받아 온 줄) 소재지가 같고
  종목만 다른 것(보물 → 국보 승격)이다.
- **장소**. 후보 37쌍이 전부 이름 겹침이었다 — 대구·대전·인천의 '서구',
  남북의 '강원도'. `same_as` 로 이어 둔 짝(엔티티 해소)도 합치지 않는다.

후보가 **다른 오류**를 가리키기도 한다: `청주 전투`가 둘이던 것은 하나가
1950년 **정주** 전투인데 라벨이 잘못 붙어서였고, '전상우' 노드에는
전두환의 설명이 붙어 있었다.

## 1-5. 원인은 결과보다 먼저다

2026-09-05 지적: 연표에서 공석신주사건(1636) 아래에 그 원인인 병자호란
(1636-12-09)이 섰다 — "원인 사건이 먼저 발생해야 하는거 아닌가?" 엣지는
참이었고 날짜가 틀렸다(탄핵은 1638년 1월). 사용자 결정: **사람이 판정하지
말고 직접 조사해서 정확한 날짜를 적는다.**

재는 것은 `uv run histgraph chronology` 다 (원인 날짜가 결과보다 뒤인
인과가 남으면 종료 코드 1). 인과 엣지를 **전부** 잰다 — 재는 자리만 결과에
따라 다르다: 사건은 **시작** 전, 나라·단체·개념은 **끝나기** 전에 원인이
있어야 한다 (명나라 건국은 임진왜란보다 앞서지만 '임진왜란 → 명나라의
쇠퇴'는 참이다). 결과가 아직 존속하면 재지 않고 센다. 판정은 `data/chronology.tsv` 에 적는다 —
`date`(정본에서 찾은 날짜) · `drop`(원인이 결과를 끝낸 것이라 인과가
아님) · `flip`(방향이 뒤집힘). 근거 칸에 어디서 온 날짜인지 적는다.
**날짜는 양력이다** — Wikidata 노드가 그레고리력이라 국편의 음력 날짜를
그대로 두면 같은 해 안에서 순서가 뒤집힌다(단발령 음력 11월 15일 = 양력
12월 30일). 하루까지 모르면 달까지만 적는다. `--apply` 는 편집 계층에
적으므로 재수집·재추출이 되돌리지 못한다. 원본과 파생본에 한 번씩:

```
uv run histgraph chronology --apply
uv run histgraph --db data/korea.sqlite chronology --apply
```

결과의 날짜가 거칠어서(연도만) 원인을 품는 것(만주사변 → 신사참배 1931)은
데이터로 못 가른다 — 연표가 그 결과를 원인 **바로 뒤**에 세운다
(`timeline.js` `sortMarks`). 연표에 원인이 결과 아래 서 있으면 먼저
`chronology` 를 돌려 본다.

## 1-6. 편은 역할이 아니다 — 정변·난·사화의 참여는 표가 판정한다

2026-09-05 지적: "정도전은 제1차 왕자의 난을 지휘했다 … 잘못된 정보야.
정도전은 제1차 왕자의 난을 지휘한적이 없어. 오히려 1차의난의로 사망한
피해자야. 모든 노드를 전수 조사해서 잘못된 정보를 삭제하거나 수정해".

원인은 셋이 겹친 것이었다. (1) 위키백과 인포박스 `지휘관1`·`지휘관2` 는
**양편의 사령관**이라 전투에서는 맞지만 정변·난에서는 한쪽이 일으킨
쪽이고 한쪽이 당한 쪽이다. (2) 인포박스가 이름 뒤에 적는 표식(†·{{KIA}}·
☠·‡)을 파서가 버렸다. (3) 화면은 소스가 여럿인 같은 관계에서 라벨이
있는 것(`지휘관`)을 골라 "지휘했다"로 읽었다.

재는 것은 `uv run histgraph roles --table` 이다 (표를 씌우고, 정변·난·사화·
옥사·사건 이름의 사건(`roles.CONTESTED`)에 **역할 없이 선 인물 참여**가
남으면 종료 코드 1). 판정은 `data/roles.tsv` 에 적는다 — `인물 id<TAB>사건
id<TAB>역할<TAB>근거`. 역할은 주도·가담·대항(참여) / 피해·표적·수습·언급
(참여가 아니라 **관련으로 물러난다**) / 삭제(동명이인 등 엣지 자체가 거짓).
근거 칸은 화면에 그 관계의 근거로 뜨므로 한국어 문장으로 적는다. 편집
계층에 남아 재수집이 되돌리지 못한다. 원본과 파생본에 한 번씩:

```
uv run histgraph roles --table
uv run histgraph --db data/korea.sqlite roles --table
```

실측 (2026-09-06~07): 사건 127+27건의 인물 참여 1,113쌍을 Claude 가 직접
읽었다. 피해 206 · 언급 250 · 가담 197 · 대항 156 · 주도 148 · 삭제 118
(야구 감독 김응용이 1594년 송유진의 난에, 배우 박훈이 기묘사화에, 2003년생
역도 선수 김일경이 신임사화에 서 있었다). **이름만으로는 아무것도 단정하지
않는다** — 국편 문서 한 편이 앞뒤 인물 수십 명을 '참여'로 만든다(사육신
문서의 송준길·허적). 그 사람이 그 사건 안에서 무엇을 했는지 문장을 찾아
적는다. 못 찾으면 '언급'이다.

인포박스 표식은 `props.fate`(사망·처형·피살·귀양·포로·부상), 교전국 이름은
`props.side_name` 에 온다 (`infobox --marks` 로 채운다). 화면은 "옥포
해전에서 조선 측을 지휘했다"·"조선 측을 지휘하다 전사했다"·"제1차 왕자의
난에서 살해되었다"로 읽는다 (`relations.js` `roleSentence`). 편 이름은 한글이
없으면 세우지 않는다.

**고치는 것이지 지우는 것이 아니다.** 2026-09-07 전수 조사에서 내가 '삭제'로
적어 정말 끊어 놓은 쌍이 114 였고, 그중 43 은 **관계가 참인데 '참여'가 아니었을
뿐**이었다 — 난 문서가 배경으로 부른 왕들, 자손이 체포된 이의징, 5대조가 순절한
안방준. 갈라야 할 것은 이렇다:

- **엣지가 거짓** (동명이인·낱말 오독): 지운다. 그 사람은 그 사건과 아무 관계가 없다.
- **관계는 참인데 참여가 아니다**: `언급`으로 낮춘다. 표에 판정을 낮춰 적으면
  `roles --table` 이 그 관계를 **다시 세운다** (근거는 표에 적은 그 줄).
- **노드가 틀렸다**: 중복 표기는 `data/duplicates.tsv` 에 적어 병합하고(이름은
  별칭으로 남는다), 추출이 단체를 인물로 세운 것('적군'·'영국 정부')은 `단체`
  판정으로 타입을 고치고 참여를 그대로 세운다. 노드 타입도 편집 계층이 지킨다.
- **인과가 참인데 설명이 한쪽 관점이다**: `chronology` 의 `drop` 은 인과를
  지우는 길이라 여기에 쓰면 안 된다. 2026-09-07 지적 "제1차 왕자의 난의 인과
  사슬에 여전히 정도전이 안 보이는데?" — 실록이 '정도전이 음모를 꾸몄다'고
  적은 것이 이방원 측 관점이라 통째로 지웠는데, **정도전이 주도한 병권집중과
  사병 혁파가 방원의 거사를 부른 것은 정본이 그대로 적는 사실**이다. 관점이
  섞인 근거는 버리고 **같은 문서의 다른 문장으로 다시 적는다** —
  `uv run histgraph causes --answers data/causes_answers.json` 이 사람이 읽고
  적은 인과를 모델 답과 같은 관문(근거가 원문에 있어야 한다)으로 넣는다.

**참여가 아닌 역할도 사건의 얼굴이다.** 2026-09-07 지적: 정도전을 피해로
옮겼더니 사건 상세의 '관련' 더미에 묻혀 "아예 지워 버렸네"가 됐다 — "정도전은
1차 왕자의 난에서 죽은 대표적인 인물이야 반드시 언급되어야 해". 역할(피해·
주도·가담·대항·표적·수습)이 묶음 머리이고 그래프 선의 이름이며, 참여 바로 뒤에
선다 (`server.ROLE_HEADS`·`relations.js` `relHead`·`pages._head`). '관련'에 남는
것은 언급·근거 없음뿐이다.

같은 자리를 전수로 훑어 셋을 더 고쳤다 (2026-09-07): 사건의 앞뒤(`다음` 27)·
실록 기사가 다루는 것(36)·스키마가 안 맞아 낮춰졌지만 라벨은 남은 것(`소속`·
`직위` 5)이 전부 '관련' 한 더미에 있었고, `set_in`(105)·`adapted_from`(5)·
`about`(1)은 **문장 규칙이 아예 없어** 화면에 "성균관 스캔들 → 제도 · 주제"라는
화살표로 섰다. 라벨 표는 `server.LABEL_HEADS` 하나이고 화면·정적 페이지·연표가
같은 것을 읽는다 (연표는 서버가 `rel.specific` 을 달아 준다). **새 엣지 타입은
문장 규칙을 같이 들고 와야 한다** — 테스트가 관문으로 잡는다.

**모델 판정(`roles`)도 편집 계층에 적는다** — 실측: 인포박스 재수집이 12·12
군사 반란의 판정 16건을 '지휘관'으로 되돌려 놓았다. 표(origin `roles`)가
모델(origin `roles-model`)을 이긴다.

## 2. 그래프를 다시 만들 때

수집(`ingest`·`enrich`)은 라벨·설명·엣지 props 를 통째로 덮어쓴다. 고친 값은
**편집 계층(`overrides` 표)**에 남아 저장소가 쓸 때마다 다시 씌운다 (2026-09-05,
README "편집 계층" 절) — `relabel`·`redescribe`·`describe`·`nikh`·`precision`·
`reigns`·`dedupe`·`chronology`·`founding`·`roles --table` 이 거기 적는다. 그래서 수집 뒤에 그 열을 **잊어도 고친
값은 돌아온다.** 다시 돌리는 것은 새로 고칠 것이 생겼을 때다. 그 다음
`scope korea` 로 파생본을 만든다. `dedupe`·`untangle`(`related_to` 갈라 내기)은
수집이 새 노드·새 `related_to` 를 내므로 수집 뒤마다 돌린다. **SQL 로
라벨·설명·날짜를 직접 고치는 경로를 새로 만들면** `overrides.record` 를 거기서 한 번 더 건다 — 안 걸면 옛 구조로
되돌아간다.
인과 엣지(`causes`, MLX 필요)는 `scope` 뒤에 `--sync-to data/korea.sqlite` 로
옮긴다 — 노드를 만들지 않으므로 파생본을 다시 만들 필요가 없다.
**정본이 아닌 설명을 우리 말로 새로 쓴 글(`paraphrase`, MLX 필요)도 같다** —
`summaries` 표에 두고 `--sync-to` 로 옮긴다. 수집이 설명을 바꾸면 옛 글은
저절로 무효가 되어 화면이 위키 도입부로 물러나므로, 수집 뒤엔 다시 돌린다
(바뀐 것만 새로 쓴다). 위키 원문을 화면에 그대로 뿌리는 길을 다시 열지
않는다 — 2026-09-05 애드센스 '주의 필요'가 그것 때문이었다.
화면이 읽는 것은 원본이 아니라 **`data/korea.sqlite`** 다 — 원본만 고치면
화면은 그대로다. 파생본은 저장소에 실려 배포되므로 다시 만들면 커밋한다.

DB 는 다른 세션이 동시에 쓴다. 파일을 복사해 되돌리지 않는다 — 그쪽
작업이 사라진다.

## 2-2. 화면을 그리는 규칙은 에이전트가 든다

2026-09-08 사용자 결정: "노드가 생성될 때마다 개발자가 어떻게 그려야 하는지 매번
알려주는 건 낭비야." 그래프·연표·인과 도면·개인 역사를 **어떻게 그리는가**(온톨로지
대응·시각 부호·배치·문장·사용자 결정)는 `.claude/agents/graph-drawer.md` 에 전부 있다.
**맡기는 자리는 파일이 아니라 물음이다.** "이 노드를 무슨 색·어느 자리·어떤 선·어떤
문장으로 그리나"가 걸리면 그 에이전트에 맡기고, 새로 정한 규칙은 그 파일 §12 에 적는다.
수치의 정본은 `design.md` 라 둘을 같이 고친다.

접기 단추·모달·입력칸·스크롤 같은 **화면 살림은 직접 한다** (2026-09-08 지적: 오른쪽
상세를 접는 단추를 이 에이전트에 넘겼더니 "그래프 전문 agent 아니였나?"). 전에는
"`web/`·`server.py`·`life.js` 를 만지면 전부"라고 적어 두었는데, 파일로 가르면 그리는
규칙이 하나도 안 걸리는 일까지 넘어간다. 헷갈리면 이렇게 가른다 — **고치는 것이
`graph-drawer.md` 에 적힐 규칙인가.** 아니면 내 일이다.
(에이전트 이름의 `drawer` 는 서랍이 아니라 draw + er, **그리는 사람**이다.)

## 3. 자주 쓰는 것

```
uv run tests/test_pipeline.py        # 파이썬 (표준 라이브러리만, 의존성 없이 돈다)
python3 tools/check_korean.py        # 배포될 DB 에 영어가 남았는지
uv run histgraph serve               # http://127.0.0.1:8100 (8000 은 다른 프로젝트)
cd web && npm test && npm run build  # 화면
```
